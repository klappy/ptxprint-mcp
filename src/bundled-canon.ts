// AUTO-GENERATED by scripts/bundle-canon.ts — do not edit. Re-run: npm run bundle-canon
// Source: canon/{articles,governance,specs,templates,handoffs} + canon/README.md (48 docs, 666215 bytes)
// Canon content hash: 4afd897b8846 (sha256 of the bundled docs, first 12 hex)

export interface CanonSection { slug: string; heading: string; start: number; end: number }
export interface CanonDoc {
  path: string; uri: string; title: string; audience: string; tags: string[]; stability: string;
  what_it_answers: string; sections: CanonSection[]; body: string;
}

export const CANON_BUNDLE = { sha: "4afd897b8846", docs: 48, bytes: 666215 } as const;

export const CANON_DOCS: CanonDoc[] = [{"path":"README.md","uri":"klappy://canon/README","title":"Canon Directory — Index and Conventions","audience":"project","tags":["canon","index","oddkit","conventions"],"stability":"working","what_it_answers":"This directory holds the project's knowledge base. Every file is intended to be:","sections":[{"slug":"layout","heading":"Layout","start":17,"end":29},{"slug":"frontmatter-convention","heading":"Frontmatter convention","start":29,"end":59},{"slug":"reading-order-for-a-new-contributor-or-agent","heading":"Reading order for a new contributor or agent","start":59,"end":67},{"slug":"status-of-canon-content","heading":"Status of canon content","start":67,"end":76},{"slug":"promotion-to-broader-klappy-canon","heading":"Promotion to broader klappy canon","start":76,"end":82}],"body":"---\ntitle: \"Canon Directory — Index and Conventions\"\naudience: project\nexposure: public\nvoice: neutral\nstability: working\ntags: [\"canon\", \"index\", \"oddkit\", \"conventions\"]\ncanonical_status: non_canonical\n---\n\n# `canon/` — PTXprint MCP Knowledge Base\n\nThis directory holds the project's knowledge base. Every file is intended to be:\n\n- **Human-readable** — the markdown is also clean documentation.\n- **Agent-retrievable** — frontmatter and structure are compatible with [`oddkit`](https://github.com/klappy/oddkit) MCP, so an AI agent pointed at this repo via `knowledge_base_url` can search and retrieve any document.\n\n## Layout\n\n| Subdirectory | Purpose |\n|---|---|\n| [`specs/`](specs/) | Versioned PTXprint MCP server specifications. v1.2 is the current build target; v1.0 and v1.1 are historical. The original 17-tool PoC lives under `specs/archive/`. Don't edit published versions — create a new file and supersede. |\n| [`governance/`](governance/) | Long-form agent-facing operational knowledge. Currently holds `headless-operations.md`; most of the per-topic content has migrated to `articles/` (per the v1.2 spec split). |\n| [`handoffs/`](handoffs/) | Cross-session work-transfer documents. Not durable canon; describe what one session is asking the next session (or another collaborator) to do. |\n| [`encodings/`](encodings/) | DOLCHEO+H session encodings — Decisions, Observations, Learnings, Constraints, Handoffs, plus Open questions captured per session. The project's running ledger. |\n| [`surfaces/`](surfaces/) | ESE (Epistemic Surface Extraction) outputs: structured `.surface.md` and `.surface.json` artifacts derived from large source materials (upstream code repositories, slide decks, etc.). Non-canonical by rule — surfaces inform but do not become canon. |\n| [`articles/`](articles/) | Topic-keyed agent-facing canon articles — the working \"user manual\" for the AI. Currently 18 articles covering payload construction, output naming, config construction & inheritance, font resolution, failure modes, diagnostic patterns, file-system map, USFM markers, the four supporting-file formats (adjlist, piclist, changes.txt, stylesheet), front matter / cover periphs, settings cookbook, workflow recipes, CLI reference, and the temporary Phase-1 PoC scope article. `_archive/` retains superseded design docs. |\n| [`PENDING_UPLOADS.md`](PENDING_UPLOADS.md) | Bootstrap-era file inventory. Mostly resolved as of 2026-04-28; retained for the optional historical-archive items in §4. |\n\n## Frontmatter convention\n\nEvery canon document carries a YAML frontmatter block at the top:\n\n```yaml\n---\ntitle: \"...\"\naudience: \"agent | operator | project\"\nexposure: \"working | public\"\nvoice: \"neutral | instructional\"\nstability: \"working | stable\"\ntags: [\"...\", \"...\"]\ncanonical_status: \"canon | canon_candidate | non_canonical\"\ncreated_at: \"2026-04-28T00:00:00Z\"\n---\n```\n\nFields:\n\n- **`title`** — Human-readable title.\n- **`audience`** — Who the document is written for. `agent` for AI-facing operational content; `operator` for the human running the system; `project` for cross-cutting docs.\n- **`exposure`** — `public` for documents safe to publish; `working` for in-progress drafts.\n- **`voice`** — `neutral` for reference material; `instructional` for how-to.\n- **`stability`** — `stable` if the content has settled; `working` if active iteration is expected.\n- **`tags`** — Keywords for retrieval. Agents search by tag; humans skim them.\n- **`canonical_status`** — Per the broader klappy canon conventions: `canon` is binding, `canon_candidate` is being tested, `non_canonical` is reference / interpretive material that informs but does not bind.\n- **`created_at`** — ISO 8601 timestamp.\n\nAdditional fields like `supersedes`, `companion_to`, `extends`, `applied_canon` are used where relevant.\n\n## Reading order for a new contributor or agent\n\n1. [`/README.md`](../README.md) — project overview\n2. [`/ARCHITECTURE.md`](../ARCHITECTURE.md) — high-level architecture\n3. [`canon/specs/`](specs/) — the current spec (latest version)\n4. [`canon/governance/`](governance/) — once populated, the agent's operational manual\n5. [`canon/encodings/`](encodings/) — historical decisions and rationale, newest session first\n\n## Status of canon content\n\n- **Specs**: v1.0, v1.1, and v1.2 are all in [`specs/`](specs/). v1.2 is the current build target; v1.0 and v1.1 are retained as historical context. The original 17-tool PoC is at [`specs/archive/`](specs/archive/).\n- **Governance**: [`governance/headless-operations.md`](governance/headless-operations.md) (Parts 0–12 + Provenance) is in repo and v1.2-aligned. The historical update handoff at [`handoffs/governance-update-handoff.md`](handoffs/governance-update-handoff.md) is now resolved.\n- **Encodings**: all five sessions (1–5) are in [`encodings/`](encodings/). Session 5 captures the architectural pivot to v1.2.\n- **Articles**: [`articles/`](articles/) exists with [`_archive/`](articles/_archive/) (superseded design history). The five v1.2 gating articles (per spec §2) are not yet authored — see [`handoffs/documentation-cleanup-handoff.md`](handoffs/documentation-cleanup-handoff.md) §1 Track 2.\n- **Surfaces**: [`surfaces/`](surfaces/) contains the ptx2pdf and PTXprint MASTER SLIDES surfaces (markdown + JSON for each).\n- **Derivatives**: [`derivatives/`](derivatives/) contains the training manual produced from the slides ESE.\n\n## Promotion to broader klappy canon\n\nThis repo's canon is project-scoped (PTXprint-specific). Insights here that prove durable across the broader klappy work — for example, the principle that \"stateless workers imply content-addressed everything\" or \"co-locating code and governance prevents drift\" — should be promoted to the broader canon by direct edit there, not by reference from this repo.\n\nThe general rule (per `klappy://canon/principles/dry-canon-says-it-once`): cross-cutting principles live once, in the broader canon. Project-specific applications live here.\n"},{"path":"articles/adjlist-format.md","uri":"klappy://canon/articles/adjlist-format","title":"AdjList Format — Paragraph Adjustment Files","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","adjlist","paragraph-adjustment","supporting-files"],"stability":"working","what_it_answers":"What's an AdjList? When does the agent need to write one? What's the file format and naming convention?","sections":[{"slug":"what-an-adjlist-is","heading":"What an AdjList is","start":22,"end":28},{"slug":"file-location-and-name","heading":"File location and name","start":28,"end":59},{"slug":"line-syntax","heading":"Line syntax","start":59,"end":79},{"slug":"worked-examples","heading":"Worked examples","start":79,"end":139},{"slug":"when-the-agent-should-write-an-adjlist","heading":"When the agent should write an AdjList","start":139,"end":146},{"slug":"when-not-to-write-an-adjlist","heading":"When NOT to write an AdjList","start":146,"end":152},{"slug":"the-right-magnitude","heading":"The right magnitude","start":152,"end":164},{"slug":"including-adjlists-in-the-payload","heading":"Including AdjLists in the payload","start":164,"end":176},{"slug":"reading-existing-adjlists-from-project-state","heading":"Reading existing AdjLists from project state","start":176,"end":186}],"body":"---\ntitle: \"AdjList Format — Paragraph Adjustment Files\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"adjlist\", \"paragraph-adjustment\", \"supporting-files\"]\nderives_from: \"canon/governance/headless-operations.md (Part 6 AdjLists subsection)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# AdjList Format\n\n> **What this answers.** What's an AdjList? When does the agent need to write one? What's the file format and naming convention?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/file-system-map`\n\n---\n\n## What an AdjList is\n\nIn the GUI, paragraph adjustment is a right-click affordance on the preview pane — the user shrinks or grows a specific paragraph by N lines to fix overfull boxes, widows, or orphans. In headless, the same fine-tuning is expressed as a per-book text file: an AdjList.\n\nThe agent constructs the AdjList content as text and includes it in the payload's `config_files` map. PTXprint reads it during typesetting and applies the adjustments.\n\n## File location and name\n\n```\nshared/ptxprint/<config>/AdjLists/<num><BOOK><prj>-<config>[.SFM|-diglot].adj\n```\n\nComposition:\n\n```\n<num><BOOK><prj>-<config>[.SFM|-diglot].adj\n  │     │     │       │       │\n  │     │     │       │       └─ `.SFM.adj` for monoglot, `-diglot.adj` for diglot\n  │     │     │       └────────  config name (case as named)\n  │     │     └────────────────  project ID (case as named)\n  │     └──────────────────────  three-letter USFM book code, uppercase\n  └────────────────────────────  two-digit canonical USFM book number, zero-padded\n```\n\nExamples:\n\n| Filename | Meaning |\n|---|---|\n| `41MATWSG-Default.SFM.adj` | Matthew, project WSG, config Default, monoglot |\n| `46ROMWSG-FancyNT.SFM.adj` | Romans, project WSG, config FancyNT, monoglot |\n| `41MATWSG-DiglotEN-FR-diglot.adj` | Matthew, diglot version |\n| `01GENWSG-Default.SFM.adj` | Genesis (book 01), project WSG, config Default |\n\nThe book number must be zero-padded to two digits. Use `01`–`66` for canonical books, plus the standard USFM peripheral numbers for non-canonical books.\n\nOne file per book. A project that adjusts paragraphs across MAT, ROM, and PSA has three files: `41MATWSG-Default.SFM.adj`, `46ROMWSG-Default.SFM.adj`, `19PSAWSG-Default.SFM.adj`.\n\n## Line syntax\n\nThe basic line shape:\n\n```\nBKK c.v ±N[P] [% hints | mrk=<marker> expand=<pct>]\n```\n\n| Token | Meaning |\n|---|---|\n| `BKK` | three-letter USFM book code; append `L` or `R` for diglot left/right column |\n| `c.v` | chapter.verse reference (e.g. `3.16`); for glossary, keyword reference (e.g. `k.Adam`) |\n| `±N` | integer line adjustment; negative shrinks, positive grows; `+0` means no adjustment (hint-only line) |\n| `[P]` | optional paragraph index when the verse contains multiple paragraphs |\n| `% hints` | candidate paragraphs as `+[2] +[3]`; PTXprint generates these automatically when interactive |\n| `mrk=<m>` | extended format: USFM marker whose paragraph text should be scaled |\n| `expand=<pct>` | extended format: text scale percentage (`98` = 98% of normal) |\n\nLines starting with `%` are comments. Blank lines are ignored.\n\n## Worked examples\n\n### Basic shrink/grow\n\n```\n% Shrink Romans 2:6 paragraph by one line; grow Matthew 1:1 by one\nROM 2.6 -1\nMAT 1.1 +1\n```\n\n### With paragraph index\n\n```\n% At Matthew 1:1, grow the 4th paragraph at that verse by one line\nMAT 1.1 +1[4]\n```\n\nUse the `[P]` index when the verse anchors multiple paragraphs and the agent needs to target a specific one.\n\n### Hint-only (auto-generated by PTXprint)\n\n```\n% Current adjustment is +0; paragraphs 2 and 3 are candidates if the user wants\n% to grow at this reference\nMAT 1.1 +0% +[2] +[3]\n```\n\nPTXprint's interactive flow generates these when the user hovers over an overfull region. The agent typically doesn't write these directly; if it does, the `+0` says \"no actual adjustment yet, just recording candidates.\"\n\n### Extended: scale paragraph text\n\n```\n% Shrink Isaiah 6:11 paragraph by 1 line AND scale its \\q-marked text to 98%\nISA 6.11 -1 % mrk=q expand=98\n\n% Grow Isaiah 7:23 paragraph by 1 line AND scale its \\p-marked text to 103%\nISA 7.23 +1 % mrk=p expand=103\n```\n\nThe extended format lets the agent (or user) bias both line count and horizontal scale. Useful when a single line adjustment alone produces awkward spacing.\n\n### Diglot: target one column\n\n```\n% In a diglot, MRKL targets the left column at Mark 1:1; MRKR targets right\nMRKL 1.1 +0\nMRKR 1.1 +2\n```\n\nDiglot AdjList filenames end in `-diglot.adj`; the L/R book-code suffixes inside select column.\n\n### Glossary: keyword reference\n\n```\n% In the Glossary book (right column), the entry for keyword \"Adam\" gets adj 0\nGLOR k.Adam 0\n```\n\n`GLO` is the glossary book code; `R` selects the right column; `k.Adam` is the keyword anchor (no chapter/verse).\n\n## When the agent should write an AdjList\n\n- The user reports an overfull box, widow, or orphan at a specific reference and wants it fixed.\n- The user wants to shrink or expand a specific paragraph by N lines without changing global layout.\n- The user is in headless context and cannot use the right-click GUI workflow.\n- A previous typeset run reported overfull boxes the agent's diagnostic pinned to specific verses.\n\n## When NOT to write an AdjList\n\n- **The user is in the GUI.** Direct them to right-click on the preview pane.\n- **The fix is layout-wide.** Adjust paragraph or font settings in `ptxprint.cfg`, not individual paragraphs.\n- **The agent is guessing about direction.** Adjustments that go the wrong way make layout worse. If unsure whether a paragraph should shrink or grow, ask the user or make a `mode: \"simple\"` test run with no adjustment first to see the current state.\n\n## The right magnitude\n\nParagraph adjustments are usually `±1` or `±2`. Larger values are unusual and may signal the wrong fix:\n\n| Magnitude | Reasonable use |\n|---|---|\n| `±1` | Most common; one-line tweak |\n| `±2` | Occasional; fixing widow/orphan plus line count |\n| `±3` or more | Rare; usually means the underlying layout is wrong |\n\nIf the agent finds itself proposing `±5`, step back and consider whether the cfg (font size, line spacing, column width) needs adjustment instead.\n\n## Including AdjLists in the payload\n\n```json\n\"config_files\": {\n  \"shared/ptxprint/Default/ptxprint.cfg\": \"...\",\n  \"shared/ptxprint/Default/AdjLists/41MATWSG-Default.SFM.adj\": \"MAT 1.1 +1\\nMAT 5.3 -1\\n\",\n  \"shared/ptxprint/Default/AdjLists/46ROMWSG-Default.SFM.adj\": \"ROM 2.6 -1\\n\"\n}\n```\n\nOne key per book that has adjustments. Books with no adjustments don't need keys.\n\n## Reading existing AdjLists from project state\n\nBefore constructing a payload, the agent should read existing AdjLists from project state and include them unchanged in the payload (unless the user is editing them). Existing adjustments matter for layout consistency across runs.\n\nIf the user adds new adjustments, the agent merges them into the existing content rather than replacing.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/piclist-format` for picture placement (similar single-file-per-config pattern).*\n"},{"path":"articles/agent-faq-customization.md","uri":"klappy://canon/articles/agent-faq-customization","title":"Agent FAQ — Customization in the Current Phase (line spacing, config_files, fonts, ptxprint.cfg)","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","faq","current-phase","line-spacing","linespacing","config_files","ptxprint-cfg","customization","progressive-customization","quickstart"],"stability":"working","what_it_answers":"User wants tighter line spacing / different fonts / a layout tweak. What does the agent send in the payload? Do I need to supply `config_files`? Should I construct `ptxprint.cfg` content? What's actually deferred to Phase 2 today, and what's already live?","sections":[{"slug":"quick-answer-table","heading":"Quick answer table","start":41,"end":57},{"slug":"q-user-wants-tighter-line-spacing-for-bsb-james-what-do-i-send","heading":"Q: User wants tighter line spacing for BSB James. What do I send?","start":57,"end":81},{"slug":"q-do-i-need-to-supply-configfiles-in-the-current-phase","heading":"Q: Do I need to supply `config_files` in the current phase?","start":81,"end":91},{"slug":"q-should-the-agent-construct-ptxprint-cfg-content-from-scratch-in-the-current-phase","heading":"Q: Should the agent construct `ptxprint.cfg` content from scratch in the current phase?","start":91,"end":109},{"slug":"q-what-about-fonts-user-wants-gentium-phase-2-or-live","heading":"Q: What about fonts? \"User wants Gentium\" — Phase 2 or live?","start":109,"end":117},{"slug":"q-what-is-legitimately-deferred-to-a-later-phase-today-2026-04-30","heading":"Q: What *is* legitimately deferred to a later phase today (2026-04-30)?","start":117,"end":132},{"slug":"q-i-see-an-article-that-says-agents-shouldn-t-construct-ptxprint-cfg-and-configfiles-stays-empty-should-i-follow-it","heading":"Q: I see an article that says agents shouldn't construct `ptxprint.cfg` and `config_files` stays empty. Should I follow it?","start":132,"end":144},{"slug":"where-to-read-next","heading":"Where to read next","start":144,"end":158}],"body":"---\ntitle: \"Agent FAQ — Customization in the Current Phase (line spacing, config_files, fonts, ptxprint.cfg)\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - agent-kb\n  - v1.2-aligned\n  - non-canonical\n  - faq\n  - current-phase\n  - line-spacing\n  - linespacing\n  - config_files\n  - ptxprint-cfg\n  - customization\n  - progressive-customization\n  - quickstart\ncompanion_to:\n  - canon/articles/progressive-customization.md\n  - canon/articles/settings-cookbook.md\n  - canon/articles/bundled-default-cfg.md\n  - canon/articles/payload-construction.md\ncanonical_status: non_canonical\ndate: 2026-04-30\nstatus: draft_for_operator_review\n---\n\n# Agent FAQ — Customization in the Current Phase\n\n> 🟢 **Live as of 2026-04-30. This article describes what the deployed `https://ptxprint.klappy.dev` server actually does today.** Empirically validated against the live deploy: BSB Gospel of John (~4 s, 360 KB PDF), BSB Psalms (~13 s, ~900 KB PDF, 184 pages with embedded Gentium Plus + SourceCodePro). If you are an agent reading the canon and you find guidance elsewhere that contradicts what this article says — **this article wins for current behavior**. Older framings live in `klappy://canon/articles/_archive/` or in superseded articles whose frontmatter declares `status: superseded`.\n>\n> **What this answers.** User wants tighter line spacing / different fonts / a layout tweak. What does the agent send in the payload? Do I need to supply `config_files`? Should I construct `ptxprint.cfg` content? What's actually deferred to Phase 2 today, and what's already live?\n>\n> **The single most useful sibling article.** `klappy://canon/articles/progressive-customization` — the canonical ladder. This FAQ exists to win retrieval against historical quotes; the ladder article is where the real reference lives.\n\n---\n\n## Quick answer table\n\n| User intent | What the agent sends | Level on the ladder | Does it need `config_files`? |\n|---|---|---|---|\n| Render this USFM as a default Bible book | `{schema_version, project_id, books, sources}` | **Level 0** | **No.** Bundle provides cfg. |\n| Tighter / looser line spacing | `{...Level 0..., define: {linespacing: <value>}}` | **Level 1** | **No.** `define` is a one-shot override. |\n| Hide cross-references; change page size; toggle headers; etc. | `{...Level 0..., define: {<key>: <value>}}` | **Level 1** | **No.** See `klappy://canon/articles/settings-cookbook`. |\n| Different typeface (Gentium, Andika, language-specific) | `{...Level 0/1..., fonts: [...]}` | **Level 3** | **No.** Fonts are a separate payload field. |\n| Wholesale layout change (different cfg entirely) | `{...Level 0..., config_files: {\".../ptxprint.cfg\": \"<full cfg text>\"}}` | **Level 2** | **Yes** — agent supplies a full cfg. |\n| Custom stylesheet, Paratext settings overrides | `config_files` extended with `.sty`, `Settings.xml`, `*-mods.sty` | **Level 4** | **Yes** — extended cfg map. |\n| Pictures / illustrations | `figures: [...]` + piclist in `config_files` | **Level 5** | **Yes** for the piclist file. |\n\nThe ladder is **purely additive**. Level 1 doesn't require Level 2; Level 3 doesn't require Level 4. Climb only the rungs the user actually needs.\n\n---\n\n## Q: User wants tighter line spacing for BSB James. What do I send?\n\n**A:** Level 1 `define`. No `config_files` map needed. Example:\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"BSB\",\n  \"books\": [\"JAS\"],\n  \"sources\": [\n    { \"book\": \"JAS\", \"filename\": \"<jas-usfm>.usfm\", \"url\": \"...\", \"sha256\": \"...\" }\n  ],\n  \"define\": {\n    \"linespacing\": \"<numeric value, e.g. font size × 1.15 for tighter than default 1.2–1.3>\"\n  }\n}\n```\n\nThe relevant cfg section is `[paragraph]`, key `linespacing`. See `klappy://canon/articles/settings-cookbook` §\"Tighter / looser line spacing\" for value guidance and watch-fors (e.g. layout `OptimizeLinesPerPage` is often a better lever for overfull-box issues than line spacing).\n\nIf a downstream user reports \"the agent told me line spacing is a Phase 2 capability and refused\" — that agent absorbed the *withdrawn* framing from `phase-1-poc-scope.md`'s historical content. The current contract supports it via `define`. Issue #38 documents the leak.\n\n---\n\n## Q: Do I need to supply `config_files` in the current phase?\n\n**A: No, not for Level 0 or Level 1 payloads.** The Container bundles a publication-quality default cfg (A5 two-column, Charis, parallel refs, footnotes, mirror header). An empty `config_files` is a valid Level 0 payload — the bundle materializes the project's `shared/ptxprint/Default/` tree at job time.\n\nYou only supply `config_files` when you've climbed to **Level 2 or higher** — i.e. you've decided to replace the cfg wholesale (different layout family) or extend it with stylesheets / Paratext settings.\n\nHistorical note: an *earlier* phase of this project required agents to supply a 5-file `config_files` map even for a default render, because the Container had no usable bundled defaults. That requirement was lifted by the bundling work tracked in `klappy://canon/handoffs/bundle-default-cfg-handoff`. If you find canon text that says \"Phase 1 requires the minimum config_files map,\" check the article's frontmatter — articles with `status: superseded` describe the intermediate state, not current behavior.\n\n---\n\n## Q: Should the agent construct `ptxprint.cfg` content from scratch in the current phase?\n\n**A: Almost never.** The bundle handles defaults; `define` handles tweaks. Hand-authoring `ptxprint.cfg` is brittle (~400 settings across ~25 sections, undocumented invariants, easy to produce silent failures the agent won't catch).\n\nConstruct cfg content directly only when **all** of the following are true:\n\n1. The user's intent cannot be expressed as a `define` override (Level 1) — i.e. it requires structural changes the cookbook doesn't cover.\n2. No bundled or canon-blessed preset cfg covers the case.\n3. The agent has a high-confidence reference (a known-working cfg from the user's project, or a canon example).\n\nIf those don't all hold, **escalate to the user** with the honest framing:\n\n> \"What you're asking for needs a full cfg replacement, which I'd rather not hand-author from scratch — it's likely to fail silently in subtle ways. Do you have an existing `ptxprint.cfg` from this project I can adapt? Or can we narrow the request to something `define` can handle (line spacing, font size, hide xrefs, page size, etc.)?\"\n\nThat's the truthful current-phase answer. It's not \"agents never construct cfg\" (Level 2+ is supported by the v1.2 server). It's \"agents shouldn't construct cfg from scratch absent a reference.\"\n\n---\n\n## Q: What about fonts? \"User wants Gentium\" — Phase 2 or live?\n\n**A: Live.** Payload-supplied fonts are validated as of 2026-04-30. Send the `fonts` array (Level 3 on the ladder); the Container materializes them into `<scratch>/<project>/shared/fonts/` and PTXprint discovers them via its own startup logic — no `fc-cache`, no `OSFONTDIR`. See `klappy://canon/articles/font-resolution` (the top-of-article banner explicitly states \"the per-payload, content-addressed font contract is the live contract, not a future target\").\n\nThe Container additionally bundles **SIL Charis** as a system-wide convenience floor. A payload that only references Charis can leave `fonts: []`.\n\n---\n\n## Q: What *is* legitimately deferred to a later phase today (2026-04-30)?\n\nThese are the real \"not yet\" items per the README and the latest handoff:\n\n- **Reliable widget-ID overrides** (`-D <widget>=<value>` flag). Blocked on widget-ID-to-cfg-key mapping (open since session 1). For now, the agent uses `define` on the cfg key directly, not the widget ID.\n- **Autofill mode.** Specced; container instance plumbing not yet built.\n- **`cancel_job` SIGTERM enforcement.** API surface exists; SIGTERM-on-flag inside the container is not yet wired.\n- **Per-pass progress streaming.** Specced; not built.\n- **Templates / composition pattern.** Drafted; PR open.\n- **Curated preset library** (\"tight-spacing preset\", \"wide-margin reader edition\", etc.). The mechanism exists at Level 2; the curated list of canon-blessed presets does not yet.\n\nIf a user's intent maps cleanly to \"deferred\" above, escalate honestly. If it maps to Level 0–5 on the ladder, just send the payload.\n\n---\n\n## Q: I see an article that says agents shouldn't construct `ptxprint.cfg` and `config_files` stays empty. Should I follow it?\n\n**A: Check the article's frontmatter.**\n\n- If `status: superseded` or `stability: archived`, the article describes a previous state and explicitly points to its replacement via `superseded_by`. Read the replacement.\n- If the article lives under `canon/articles/_archive/`, it's preserved for diff visibility only. Do not treat it as current.\n- If the guidance is the \"Old version (preserved for diff visibility)\" section *inside* `phase-1-poc-scope.md`, that's a known retrieval leak documented in issue #38. The article's own frontmatter (`status: superseded`, `superseded_by: progressive-customization`) tells you to read the replacement instead.\n\nWhen in doubt, this FAQ + `klappy://canon/articles/progressive-customization` + `klappy://canon/articles/settings-cookbook` together describe current behavior. They are the active references.\n\n---\n\n## Where to read next\n\n- **Most important:** `klappy://canon/articles/progressive-customization` — the full ladder with payload examples for each level.\n- `klappy://canon/articles/settings-cookbook` — intent-keyed lookup for `define` overrides (\"Tighter line spacing,\" \"Hide cross-references,\" \"Different page size,\" etc.).\n- `klappy://canon/articles/bundled-default-cfg` — what Level 0 actually produces, plus the bundled cfg's exact contents.\n- `klappy://canon/articles/payload-construction` — full payload schema reference.\n- `klappy://canon/articles/font-resolution` — Level 3 detail.\n\nIf a query routed you here through `docs(...)`, the next-most-helpful retrieval target is `progressive-customization`. Try `docs({ query: \"progressive customization ladder\", depth: 2 })`.\n\n---\n\n*Created 2026-04-30 in response to issue #38. The corpus contained current-state guidance for the line-spacing case (`settings-cookbook` §\"Tighter / looser line spacing\", Level 1 `define: linespacing`), but a withdrawn quote inside a superseded article was outranking it on natural-language queries. This article exists to win that retrieval contest by being denser on the failing-query keywords while staying strictly current.*\n"},{"path":"articles/bundled-default-cfg.md","uri":"klappy://canon/articles/bundled-default-cfg","title":"Bundled Default Cfg — What the Container Ships","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","default-cfg","container","bundle","upstream","bsb","charis","phase-1","source-of-truth"],"stability":"working","what_it_answers":"What `ptxprint.cfg` does the Container ship with? What does it produce? When does an agent need to override it?","sections":[{"slug":"why-this-article-exists","heading":"Why this article exists","start":43,"end":49},{"slug":"what-this-cfg-produces","heading":"What this cfg produces","start":49,"end":65},{"slug":"where-this-cfg-comes-from","heading":"Where this cfg comes from","start":65,"end":71},{"slug":"anatomy-at-a-glance","heading":"Anatomy at a glance","start":71,"end":113},{"slug":"known-characteristics-worth-knowing-as-an-agent","heading":"Known characteristics worth knowing as an agent","start":113,"end":127},{"slug":"override-path-summary","heading":"Override path summary","start":127,"end":141},{"slug":"maintenance","heading":"Maintenance","start":141,"end":152},{"slug":"see-also","heading":"See also","start":152,"end":164},{"slug":"the-cfg","heading":"The cfg","start":164,"end":646}],"body":"---\ntitle: \"Bundled Default Cfg — What the Container Ships\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - agent-kb\n  - v1.2-aligned\n  - non-canonical\n  - default-cfg\n  - container\n  - bundle\n  - upstream\n  - bsb\n  - charis\n  - phase-1\n  - source-of-truth\ntype: bundled_asset\nasset_id: bundled-default-cfg\nasset_version: \"1.0\"\nptxprint_version_origin: \"2.9.9-gc5521963\"\ncfg_schema_version: \"2.24\"\nupstream_provided: true\ncompanion_to:\n  - canon/articles/progressive-customization.md\n  - canon/articles/payload-construction.md\n  - canon/handoffs/bundle-default-cfg-handoff.md\ncanonical_status: non_canonical\ndate: 2026-04-30\nstatus: working\n---\n\n# Bundled Default Cfg\n\n> **What this answers.** What `ptxprint.cfg` does the Container ship with? What does it produce? When does an agent need to override it?\n>\n> **What this is.** The source of truth for the cfg the Container stages as the default when the caller's payload provides no `shared/ptxprint/Default/ptxprint.cfg` entry. The fenced INI block at the bottom is the literal file content. Container build copies from this article; the article is canonical, the build artifact is derived.\n\n---\n\n## Why this article exists\n\nPer `klappy://canon/principles/dry-canon-says-it-once`, the bundled cfg has exactly one home. That home is this article. The Container build pulls from here. If the upstream PTXprint team sends a new default, the cfg in this article is updated, the Container rebuilds, and every Phase-0 caller picks up the new defaults on the next render.\n\nIf the cfg lived in the Container repo only, agents reasoning about \"what does the default produce\" would have to inspect Container internals — opaque to the canon-driven retrieval pattern. By living here, the cfg is `docs`-discoverable.\n\n## What this cfg produces\n\nWhen a caller submits the smallest possible payload (`{books, sources}`) the Container stages this cfg at the project's Default config path, runs PTXprint, and emits:\n\n- **Two-column justified** A5 layout (148mm × 210mm)\n- **12pt margins**, 18pt top, 10.5pt bottom\n- **Charis** (regular / bold / italic / bold-italic) — resolved via the Container's bundled `fonts-sil-charis` package\n- **Drop cap** on chapter 1, regular chapter numbers thereafter\n- **Section heads in italic, parallel-passage references** below them in italic\n- **Footnotes** at column foot with autogenerated alphabetic callers (`a`, `b`, `c`, …)\n- **Cross-references** at column foot, separately ruled\n- **Mirror-layout running header**: book name + chapter on outer, page number on inner\n- **No front matter, no figures, no cover, no decorations**\n\nEmpirically validated: BSB John (one chapter through 21) renders to a 37-page PDF/X-4 in ~15 seconds container time, 0 errors, 0 overfull boxes. See `klappy://canon/handoffs/bundle-default-cfg-handoff.md` for the smoke-test contract.\n\n## Where this cfg comes from\n\nProvided by the upstream PTXprint team on 2026-04-30 as the cfg that ships with PTXprint by default in a real working BSB project. PTXprint version stamp embedded in the cfg: `2.9.9-gc5521963`. Cfg schema version: `2.24`.\n\nThe upstream team's framing: this is the default that will be in PTXprint. Adopting it as the Container's bundled fallback is therefore a forward-compatibility move, not a custom opinion. When upstream ships a new release with a different default, this article is updated to match.\n\n## Anatomy at a glance\n\nThe cfg has 25 INI sections (~474 lines). The keys that produce the render described above:\n\n| Section | Key | Value | What it does |\n|---|---|---|---|\n| `[project]` | `id` | `BSB` | Project namespacing label |\n| `[project]` | `book` | `JHN` | Single-book scope target |\n| `[project]` | `bookscope` | `single` | Render one book at a time |\n| `[project]` | `iffrontmatter` | `False` | No front matter |\n| `[project]` | `ifinclbackpdf` | `False` | No back matter |\n| `[project]` | `ifcolophon` | `True` | Colophon allowed (rendered if scope includes it) |\n| `[document]` | `fontregular` | `Charis\\|\\|false\\|false\\|` | Body font |\n| `[document]` | `script` | `Latn` | Latin script |\n| `[document]` | `parallelrefs` | `True` | Parallel-passage references on |\n| `[document]` | `cloptimizepoetry` | `True` | Poetry layout optimized |\n| `[document]` | `bookintro` | `True` | Book introduction rendered |\n| `[document]` | `multibook` | `False` | Single-book mode |\n| `[document]` | `ifinclfigs` | `False` | No figures |\n| `[document]` | `iffigshowcaptions` | `False` | (moot since no figures) |\n| `[paper]` | `pagesize` | `148mm, 210mm (A5)` | A5 paper |\n| `[paper]` | `columns` | `True` | Two-column body |\n| `[paper]` | `margins` | `12` | 12pt margins |\n| `[paper]` | `bottommargin` | `10.5` | 10.5pt bottom margin |\n| `[paper]` | `topmargin` | `18` | 18pt top margin |\n| `[paper]` | `fontfactor` | `9.5` | Body font scale |\n| `[paragraph]` | `linespacing` | `11.642` | Body line spacing |\n| `[paragraph]` | `ifjustify` | `True` | Justified body |\n| `[paragraph]` | `ifhyphenate` | `False` | Hyphenation off |\n| `[notes]` | `iffnautocallers` | `True` | Footnote callers auto-generated |\n| `[notes]` | `iffootnoterule` | `True` | Footnote rule on |\n| `[notes]` | `ifxrautocallers` | `True` | Cross-ref callers auto-generated |\n| `[notes]` | `ifxrefrule` | `True` | Cross-ref rule on |\n| `[fancy]` | `pageborders` | `False` | No page borders |\n| `[fancy]` | `sectionheader` | `False` | No fancy section headers |\n| `[fancy]` | `versedecorator` | `False` | No verse decorators |\n| `[snippets]` | `fancyborders` | `False` | No fancy borders |\n| `[header]` | `mirrorlayout` | `True` | Mirror-layout running header |\n| `[header]` | `hdrcenter` | `Page Number` | Page number centered |\n\nFor the full key list, the cfg block at the bottom of this article is the authority.\n\n## Known characteristics worth knowing as an agent\n\nThese aren't bugs — they're knowable behaviors of the bundle:\n\n1. **No `[vars]` section.** The cfg has no `[vars]` block. The PDF metadata `Title` field shows as `[Unknown]`. To set a real title, callers either add `[vars] maintitle = ...` via a full `config_files` cfg replacement, or override at run-time via `define = {\"maintitle\": \"...\"}` (preferred for one-shot use). See `klappy://canon/articles/progressive-customization`.\n\n2. **Hard-coded `book = JHN, bookscope = single`.** This cfg targets John specifically. PTXprint reads the payload's `books` array as the actual run scope, but the cfg's `book` field surfaces in metadata. For other books, callers either (a) ignore the discrepancy (cosmetic — not a render error), or (b) override via `define = {\"book\": \"ROM\"}` (PTXprint widget identifier). For multi-book renders, override `bookscope` and `booklist` via `define` or replace the cfg.\n\n3. **Underfill on light-content pages.** Tight typography (`linespacing 11.642`, `fontfactor 9.5`) leaves visible empty space at the bottom of columns where text doesn't fill. On BSB John this hits ~59% of pages (22 of 37). The PDF is valid; the layout is conservative. The canonical fix is `mode: autofill` (deferred). Interim mitigations available via `define`: `s_linespacing`, `c_preventwidows`, `c_preventorphans`.\n\n4. **License-vs-colophon contradiction.** `[project] colophontext` says \"dedicated to the public domain\"; `[project] license` says `CC BY-NC-ND 4.0`. The colophon text wins in the rendered PDF (when colophon material is in scope). Callers publishing under their own license should override `colophontext` and `license` via a `config_files` replacement. Surfaces only when front/back matter is enabled.\n\n5. **`[import]` section is for the import-settings GUI workflow, not for parent-config inheritance.** PTXprint's GUI uses `[import]` to track \"where I last imported settings from\"; it is **not** the same as the `[import] config = Parent` inheritance pattern documented in `klappy://canon/articles/config-inheritance-and-overrides`. Treat the section as opaque metadata; do not edit it to attempt inheritance.\n\n## Override path summary\n\n| What you want | How |\n|---|---|\n| Use the bundle as-is | Send `{books, sources}` only — empty `config_files` |\n| Change one knob | Add `define = {\"<widget_id>\": \"<value>\"}` |\n| Change many knobs / replace cfg | Add `config_files[\"shared/ptxprint/Default/ptxprint.cfg\"]` with your own cfg |\n| Different fonts | Add `fonts: [...]` array with URL+sha256 entries |\n| Custom stylesheet | Add `config_files[\"shared/ptxprint/Default/ptxprint.sty\"]` |\n| Add figures | Add `figures: [...]` array + a piclist in `config_files` |\n| Front matter | Add `config_files[\"shared/ptxprint/Default/FRTlocal.sfm\"]` and override `iffrontmatter` |\n\nThe full ladder lives in `klappy://canon/articles/progressive-customization`.\n\n## Maintenance\n\nWhen upstream provides an updated default cfg:\n\n1. Replace the fenced INI block at the bottom of this article with the new content.\n2. Bump `asset_version` and `ptxprint_version_origin` / `cfg_schema_version` in this article's frontmatter.\n3. Trigger a Container rebuild. The Container's build step reads this article's INI block at build time (or the canon-as-asset feed) and stages it at the install path.\n4. Run the smoke test in `klappy://canon/handoffs/bundle-default-cfg-handoff.md`.\n\nThe cfg is content-addressed by the Container build; same article SHA → same Container behavior.\n\n## See also\n\n- `klappy://canon/articles/progressive-customization` — the override ladder\n- `klappy://canon/articles/payload-construction` — payload schema\n- `klappy://canon/articles/config-construction` — when you need to write your own cfg\n- `klappy://canon/articles/config-inheritance-and-overrides` — the `[import] config = Parent` pattern (not used by this bundle)\n- `klappy://canon/articles/settings-cookbook` — intent-keyed `define` examples\n- `klappy://canon/handoffs/bundle-default-cfg-handoff.md` — Container-side implementation\n- `klappy://canon/templates/english-single-book` — Level-4 fully-customized example (Gentium fonts, Settings.xml, sty)\n\n---\n\n## The cfg\n\n```ini\n[import]\nproject = BSB\naddnewpics = True\nadvanced = False\nbody = True\ncaptions = False\nconfig = Default\ncopyright = True\ncover = False\ndeloldpics = False\neverything = False\nfontsscript = True\nfrontmatter = False\nheaderfooter = True\nimppics = entire\nimpsource = config\nimpsourcepdf = \nimptarget = prjcfg\nlayout = True\nnotesrefs = True\nother = True\noverrideallstyles = False\noverwitefrtmatter = False\npictures = True\nsizeposn = True\nstyles = True\ntabsborders = False\ntgtconfig = Default\ntgtproject = \n\n[color]\nspotcolrange = 15\n\n[config]\nautosave = True\ndisplayfontsize = 12\nfilterpics = False\ngitversion = 2.9.9-gc5521963\nlockui4layout = False\nlockxetexlayout = False\nname = Default\nnotes = \npdfzoomlevel = 166.142\nprintcount = 15\npwd = \ntexperthacks = False\nupdatepdf = True\nversion = 2.24\n\n[cover]\ncoveradjust = 0.14\ncoverartbleed = 5\ncoverbleed = 5\ncovercropmarks = True\nincludespine = False\nmakecoverpage = False\nmakeseparatepdf = True\noverridepagecount = False\npapercalcunits = weight\nrotatespine = 270\nrtlbookbinding = False\nspineoverlapback = 0\nspineoverlapfront = 0\nspinewidth = 34.526 mm\ntotalpages = 100\nweightorthick = 70\n\n[covergen]\nbordercolor = rgb(94,92,100)\nborderstyle = Han1\nimagealpha = 0.7\noverwriteperiphs = False\nshadingalpha = 0.5\nshadingcolor = rgb(255,255,255)\ntextcolor = rgb(0,0,0)\ntextscale = 1.7\nuseborder = False\nuseimage = False\nuseshading = False\n\n[document]\nbookintro = True\nch1pagebreak = False\nchapfrom = 1\nchapto = 21\nclabel = \nclabelbooks = \ncloptimizepoetry = True\ncolgutterfactor = 4\ncolumnshift = 12\ncustomfigfolder =\ncustomfiglocn = False\ndiffcolayout = False\ndiffcolayoutbooks = \ndiffpages = 20\ndiffpdf = \ndiglotadjcenter = False\ndiglotcolour = \ndiglotheaders = False\ndiglotjoinvrule = False\ndiglotlayout = \ndiglotmergemode = simple\ndiglotnotesrule = False\ndiglotsepnotes = True\ndiglotserialbooks = FRT BAK GLO XXA XXB\nelipsizemptyvs = False\nexclusivefolder = False\nfilterglossary = False\nfirstparaindent = True\nfontbold = Charis| Bold|false|false|\nfontbolditalic = Charis| Italic Bold|false|false|\nfontitalic = Charis| Italic|false|false|\nfontregular = Charis||false|false|\nglossarydepth = 0\nglossarymarkupstyle = no\nglueredupwords = False\nhangpoetry = False\nhidemptyverses = False\nifchaplabels = False\nifcolorfonts = False\nifdiglot = False\nifdiglotcolour = \niffigcrop = False\niffigexclwebapp = True\niffighiderefs = False\niffigplaceholders = False\niffigshowcaptions = False\niffigskipmissing = True\nifinclfigs = False\nifletter = True\niflinebreakon = False\nifmainbodytext = True\nifndiglot = False\nifomitverseone = True\nifrtl = ltr\nifshow1chbooknum = True\nifshowchapternums = True\nifshowversenums = True\nifspacing = True\nifusepiclist = False\nimagetypepref = \nincludeimg = False\nindentunit = 0.8\nintrooutline = True\nkeepversions = 1\nlettershrink = 2\nletterspace = True\nletterstretch = 0.5\nlinebreaklocale = \nmarginalposn = left\nmarginalverses = False\nmultibook = False\nndiffcolor = rgb(26,95,180)\nnoblankpage = False\nnogrid = False\nodiffcolor = rgb(204,0,0)\nonlyshowdiffs = True\npagebreakallchs = False\nparallelrefs = True\npicresolution = High\npreventorphans = False\npreventwidows = False\nprintarchive = False\nrulethickness = 0.4\nscript = Latn\nsectionheads = True\nsensitive = False\nsettingsinpdf = True\nshowxtrachapnums = False\nspaceshrink = 50\nspacestretch = 200\nstartpagenum = 1\nsubject = \ntoc = False\ntocleaders = 1\ntoctitle = \ntoptobottom = ltr\nusetoc1 = True\nusetoc2 = False\nusetoc3 = True\nvarlinespacing = False\n\n[fancy]\nbooktitleborder = False\nenableornaments = False\nendayah = False\nendofbook = False\nendofbookpdf = \npageborder = pdf\npageborderfullpage = False\npageborderpdf = \npageborders = False\npagebordertype = pdf\nsectionborder = legacy\nsectionbordertype = legacy\nsectionheader = False\nsectionheaderpdf = \nsectionheaderscale = 1\nsectionheadershift = 0\nversedecorator = False\nversedecoratorpdf = \nversedecoratorscale = 1\nversedecoratorshift = 0\nversedecoratortype = ayah\n\n[finishing]\nextractinserts = False\nextraxdvproc = False\nfoldcutmargin = 0\nfoldfirst = False\ninclsettings = True\npgsperspread = 1\nrtlpagination = False\nscaletofit = False\nsheetsinsigntr = 0\nsheetsize = 210mm, 297mm (A4)\nspotcolor = rgb(28,113,216)\nspottolerance = 15\n\n[footer]\nftrcenter = -empty-\nftrcenterside = Sec\nifftrtitlepagenum = True\nifprintconfigname = False\nnoinkinmargin = False\n\n[grid]\ndivisions = 12\ngridgraph = False\ngridlines = True\nmajorcolor = rgb(237,51,59)\nmajorthickness = 0.2\nminorcolor = rgb(98,160,234)\nminorthickness = 0.1\nunits = in\nxyadvance = 12\nxyoffset = page\n\n[header]\nchvseparator = colon\nhdrcenter = Page Number\nhdrcenterside = Pri\nhdrleft = -empty-\nhdrleftside = Pri\nhdrright = Reference Range\nhdrrightside = Pri\nifrhrule = False\nifshowbook = True\nifshowchapter = True\nifshowverse = False\nmirrorlayout = True\n\n[notes]\nabovenotespace = 5\nabovestudyspace = 5\nabovexrefspace = 6\naddcolon = False\nbelownoterulespace = 1\nbelowstudyrulespace = 1\nbelowxrefrulespace = 2\nfneachnewline = False\nfnomitcaller = True\nfnoverride = False\nfnpos = normal\nfnresetcallers = True\nfnruleindent = 0\nfnrulelength = 35\nfnruleposn = 1\nfnrulethick = 0.5\nfrverseonly = False\nglossaryfootnotes = False\nhoriznotespacemax = 25\nhoriznotespacemin = 5\niffnautocallers = True\niffootnoterule = True\nifstudynoterule = True\nifxrautocallers = True\nifxrefrule = True\nifxrexternalist = False\nincludefootnotes = True\nincludexrefs = True\ninternotespace = 3.5\nkeepbookwithrefs = True\nshowextxrefs = False\nsnruleindent = 0\nsnrulelength = 35\nsnruleposn = 1\nsnrulethick = 0.5\nxrcentrecolwidth = 60\nxrcolalign = False\nxrcolbottom = False\nxrcolrule = True\nxrcolside = 3\nxreachnewline = False\nxrextlistsource = standard_2\nxrfilterbooks = all\nxrguttermargin = 2\nxrlistsource = standard_2\nxrlocation = normal\nxromitcaller = True\nxroverride = False\nxrpos = normal\nxrresetcallers = True\nxrruleindent = 0\nxrrulelength = 100\nxrruleposn = 1\nxrrulethick = 0.5\nxrverseonly = False\n\n[paper]\nallowunbalanced = False\nbottommargin = 10.5\ncolgutteroffset = 3\ncolumns = True\ncropmarks = False\nfontfactor = 9.5\nfooterpos = 10.5\ngutter = 5\nheaderpos = 15\nheight = 148mm, 210mm (A5)\nifaddgutter = False\nifoutergutter = False\nifverticalrule = True\nifwatermark = False\nlockfont2baseline = False\nmajornlcolor = rgb(119,118,123)\nmargins = 12\nnotelines = False\npagesize = 148mm, 210mm (A5)\nrulegap = 0\ntopmargin = 18\nwatermarkpdf = \nwidth = 148mm, 210mm (A5)\n\n[paragraph]\nifhyphapproved = True\nifhyphenate = False\nifhyphlimitbks = False\nifjustify = True\nifnbhyphens = False\nifnothyphenate = False\nifomithyphen = False\nifsylhyphens = False\nifusefallback = False\nlinespacing = 11.642\nmissingchars = \n\n[poly]\nfraction = \n\n[project]\nautotaghebgrk = True\nbackincludes = \nbook = JHN\nbooklist = \nbookscope = single\ncanonicalise = True\ncolophontext = \\pc The Holy Bible, Berean Standard Bible, BSB is produced in cooperation with Bible Hub, Discovery Bible, OpenBible.com, and the Berean Bible Translation Committee. This text of God's Word has been dedicated to the public domain.\ncopyright = \nfrontincludes = \nid = BSB\nifcolophon = True\niffrontmatter = False\nifinclbackpdf = False\nifinclfrontpdf = False\nifusecustomsty = False\nifusemodssty = False\nifusemodstex = False\nifusepremodstex = False\ninclcoverperiphs = True\ninclmaps = False\ninterlang = \ninterlinear = False\ninterpunc = False\nlicense = This work is licensed under a Creative Commons Attribution-NonCommercial-NoDerivatives 4.0 International License (CC BY-NC-ND).\nmodulefile = \nperiphpagebreak = True\npgbreakcolophon = False\nplugins = \nprocessscript = False\nrandompicposn = True\nruby = False\nsectintros = False\nselectscript = \nselectxrfile = \nuilevel = \nusechangesfile = True\nwhen2processscript = before\n\n[scripts]\narab/lrcolon = False\ncjk/grid = False\ncjk/halfpunc = False\nindic/showhyphen = False\nindic/syllables = False\nmymr/syllables = False\n\n[slice]\nlength = 3\nmarker = \\m\nref = \nword = \n\n[snippets]\ndiglot = False\nfancyborders = False\nfancyintro = False\npdfoutput = Screen\n\n[strongsndx]\ndefinitions = False\nfallbackprj = \nkeyvrsrefs = True\nmajorlang = en\nndxbookid = XXS\nnocomments = False\nopenineditor = False\nraglines = 0\nrenderings = True\nshowall = False\nshowgreek = True\nshowhebrew = True\nshowindex = False\nshowintext = False\nshownums = True\nsourcelang = True\ntransliterate = False\ntwocols = True\nwildcards = keep\n\n[studynotes]\ncolgutterfactor = 4\ncolgutteroffset = 0\nfiltercats = False\nifverticalrule = False\nincludesidebar = False\nincludextfn = False\ninternote = 0\nshowcallers = False\ntxlinclquestions = False\ntxllangtag = en\ntxlnumbered = True\ntxloverview = False\ntxlshowrefs = False\n\n[thumbtabs]\nbackground = rgb(0,0,0)\ngroups = RUT 1SA; EZR NEH EST; ECC SNG; HOS JOL AMO OBA JON MIC; NAM HAB ZEP HAG ZEC MAL; GAL EPH PHP COL; 1TH 2TH 1TI 2TI TIT PHM; JAS 1PE 2PE 1JN 2JN 3JN JUD\nheight = 6\nifthumbtabs = False\nlength = 15\nnumtabs = 0\nrestart = False\nrotate = False\nrotatetype = 1\ntabsoddonly = False\nthumbtextmkr = toc3\n\n[viewer]\nfindcollisions = True\nfindrivers = True\nfindunbalanced = True\nfindwhitespace = True\nspacetoobig = 1.3\n\n[strongsvars]\nindex_book_title = Strong's Number Index\nhebrew_section_title = Hebrew Words\ngreek_section_title = Greek Words\nreverse_index_title = Reversal Index\n\n[texpert]\nriverthreshold = 3\n```\n\n---\n\n*This article is the source of truth for the bundled cfg. The Container build derives from it; nothing else does. When upstream sends a new default, this article is updated; the Container rebuilds; downstream callers pick it up on the next render.*\n"},{"path":"articles/changes-txt-format.md","uri":"klappy://canon/articles/changes-txt-format","title":"changes.txt Format — Regex USFM Transforms","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","changes-txt","regex","usfm-transforms","supporting-files"],"stability":"working","what_it_answers":"What's `changes.txt`? When does the agent edit it? What's the regex grammar?","sections":[{"slug":"before-anything-else-the-setting-you-must-flip","heading":"Before anything else: the setting you must flip","start":22,"end":26},{"slug":"what-changes-txt-is","heading":"What `changes.txt` is","start":26,"end":39},{"slug":"the-five-scoping-levels","heading":"The five scoping levels","start":39,"end":52},{"slug":"worked-examples","heading":"Worked examples","start":52,"end":102},{"slug":"regex-syntax-notes","heading":"Regex syntax notes","start":102,"end":125},{"slug":"include-directives","heading":"Include directives","start":125,"end":141},{"slug":"when-to-use-changes-txt","heading":"When to use `changes.txt`","start":141,"end":149},{"slug":"when-not-to-use-changes-txt","heading":"When NOT to use `changes.txt`","start":149,"end":155},{"slug":"testing-changes-txt-edits","heading":"Testing changes.txt edits","start":155,"end":165},{"slug":"including-in-the-payload","heading":"Including in the payload","start":165,"end":185},{"slug":"reading-existing-changes-txt-from-project-state","heading":"Reading existing changes.txt from project state","start":185,"end":195}],"body":"---\ntitle: \"changes.txt Format — Regex USFM Transforms\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"changes-txt\", \"regex\", \"usfm-transforms\", \"supporting-files\"]\nderives_from: \"canon/governance/headless-operations.md (Part 6 changes.txt subsection)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# changes.txt Format\n\n> **What this answers.** What's `changes.txt`? When does the agent edit it? What's the regex grammar?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/file-system-map`\n\n---\n\n## Before anything else: the setting you must flip\n\n`changes.txt` is ignored unless `[project] usechangesfile = True` is in the config's `ptxprint.cfg`. A payload that adds rules without it renders cleanly and applies none of them — no error, no warning (observed 2026-09-05; see `klappy://canon/articles/study-notes-and-footnotes`).\n\n## What `changes.txt` is\n\n`changes.txt` is a regex-based USFM transformation language applied to source USFM **before** typesetting. It's the single most useful supporting file for surgical text manipulation: change \"Jesus\" to a vernacular name throughout, swap a USFM marker at specific references, insert page breaks, etc.\n\nTwo scopes:\n\n```\nshared/ptxprint/changes.txt              project-wide (often `include`s a `PrintDraftChanges.txt` the team maintains)\nshared/ptxprint/<config>/changes.txt     config-specific (this run only)\n```\n\nBoth are processed; rules in both apply. The agent constructs the content and includes it in the payload's `config_files` map.\n\n## The five scoping levels\n\n```\n\"<pattern>\" > \"<replacement>\"                          # global\nat BKK \"<pattern>\" > \"<replacement>\"                   # book-restricted\nat BKK c \"<pattern>\" > \"<replacement>\"                 # book + chapter\nat BKK c:v \"<pattern>\" > \"<replacement>\"               # book + chapter + verse\nin '<context regex>': '<pattern>' > '<replacement>'    # environment-restricted\nat BKK c:v in '<ctx>': \"<pattern>\" > \"<replacement>\"   # combined book+chapter+verse + environment\n```\n\nEach line is a transformation. Lines starting with `#` are comments. Blank lines are ignored.\n\n## Worked examples\n\n### Global replacement\n\n```\n\"Jesus\" > \"Yesu\"\n```\n\nReplaces every occurrence of \"Jesus\" in every book with \"Yesu\". Use carefully — the pattern matches anywhere, including inside USFM markers.\n\n### Book-restricted\n\n```\nat GLO \"\\\\p \\\\k \" > \"\\\\ili \\\\k \"\n```\n\nIn the Glossary book only, change `\\p \\k ` to `\\ili \\k ` (different USFM markers). The `\\\\` is a regex-escaped backslash for the USFM marker.\n\n### Book + chapter\n\n```\nat JHN 2 \"grape juice\" > \"wine\"\n```\n\nIn John chapter 2 only.\n\n### Book + chapter + verse\n\n```\nat MAT 7:23 \"old text\" > \"new text\"\n```\n\nAt Matthew 7:23 only. Note the `:` between chapter and verse in the `at` clause (different from the `c.v` style used in piclists and AdjLists).\n\n### Environment-restricted\n\n```\nin '\\\\f .+?\\\\f\\\\*': 'Syria' > 'Aram'\n```\n\nIn any footnote (`\\f ... \\f*`), replace `Syria` with `Aram`. Doesn't change body text.\n\n### Combined: at-clause + in-clause\n\n```\nat LUK 3:10 in '\\\\f .+?\\\\f\\\\*': \"old\" > \"new\"\n```\n\nIn footnotes within Luke 3:10 only.\n\n## Regex syntax notes\n\n`changes.txt` uses standard regex syntax (effectively Python's `re` with some PTXprint extensions). Key things:\n\n| Pattern | Matches |\n|---|---|\n| `\\\\` | literal backslash (USFM markers contain backslashes; `\\\\` in a regex is one literal `\\`) |\n| `\\\\p` | the USFM `\\p` marker |\n| `\\\\f .+?\\\\f\\\\*` | a complete footnote, non-greedy |\n| `[^\\\\]` | any character except backslash |\n| `(?:...)` | non-capturing group |\n| `(...)` | capturing group; reference in replacement as `\\1`, `\\2` |\n\n**Common mistake:** forgetting that USFM markers begin with `\\`. To match `\\p`, you write `\\\\p` in the regex.\n\n### Capturing groups in replacement\n\n```\n\"\\\\f \\\\+ ([^\\\\]+)\\\\f\\\\*\" > \"\\\\f - \\\\1\\\\f*\"\n```\n\nCaptures the footnote text (everything between `\\f +` and `\\f*`), reproduces it after `\\f - ` (changes the caller from `+` to `-`).\n\n## Include directives\n\n```\ninclude \"PrintDraftChanges.txt\"\ninclude \"../../../PrintDraftChanges.txt\"\n```\n\nThe `include` directive pulls in another file's rules. Common pattern: the project-wide `shared/ptxprint/changes.txt` includes a team-maintained `PrintDraftChanges.txt` from the project root, and the config-specific `changes.txt` adds run-specific rules.\n\nIn the v1.2 payload model, the agent has two choices:\n\n- Include the included content **inline** in the payload's `changes.txt` content (flatten).\n- Include both files in `config_files` at their relative paths (preserve the `include` directive).\n\nFlattening is simpler when the team-maintained file is small. Preserving structure is better when the team-maintained file is the source of truth and the agent is only adding config-specific rules.\n\n## When to use `changes.txt`\n\n- **Temporary fix without round-tripping to Paratext** — the team can leave the source USFM unchanged.\n- **Cross-cutting changes** — replace all instances of a name, fix a recurring typo without 50 individual edits.\n- **USFM markup adjustment** — change `\\p` to `\\nb` at specific locations to control paragraph behavior.\n- **Inserting ornament rules** — insert `\\zrule` markers under all main titles.\n- **Inserting page breaks** at specific references.\n\n## When NOT to use `changes.txt`\n\n- **The change should be permanent in source.** Let the team make it in Paratext. `changes.txt` is for typesetting-time tweaks, not source-of-truth corrections.\n- **The agent is uncertain about the regex.** Test on a single book first. The failure mode is silent text corruption — wrong characters appear in the PDF and the agent doesn't notice.\n- **The change touches many places.** A broad regex with unintended matches produces unpredictable output. Prefer narrow scoping (`at BKK c:v`).\n\n## Testing changes.txt edits\n\nThe agent's standard flow for a `changes.txt` change:\n\n1. Submit a payload typesetting **one short book** (JUD, 3JN, PHM all under 1 chapter) with the new rule.\n2. Inspect the result. Verify the rule fired where expected and didn't fire where it shouldn't.\n3. Once confirmed on the test book, submit the full-scope run.\n\nSkip step 1 only when the rule is so narrow (e.g. `at JHN 3:16`) that the test book wouldn't exercise it. Even then, prefer to test against the actual book the rule targets.\n\n## Including in the payload\n\n```json\n\"config_files\": {\n  \"shared/ptxprint/Default/changes.txt\": \"# Config-specific transforms\\nat MAT 5:3 \\\"old\\\" > \\\"new\\\"\\n\",\n  \"shared/ptxprint/changes.txt\": \"# Project-wide transforms\\ninclude \\\"PrintDraftChanges.txt\\\"\\n\\\"Jesus\\\" > \\\"Yesu\\\"\\n\"\n}\n```\n\nFor the included `PrintDraftChanges.txt`, either flatten its content into the project-wide `changes.txt` or include the file at its referenced path:\n\n```json\n\"config_files\": {\n  ...\n  \"PrintDraftChanges.txt\": \"# Team-maintained transforms\\n\\\"common typo\\\" > \\\"correction\\\"\\n\"\n}\n```\n\nVerify the `include` directive's relative path resolves to an actual key in `config_files`, or PTXprint will fail to find the included file.\n\n## Reading existing changes.txt from project state\n\nMost projects have established `changes.txt` content the team maintains. The agent reads it from project state, includes it unchanged unless the user is explicitly editing, and adds new rules at the bottom rather than reordering.\n\nIf the user provides a new rule, surface where it will go (\"I'll add this to the config-level `changes.txt`, not the project-wide one — that way it only applies to this configuration\") and confirm before submitting.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/usfm-markers-headless` for the marker reference (helps in writing regex patterns).*\n"},{"path":"articles/cli-reference.md","uri":"klappy://canon/articles/cli-reference","title":"CLI Reference — How the Worker Invokes PTXprint","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","cli","flags","exit-codes"],"stability":"working","what_it_answers":"What flags does PTXprint's CLI accept? How do payload fields map to those flags? What do the exit codes mean?","sections":[{"slug":"in-v1-2-the-agent-does-not-invoke-this-directly","heading":"In v1.2, the agent does not invoke this directly","start":22,"end":41},{"slug":"full-cli-signature","heading":"Full CLI signature","start":41,"end":56},{"slug":"flag-reference","heading":"Flag reference","start":56,"end":69},{"slug":"exit-codes","heading":"Exit codes","start":69,"end":80},{"slug":"the-d-runtime-override-mechanism","heading":"The `-D` runtime override mechanism","start":80,"end":111},{"slug":"what-the-worker-adds-beyond-the-payload","heading":"What the worker adds beyond the payload","start":111,"end":125},{"slug":"what-the-agent-must-not-include-in-the-payload","heading":"What the agent must NOT include in the payload","start":125,"end":131},{"slug":"when-the-user-asks-about-cli-directly","heading":"When the user asks about CLI directly","start":131,"end":151}],"body":"---\ntitle: \"CLI Reference — How the Worker Invokes PTXprint\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"cli\", \"flags\", \"exit-codes\"]\nderives_from: \"canon/governance/headless-operations.md (Part 1 The CLI Reference)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# CLI Reference\n\n> **What this answers.** What flags does PTXprint's CLI accept? How do payload fields map to those flags? What do the exit codes mean?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/failure-mode-taxonomy`\n\n---\n\n## In v1.2, the agent does not invoke this directly\n\nThe Container worker invokes PTXprint on the agent's behalf. This reference exists because the payload's fields map directly to CLI flags, and understanding the CLI surface helps the agent construct payloads that work.\n\nThe worker translates the payload approximately as follows:\n\n| Payload field | Becomes |\n|---|---|\n| `project_id: \"WSG\"` | `-P WSG` |\n| `config_name: \"Default\"` | `-c Default` |\n| `books: [\"JHN\"]` | `-b \"JHN\"` |\n| `books: [\"MAT\",\"MRK\",\"LUK\",\"JHN\"]` | `-b \"MAT MRK LUK JHN\"` |\n| `define: { \"s_linespacing\": \"14\" }` | `-D s_linespacing=14` |\n| `define: { ..., \"c_fighiderefs\": \"True\" }` | `-D s_linespacing=14 -D c_fighiderefs=True` (one `-D` per key) |\n| `mode: \"autofill\"` | (autofill flag — exact name to confirm at implementation time) |\n| `timeout: 1800` | `--timeout 1800` |\n\nThe worker also adds `-p <scratch_dir>` (pointing at the materialized project tree) and `-q` (quiet mode).\n\n## Full CLI signature\n\n```\nptxprint -P <prjid> [-c <config_name>] [-b \"<books>\"]\n                    [-p <projects_dir>] [-q]\n                    [-D key=value ...]\n                    [--timeout <seconds>]\n```\n\nThe deck's example invocation:\n\n```\nptxprint.exe -b ROM -c SingleSpaceDraft -P XYZ\n```\n\n## Flag reference\n\n| Flag | Required | Meaning |\n|---|---|---|\n| `-P <prjid>` | yes | Paratext project ID. The worker materializes a directory with this name from the payload. |\n| `-c <config>` | no (defaults to `Default`) | Named configuration to use |\n| `-b \"<codes>\"` | no | Space-separated USFM book codes; overrides the cfg's `[project] booklist` for this run only |\n| `-p <path>` | no | Projects root directory; the worker sets this to its scratch dir |\n| `-q` | no | Quiet; suppresses splash and any GUI artifacts |\n| `-D key=value` | no, repeatable | Override any UI setting at runtime without modifying the stored cfg |\n| `--timeout <s>` | no (defaults to 1200) | Maximum seconds to wait for XeTeX |\n| `-h` | no | Print all arguments and exit |\n\n## Exit codes\n\n| Code | Meaning | Typical cause |\n|---|---|---|\n| 0 | Success | PDF was produced |\n| 1 | Startup failure | Project not found, bad config path, missing dependency |\n| 3 | XeTeX completed but produced no PDF | XeTeX errors prevented final write |\n| 4 | XeTeX returned non-zero | Typesetting errors during macro processing |\n\nA non-zero exit code is necessary but not sufficient evidence of failure. A zero exit code is necessary but not sufficient evidence of success. The worker classifies into hard/soft/success — see `klappy://canon/articles/failure-mode-taxonomy`.\n\n## The `-D` runtime override mechanism\n\n`-D key=value` accepts any PTXprint widget identifier and value, applied at runtime without persisting to `ptxprint.cfg`. The agent populates the payload's `define` field with key/value pairs; the worker translates them into `-D` flags.\n\n### Widget identifier conventions\n\n| Prefix | Type | Example |\n|---|---|---|\n| `s_` | string or numeric | `s_linespacing`, `s_fontsize` |\n| `c_` | checkbox / boolean | `c_fighiderefs`, `c_disablehyphen` |\n| `r_` | radio / enum | `r_outputformat` |\n| `t_` | text-area | `t_headertext` |\n\nPrefixes are not 100% consistent across all widgets. Always verify against canon's cfg-key index (seeded from the running tooltip dump) before relying on a specific name.\n\n### Boolean values\n\nUse `True` / `False` (capitalized). Lowercase `true`/`false` may or may not be accepted depending on the widget; capitalized is safest.\n\n### When `-D` (i.e., `define`) wins\n\n- The override is for this run only — the user is iterating, exploring, or producing a one-off\n- The override should not pollute the saved config\n\n### When the cfg wins (i.e., `config_files`)\n\n- The change should persist to the user's project state\n- The user has committed to the change\n\nIf both are set for the same key, `-D` (define) wins for this specific run; the cfg keeps its old value for future runs that don't override.\n\n## What the worker adds beyond the payload\n\nThe worker invokes PTXprint with:\n\n- `-P <project_id>` — from payload\n- `-c <config_name>` — from payload (or default `Default` if not provided)\n- `-b \"<books>\"` — from payload's `books` array, joined with spaces\n- `-p <scratch_dir>` — the worker's per-job temporary directory (the agent never sees this path)\n- `-q` — always quiet\n- `-D <key>=<value>` — one per entry in payload's `define`\n- `--timeout <s>` — from payload's `timeout` (or default per spec)\n\nThe worker handles paths, temporary directory creation, and cleanup. The agent doesn't.\n\n## What the agent must NOT include in the payload\n\n- `-D` keys referencing settings that don't exist (silently ignored — see *Common cfg-construction mistakes* in `klappy://canon/articles/config-construction`)\n- Negative timeouts or zero timeouts\n- `--<flag>` style overrides outside what the v1.2 schema allows\n\n## When the user asks about CLI directly\n\nA user familiar with PTXprint's GUI may ask about the CLI (\"can I just run `ptxprint.exe -b ROM -c MyConfig -P XYZ`?\"). The answer:\n\n> \"Yes — that command runs PTXprint locally. The MCP server runs the same CLI inside a Container worker, but you don't invoke it directly. Instead, you construct a payload that describes the same inputs (project, config, books, plus overrides), and the worker translates.\"\n\nThe mapping is:\n\n| Local CLI invocation | Payload |\n|---|---|\n| `ptxprint.exe -b ROM -c MyConfig -P WSG` | `{ project_id: \"WSG\", config_name: \"MyConfig\", books: [\"ROM\"], ... }` |\n| `... -D s_linespacing=14` added | `define: { \"s_linespacing\": \"14\" }` added |\n| `... --timeout 1800` added | `timeout: 1800` added |\n\nThe user's mental model from local CLI experience transfers cleanly.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`.*\n"},{"path":"articles/composition-and-templates.md","uri":"klappy://canon/articles/composition-and-templates","title":"Templates and Composition — How Clients Bootstrap a submit_typeset Payload","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","composition","templates","pattern","bootstrap","quickstart","bt-servant","downstream-agent"],"stability":"working","what_it_answers":"A downstream agent (BT Servant or any LLM client) needs to construct a working `submit_typeset` payload but doesn't want to figure out PTXprint's 400+ cfg keys, sty syntax, and font manifest from scratch. Templates are the answer: pre-baked partial payloads, retrievable via the `docs` tool, that the client merges with its own minimal inputs.","sections":[{"slug":"the-composition-pattern","heading":"The composition pattern","start":33,"end":62},{"slug":"how-a-client-uses-a-template-mcp-only-flow","heading":"How a client uses a template (MCP-only flow)","start":62,"end":86},{"slug":"discovery-how-to-find-available-templates","heading":"Discovery — how to find available templates","start":86,"end":107},{"slug":"where-templates-live","heading":"Where templates live","start":107,"end":119},{"slug":"adding-a-new-template","heading":"Adding a new template","start":119,"end":135},{"slug":"what-this-pattern-is-and-isn-t","heading":"What this pattern is and isn't","start":135,"end":143},{"slug":"vodka-compliance-the-constraint-test","heading":"Vodka compliance — the constraint test","start":143,"end":158},{"slug":"validation-principles","heading":"Validation principles","start":158,"end":169},{"slug":"see-also","heading":"See also","start":169,"end":179}],"body":"---\ntitle: \"Templates and Composition — How Clients Bootstrap a submit_typeset Payload\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - agent-kb\n  - v1.2-aligned\n  - non-canonical\n  - composition\n  - templates\n  - pattern\n  - bootstrap\n  - quickstart\n  - bt-servant\n  - downstream-agent\nderives_from: \"klappy://canon/specs/ptxprint-mcp-v1.2-spec, klappy://canon/articles/payload-construction\"\ncanonical_status: non_canonical\ndate: 2026-04-30\nstatus: draft_for_operator_review\n---\n\n# Templates and Composition\n\n> **What this answers.** A downstream agent (BT Servant or any LLM client) needs to construct a working `submit_typeset` payload but doesn't want to figure out PTXprint's 400+ cfg keys, sty syntax, and font manifest from scratch. Templates are the answer: pre-baked partial payloads, retrievable via the `docs` tool, that the client merges with its own minimal inputs.\n>\n> **What this is not.** Templates are not \"official PTXprint configurations.\" They are agent-facing bootstrap surfaces — clean baselines that produce nice-looking PDFs for common use cases. Anything beyond a template requires understanding the underlying payload schema (see `klappy://canon/articles/payload-construction`).\n\n---\n\n## The composition pattern\n\nA `submit_typeset` payload has two kinds of fields:\n\n**Caller-owned** (the agent always knows or can derive these):\n- `books` — USFM 3-letter book code(s)\n- `sources` — URL + sha256 of the USFM file(s) the caller wants rendered\n- `vars.maintitle` — what to put on the title page\n- `project_id`, `config_name` — namespacing labels\n\n**Template-owned** (the agent doesn't want opinions about these):\n- `config_files` — the cfg, sty, and Settings.xml content\n- `fonts` — the font manifest with URLs + hashes\n- All the cfg defaults that produce a particular look (margins, line spacing, parallel-passage refs, decorations off, etc.)\n\nA *template* is a partial payload containing only the template-owned fields. The client retrieves the template, lays its caller-owned fields on top, and submits the merged result.\n\n```\ntemplate (partial payload)\n    +\ncaller-owned fields (books, sources, vars)\n    =\nfull submit_typeset payload\n```\n\nThat merged result is what the worker hashes and runs. Same merged payload always returns the same `job_id` (content-addressed), so re-rendering is free.\n\n---\n\n## How a client uses a template (MCP-only flow)\n\n1. **Discover.** Call `docs(query=\"<describe what you want to render>\", depth=2)` — see \"Discovery\" below for examples. The tool returns the full top-ranked template article including a fenced `json` code block with the partial payload.\n\n2. **Extract.** Pull the JSON code block out of the markdown response. One line of string handling.\n\n3. **Merge.** Layer the client's `{books, sources, vars}` on top of the template:\n\n   ```python\n   merged = template.copy()\n   merged[\"books\"] = [\"JHN\"]\n   merged[\"sources\"] = [{\"book\": \"JHN\", \"filename\": \"...\", \"url\": \"...\", \"sha256\": \"...\"}]\n   # vars is nested inside config_files's cfg INI — see the template article for\n   # the exact pattern, since the caller-owned vars sit inside an embedded INI string\n   ```\n\n   The shallow merge handles `books`, `sources`, top-level fields. The `vars` overlay requires editing the cfg INI text inside `config_files`, which the per-template article documents.\n\n4. **Submit.** Call `submit_typeset(payload=merged)`. Receive `job_id`. Poll `get_job_status` until `state == \"succeeded\"`.\n\nNo new MCP tool is required. No HTTPS fetch outside MCP is required. No repo access is required. Just `docs` + `submit_typeset` + `get_job_status`.\n\n---\n\n## Discovery — how to find available templates\n\nThe list of templates is **not** maintained as a static index. Static indexes drift the moment a template is added without updating the index, and a stale index is a silent liar. Discovery is tool-driven instead, so the answer is always live.\n\n**To find templates that match your render task**, query `docs` with the use case in natural language:\n\n| If you want to render… | Try a query like |\n|---|---|\n| A single English Bible book | `docs(query=\"english single book template\", depth=2)` |\n| (future) An English NT or whole Bible | `docs(query=\"english multi book bible\", depth=2)` |\n| (future) A poetry-heavy book (Psalms, Proverbs) | `docs(query=\"english poetry book template\", depth=2)` |\n| (future) A diglot | `docs(query=\"diglot template\", depth=2)` |\n\n`depth=2` returns the full top document. If the top result isn't what you wanted, `depth=3` returns the top result plus the next two ranked docs in full.\n\n**To enumerate every template** (e.g. to populate a UI picker), use the canon catalog tool against the PTXprint MCP knowledge base — for example via `oddkit_catalog` with the PTXprint MCP repo as the knowledge base. Filter the result list to paths starting with `canon/templates/`. The filesystem is authoritative.\n\nThe natural-language query is the path most agents will use. The catalog enumeration is for tooling that needs the complete list.\n\n---\n\n## Where templates live\n\n`canon/templates/` in the PTXprint MCP repo. Each template is a single markdown file with:\n\n- Frontmatter declaring `template_id`, `template_version`, `caller_supplies` list, `applies_to` description, `status`\n- Narrative sections — when to use, what's in the template, what the caller controls, what to *not* change, validation contract, known limitations\n- A fenced `json` code block containing the partial payload\n\nThe `docs` tool indexes everything under `canon/`, so templates surface naturally to the queries shown above. The `status` field in each template's frontmatter signals maturity: `draft_for_operator_review`, `working`, `stable`, etc.\n\n---\n\n## Adding a new template\n\nThe growth model: an agent (or a human) discovers a new \"common shape\" — a poetry book, a diglot, a multi-book NT, a non-A5 format, a non-English language — and adds a template for it.\n\n1. **Identify the shape.** What use case does this template cover? What makes it distinct from existing templates? Search `docs` first to confirm a similar template doesn't already exist.\n2. **Distill from a working render.** Templates start as a successful smoke fixture or empirical render. Strip the caller-owned fields (`books`, `sources`, `vars` values that vary per render) — leave the keys but blank the values so the structure is obvious.\n3. **Write the article.** Create `canon/templates/<template-id>.md`. Frontmatter declares `template_id`, `template_version` (start at `0.1`), `caller_supplies`, `applies_to`, `derived_from` (which smoke fixture or commit the template was distilled from), `status: draft_for_operator_review`.\n4. **Document the merge contract.** What fields the caller MUST supply, what they MAY override, what's in the template that they shouldn't change. Validation expectations. Known limitations.\n5. **Embed the partial payload.** A single fenced `json` code block at the bottom of the article. Caller-owned fields absent or empty — never hardcoded to a real value.\n6. **Open a PR.** No need to update an index — discovery is tool-driven.\n7. **Validate.** Post-merge, a fresh-context call to `docs` should surface the new template. A merged payload (template + a representative caller input) submitted via `submit_typeset` should return a successful render. When validated, bump `template_version` to `1.0` and `status` to `working`.\n\nThe catalog scales with use cases. The worker doesn't change. The schema doesn't change.\n\n---\n\n## What this pattern is and isn't\n\n**Is.** A documented, vodka-compliant convention for partial-payload + caller-overlay composition. Mirrors well-established overlay patterns (Kustomize bases, Helm chart values, OpenAPI `$ref` components, dotfiles, terraform modules). The novelty is local — naming the pattern and committing to it — not the pattern itself.\n\n**Isn't.** A schema-level feature. The worker has zero knowledge of templates. A \"template\" is just a markdown file in canon that happens to contain a partial payload; the worker only sees the merged payload after the client submits it. If templates disappeared tomorrow, the worker would not notice.\n\n---\n\n## Vodka compliance — the constraint test\n\n| Constraint | This pattern's posture |\n|---|---|\n| **Server holds zero domain opinion** | Worker has no template-aware code. `submit_typeset` runs whatever payload the client sends. ✅ |\n| **DRY — canon says it once** | Each template's content lives in exactly one file. Discovery is computed live from the filesystem, never duplicated in a hand-maintained index. Smoke fixtures should be regenerated from templates + known inputs (validation closes the DRY loop). ✅ |\n| **No new tool surface** | Existing `docs` retrieves; existing `submit_typeset` runs. ✅ |\n| **Reversible** | Templates are markdown files. Delete the directory and the only consequence is loss of the bootstrap surface; nothing else breaks. ✅ |\n| **No content embedded in governance articles** | This article describes the pattern; it has zero embedded payload. The template articles ARE the templates — their payload is their substance, not embedded supplementary content. ✅ |\n| **No lie-in-wait static indexes** | Discovery is tool-driven (`docs` query, catalog enumeration). The filesystem is authoritative; nothing duplicates it. ✅ |\n\nThe split-of-concerns: governance describes *when* and *how*; templates contain the *what*. They share the directory `canon/` because both are knowledge-base content, but they live in separate folders (`canon/articles/` vs `canon/templates/`) so the distinction is structural, not stylistic.\n\n---\n\n## Validation principles\n\nA template is valid when:\n1. It produces a successful render (`failure_mode == \"success\"`) when merged with a representative caller payload.\n2. The PDF output is what the template's `applies_to` description promises.\n3. The merge contract documented in the template article is correct — i.e., applying the documented caller-owned fields to the partial payload produces a payload the worker accepts.\n\nThe smoke fixtures in `smoke/` should be regenerated periodically from the current templates plus their known inputs. When a template and its smoke fixture diverge, the smoke fixture wins for the moment but the template is the bug to fix.\n\n---\n\n## See also\n\n- `klappy://canon/articles/payload-construction` — reference for what's in the payload schema\n- `klappy://canon/articles/font-resolution` — how the `fonts` array is materialized\n- `klappy://canon/specs/ptxprint-mcp-v1.2-spec` — the spec templates conform to\n\n---\n\n*This article describes the pattern. The `canon/templates/` folder contains the data. Both are in canon; neither contains the other; nothing lists the other.*\n"},{"path":"articles/config-construction.md","uri":"klappy://canon/articles/config-construction","title":"Config Construction — Building a ptxprint.cfg from Scratch or Existing State","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","ptxprint-cfg","config-construction","ini"],"stability":"working","what_it_answers":"What's in `ptxprint.cfg`? Which sections do I touch most? How do I create a new config? When do I use `define` vs editing the cfg?","sections":[{"slug":"what-ptxprint-cfg-is","heading":"What `ptxprint.cfg` is","start":24,"end":44},{"slug":"sections-the-agent-touches-most-often","heading":"Sections the agent touches most often","start":44,"end":63},{"slug":"sections-to-leave-alone-unless-you-know-exactly-what-you-re-doing","heading":"Sections to leave alone unless you know exactly what you're doing","start":63,"end":76},{"slug":"key-settings-in-project","heading":"Key settings in `[project]`","start":76,"end":89},{"slug":"creating-a-new-config","heading":"Creating a new config","start":89,"end":123},{"slug":"when-to-use-define-runtime-override-vs-editing-configfiles","heading":"When to use `define` (runtime override) vs. editing `config_files`","start":123,"end":145},{"slug":"modifying-an-existing-config","heading":"Modifying an existing config","start":145,"end":158},{"slug":"common-cfg-construction-mistakes","heading":"Common cfg-construction mistakes","start":158,"end":186},{"slug":"when-a-config-is-ready-to-share","heading":"When a config is \"ready to share\"","start":186,"end":202}],"body":"---\ntitle: \"Config Construction — Building a ptxprint.cfg from Scratch or Existing State\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"ptxprint-cfg\", \"config-construction\", \"ini\"]\nderives_from: \"canon/governance/headless-operations.md (Part 3 Configuration Model, Part 1 -D mechanism)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n> **When this article applies.** The Container ships with a publication-quality default cfg (see `klappy://canon/articles/bundled-default-cfg`), so a payload of `{books, sources}` produces a finished A5 two-column Bible book without the agent authoring any cfg content at all. Read this article when the user wants something the bundled cfg does not give them — a different page size, a different layout, multi-book mode, or any change that requires the agent to construct or replace `config_files[\"shared/ptxprint/<config>/ptxprint.cfg\"]`. That is **Level 2** of the customization ladder (`klappy://canon/articles/progressive-customization`); the levels below (`define`-only tweaks, font swaps) do not require the content below. The cfg-replacement contract this article describes is live, not a future target.\n\n# Config Construction\n\n> **What this answers.** What's in `ptxprint.cfg`? Which sections do I touch most? How do I create a new config? When do I use `define` vs editing the cfg?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/config-inheritance-and-overrides` · `klappy://canon/articles/settings-cookbook` · `klappy://canon/articles/cli-reference`\n\n---\n\n## What `ptxprint.cfg` is\n\nA standard sectioned INI file (Python `configparser`-compatible) with about 25 sections and ~400 keys. Multi-line values use tab/space continuation. The agent constructs or modifies this content as text and includes it in the payload's `config_files` map at the appropriate path.\n\nA minimal `ptxprint.cfg` looks like:\n\n```ini\n[config]\nname = Default\n\n[paper]\npagesize = 148mm, 210mm (A5)\nmargins = 12\n\n[paragraph]\nlinespacing = 14\n```\n\nPTXprint fills in defaults for everything not specified. Most user-facing changes touch a small subset of sections.\n\n## Sections the agent touches most often\n\n| Section | What it controls | Common user asks |\n|---|---|---|\n| `[project]` | Book scope, copyright, plugins, scripts | \"Print only Romans\"; \"Add my project's copyright line\" |\n| `[paper]` | Page size, margins, column gutters | \"Make it A5\"; \"Wider margins\" |\n| `[document]` | Body fonts, columns, diglot mode, figures, TOC, script direction | \"Bigger text\"; \"Two columns\"; \"Right-to-left\" |\n| `[paragraph]` | Hyphenation, justification, line spacing | \"Tighter line spacing\"; \"Disable hyphenation\" |\n| `[notes]` | Footnote and cross-reference placement and formatting | \"Footnotes at end of chapter\"; \"Cross-refs in centre column\" |\n| `[header]` | Header content, chapter/verse range display | \"Custom header text\"; \"Show chapter range\" |\n| `[footer]` | Footer content and layout | \"Page numbers in footer\" |\n| `[fancy]` | Page borders, ornaments, verse decorators | \"Decorative border on cover\" |\n| `[finishing]` | PDF imposition, booklet output, spot colour | \"Make a booklet\"; \"Black-and-white output\" |\n| `[vars]` | User-defined substitution variables for `\\zvar\\|name\\*` | \"Add a variable for the imprint year\" |\n| `[diglot_L]` / `[diglot_R]` / `[diglot_A]` / `[diglot_B]` | Diglot/polyglot column settings | \"Set up the secondary text column\" |\n| `[import]` | Config inheritance | (see config-inheritance-and-overrides) |\n\nFor specific intent → setting mappings, see `klappy://canon/articles/settings-cookbook`.\n\n## Sections to leave alone unless you know exactly what you're doing\n\n| Section | Reason for caution |\n|---|---|\n| `[texpert]` | Expert XeTeX settings; bad values produce silent layout breakage |\n| `[scripts]` | Script-specific features for CJK, Indic, Arabic, Myanmar; need script knowledge |\n| `[strongsndx]` / `[strongsvars]` | Strong's index generation; complex interactions with Biblical Terms data |\n| `[studynotes]` | Study Bible sidebar layout; high blast radius |\n| `[grid]` | Layout debugging grid; only useful interactively |\n| `[thumbtabs]` | Thumb tab layout; under-documented |\n\nIf the user explicitly asks for one of these, walk them through the change carefully and test on a single short book before committing.\n\n## Key settings in `[project]`\n\n| Key | Values | Meaning |\n|---|---|---|\n| `bookscope` | `single` \\| `booklist` \\| `module` | What to print |\n| `book` | USFM code e.g. `JHN` | Used when `bookscope = single` |\n| `booklist` | Space-separated codes e.g. `MAT MRK LUK` | Used when `bookscope = booklist` |\n| `plugins` | Comma-separated plugin names | Optional plugins to activate |\n| `selectscript` | Path to a `.py` file | Python pre/post-processor script |\n| `when2processscript` | `before` \\| `after` | Whether script runs before or after `changes.txt` |\n\nNote: the payload's top-level `books` field overrides `[project] book` / `[project] booklist` for the run. The agent typically sets `books` in the payload and leaves `[project]` alone unless the user wants the change to persist in the saved config.\n\n## Creating a new config\n\nIn v1.2, creating a new config is an agent-side operation against project state, not a server operation:\n\n1. **Update the working representation of the project** to include a new config folder under `shared/ptxprint/<NewName>/`.\n\n2. **Write a minimal `ptxprint.cfg` for the new config:**\n   ```ini\n   [config]\n   name = NewName\n\n   [import]\n   config = Default\n   ```\n   That's it. PTXprint inherits everything else from the parent.\n\n3. **When the user wants to typeset using the new config**, construct a payload with `config_name = \"NewName\"` and include both the new config's files and the parent config's files in `config_files`:\n   ```json\n   \"config_files\": {\n     \"shared/ptxprint/NewName/ptxprint.cfg\": \"[config]\\nname = NewName\\n[import]\\nconfig = Default\\n[paper]\\npagesize = ...\\n\",\n     \"shared/ptxprint/Default/ptxprint.cfg\": \"<existing parent content>\",\n     \"shared/ptxprint/Default/ptxprint.sty\": \"<existing parent content>\"\n   }\n   ```\n   The worker materializes both; PTXprint resolves inheritance.\n\n4. **After successful typesetting**, the agent persists the new config back to project state (Git commit, file write via Claude Desktop, etc.) so it's available next session.\n\n### What to avoid\n\n- **Full copies of the parent.** Defeats inheritance. Becomes unmaintainable.\n- **Configs for one-time tweaks.** That belongs as a `define` runtime override.\n- **Names like `Default-12pt-spacing`.** Name configs after their **purpose** (`DraftReview`, `FinalNT`, `JournalingEdition`), not their settings.\n\n## When to use `define` (runtime override) vs. editing `config_files`\n\nThe payload's `define` field maps to PTXprint's `-D key=value` flags — runtime overrides applied without persisting to `ptxprint.cfg`.\n\n| Use `define` | Use `config_files[\"...ptxprint.cfg\"]` |\n|---|---|\n| Producing a one-off draft with different spacing | Committing to a finalised configuration |\n| Exploring options without persisting | The change should round-trip back to the user's project store |\n| Parameterised runs (rotating output formats) | The change is part of the user's saved working state |\n| The user is unsure and wants to compare | The user has decided |\n\nCommon pattern: explore with `define`, then promote to `config_files` once committed.\n\n`define` keys are PTXprint widget identifiers, not cfg keys directly:\n\n- `s_<name>` — string or numeric setting (`s_linespacing`, `s_fontsize`)\n- `c_<name>` — checkbox / boolean (`c_fighiderefs`, `c_disablehyphen`)\n- `r_<name>` — radio / enum (`r_outputformat`)\n- `t_<name>` — text-area (`t_headertext`)\n\nPrefixes are not 100% consistent. Verify against the running tooltip dump (canon-seeded) before relying on a specific name.\n\n## Modifying an existing config\n\nThe standard flow:\n\n1. **Read** the current `ptxprint.cfg` content from project state (the user's filesystem, Git, etc.).\n2. **Check** both override files (`ptxprint_project.cfg` and `ptxprint_override.cfg`) for the key being changed. If locked there, surface to user — see `klappy://canon/articles/config-inheritance-and-overrides`.\n3. **Modify** the relevant section in working memory.\n4. **Include** the modified content in the payload's `config_files` at the same relative-path key.\n5. **Submit** and observe.\n6. **Persist** the change back to project state if the user is happy with the result.\n\nDo not skip the override-file check. A silent override is the most common cause of \"I changed it but nothing happened.\"\n\n## Common cfg-construction mistakes\n\n### 1. Misspelled keys\n\nPTXprint silently ignores unknown keys. The agent's change appears to do nothing. Fix: verify keys against canon's cfg-key index (seeded from the tooltip dump) before relying on a name. Never invent.\n\n### 2. Wrong section\n\nSame effect — unknown-key-in-section is also silent. The cfg key index identifies which section each key lives in.\n\n### 3. Wrong value type\n\nA boolean key set to `yes` instead of `True` may be ignored or misinterpreted. Common types:\n- Booleans: `True` / `False` (capitalized)\n- Numbers: bare digits, no quotes (`14`, not `\"14\"`)\n- Strings with units: `12mm`, `0.5in`, `1.2cm`\n- Page sizes: `<width>, <height> (label)` — example: `148mm, 210mm (A5)`\n\nWhen uncertain, verify a known-working value from an existing project's cfg.\n\n### 4. Multi-line values without continuation\n\n`configparser` requires tab or space indent to continue a value across lines. The cfg-key index notes which keys take multi-line values; most don't.\n\n### 5. Trying to set things that aren't cfg keys\n\nSome PTXprint behaviour is set via supporting files (changes.txt, AdjLists, piclist), not via cfg keys. If the agent can't find a cfg key for the user's intent, the answer may be in another file. See `klappy://canon/articles/payload-construction` for the full set of supporting files.\n\n## When a config is \"ready to share\"\n\nBefore the user pushes a config back to their project store (Paratext Send/Receive, Git commit, etc.), check:\n\n- The config has a meaningful name (purpose, not settings).\n- The view level the user wants the recipient to see is set (Mini/Basic/Full in `[config]`).\n- Any settings the user does not want changed are locked via the override mechanism.\n- The cfg has no broken inheritance (the `[import] config` actually exists in project state).\n- A test PDF has been produced and looks correct.\n\nThis is project-state hygiene, not MCP-server work. The agent guides; the user (or BT Servant) commits.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/config-inheritance-and-overrides` for how multiple cfg files compose at runtime.*\n"},{"path":"articles/config-inheritance-and-overrides.md","uri":"klappy://canon/articles/config-inheritance-and-overrides","title":"Config Inheritance and Overrides — How Multiple Cfg Files Compose at Runtime","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","inheritance","overrides","import","resolution-order"],"stability":"working","what_it_answers":"What does `[import]` do? What are override files? When the agent changes a setting and nothing happens, why?","sections":[{"slug":"the-five-layer-resolution-order","heading":"The five-layer resolution order","start":22,"end":38},{"slug":"the-import-section-config-inheritance","heading":"The `[import]` section: config inheritance","start":38,"end":86},{"slug":"override-files-locking-settings","heading":"Override files: locking settings","start":86,"end":123},{"slug":"three-failure-modes-the-agent-must-guard-against","heading":"Three failure modes the agent must guard against","start":123,"end":149},{"slug":"authoring-an-override-file","heading":"Authoring an override file","start":149,"end":170},{"slug":"the-full-resolution-example","heading":"The full resolution example","start":170,"end":200}],"body":"---\ntitle: \"Config Inheritance and Overrides — How Multiple Cfg Files Compose at Runtime\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"inheritance\", \"overrides\", \"import\", \"resolution-order\"]\nderives_from: \"canon/governance/headless-operations.md (Part 3 Config inheritance, Part 4 Override Mechanism)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Config Inheritance and Overrides\n\n> **What this answers.** What does `[import]` do? What are override files? When the agent changes a setting and nothing happens, why?\n>\n> **Related articles.** `klappy://canon/articles/config-construction` · `klappy://canon/articles/payload-construction` · `klappy://canon/articles/file-system-map`\n\n---\n\n## The five-layer resolution order\n\nWhen PTXprint resolves the value of any cfg key inside the worker, it walks five layers in order. The first layer that has the key wins:\n\n```\n1. Config-level override file:    shared/ptxprint/<config>/ptxprint_override.cfg\n2. Project-level override file:   shared/ptxprint/ptxprint_project.cfg\n3. Config's own ptxprint.cfg:     shared/ptxprint/<config>/ptxprint.cfg\n4. Parent config's ptxprint.cfg:  shared/ptxprint/<parent>/ptxprint.cfg (recursively)\n5. PTXprint's built-in defaults\n```\n\nEach layer is optional. Most projects have only layers 3 and 5. A project with inheritance adds layer 4. A project with cluster lockdown adds layers 1 and/or 2.\n\n**The agent's most common debugging question is: \"I changed layer 3 and nothing happened. Why?\"** Answer: layer 1 or 2 is winning. See *Failure modes* below.\n\n## The `[import]` section: config inheritance\n\nA config whose `ptxprint.cfg` contains:\n\n```ini\n[import]\nconfig = Default\n```\n\ndeclares that `Default` is its parent. PTXprint loads the parent's full settings first, then layers the child's settings on top. **The child stores only the keys that differ from the parent.**\n\nInheritance can chain:\n```\nMySpecial → FancyNT → Default\n```\n\nPTXprint resolves the chain by walking from base to most-specific.\n\n### Implications for the agent\n\n- **The agent walks the chain itself before constructing the payload.** Read each parent's `ptxprint.cfg` from project state.\n\n- **Include the chain explicitly in `config_files`.** Don't flatten — let the worker materialize each parent at its expected path, and PTXprint resolves inheritance the way it always does.\n\n  ```json\n  \"config_files\": {\n    \"shared/ptxprint/MySpecial/ptxprint.cfg\": \"[config]\\nname = MySpecial\\n[import]\\nconfig = FancyNT\\n[paper]\\npagesize = ...\\n\",\n    \"shared/ptxprint/FancyNT/ptxprint.cfg\":   \"[config]\\nname = FancyNT\\n[import]\\nconfig = Default\\n...\",\n    \"shared/ptxprint/Default/ptxprint.cfg\":   \"[config]\\nname = Default\\n...\"\n  }\n  ```\n\n  This preserves the user's intent and makes the payload roundtrip-able to a real project tree.\n\n- **There is no `resolve_config` server tool.** If the agent needs effective settings for reasoning, it computes them from the payload's `config_files` content (or from project state before payload construction).\n\n- **There is no reset mechanism.** A key set in the parent persists in the child unless the child explicitly overrides it.\n\n### When to use inheritance vs a copy\n\n| Use inheritance | Use a copy |\n|---|---|\n| The new config is a small variant of an existing one | The new config is fundamentally different |\n| The user wants changes to the parent to flow to the child | The user wants a snapshot that won't drift |\n| Cluster discipline — children customize, parent enforces | (rarely; explain why before doing this) |\n\nMost cases want inheritance.\n\n## Override files: locking settings\n\nThe two override files have the same INI shape as `ptxprint.cfg` but mean something different:\n\n```\nshared/ptxprint/ptxprint_project.cfg              PROJECT-WIDE\nshared/ptxprint/<config>/ptxprint_override.cfg    CONFIG-SPECIFIC\n```\n\nAn entry in either file does two things:\n\n1. **In headless terms**, the value wins over `ptxprint.cfg` (per the resolution order above).\n2. **In the GUI**, the corresponding control is greyed out — the user cannot change it through the UI.\n\n### The `*` prefix: soft lock\n\nA value with `*` prefix is a **soft lock** — the GUI allows temporary change, but the override default is restored when the project is closed and reopened.\n\n```ini\n[paper]\npagesize = * 148mm, 210mm (A5)\n```\n\nWithout the `*`, the lock is **hard** — the GUI doesn't allow change at all.\n\nIn headless context, both behave the same — the override value is what the worker sees. The `*` matters when the user later opens the project in the GUI.\n\n### When override files exist\n\nOverride files are typically created by:\n\n- **Cluster admins** enforcing house-style across multiple projects.\n- **Project leads** locking settings team members keep accidentally changing.\n- **Quality control** pinning settings that affect downstream printer workflow.\n\nThe agent doesn't usually create them — but must check for them.\n\n## Three failure modes the agent must guard against\n\n### Failure mode 1: silent override\n\n**Symptom:** the agent updates `ptxprint.cfg` content in the payload to change `[paper] pagesize`. The worker runs typesetting. The page size doesn't change.\n\n**Cause:** a project-level override file pinned the page size, and the agent included it (or carried it forward from project state) without updating it.\n\n**Mitigation:** before adding a setting to the payload's `config_files[\"shared/ptxprint/<config>/ptxprint.cfg\"]`, check both override files in project state. If the key is locked there, surface to user:\n\n> \"I'm about to set `[paper] pagesize = A5`, but the project-level override at `ptxprint_project.cfg` has it locked to `Letter`. To change it for real, I'd need to update the override file too. Do you want me to?\"\n\n### Failure mode 2: breaking cluster lockdown\n\n**Symptom:** the user is part of a publishing cluster whose admin has locked house-style settings via override files. The agent edits the override file in project state to \"fix\" something. The next Send/Receive (in the user's Paratext / Git workflow) propagates the change to every project in the cluster.\n\n**Mitigation:** treat override files as administrator-controlled. The agent must not modify them in project state — and therefore must not include modified versions in the payload — without explicit user confirmation, with cluster propagation implications spelled out.\n\n> \"This change would touch `ptxprint_project.cfg`, which is the cluster-wide lock file. If you Send/Receive after this, the change will reach every project in your cluster. Are you sure?\"\n\n### Failure mode 3: orphaned override\n\n**Symptom:** a previous payload (or the user's earlier work) left a value in an override file. Later, the agent updates the same key in the main `ptxprint.cfg` content of the payload, and tells the user the change is in effect. It is not — the override is still in the payload (and in project state).\n\n**Mitigation:** when the agent's payload change does not produce the expected output, check both override files first. The diagnostic checklist in `klappy://canon/articles/diagnostic-patterns` includes this step.\n\n## Authoring an override file\n\nThe deck describes the manual workflow: copy `ptxprint.cfg` to the override filename, delete the lines that should NOT be locked, save. The agent does the same:\n\n1. Read the relevant section of the working `ptxprint.cfg`.\n2. Extract the keys to lock.\n3. Write a new override file at the appropriate path in project state, containing only those keys.\n4. Include both the modified main cfg AND the new override file in subsequent payloads.\n\n### When to suggest creating an override file\n\n- The user is administering multiple projects and wants house-style consistency.\n- The user wants to enforce a setting that team members cannot accidentally change.\n- A specific setting has been \"lost\" multiple times because team members keep changing it.\n\n### When NOT to create an override file\n\n- The user is making a one-off layout decision for their own project.\n- The user is exploring options and may want to revert.\n- The user is not aware override files exist (introduce the concept first; do not silently create one).\n\n## The full resolution example\n\nProject `WSG`, config `MySpecial → FancyNT → Default`. The agent submits a payload that sets `[paper] pagesize = A4` in `MySpecial/ptxprint.cfg`. What happens?\n\n```\nLayer 1 — MySpecial/ptxprint_override.cfg          not in payload  → skip\nLayer 2 — ptxprint_project.cfg                     in payload, no [paper] section  → skip\nLayer 3 — MySpecial/ptxprint.cfg                   in payload, [paper] pagesize = A4  → WIN, value = A4\n```\n\nA4 is used. Good.\n\nNow suppose `ptxprint_project.cfg` has `[paper] pagesize = Letter`:\n\n```\nLayer 1 — MySpecial/ptxprint_override.cfg          not in payload  → skip\nLayer 2 — ptxprint_project.cfg                     in payload, [paper] pagesize = Letter  → WIN, value = Letter\nLayer 3 — MySpecial/ptxprint.cfg                   has pagesize = A4  → ignored\n```\n\nLetter is used. The agent's change to `MySpecial` was silently overridden. This is failure mode 1.\n\nThe agent must catch this before submitting. Either:\n- Update the override file to remove the lock (with user confirmation, with cluster implications spelled out).\n- Tell the user the change cannot take effect without admin authorization.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/config-construction` for cfg-section reference and `klappy://canon/articles/diagnostic-patterns` for \"my change didn't take effect\" debugging.*\n"},{"path":"articles/diagnostic-patterns.md","uri":"klappy://canon/articles/diagnostic-patterns","title":"Diagnostic Patterns — Reading XeTeX Logs and Recovering from Failures","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","diagnostics","log-reading","recovery","errors"],"stability":"working","what_it_answers":"How does the agent read a XeTeX log? What patterns indicate which problems? How does the recovery loop work?","sections":[{"slug":"reading-the-xetex-log","heading":"Reading the XeTeX log","start":22,"end":31},{"slug":"patterns-to-grep","heading":"Patterns to grep","start":31,"end":45},{"slug":"common-error-signatures-and-causes","heading":"Common error signatures and causes","start":45,"end":57},{"slug":"the-seven-point-picture-checklist","heading":"The seven-point picture checklist","start":57,"end":90},{"slug":"i-changed-it-but-nothing-happened","heading":"\"I changed it but nothing happened\"","start":90,"end":112},{"slug":"when-to-surface-partial-results","heading":"When to surface partial results","start":112,"end":120},{"slug":"overfull-box-count-a-signal-not-a-failure","heading":"Overfull box count: a signal, not a failure","start":120,"end":138},{"slug":"the-recovery-loop","heading":"The recovery loop","start":138,"end":166},{"slug":"specific-failure-patterns","heading":"Specific failure patterns","start":166,"end":228}],"body":"---\ntitle: \"Diagnostic Patterns — Reading XeTeX Logs and Recovering from Failures\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"diagnostics\", \"log-reading\", \"recovery\", \"errors\"]\nderives_from: \"canon/governance/headless-operations.md (Part 9 Diagnostic Patterns)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Diagnostic Patterns\n\n> **What this answers.** How does the agent read a XeTeX log? What patterns indicate which problems? How does the recovery loop work?\n>\n> **Related articles.** `klappy://canon/articles/failure-mode-taxonomy` · `klappy://canon/articles/payload-construction` · `klappy://canon/articles/config-inheritance-and-overrides`\n\n---\n\n## Reading the XeTeX log\n\nThe log is uploaded to R2 alongside the PDF and surfaced two ways in `get_job_status`:\n\n- `log_url` — presigned R2 GET URL for the full log\n- `log_tail` — last 100 lines inline in the response\n\nFor long jobs, use `log_tail` for incremental visibility during the run; fetch the full log via `log_url` after completion if more context is needed.\n\n## Patterns to grep\n\n| Pattern | Meaning |\n|---|---|\n| `^!` | TeX fatal error. Read the next 5–10 lines for the cause. |\n| `Overfull \\hbox` | Horizontal overfull (line too long). One per occurrence. |\n| `Overfull \\vbox` | Vertical overfull (page too long). One per occurrence. |\n| `Underfull \\hbox` | Line too short (typically with stretched spacing). |\n| `Output written on ...` | Successful PDF write; the canonical \"did it complete\" marker. |\n| `! Undefined control sequence.` | A USFM marker the macros don't recognize. Check `ptxprint.sty`. |\n| `! Missing number, treated as zero.` | A setting expected a number and got something else. |\n| `image not found` (or similar) | Picture file missing from `figures` array or wrong URL. |\n| `Output written on ... (0 pages)` | XeTeX completed but produced an empty PDF. Soft failure indicator. |\n\n## Common error signatures and causes\n\n| Error pattern | Likely cause | Fix |\n|---|---|---|\n| `! Undefined control sequence \\xyz` | Custom marker not declared in `.sty` | Add the marker to `ptxprint.sty` content in the payload, or remove from source |\n| `! Font \\xyz not loadable` | Font missing from `fonts` array | Add the font with valid URL+sha256 |\n| `! TeX capacity exceeded, sorry` | Document complexity exceeds XeTeX limits | Reduce complexity (fewer pictures per page, simpler styles) |\n| Empty PDF / \"0 pages\" | Source USFM is empty or malformed | Check source files; PTXprint 3.0.9+ has pre-flight checks for this |\n| Long hang during run | Hyphenation cache rebuild, or first-time picture processing | Increase the payload's timeout; check progress in `log_tail` |\n| Pictures all missing | Piclist wrong, or `figures` array mismatch | Run the seven-point checklist below |\n| Layout looks completely wrong | Override file silently overriding | Check `ptxprint_project.cfg` and `ptxprint_override.cfg` in the payload |\n\n## The seven-point picture checklist\n\nWhen `failure_mode == \"soft\"` and the user reports pictures missing:\n\n```\n1. Are pictures listed in the piclist for this configuration?\n   → Inspect the piclist content in the working payload's config_files; confirm\n     entries for the books being typeset.\n\n2. Are the picture files in the payload's figures array, with valid URLs and\n   matching sha256 hashes?\n   → Inspect the payload (the agent has the working copy). Verify that the\n     figures referenced by the piclist are present in the array and that their\n     URLs return content matching the declared hashes.\n\n3. Can PTXprint see them?\n   → Look for \"image not found\" warnings in the log surfaced via log_tail or log_url.\n\n4. Are all missing or just some?\n   → Diff the piclist's expected entries against the log's rendered count.\n\n5. Are Anchor Refs valid (do those verses exist)?\n   → Verify against the source USFM; bridged verses can confuse anchors.\n\n6. Are pictures too large?\n   → Check the scale values in the piclist; reduce if oversize.\n\n7. Are too many on one page?\n   → Spread them across more verses by adjusting Anchor Refs.\n```\n\nSurface the specific fix to the user. If multiple issues, fix one at a time and re-submit.\n\n## \"I changed it but nothing happened\"\n\nWhen the agent updates a cfg key in the payload and the output doesn't change:\n\n```\n1. Confirm the cfg key was actually changed in the payload that was submitted.\n   Read it back from the working payload state.\n2. Check ptxprint_override.cfg in the payload's config_files (and project state).\n3. Check ptxprint_project.cfg in the payload's config_files (and project state).\n4. If neither override file has the key, the issue is elsewhere:\n   - Was the most recent payload actually submitted? (Look at the payload_hash\n     in the last submit_typeset response.)\n   - Did the agent receive a cache hit on a stale payload? (The `cached` field\n     on the submit_typeset response indicates this.)\n   - Is the parent config's value being inherited and the child's value not\n     overriding? (Walk the inheritance chain.)\n5. If an override file does have the key, surface that to the user; ask whether\n   to edit the override.\n```\n\nThe most common cause is #2 or #3 — see `klappy://canon/articles/config-inheritance-and-overrides` for the resolution order.\n\n## When to surface partial results\n\nWhen a job fails after producing some output (a partial log, a PDF from a previous payload), surface what exists with explicit caveats:\n\n> \"The run did not complete. The log at log_url shows the last successful step was X. The pdf_url from a previous payload (\\<earlier_payload_hash\\>) is still available and dates to \\<date\\>; it does not reflect this attempt.\"\n\nDo not silently return an earlier payload's pdf_url as if it were the new result. Cache hits are deliberate (same payload → same output); ghost results from old payloads confuse the user.\n\n## Overfull box count: a signal, not a failure\n\n`get_job_status.overfull_count` reports `Overfull \\hbox` and `Overfull \\vbox` warning counts. Non-zero is normal.\n\nRough thresholds for an NT-sized publication:\n\n| Count | Interpretation |\n|---|---|\n| < 20 | Excellent; nothing to do |\n| 20–50 | Normal; some justification stretching |\n| 50–200 | Worth investigating; specific paragraphs may need adjustment |\n| 200–500 | Layout has problems; review font size, line spacing, column count |\n| > 500 | Likely a fundamental layout mismatch (text too large for column width) |\n\nHeuristics, not hard rules. Poetry runs higher than narrative.\n\nThe `failure_mode` classification handles low-medium counts as success automatically. For counts ≥ 200, the agent should mention to the user as a concern even when `failure_mode: \"success\"`.\n\n## The recovery loop\n\nPer session 6 D-024, the failure → log → fix → resubmit loop is the agent's primary error-recovery mechanism:\n\n1. `submit_typeset(payload)` → `job_id`\n2. `get_job_status(job_id)` → `failure_mode`, `log_tail`, `errors[]`\n3. Agent reads log evidence, identifies cause, modifies the payload (cfg key, font URL, source content, whatever).\n4. Re-submits modified payload. New `payload_hash` → fresh run. Cache hits if the change matches a previous attempt.\n5. Repeat until `failure_mode: \"success\"`.\n\n### Latency budget\n\nPer session 6 C-009, BT Servant cannot afford four minutes of back-and-forth on long autofill jobs. For autofill failures, prefer to:\n\n- Fail fast with `mode: \"simple\"` first\n- Fix visible problems (missing fonts, malformed cfg, bad sources)\n- Promote to `mode: \"autofill\"` only when the simple run succeeds\n\nThis separates the long autofill cost from the short debugging cost.\n\n### When to give up and ask the user\n\nThe recovery loop has limits. After 2–3 failed attempts on the same problem, surface to the user:\n\n> \"I've tried three approaches and none have worked. The log shows X. Here's what I've tried: A, B, C. Could you confirm Y, or check the underlying USFM?\"\n\nBetter to ask one well-formed question than to keep iterating blindly.\n\n## Specific failure patterns\n\n### Hard failure: `! Undefined control sequence`\n\n```\n! Undefined control sequence.\nl.123 \\mycustommarker\n```\n\nCause: a USFM marker in the source USFM that PTXprint's macros don't recognize.\n\nFix: declare the marker in `ptxprint.sty` content. Minimal declaration:\n\n```\n\\Marker mycustommarker\n\\TextProperties paragraph\n```\n\nIf the user authored the marker, this is the fix. If the marker is a typo in the source, surface to the user and let them fix in Paratext.\n\n### Hard failure: `! Font ... not loadable`\n\n```\n! Font \\specialfont/300:script=latn at 12pt not loadable: Metric (TFM) file or installed font not found.\n```\n\nCause: the font referenced by the cfg or stylesheet isn't in the payload's `fonts` array.\n\nFix: add the font entry. See `klappy://canon/articles/font-resolution`.\n\n### Hard failure: `! Missing number, treated as zero`\n\n```\n! Missing number, treated as zero.\n<to be read again> \n                  =\n```\n\nCause: a cfg value or `\\zvar` substitution gave non-numeric content where a number was expected.\n\nFix: locate the cfg key in the log context (the `l.<num>` indicator before this error) and check the value type. Common: a `\\zvar` whose value isn't set in `[vars]`, or a cfg key set to a string when the macro expects a number.\n\n### Soft failure: missing pictures\n\nSee the seven-point checklist above.\n\n### Soft failure: blank or truncated ToC\n\nCause: the `\\ztoc` marker isn't in `FRTlocal.sfm`, or the `\\toc1`/`\\toc2`/`\\toc3` markers aren't set in the source USFM.\n\nFix: verify `\\ztoc|main\\*` (or appropriate scope) is in `FRTlocal.sfm` content. Verify each book's `\\toc1` is set.\n\n### Soft failure: PDF page count too low\n\nCause: source USFM is empty, malformed, or only partially included.\n\nFix: verify each book's USFM source in the `sources` array has actual content. Empty `\\id`-only files produce empty pages.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/failure-mode-taxonomy` for the hard/soft/success classification.*\n"},{"path":"articles/failure-mode-taxonomy.md","uri":"klappy://canon/articles/failure-mode-taxonomy","title":"Failure Mode Taxonomy — Hard, Soft, and Success","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","failure-modes","diagnostics","validation"],"stability":"working","what_it_answers":"When `get_job_status` returns, how does the agent know the job actually worked? What does \"soft failure\" mean? When is an exit code 0 still a problem?","sections":[{"slug":"the-three-outcomes","heading":"The three outcomes","start":22,"end":34},{"slug":"why-the-distinction-matters","heading":"Why the distinction matters","start":34,"end":46},{"slug":"how-the-worker-classifies","heading":"How the worker classifies","start":46,"end":65},{"slug":"what-to-do-per-failure-mode","heading":"What to do per failure mode","start":65,"end":104},{"slug":"the-recovery-loop","heading":"The recovery loop","start":104,"end":116},{"slug":"overfull-box-count-a-soft-signal-not-a-failure","heading":"Overfull-box count: a soft signal, not a failure","start":116,"end":138},{"slug":"what-success-doesn-t-guarantee","heading":"What \"success\" doesn't guarantee","start":138,"end":149},{"slug":"when-to-surface-partial-results","heading":"When to surface partial results","start":149,"end":161}],"body":"---\ntitle: \"Failure Mode Taxonomy — Hard, Soft, and Success\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"failure-modes\", \"diagnostics\", \"validation\"]\nderives_from: \"canon/governance/headless-operations.md (Part 0 Three failure modes, Part 9 Diagnostic Patterns)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Failure Mode Taxonomy\n\n> **What this answers.** When `get_job_status` returns, how does the agent know the job actually worked? What does \"soft failure\" mean? When is an exit code 0 still a problem?\n>\n> **Related articles.** `klappy://canon/articles/diagnostic-patterns` · `klappy://canon/articles/payload-construction` · `klappy://canon/articles/output-naming`\n\n---\n\n## The three outcomes\n\nHeadless typesetting has three outcomes, not two. An agent that checks only exit code or only PDF existence will report soft failures as success.\n\n| Outcome | exit_code | PDF on disk | Content quality |\n|---|---|---|---|\n| **success** | 0 | yes | structural checks pass |\n| **soft failure** | 0 | yes | structural checks fail (degraded output) |\n| **hard failure** | non-zero | usually no | XeTeX errored or didn't produce a file |\n\n`get_job_status` returns the classification result in the `failure_mode` field — one of `\"hard\" | \"soft\" | \"success\"`. **The agent reads this field directly. It does not re-derive the classification.** The worker has more information (PDF page count, structural checks, log inspection) than the agent receives.\n\n## Why the distinction matters\n\nA soft failure looks like a success on the surface: exit code 0, PDF in R2, presigned URL ready to share. Without the `failure_mode` classification, the agent surfaces a degraded PDF to the user as \"your typesetting succeeded.\" Common soft failures:\n\n- All pictures missing from a PDF that was supposed to have them\n- ToC blank when the user expected one\n- Footnotes silently dropped\n- Pages collapsed when the source had more content\n- Empty PDF from empty/malformed source USFM\n\nEach of these is a \"the run technically completed\" outcome that hides a real problem. The taxonomy exists so the agent reports them as failures.\n\n## How the worker classifies\n\nThe worker computes `failure_mode` using this truth table:\n\n```\nexit_code == 0 and pdf_exists and overfull_count < 50           → success\nexit_code == 0 and pdf_exists and overfull_count >= 50          → success-with-concerns (still success)\nexit_code == 0 and pdf_exists and pdf_page_count < expected     → soft failure (degraded)\nexit_code == 0 and pdf_exists and \"image not found\" in log      → soft failure (missing pictures)\nexit_code == 0 and not pdf_exists                                → impossible (XeTeX bug); investigate\nexit_code != 0 and pdf_exists                                    → partial: PDF exists from previous run; ignore\nexit_code != 0 and not pdf_exists                                → hard failure\nexit_code == 4                                                   → hard failure (XeTeX errors during macro processing)\nexit_code == 3                                                   → hard failure (XeTeX completed but didn't write PDF)\nexit_code == 1                                                   → hard failure (startup; project not found, bad config, missing executable)\n```\n\nThese are heuristics. The worker's exact logic may evolve; the agent always reads `failure_mode` rather than computing it.\n\n## What to do per failure mode\n\n### success\n\nSurface the `pdf_url` to the user. Optionally mention `overfull_count` if it's notable but not blocking (see `klappy://canon/articles/diagnostic-patterns` for thresholds). Done.\n\n### soft failure\n\nSurface to the user as a problem, not a result. Read the `errors` array and `log_tail` for the cause.\n\nCommon patterns and fixes:\n\n| Cause | Diagnostic | Fix |\n|---|---|---|\n| Pictures missing | `\"image not found\"` in log | Run the seven-point picture checklist; verify `figures` array URLs and sha256s |\n| Page count too low | `pdf_page_count < expected` | Source USFM may be empty; check `sources` array contents |\n| ToC blank | No `\\ztoc` rendering, or `FRTlocal.sfm` missing the marker | Add `\\ip \\ztoc|main\\*` to FRT |\n| Footnotes dropped | `\\f` styles not declared in `.sty` | Verify `ptxprint.sty` content has footnote style declarations |\n\nThe soft-failure PDF is still in R2 at `pdf_url`. The agent can surface it to the user as evidence of what went wrong (\"here's what came out — pictures are missing because...\"), but should not present it as the deliverable.\n\n### hard failure\n\nSurface to the user as a failure. Read `errors[]` and `log_tail` for the cause. The PDF (if any) referenced by `pdf_url` is from a prior run — do not surface it as the new result.\n\nCommon patterns and fixes:\n\n| `exit_code` | Pattern | Likely cause | Fix |\n|---|---|---|---|\n| 1 | \"project not found\" | `project_id` in payload doesn't match a directory the worker can construct | Verify `project_id` value |\n| 1 | \"config path bad\" | `config_name` doesn't have a corresponding `config_files` entry | Add the cfg content to `config_files` |\n| 4 | `! Undefined control sequence \\xyz` | A USFM marker not declared in `.sty` | Add the marker to `ptxprint.sty` content, or remove from source |\n| 4 | `! Font \\xyz not loadable` | Font missing from `fonts` array | Add the font with valid URL+sha256 |\n| 4 | `! Missing number, treated as zero` | A cfg value expected a number and got something else | Find the misspelled or wrong-typed value |\n| 4 | `! TeX capacity exceeded, sorry` | Document complexity exceeds XeTeX limits | Reduce complexity (fewer pictures per page, simpler styles) |\n| 3 | XeTeX completed but no PDF | Permissions or disk issue inside the worker | Re-submit; if persistent, escalate as infrastructure issue |\n| 3a | PTXprint **exited 0 with no PDF and no log** in ~2 s (`log_tail`: \"[container diagnostic] PTXprint exited 0 with no PDF and no log file\") | Payload has no `config_files` (define-only or `{books, sources}` only): PTXprint finds no config and quits silently — issue #40, reproduced 2026-09-05 | Include a `ptxprint.cfg` (start from `klappy://canon/articles/bundled-default-cfg` or a smoke fixture) |\n| 3b | `! Dimension too large` | The footnotes anchored on one page are taller than the page (study-note stacks) | `klappy://canon/articles/study-notes-and-footnotes` §Why a full book fails |\n\n## The recovery loop\n\nPer session 6 D-024, the failure→fix loop is the agent's primary error-recovery mechanism (no separate validator):\n\n1. `submit_typeset` → `job_id`\n2. `get_job_status` → `failure_mode: \"hard\" | \"soft\"`, `log_tail`, `errors[]`\n3. Agent reads the log evidence, identifies the cause, modifies the payload (cfg key, font URL, source content, whatever).\n4. Re-submits the modified payload. New `payload_hash` → fresh run.\n5. Repeat until `failure_mode: \"success\"`.\n\nLatency budget matters. Per session 6 C-009, BT Servant cannot afford four minutes of back-and-forth on a long autofill. For autofill failures, prefer to fail fast with `mode: \"simple\"` first, fix the visible problems, then promote to autofill.\n\n## Overfull-box count: a soft signal, not a failure\n\n`get_job_status.overfull_count` reports the number of `Overfull \\hbox` and `Overfull \\vbox` warnings in the XeTeX log. **Non-zero is normal.** A book of poetry, a tight column width, or aggressive justification all produce overfulls without indicating failure.\n\nRough thresholds for an NT-sized publication:\n\n| Count | Interpretation |\n|---|---|\n| < 20 | Excellent; nothing to do |\n| 20–50 | Normal; some justification stretching |\n| 50–200 | Worth investigating; specific paragraphs may need adjustment |\n| 200–500 | Layout has problems; review font size, line spacing, column count |\n| > 500 | Likely a fundamental layout mismatch (e.g., text too large for column width) |\n\nThese are heuristics, not rules. The classification flow:\n\n- `overfull_count < 50` → `failure_mode: \"success\"` (no surface to user)\n- `50 ≤ overfull_count < 200` → `failure_mode: \"success\"`, mention to user as info\n- `overfull_count ≥ 200` → `failure_mode: \"success\"` per the truth table, but agent should surface as a concern: \"your PDF rendered, but with N overfull boxes which may indicate layout problems\"\n\nThe threshold is conservative because the cost of false alarms (interrupting a successful run) is higher than the cost of letting overfulls through.\n\n## What \"success\" doesn't guarantee\n\nA `failure_mode: \"success\"` means: exit code 0, PDF exists, structural checks passed. It does **not** guarantee:\n\n- The PDF looks the way the user wanted (subjective; agent can't check this)\n- The text is correct (the agent isn't a translation reviewer)\n- The fonts are appropriate for the script (agent should check via canon if uncertain)\n- The user will be happy with the result\n\nFor subjective qualities, the agent surfaces the PDF and lets the user judge. For technical qualities (correct fonts, expected pictures, expected page count), the structural checks catch most issues — but the agent can run additional checks if the user has stated specific expectations.\n\n## When to surface partial results\n\nWhen a job fails after producing some output (a partial log, a PDF from a previous payload, a half-written piclist), surface what exists with explicit caveats:\n\n> \"The run did not complete. The log at log_url shows the last successful step was X. The pdf_url from a previous payload (\\<earlier_payload_hash\\>) is still available and dates to \\<date\\>; it does not reflect this attempt.\"\n\nDo not silently return an earlier payload's `pdf_url` as if it were the new result. Cache hits are deliberate (same payload → same output); ghost results from old payloads are confusing.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/diagnostic-patterns` for log-reading patterns and common error signatures.*\n"},{"path":"articles/file-system-map.md","uri":"klappy://canon/articles/file-system-map","title":"File System Map — What the Worker Materializes","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","filesystem","directory-tree","config-files"],"stability":"working","what_it_answers":"What directory tree does PTXprint expect? Which paths in the payload's `config_files` map are valid? Where does the worker put outputs?","sections":[{"slug":"why-this-matters","heading":"Why this matters","start":22,"end":28},{"slug":"the-tree","heading":"The tree","start":28,"end":67},{"slug":"two-trees-shared-and-local","heading":"Two trees: `shared/` and `local/`","start":67,"end":76},{"slug":"mapping-payload-entries-to-paths","heading":"Mapping payload entries to paths","start":76,"end":114},{"slug":"where-usfm-source-files-go","heading":"Where USFM source files go","start":114,"end":120},{"slug":"where-fonts-and-figures-go","heading":"Where fonts and figures go","start":120,"end":124},{"slug":"where-outputs-go","heading":"Where outputs go","start":124,"end":135},{"slug":"things-in-the-tree-the-agent-does-not-include-in-the-payload","heading":"Things in the tree the agent does NOT include in the payload","start":135,"end":148},{"slug":"things-in-the-tree-that-are-optional-inputs","heading":"Things in the tree that ARE optional inputs","start":148,"end":161},{"slug":"what-about-the-prjid-value","heading":"What about the `<prjid>` value?","start":161,"end":171}],"body":"---\ntitle: \"File System Map — What the Worker Materializes\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"filesystem\", \"directory-tree\", \"config-files\"]\nderives_from: \"canon/governance/headless-operations.md (Part 2 File System Map)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# File System Map\n\n> **What this answers.** What directory tree does PTXprint expect? Which paths in the payload's `config_files` map are valid? Where does the worker put outputs?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/output-naming` · `klappy://canon/articles/config-inheritance-and-overrides`\n\n---\n\n## Why this matters\n\nThe agent does not operate against a real filesystem on the server. It builds a payload. But the payload's `config_files` map keys are **relative paths** that the worker materializes inside its scratch directory before invoking PTXprint. Knowing the expected tree tells the agent which paths are valid.\n\nPTXprint expects a Paratext-style project layout. The worker reproduces that layout from the payload, runs PTXprint, then discards the directory.\n\n## The tree\n\n```\n<projects_root>/<prjid>/\n├── shared/                          team-visible part (origin in the user's project store)\n│   └── ptxprint/\n│       ├── ptxprint.sty             project-wide stylesheet (often blank)\n│       ├── ptxprint-premods.tex     project-wide TeX pre-processing (rarely modified)\n│       ├── changes.txt              project-wide regex transforms\n│       ├── picInfo.txt              project-wide picture clearance\n│       ├── picChecks.txt            project-wide picture approvals\n│       ├── hyphen-<prj>.tex         hyphenation exceptions\n│       ├── hyphenation.pkl          compiled hyphenation cache (auto-generated)\n│       ├── ptxprint_project.cfg     PROJECT-LEVEL OVERRIDE FILE\n│       └── <ConfigName>/            named configuration folder\n│           ├── ptxprint.cfg         PRIMARY SETTINGS (the ~400 keys)\n│           ├── ptxprint.sty         config-level stylesheet overrides\n│           ├── ptxprint-mods.sty    additional style modifications\n│           ├── changes.txt          config-specific regex transforms\n│           ├── FRTlocal.sfm         front matter USFM content\n│           ├── <PRJ>-<Config>.piclist     picture placement list\n│           ├── picInfo.txt          config-level picture clearance\n│           ├── picChecks.txt        config-level picture approvals\n│           ├── ptxprint_override.cfg     CONFIG-LEVEL OVERRIDE FILE\n│           ├── merge-chapter.cfg    diglot chapter-merge strategy (auto-generated)\n│           ├── merge-normal.cfg     diglot normal-merge strategy (auto-generated)\n│           ├── merge-verse.cfg      diglot verse-merge strategy (auto-generated)\n│           └── AdjLists/            paragraph adjustment files\n│               └── <num><BOOK><prj>-<Config>[.SFM|-diglot].adj\n└── local/                           machine-local; the worker's PDF output staging\n    └── ptxprint/\n        ├── <PRJ>_<Config>_<bks>_ptxp.pdf      output PDF\n        └── <ConfigName>/\n            └── <PRJ>_<Config>_<bks>_ptxp.log  XeTeX log\n```\n\n`<prjid>` is uppercased Paratext project ID (`WSG`, `BSB`, `ULT`).\n`<ConfigName>` is the config name in the case the user named it (`Default`, `FancyNT`, `JournalEdition`).\n\n## Two trees: `shared/` and `local/`\n\n| Tree | Origin | What lives here | Agent involvement |\n|---|---|---|---|\n| `shared/` | user's project store (Paratext, Git, etc.) | configs, stylesheets, supporting files | Agent reads from project state, includes in payload's `config_files` |\n| `local/` | machine-local | PTXprint's PDF and log output | Agent does not touch — outputs are surfaced via R2 presigned URLs |\n\nThe agent's `config_files` map keys all start with `shared/`. The `local/` tree is created by the worker as part of running PTXprint and never appears in payload submissions.\n\n## Mapping payload entries to paths\n\nEvery key in the payload's `config_files` is a relative path under `<prjid>/`. The most common keys, in order of how often they appear:\n\n```\nshared/ptxprint/<config>/ptxprint.cfg                primary settings (always)\nshared/ptxprint/<config>/ptxprint.sty                style overrides (often)\nshared/ptxprint/<config>/changes.txt                 regex transforms (sometimes)\nshared/ptxprint/<config>/FRTlocal.sfm                front matter (when ToC/cover needed)\nshared/ptxprint/<config>/<PRJ>-<config>.piclist      picture placements (when figures present)\nshared/ptxprint/<config>/AdjLists/<num><BOOK><PRJ>-<config>.SFM.adj\n                                                     paragraph adjustments (rare, per-book)\nshared/ptxprint/<config>/ptxprint_override.cfg       config-level overrides (admin)\nshared/ptxprint/ptxprint_project.cfg                 project-level overrides (admin)\nshared/ptxprint/changes.txt                          project-wide regex transforms (rare)\nshared/ptxprint/ptxprint.sty                         project-wide stylesheet (rare)\n```\n\nNote `<PRJ>` (uppercase project id) and `<config>` (case-as-named) appear in some filenames. Get them right or PTXprint won't find the file.\n\n### AdjList filename composition\n\n```\n<num><BOOK><prj>-<config>[.SFM|-diglot].adj\n  │     │      │        │     │\n  │     │      │        │     └─ `.SFM.adj` for monoglot, `-diglot.adj` for diglot\n  │     │      │        └────── config name (case as named)\n  │     │      └─────────────── project ID (case as named)\n  │     └────────────────────── three-letter USFM book code, uppercase\n  └──────────────────────────── two-digit book number, zero-padded\n```\n\nExamples:\n- `46ROMWSG-FancyNT.SFM.adj` — Romans (book 46), project WSG, config FancyNT, monoglot\n- `41MATWSG-Diglot-diglot.adj` — Matthew (book 41), project WSG, config Diglot, diglot\n\nThe book number is the canonical USFM book number — `01` for GEN through `66` for REV plus peripheral codes. The agent should use the exact zero-padded number; PTXprint matches on filename.\n\n## Where USFM source files go\n\n`sources` array entries don't appear in `config_files`. The worker writes them to a per-project directory derived from `project_id` and PTXprint's expected source layout. The agent only specifies `book` and `filename` in each `sources` entry; the worker handles placement.\n\nThe standard USFM book filename pattern is `<num><BOOK><prj>.SFM` (same number+book+project pattern as AdjList filenames). Examples: `41MATWSG.SFM`, `66REVWSG.SFM`. The agent provides this in the `sources[].filename` field.\n\n## Where fonts and figures go\n\nSame principle: the worker handles placement. Fonts are installed into a per-job fontconfig directory; figures are placed where PTXprint's piclist resolution looks for them. The agent specifies URL+sha256+filename; the worker materializes.\n\n## Where outputs go\n\nThe worker stages outputs in `local/ptxprint/` (PDF) and `local/ptxprint/<ConfigName>/` (log) inside its scratch directory, then uploads them to R2 at:\n\n```\nptxprint-outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf\nptxprint-outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.log\n```\n\nThe agent never reads from `local/`. It receives presigned URLs in `get_job_status.pdf_url` and `log_url`. See `klappy://canon/articles/output-naming`.\n\n## Things in the tree the agent does NOT include in the payload\n\n| Path | Why excluded |\n|---|---|\n| `hyphenation.pkl` | Compiled cache; PTXprint regenerates from `hyphen-<prj>.tex` |\n| `local/...` | Output tree, not input |\n| `merge-*.cfg` | Auto-generated by PTXprint when diglot config is set up — agent doesn't author |\n| `picChecks.txt` | Picture approval state usually managed by GUI; agent may include if user has explicitly authorized |\n| `ptxprint-premods.tex` | TeX macro injection — v1.2 defers as too risky for automation |\n| `*.bak` files | Backups |\n\nIf the agent has read these files from project state and is uncertain whether to include them, the rule is: **include only what's needed.** Smaller payloads hash differently when something irrelevant changes; larger payloads cache-miss more often.\n\n## Things in the tree that ARE optional inputs\n\nThe agent **may** include these when relevant:\n\n| Path | When to include |\n|---|---|\n| `shared/ptxprint/changes.txt` | Project-wide transforms — usually contains `include` directives to a `PrintDraftChanges.txt` file the user maintains |\n| `shared/ptxprint/<config>/changes.txt` | Config-specific transforms (this run only) |\n| `shared/ptxprint/<config>/ptxprint-mods.sty` | Additional style modifications layered on top of `ptxprint.sty` |\n| `shared/ptxprint/<config>/picInfo.txt` | Picture copyright/clearance metadata, when the agent has the data |\n\nWhen in doubt, include text artifacts that exist in project state and are non-empty. They cost little; their absence may cause rendering differences the agent can't predict.\n\n## What about the `<prjid>` value?\n\n`<prjid>` in the payload's `project_id` field should match what PTXprint expects: the Paratext project ID, uppercased, no extension. Examples: `WSG`, `BSB`, `ULT`, `WEB`. This becomes the directory name the worker creates and uses to construct USFM source paths.\n\nIf the user's project store uses a different name, the agent normalizes to the Paratext ID before constructing the payload.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/payload-construction` for how this tree maps into a JSON payload.*\n"},{"path":"articles/font-resolution.md","uri":"klappy://canon/articles/font-resolution","title":"Font Resolution — How Fonts Reach the Worker","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","fonts","lff","sha256","manifest"],"stability":"working","what_it_answers":"How does the worker get fonts? What goes in the payload's `fonts` array? What if a font is missing?","sections":[{"slug":"the-contract","heading":"The contract","start":24,"end":53},{"slug":"per-style-entries","heading":"Per-style entries","start":53,"end":68},{"slug":"where-fonts-come-from","heading":"Where fonts come from","start":68,"end":107},{"slug":"computing-the-sha256","heading":"Computing the sha256","start":107,"end":117},{"slug":"which-fonts-the-project-references","heading":"Which fonts the project references","start":117,"end":131},{"slug":"what-happens-if-a-font-is-missing","heading":"What happens if a font is missing","start":131,"end":143},{"slug":"what-happens-if-a-font-s-sha256-doesn-t-match","heading":"What happens if a font's sha256 doesn't match","start":143,"end":155},{"slug":"url-stability-and-the-cache","heading":"URL stability and the cache","start":155,"end":165},{"slug":"multi-script-projects","heading":"Multi-script projects","start":165,"end":173},{"slug":"a-worked-minimum-english-bible-payload","heading":"A worked-minimum English-Bible payload","start":173,"end":220}],"body":"---\ntitle: \"Font Resolution — How Fonts Reach the Worker\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"fonts\", \"lff\", \"sha256\", \"manifest\"]\nderives_from: \"canon/governance/headless-operations.md (font references), canon/articles/_archive/font-resolution-design.md (design history)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n> **Live behavior (validated 2026-04-30).** Payload-supplied fonts work end-to-end on the live deploy — the BSB Psalms render uses Gentium Plus + SourceCodePro supplied entirely via the payload's `fonts` array, no system fonts required (session 11 / 2026-04-29; reproduced and re-verified 2026-04-30). The Container additionally bundles SIL Charis system-wide as a convenience floor: a payload that only references Charis may leave `fonts: []` and PTXprint resolves Charis via fontconfig. For anything else — different family, different version, multi-script — populate the `fonts` array as described below. The per-payload, content-addressed font contract is the live contract, not a future target. Multi-script support is the next horizon, built on top of this same contract; nothing below is \"not yet implemented.\"\n\n# Font Resolution\n\n> **What this answers.** How does the worker get fonts? What goes in the payload's `fonts` array? What if a font is missing?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/failure-mode-taxonomy`\n\n---\n\n## The contract\n\nIn v1.2, every font the project references is a payload entry with a URL and sha256:\n\n```json\n\"fonts\": [\n  {\n    \"family_id\": \"charissil\",\n    \"version\": \"7.000\",\n    \"filename\": \"CharisSIL-Regular.ttf\",\n    \"url\": \"https://lff.api.languagetechnology.org/...\",\n    \"sha256\": \"<sha256 of the file at the url>\"\n  },\n  {\n    \"family_id\": \"charissil\",\n    \"version\": \"7.000\",\n    \"filename\": \"CharisSIL-Bold.ttf\",\n    \"url\": \"...\",\n    \"sha256\": \"...\"\n  }\n]\n```\n\nThe Container worker fetches each URL in parallel, hashes the bytes as they arrive, and aborts the job with a hard failure if any sha256 doesn't match. Fonts that pass verification are placed where PTXprint can find them via fontconfig.\n\n**The worker has no font cache shared with other jobs.** Every job re-fetches its fonts. URL stability matters for performance.\n\n**The worker excludes system fonts.** Per session 3 C-007, the Container does not install `fonts-sil-charis`, `fonts-noto-*`, or any `fonts-*` package. If a font isn't in the payload, PTXprint won't find it, and the run fails loudly. There is no silent fallback.\n\n## Per-style entries\n\nMost font families have multiple style files (Regular, Bold, Italic, Bold Italic). Each is a separate `fonts` array entry. PTXprint picks the right one by querying fontconfig at typeset time; the agent's job is to put all required styles in the array.\n\nCommon scriptural fonts and their typical files:\n\n| Family | Common files |\n|---|---|\n| Charis SIL | `CharisSIL-Regular.ttf`, `CharisSIL-Bold.ttf`, `CharisSIL-Italic.ttf`, `CharisSIL-BoldItalic.ttf` |\n| Andika | `Andika-Regular.ttf`, `Andika-Bold.ttf`, `Andika-Italic.ttf`, `Andika-BoldItalic.ttf` |\n| Doulos SIL | `DoulosSIL-R.ttf` (single weight) |\n| Noto Sans | per-script: `NotoSansHebrew-Regular.ttf`, `NotoSansArabic-Regular.ttf`, etc. |\n\nWhen in doubt about which styles a font family ships, query the source (LFF, the foundry's site, etc.) for its file inventory.\n\n## Where fonts come from\n\nThe agent has four typical paths to a font URL:\n\n### 1. SIL Language Font Finder (LFF)\n\nFor SIL fonts and many open-script fonts, LFF (`lff.api.languagetechnology.org`) provides versioned, content-addressed downloads. The URL stays stable as long as the version is referenced. Recommended for English Bibles per session 1 D-002 (initial scope: English with Charis SIL).\n\n### 2. Hosting is the agent's concern\n\nWhen the user has a font on local disk that's not on a public CDN, the agent's host environment is responsible for making it reachable at an HTTPS URL — not the typesetting MCP server. The MCP server has no `get_upload_url` tool and does not stage input files. Common patterns:\n\n- The agent's host already runs an HTTP server with file access (Claude Desktop's filesystem MCP plus a separate static file server, a developer machine with a tunnel, etc.).\n- The user puts the font in a Git repo, S3 bucket, or any HTTPS-reachable location they already control.\n- The font is stored alongside the Paratext project on a server the agent's host can read from.\n\nWhichever pattern the agent's environment provides, the agent supplies a final HTTPS URL and a sha256 in the payload. The `fonts` array does not assume any particular hosting.\n\n### 3. Other URL sources\n\nAny HTTPS-fetchable URL works as long as the worker can reach it without auth. Foundry CDNs, the user's own server, content-addressed CDNs. The agent verifies that the URL returns the expected content (or computes the sha256 from a known-good copy first).\n\n### 4. R2-staged fixtures (this Worker's OUTPUTS bucket)\n\nSome upstream fonts ship only as ZIPs (`silnrsi/font-gentium`'s GitHub releases, for example) — fine for desktop install, awkward for the per-file URL contract this schema requires. The pragmatic answer: stage the extracted TTFs once into the Worker's existing OUTPUTS bucket under a stable `outputs/fixtures/fonts/<family>-<version>/` prefix, then reference those URLs in payloads. Session 11 used this for Gentium Plus 6.200; the resulting URLs are content-addressed by both the bucket key and the embedded sha256, and persist as long as the bucket does. To stage:\n\n```bash\n# Extract from the upstream ZIP, then PUT into R2 via the Worker's internal upload route.\n# (Day-2 will replace this with presigned URLs; for now /internal/upload is unauthenticated\n# and accepts keys under outputs/.)\nunzip -j GentiumPlus-6.200.zip \"*/GentiumPlus-Regular.ttf\" -d /tmp\ncurl -X PUT --data-binary @/tmp/GentiumPlus-Regular.ttf \\\n  -H \"Content-Type: font/ttf\" \\\n  \"$WORKER_URL/internal/upload?key=outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Regular.ttf\"\n# Then sha256sum the local file and reference both URL and sha256 in the payload.\n```\n\nThis is appropriate for stable test fixtures and reusable canonical fonts. It is **not** appropriate for per-job user font supply — the `fonts` array in a normal payload should reference the user's own URL, not require pre-staging into our bucket.\n\n## Computing the sha256\n\nThe agent has a few options:\n\n- **From a local file:** standard `sha256sum file.ttf` or equivalent in the agent's host environment.\n- **From a known-good source:** if LFF or the foundry publishes hashes, use those.\n- **By staging-then-hashing:** make the file reachable at an HTTPS URL via the agent's host, then fetch it back and compute the hash.\n\nThe hash must be hex-encoded lowercase, 64 characters. The worker is strict about format.\n\n## Which fonts the project references\n\nTwo ways to know:\n\n### From the cfg\n\nThe `[document]` section's font keys (exact names vary; verify against canon's tooltip-derived index) name the body, italic, bold, and special-purpose fonts. For diglot, `[diglot_L]` and `[diglot_R]` carry their own font keys.\n\n### From inspection\n\nThe agent reads the project's existing `ptxprint.cfg` from project state, grep for keys mentioning `font`, and assembles the list.\n\nFor brand-new configs, default fonts apply. For Bible-scope configs in English, Charis SIL is the standard starting point.\n\n## What happens if a font is missing\n\nThe job fails hard:\n\n```\nexit_code: 4\nerrors: [\"! Font \\\\xyz not loadable\"]\nfailure_mode: \"hard\"\n```\n\nThe fix is to add the missing font to the payload's `fonts` array and re-submit. New `payload_hash`, fresh run.\n\n## What happens if a font's sha256 doesn't match\n\nThe worker aborts before invoking PTXprint:\n\n```\nexit_code: 1\nerrors: [\"sha256 mismatch on fonts[2]: CharisSIL-Bold.ttf\"]\nfailure_mode: \"hard\"\n```\n\nThe fix is to recompute the hash from the actual content at the URL, update the payload, and re-submit. If the URL's content has changed since the agent last verified it, that's a different problem (URL instability) — see below.\n\n## URL stability and the cache\n\n`payload_hash` includes the URLs in `fonts`, `sources`, `figures`. Two payloads pointing at the same font via different URLs hash differently and miss the cache. Two payloads pointing at the same URL but the URL's content has changed will both fail sha256 verification (because the agent's recorded hash was for the old content).\n\nFor best cache effectiveness:\n\n- Prefer LFF or content-addressed URLs (the hash is in the URL — same content, same URL, forever).\n- Avoid presigned URLs with short expiry as font sources unless the alternative is no font at all.\n- When a font is updated upstream, the agent's stored hash becomes obsolete; surface this to the user before re-running.\n\n## Multi-script projects\n\nOut of scope for v1.2 hackathon (per session 1 D-002, initial scope is English Bibles). Notes for future:\n\n- Each script needs its own font(s) installed. Hebrew, Arabic, CJK, Indic, Myanmar all have specific font requirements.\n- Some scripts need shaping engines (HarfBuzz). PTXprint handles this when fonts are present; the agent doesn't configure it.\n- The cfg's `[scripts]` section gates per-script behaviour. Don't enable scripts the project doesn't actually use.\n\n## A worked-minimum English-Bible payload\n\nSession 11 demonstrated this concrete payload end-to-end (job `802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd`, 2026-04-29, 4.7s wall-clock, two-page PDF, all four faces embedded per `pdffonts`). Sha256s are computed from the original `GentiumPlus-6.200.zip` ZIP extract and verified to match the bytes served by these URLs:\n\n```json\n\"fonts\": [\n  {\n    \"family_id\": \"gentiumplus\",\n    \"version\": \"6.200\",\n    \"filename\": \"GentiumPlus-Regular.ttf\",\n    \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Regular.ttf\",\n    \"sha256\": \"2c27e7da23ba44d135685836056833b304a388d3da346813189c60656dc02019\"\n  },\n  {\n    \"family_id\": \"gentiumplus\",\n    \"version\": \"6.200\",\n    \"filename\": \"GentiumPlus-Bold.ttf\",\n    \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Bold.ttf\",\n    \"sha256\": \"622ea9f2709d74f99d45c08d93cdf2a6d096406d3a1ec2939d02714f558b3dac\"\n  },\n  {\n    \"family_id\": \"gentiumplus\",\n    \"version\": \"6.200\",\n    \"filename\": \"GentiumPlus-Italic.ttf\",\n    \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Italic.ttf\",\n    \"sha256\": \"fedc1acdd2f1080941ed998cabee9759456f0e486fbd8169ff4238b37d3ac60d\"\n  },\n  {\n    \"family_id\": \"gentiumplus\",\n    \"version\": \"6.200\",\n    \"filename\": \"GentiumPlus-BoldItalic.ttf\",\n    \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-BoldItalic.ttf\",\n    \"sha256\": \"960e0a58ce1d7849c7a3e49f4fbc1ac4a27b58ef19a2d013ce637fe364b0a1f0\"\n  }\n]\n```\n\nFor a Charis SIL payload, the same shape applies — four entries, one per style file (Regular, Bold, Italic, BoldItalic), each with its own URL and sha256. When constructing a Charis SIL fonts array, source files from `silnrsi/font-charis` GitHub releases (or any of the LFF / R2 / generic-URL paths in the previous section) and verify the hash before submitting.\n\n### What PTXprint does with materialised fonts\n\nThe container materialises payload fonts into `<scratch>/<project_id>/shared/fonts/<filename>` (per `container/main.py: fetch_inputs`) before invoking PTXprint. **No `fc-cache` invocation or `OSFONTDIR` env var is required** — PTXprint's own startup logic adds the project's `shared/fonts/` directory to XeTeX's font resolution paths automatically. This was an open empirical question through session 10 and was confirmed working in session 11. Future sessions revisiting font wiring should not assume fontconfig refresh is needed unless a specific failure points there.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/_archive/font-resolution-design` for the (superseded) three-MCP design that v1.2 D-021 retired.*\n"},{"path":"articles/frt-local-and-cover-periphs.md","uri":"klappy://canon/articles/frt-local-and-cover-periphs","title":"FRTlocal and Cover Periphs — Front Matter Content","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","frt","front-matter","cover","periph"],"stability":"working","what_it_answers":"What goes in `FRTlocal.sfm`? How do covers work in headless? Which special markers does the agent need?","sections":[{"slug":"what-frtlocal-sfm-is","heading":"What `FRTlocal.sfm` is","start":22,"end":32},{"slug":"three-modes","heading":"Three modes","start":32,"end":88},{"slug":"special-ptxprint-codes-in-front-matter","heading":"Special PTXprint codes in front matter","start":88,"end":104},{"slug":"cover-periphs","heading":"Cover periphs","start":104,"end":136},{"slug":"variables-the-vars-cfg-section-zvar","heading":"Variables: the `[vars]` cfg section + `\\zvar`","start":136,"end":155},{"slug":"including-in-the-payload","heading":"Including in the payload","start":155,"end":172},{"slug":"testing-front-matter-changes","heading":"Testing front matter changes","start":172,"end":182},{"slug":"when-not-to-use-frtlocal","heading":"When NOT to use FRTlocal","start":182,"end":191}],"body":"---\ntitle: \"FRTlocal and Cover Periphs — Front Matter Content\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"frt\", \"front-matter\", \"cover\", \"periph\"]\nderives_from: \"canon/governance/headless-operations.md (Part 6 FRTlocal.sfm subsection, cover periphs)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# FRTlocal and Cover Periphs\n\n> **What this answers.** What goes in `FRTlocal.sfm`? How do covers work in headless? Which special markers does the agent need?\n>\n> **Related articles.** `klappy://canon/articles/usfm-markers-headless` · `klappy://canon/articles/payload-construction` · `klappy://canon/articles/stylesheet-format`\n\n---\n\n## What `FRTlocal.sfm` is\n\nUSFM source for the publication's front matter — title page, copyright block, foreword, ToC, alphabetical contents, and (when wrap-around covers are wanted) cover periphs. Lives at:\n\n```\nshared/ptxprint/<config>/FRTlocal.sfm\n```\n\nIncluded in the payload's `config_files` map. PTXprint typesets the FRT book using this content when the configuration's book scope includes FRT, or when the agent passes `FRT` in the payload's `books` array.\n\n## Three modes\n\nPTXprint's GUI offers three starting templates the agent can mirror in headless:\n\n### Basic\n\nTitle page, publication data block, ToC. Minimal.\n\n```\n\\id FRT\n\\mt1 \\zvar|booktitle\\*\n\\mt2 \\zvar|booksubtitle\\*\n\n\\ip \\zcopyright\n\n\\ip \\ztoc|main\\*\n```\n\n### Advanced\n\nTitle, pubdata, foreword, preface, ToC, alphabetical contents, four cover periphs.\n\n```\n\\id FRT\n\n\\periph Title Page|id=\"title\"\n\\mt1 \\zvar|booktitle\\*\n\\ip Published \\zvar|imprint_year\\*\n\n\\periph Publication Data|id=\"pubdata\"\n\\zcopyright\n\\zlicense\n\n\\periph Foreword|id=\"foreword\"\n\\ip Content...\n\n\\periph Table of Contents|id=\"contents\"\n\\ip \\ztoc|main\\*\n\n\\periph Front Cover|id=\"coverfront\"\n... cover content ...\n\n\\periph Back Cover|id=\"coverback\"\n... cover content ...\n\n\\periph Spine|id=\"coverspine\"\n... cover content ...\n\n\\periph Whole Cover|id=\"coverwhole\"\n... cover content ...\n```\n\n### Paratext copy\n\nA local copy of the project's source FRT book the user customizes. The agent reads from project state and includes unchanged unless edits are requested.\n\n## Special PTXprint codes in front matter\n\n| Marker | Form | Purpose |\n|---|---|---|\n| `\\zcopyright` | bare (no `\\|arg`, no closing `\\*`) | Copyright block from cfg |\n| `\\zlicense` | bare | License block from cfg |\n| `\\zimagecopyrights[LANG]` | bare with LANG suffix | Image copyrights (`en`, `hu`, `ro`, `fr`, `es`, `id`) |\n| `\\zccimg <license>\\|size=\"...\"\\*` | takes args | Creative Commons logo |\n| `\\ztoc\\|main\\*` | takes scope arg | Generate ToC (`main`, `nt`, `ot`, `sortc`, `sorta`) |\n| `\\zvar\\|name\\*` | takes variable name | Substitute from `[vars]` cfg section |\n| `\\zbl\\|N\\*` | takes line count | N blank lines |\n| `\\zgap\\|N<unit>\\*` | takes length | Vertical gap (`mm`, `in`, `em`, `pt`, `cm`) |\n| `\\nopagenums` / `\\dopagenums` / `\\resetpagenums -1` | bare | Page numbering control |\n\nSee `klappy://canon/articles/usfm-markers-headless` for the full extension reference.\n\n## Cover periphs\n\nWhen the user wants wrap-around covers, four periphs go in `FRTlocal.sfm`:\n\n```\n\\periph Front Cover|id=\"coverfront\"\n\\mt1 \\zvar|booktitle\\*\n\\ip \\zccimg by-nd\\|size=\"col\" pgpos=\"p\" scale=\"0.20\"\\*\n\n\\periph Back Cover|id=\"coverback\"\n\\ip Brief description of the publication.\n\\ip \\zqrcode\\|pgpos=\"cr\" size=\"1.2cm\" data=\"https://...\"\\*\n\n\\periph Spine|id=\"coverspine\"\n\\mt1 \\zvar|booktitle\\*\n\n\\periph Whole Cover|id=\"coverwhole\"\n% Fallback content used when the cover layout collapses to one piece\n\\ip ...\n```\n\nStyle cover content via the category-and-id qualifier in `ptxprint.sty`:\n\n```\n\\Marker cat:coverfront|mt1\n\\Color FFFFFF\n\\FontSize 2.4\n\\Bold\n```\n\nSee `klappy://canon/articles/stylesheet-format` for the full pattern.\n\n## Variables: the `[vars]` cfg section + `\\zvar`\n\nDefine variables in the cfg's `[vars]` section, reference them in FRT content:\n\n```ini\n[vars]\nbooktitle = New Testament\nbooksubtitle = First Edition\nimprint_year = 2026\n```\n\n```\n\\mt1 \\zvar|booktitle\\*\n\\mt2 \\zvar|booksubtitle\\*\n\\ip Published \\zvar|imprint_year\\*\n```\n\nPTXprint substitutes at typeset time. Useful for keeping the title text in one place and reusing it in headers, footers, and front matter.\n\n## Including in the payload\n\n```json\n\"config_files\": {\n  \"shared/ptxprint/Default/ptxprint.cfg\": \"...\",\n  \"shared/ptxprint/Default/FRTlocal.sfm\": \"\\\\id FRT\\n\\\\mt1 \\\\zvar|booktitle\\\\*\\n\\\\ip \\\\ztoc|main\\\\*\\n\"\n}\n```\n\nMake sure `FRT` is in the payload's `books` array if the FRT content should be typeset:\n\n```json\n\"books\": [\"FRT\", \"MAT\", \"MRK\", \"LUK\", \"JHN\"]\n```\n\nIf `FRT` isn't listed, the front matter content is parsed but not output.\n\n## Testing front matter changes\n\nFront matter changes are visible only when FRT is in `books`. For fast iteration:\n\n1. Set `books: [\"FRT\", \"JUD\"]` (FRT plus a single short book) and `mode: \"simple\"`.\n2. Submit, inspect.\n3. Once the front matter looks right, expand `books` to the full scope.\n\nThis avoids waiting for a full NT autofill while debugging cover layout.\n\n## When NOT to use FRTlocal\n\n- If the project's source FRT book in Paratext is the source of truth — the team maintains it there and the agent shouldn't fork it. Use Paratext-copy mode instead.\n- For per-book introductions — those go in each book's `\\imt`/`\\ip` markers in source USFM, not in FRT.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`.*\n"},{"path":"articles/hero-metrics-and-storytelling.md","uri":"klappy://canon/articles/hero-metrics-and-storytelling","title":"Hero Metrics & Storytelling — Total Pages Typeset, X-Ray Timelines, and the Public Narrative","audience":"project","tags":["canon","article","telemetry","hero-metrics","storytelling","pages-typeset","x-ray","waterfall","phase-timing","snapshot","long-term-retention","public-dashboard","vodka-architecture","ptxprint","mcp","v1.2-aligned","ptxprint_telemetry","analytics-engine"],"stability":"working","what_it_answers":"Telemetry governance defines what is allowed to be collected and why. This article defines what — of everything that is allowed — actually tells the story. The hero metric is **total pages of scripture typeset, lifetime**. The X-Ray view is","sections":[{"slug":"why-this-article-exists-separately-from-governance","heading":"Why This Article Exists Separately From Governance","start":22,"end":37},{"slug":"the-hero-stat-total-pages-typeset-lifetime","heading":"The Hero Stat — Total Pages Typeset, Lifetime","start":37,"end":77},{"slug":"the-x-ray-view-per-job-phase-waterfall","heading":"The X-Ray View — Per-Job Phase Waterfall","start":77,"end":120},{"slug":"storytelling-queries-the-weekly-and-monthly-rollups","heading":"Storytelling Queries — The Weekly And Monthly Rollups","start":120,"end":209},{"slug":"long-term-retention-strategy-surviving-the-90-day-window","heading":"Long-Term Retention Strategy — Surviving The 90-Day Window","start":209,"end":291},{"slug":"public-dashboard-composition","heading":"Public Dashboard Composition","start":291,"end":327},{"slug":"vodka-boundary-check","heading":"Vodka Boundary Check","start":327,"end":343},{"slug":"future-hero-metrics-when-they-justify-themselves","heading":"Future Hero Metrics — When They Justify Themselves","start":343,"end":379},{"slug":"operating-principle","heading":"Operating Principle","start":379,"end":392},{"slug":"see-also","heading":"See Also","start":392,"end":408}],"body":"---\nuri: klappy://canon/articles/hero-metrics-and-storytelling\ntitle: \"Hero Metrics & Storytelling — Total Pages Typeset, X-Ray Timelines, and the Public Narrative\"\naudience: project\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"canon\", \"article\", \"telemetry\", \"hero-metrics\", \"storytelling\", \"pages-typeset\", \"x-ray\", \"waterfall\", \"phase-timing\", \"snapshot\", \"long-term-retention\", \"public-dashboard\", \"vodka-architecture\", \"ptxprint\", \"mcp\", \"v1.2-aligned\", \"ptxprint_telemetry\", \"analytics-engine\"]\ndate: 2026-04-30\nderives_from: \"klappy://canon/governance/telemetry-governance, klappy://canon/specs/ptxprint-mcp-v1-2-spec\"\ncompanion_to: \"klappy://canon/governance/telemetry-governance\"\ncanonical_status: non_canonical\ngoverns: \"How telemetry already collected per the governance schema is composed into the project's public hero stat, X-Ray per-job timeline view, weekly/monthly storytelling rollups, and snapshot strategy for surviving the 90-day Analytics Engine retention window.\"\nstatus: draft_pending_fresh_review\n---\n\n# Hero Metrics & Storytelling — Total Pages Typeset, X-Ray Timelines, and the Public Narrative\n\n> Telemetry governance defines what is allowed to be collected and why. This article defines what — of everything that is allowed — actually tells the story. The hero metric is **total pages of scripture typeset, lifetime**. The X-Ray view is the **per-job phase waterfall**. The narrative is **shared infrastructure quietly producing publication-quality scripture, measured in pages put into the world**. Nothing in this article adds a single new schema slot, a single new server opinion, or a single new privacy boundary. Everything is composition over the existing `ptxprint_telemetry` dataset documented in `klappy://canon/governance/telemetry-governance`.\n\n---\n\n## Why This Article Exists Separately From Governance\n\n`klappy://canon/governance/telemetry-governance` is **constraint canon**. It specifies what fields are collected, what is excluded, the privacy floor, the security guards, the schema hygiene rules. Constraint canon changes only when the data contract changes.\n\nThis article is **derived-practice canon**. It specifies which compositions of the existing data tell the story worth telling — a hero stat, a debugging waterfall, a public dashboard, a long-term snapshot strategy. Practice canon changes whenever the maintainer learns a better way to tell the story; the schema underneath stays the same.\n\nMixing the two would have produced one of two failure modes:\n\n1. **Governance creep** — the storytelling section grows, gets opinionated, and someone treats \"the dashboard should show X\" as a contract that requires a schema change to honor. The schema is supposed to be answer-driven, not dashboard-driven.\n2. **Practice ossification** — the governance review gate (PR review by a fresh session) starts blocking presentation tweaks. Story telling should be cheap to revise; data contracts should be expensive.\n\nSplitting them keeps each free to evolve at its own cadence.\n\n---\n\n## The Hero Stat — Total Pages Typeset, Lifetime\n\n### Why Pages, Not Jobs Or Builds\n\nThe choice of hero metric is a values statement disguised as a number.\n\nA jobs-completed counter rewards activity. A builds-shipped counter rewards throughput. Neither maps to the mission. The mission is **scripture put into the world** — and a 1,200-page Old Testament build is 1,200× more of that than a 1-page proof print, even though both increment \"jobs\" by exactly one.\n\nPages typeset is the closest available proxy for the actual unit of value the project produces. It is:\n\n- **Mission-aligned** — every page typeset is a page of formatted scripture that did not exist in publication-quality form before the run. A team somewhere can print, fold, and distribute it.\n- **Already in the schema** — `pages_count` is `double10` per `klappy://canon/governance/telemetry-governance` § \"Numeric Values (Doubles)\". It is populated only on `job_terminal` events with `failure_mode = success`. Adding it to the dashboard requires zero schema change.\n- **Honest** — soft failures and hard failures do not contribute. A degraded run that emitted a PDF without pictures (the `klappy://canon/articles/failure-mode-taxonomy` \"soft\" case) does not count toward the hero stat. The number rewards real output, not partial output.\n- **Free to compute** — it is one `SUM` over one column with one `WHERE` clause. The query runs in milliseconds against Analytics Engine.\n\n### The Query\n\n```sql\nSELECT\n  SUM(pages_count * _sample_interval) AS pages_typeset_lifetime\nFROM ptxprint_telemetry\nWHERE event_type = 'job_terminal'\n  AND failure_mode = 'success'\n```\n\nNote the `* _sample_interval` factor. Cloudflare Analytics Engine samples writes under load; multiplying each row's value by `_sample_interval` recovers the unsampled aggregate. This is the same pattern documented in governance for count aggregations, applied to a numeric sum.\n\nThe lifetime window is unbounded — no `WHERE timestamp > NOW() - INTERVAL '...'`. This intentionally exposes the 90-day retention boundary: lifetime totals computed against raw Analytics Engine data will silently miss everything older than 90 days. The fix is in § \"Long-Term Retention Strategy\" below.\n\n### The Tagline Form\n\nThe hero stat is meant to be displayed as one number with one label. Examples:\n\n- **N,NNN,NNN pages of scripture typeset by PTXprint MCP**\n- **N,NNN,NNN pages typeset · M,MMM successful builds · K,KKK distinct teams**\n\nAvoid framings that imply unique books, unique translations, or unique people. The data does not support those claims (no project IDs, no book codes, only opt-in consumer labels). It does support pages, builds, and distinct labels.\n\n---\n\n## The X-Ray View — Per-Job Phase Waterfall\n\n### What It Is\n\nA waterfall view of one specific job: queue wait, input fetch, typesetting, autofill, upload, end-to-end. Each phase as a horizontal bar with its `duration_ms`. The total height of the bar shows where time actually went.\n\nThis is the diagnostic artifact that turns \"your job took 47 seconds\" into \"your job took 47 seconds, of which 38 seconds was the typesetting phase, 4 seconds was input fetch, 3 seconds was upload, and the rest was queue wait.\" It is the difference between a number and an explanation.\n\n### Why It Stays Vodka-Pure\n\nThe data is already collected. Per governance § \"Job Lifecycle Events\", the Container emits a `job_phase` event at every phase transition with `duration_ms` recording how long the **just-completed** phase took. Six phase values exist in the enum (`queued | fetching_inputs | typesetting | autofill | uploading | done`). Every dispatched job produces a complete sequence.\n\nThe X-Ray view is a query over that data, not a new event class. The server adds zero new opinions. The waterfall is a visualization choice the consumer makes.\n\n### The Query\n\nA single job's waterfall is keyed by `payload_hash_prefix`. Per governance, the prefix is the explicit pseudonymous boundary — 8 hex chars, designed to collide privacy-positively when two teams typeset the same public Bible. Querying by it for a specific job is the documented use case.\n\n```sql\nSELECT\n  phase,\n  duration_ms,\n  timestamp\nFROM ptxprint_telemetry\nWHERE event_type = 'job_phase'\n  AND payload_hash_prefix = ?\nORDER BY timestamp ASC\n```\n\nThis returns the phase sequence for one job in chronological order. The consumer composes the waterfall by stacking the durations.\n\n### Aggregate Phase-Distribution View\n\nThe aggregate \"where does time go on average\" view is in governance § \"Where time goes — average phase duration\". The X-Ray view is its per-job complement: governance shows the distribution shape across all jobs; the X-Ray shows one specific bar inside that distribution.\n\nUseful pairing: render both side-by-side in the per-job result page. \"Your job was 47 seconds. Average for all jobs is 38 seconds. Here's why yours was 9 seconds slower\" — the typesetting phase took 12 seconds longer than typical, autofill took 3 seconds less, the rest was within 1 second of average.\n\n### What X-Ray Is Not\n\nThe X-Ray view is not a profiler. It does not break time down within a phase. If `typesetting` took 38 seconds, the X-Ray says `typesetting: 38000ms` and stops. Sub-phase profiling (e.g., XeTeX pass 1 vs. pass 2 vs. pass 3) is **not in the schema today** and should not be added speculatively per governance § \"Schema Hygiene\". If sub-phase distinctions become operationally necessary, the path is: amend governance to define the schema slot, ship the Container change to emit it, then update this article to add the deeper waterfall query.\n\n---\n\n## Storytelling Queries — The Weekly And Monthly Rollups\n\nThe hero stat is the headline. These are the supporting numbers that make the headline credible.\n\n### Pages typeset this week\n\n```sql\nSELECT\n  SUM(pages_count * _sample_interval) AS pages_typeset\nFROM ptxprint_telemetry\nWHERE event_type = 'job_terminal'\n  AND failure_mode = 'success'\n  AND timestamp > NOW() - INTERVAL '7' DAY\n```\n\n### Pages typeset by week, last 90 days\n\n```sql\nSELECT\n  toStartOfWeek(timestamp) AS week,\n  SUM(pages_count * _sample_interval) AS pages_typeset,\n  SUM(_sample_interval) AS successful_builds\nFROM ptxprint_telemetry\nWHERE event_type = 'job_terminal'\n  AND failure_mode = 'success'\n  AND timestamp > NOW() - INTERVAL '90' DAY\nGROUP BY week\nORDER BY week ASC\n```\n\nThe `ORDER BY week ASC` is intentional. Storytelling rollups read left-to-right as a trajectory; they should ascend through time so the eye lands on the most recent value last. Operational dashboards use `DESC` so the freshest row is at the top; storytelling charts use `ASC` so the trend points forward.\n\n### Pages typeset per distinct team this month\n\n```sql\nSELECT\n  consumer_label,\n  SUM(pages_count * _sample_interval) AS pages_typeset,\n  SUM(_sample_interval) AS successful_builds\nFROM ptxprint_telemetry\nWHERE event_type = 'job_terminal'\n  AND failure_mode = 'success'\n  AND timestamp > NOW() - INTERVAL '30' DAY\n  AND consumer_label != 'unknown'\nGROUP BY consumer_label\nORDER BY pages_typeset DESC\nLIMIT 25\n```\n\nThe `consumer_label != 'unknown'` filter excludes traffic that did not self-identify. Per governance § \"Consumer Identification Model\", unidentified traffic is the default; this query intentionally rewards identification by surfacing only the labels that participated in the transparency leaderboard.\n\n### Time saved by cache hits\n\nA cache hit returns in seconds and consumes essentially zero Container minutes. A cache miss dispatches a fresh build that may take 30 minutes. The time saved by the cache layer is one of the strongest arguments for content-addressed storage.\n\n```sql\nSELECT\n  SUM(IF(cache_outcome = 'hit', _sample_interval, 0)) AS cache_hits,\n  AVG(IF(event_type = 'job_terminal', duration_ms, NULL)) AS avg_dispatch_ms,\n  SUM(IF(cache_outcome = 'hit', _sample_interval, 0)) *\n    AVG(IF(event_type = 'job_terminal', duration_ms, NULL)) / 1000 / 60 AS minutes_saved_estimate\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n```\n\nThis is an estimate, not a measurement. The assumption is that the average cache hit, had it missed, would have taken roughly the average dispatch time. That assumption is reasonable for a content-addressed cache (popular payloads probably do not deviate wildly from the population mean) but it is not exact. Frame it accordingly: \"approximately N hours of Container time saved by cache hits this month\" — never an exact number.\n\n### Overfull-box health trend\n\nOverfull `\\hbox` warnings are the most common silent-degradation signal. A jump in average overfull count per build often precedes user complaints about layout quality.\n\n```sql\nSELECT\n  toStartOfWeek(timestamp) AS week,\n  AVG(overfull_count) AS avg_overfull,\n  quantile(0.95)(overfull_count) AS p95_overfull,\n  SUM(_sample_interval) AS builds\nFROM ptxprint_telemetry\nWHERE event_type = 'job_terminal'\n  AND failure_mode IN ('success', 'soft')\n  AND timestamp > NOW() - INTERVAL '90' DAY\nGROUP BY week\nORDER BY week ASC\n```\n\nThis is operational, not strictly storytelling — but it pairs with the hero stat as a quality watch. \"We typeset N pages this week, with average overfull count flat at K — output volume is up, quality stable.\" That's a healthy story. \"We typeset N pages this week, average overfull jumped from K to 4K\" is the silent-degradation alarm.\n\n---\n\n## Long-Term Retention Strategy — Surviving The 90-Day Window\n\n### The Problem\n\nPer governance § \"Long-Term Retention\", Cloudflare Analytics Engine retains data for 3 months. After 90 days, the raw events are gone. A lifetime hero stat computed against raw Analytics Engine data is therefore **not lifetime** — it is \"last 90 days.\" Six months in, that gap stops being acceptable for a stat that is supposed to be monotonic.\n\nGovernance correctly notes this is a known constraint and explicitly defers the snapshot mechanism: \"No automated weekly snapshot job exists in v1.\" This article specifies the snapshot mechanism so the deferral can be closed when implementation catches up.\n\n### The Snapshot Pattern\n\nA Worker Cron Trigger runs once a week. It executes a small set of aggregate queries against Analytics Engine, reads the results, and appends one row per query to a durable store. The durable store persists past the Analytics Engine retention window; lifetime totals are then computed by summing the snapshot rows, not by re-querying raw events.\n\nThe snapshot tier is not the source of truth. Raw Analytics Engine within the retention window stays authoritative for the last 90 days. The snapshot is the **archive of what raw Analytics Engine knew about each week, captured before the data expired**. Lifetime totals = sum of weekly snapshots + current incomplete week from raw.\n\n### Storage Choice — R2\n\nThree candidates exist for the snapshot store: Cloudflare KV, R2, or a canon-article markdown file.\n\n- **KV** is fast and simple but has no schema, awkward for tabular data, and inspecting the contents requires either the dashboard or an API call.\n- **R2** is object storage with no schema constraints, supports straightforward append patterns (one JSONL row per snapshot), and can be made publicly readable so the same transparency principle as `telemetry_public` extends to the archive.\n- **A canon markdown file** is human-visible and version-controlled but pollutes the canon repo with machine-generated data and creates merge conflicts if humans edit nearby content.\n\n**R2 is the recommended store.** A single bucket (`ptxprint-telemetry-snapshots`) with one object per metric (`pages-typeset-weekly.jsonl`, `cache-hits-weekly.jsonl`, etc.) keeps the snapshot independent per metric, supports cheap reads, and exposes the archive at a stable public URL.\n\n### What Gets Snapshotted\n\nOnly the queries this article designates as load-bearing for hero/storytelling rollups:\n\n- Weekly `pages_typeset` (success only) — the hero stat archive\n- Weekly `successful_builds` count — the supporting denominator\n- Weekly `cache_hits` and `cache_misses` — for cache-effectiveness trend beyond 90 days\n- Weekly `failure_mode` distribution — for soft-failure rate trend beyond 90 days\n\nSnapshotting raw events would defeat the privacy floor and the retention design. Only **pre-aggregated counts** go into the archive. The granularity is the week. Sub-week resolution is not preserved past 90 days; that is intentional — anyone needing per-day historical data should query within the window before it expires.\n\n### The Snapshot Schema\n\nOne JSONL line per snapshot row, written to R2:\n\n```json\n{\n  \"metric\": \"pages_typeset_weekly\",\n  \"week_start\": \"2026-04-27\",\n  \"value\": 14823,\n  \"snapshotted_at\": \"2026-05-04T00:00:00Z\",\n  \"source\": \"ptxprint_telemetry\"\n}\n```\n\nThe `snapshotted_at` field captures when the snapshot was taken (not when the data describes), so a re-snapshot can detect and skip already-archived weeks. The `source` field reserves space for future migrations if the dataset name ever changes.\n\n### The Cron Schedule\n\nOnce a week, on Monday at 00:00 UTC. Captures the just-completed week before any chance the data starts expiring (90 days is well beyond a week of safety margin). Cron runs are idempotent — re-running a snapshot for a week already archived overwrites with the same values. No deduplication logic needed in the snapshot writer.\n\n### Lifetime Hero Stat — The Composite Query\n\nLifetime totals once snapshots exist:\n\n```text\nlifetime_pages_typeset =\n  SUM(value FROM R2 ptxprint-telemetry-snapshots/pages-typeset-weekly.jsonl)\n  +\n  SELECT SUM(pages_count * _sample_interval)\n  FROM ptxprint_telemetry\n  WHERE event_type = 'job_terminal'\n    AND failure_mode = 'success'\n    AND timestamp >= toStartOfWeek(NOW() - INTERVAL '7' DAY)\n```\n\nRead the snapshot archive, sum the historical weeks, add the current incomplete week from raw. The current week is not yet snapshotted, so it must come from raw; everything older comes from the archive even if still within the 90-day window (consistency is more important than freshness past the current week).\n\nThis composition is what the public dashboard's hero number actually queries. Document it as a single tool — `get_lifetime_hero_stat()` is a candidate composite query, not a new schema slot.\n\n### Rebuild From Raw — The Bootstrap\n\nUntil the cron runs once, the snapshot archive is empty. The bootstrap procedure: a one-shot script reads all weeks within the current 90-day window from raw Analytics Engine and writes them to the archive. After bootstrap, the lifetime stat reflects only the last 90 days; future weeks accumulate normally.\n\nThis is a one-time data loss for any pages typeset before the bootstrap point. The mitigation is to schedule the bootstrap as soon as the snapshot mechanism ships — every week of delay between v1.2 telemetry shipping and snapshot bootstrapping is a week of pages that will not survive the retention window.\n\n---\n\n## Public Dashboard Composition\n\nThe dashboard is the user-facing surface. It is what someone seeing PTXprint MCP for the first time encounters. The composition decisions here matter as much as the metric choices.\n\n### Above The Fold — One Hero, Three Supports\n\n```\n                  N,NNN,NNN\n            pages of scripture typeset\n\n   M,MMM successful builds   ·   K,KKK distinct teams   ·   J,JJJ% cache hit rate\n```\n\nOne number large enough to read from across the room. Three smaller numbers as supporting context. Nothing else above the fold. Resist the temptation to add a fourth or fifth number; each one dilutes the hero.\n\n### Below The Fold — Three Charts\n\n1. **Pages typeset per week, last 13 weeks** — a column chart showing trajectory. Adds the time dimension to the headline number.\n2. **Phase distribution, last 30 days** — a stacked horizontal bar showing how time gets spent on average across `queued | fetching_inputs | typesetting | autofill | uploading`. Tells the reader what the system actually spends its time doing.\n3. **Failure mix, last 30 days** — a stacked bar (or simple bar) showing `success | soft | hard | cancelled | timeout` as percentages. Honest about what does and doesn't work.\n\n### What Not To Show On The Public Dashboard\n\n- **Per-job X-Ray waterfalls.** The X-Ray view is a debugging artifact, not a public-facing one. It belongs on a per-job result page (the agent's tool response, the BT Servant relay message), not on the homepage.\n- **Document leaderboard ranks.** Useful for the maintainer to see which canon docs do work. Not interesting to the average visitor. Belongs in a separate transparency page, not the headline dashboard.\n- **Consumer leaderboard ranks beyond top 10.** Encouraging participation matters; surfacing a long tail of unidentified traffic does not.\n- **Raw query interface.** `telemetry_public` is reachable from the docs page or the transparency page. It does not belong on the marketing dashboard. Visitors who want to query are a different audience than visitors who want to read the headline.\n\n### The Labels\n\nWords on a dashboard carry as much weight as numbers. Use **pages typeset** not \"pages rendered\" or \"pages produced\" — typesetting is what PTXprint does, and the verb belongs to the discipline. Use **distinct teams** not \"users\" or \"consumers\" — translation teams are the audience, and the noun honors them. Use **cache hit rate** not \"efficiency percentage\" — the reader who knows cache infrastructure understands immediately, and the reader who does not is not blocked.\n\nAvoid \"powered by AI\", \"intelligent typesetting\", or any other adjective that introduces noise where the number itself does the work.\n\n---\n\n## Vodka Boundary Check\n\nThis article is canon. Canon is allowed to express opinion. The question is whether **the server** has acquired any new opinions as a consequence of this article shipping.\n\n| Concern | Answer |\n|---|---|\n| Does this article add a schema slot? | No. Every metric used (`pages_count`, `duration_ms`, `phase`, `failure_mode`, `cache_outcome`, `payload_hash_prefix`, `consumer_label`, `overfull_count`) is already documented in `klappy://canon/governance/telemetry-governance` § \"Structural Dimensions\" and § \"Numeric Values\". |\n| Does this article add a tool? | One candidate composite tool is named — `get_lifetime_hero_stat()`. It is a SQL composition over the existing `telemetry_public` interface plus an R2 read. If shipped, it is infrastructure (composition) not opinion (new collection). The minimum implementation can ship as a documented query the agent runs through the existing tool surface; promoting it to a dedicated tool is a Cron-and-R2 question, not a schema question. |\n| Does this article add infrastructure? | One: the weekly Cron Trigger and R2 snapshot bucket described in § \"Long-Term Retention Strategy\". The Cron is generic (\"read aggregate queries, write to R2\") not domain-shaped. The R2 bucket holds aggregates only; no raw events, no privacy-floor surface widening. |\n| Does this article move the privacy floor? | No. Every query filters on existing fields. The snapshot archive contains only weekly aggregates that are already public via `telemetry_public`. Two teams typesetting the same Bible still share a `payload_hash_prefix`; the archive cannot uniquely identify either. |\n| Could this article be removed without consequence? | The article could be removed; the data underneath persists. What removal would lose: the maintainer's documented choice of which compositions tell the story, the hero-stat tagline, the snapshot recipe. Those would have to be re-derived. The cost is real but bounded — this article is load-bearing for narrative, not for operational correctness. |\n\nThe line passes. Storytelling and presentation choices live in canon (here). Schema, privacy, and security choices live in governance. Code lives in the server and changes only when governance changes.\n\n---\n\n## Future Hero Metrics — When They Justify Themselves\n\nThis section names hero candidates that **do not exist today** and the conditions under which they would justify a schema addition. Per governance § \"Schema Hygiene\", new slots are added only when a real operational question demands them. Speculative slots reproduce the oddkit `cache_tier` deprecation problem.\n\n### Pages-by-script\n\n\"Pages typeset in Devanagari script\" is a meaningful storytelling number. It would require either a new `script_family` enum populated by the Container at typeset time, or derivation from the `fonts` array in the payload. The Container has the information (it just shipped fonts to XeTeX) but does not currently emit it.\n\n**Justification threshold:** when the project starts measurably serving non-Latin-script translation teams and a maintainer wants to show that publicly. Until then, \"pages typeset\" without script breakdown is honest.\n\n### Books-typeset\n\nDistinct count of books shipped (per build, per week, per lifetime). The payload contains `books`; `sources_count` is collected but does not equal book count (one source can contain multiple books; one book can come from multiple sources).\n\n**Justification threshold:** when \"pages\" is not enough to communicate scope and \"books\" would meaningfully change the story. Probably never — pages is more granular and more honest.\n\n### Time-saved estimate as a first-class metric\n\nThe cache time-saved query in § \"Storytelling Queries\" is an estimate computed at query time. Promoting it to a dashboard metric would warrant validating the estimate against real measurements (some sample of cache hits run through full dispatch to compare). \n\n**Justification threshold:** when someone asks \"how much CPU did the cache save?\" and the maintainer wants a more authoritative answer than \"approximately.\"\n\n### CO2 estimate\n\nCloudflare publishes regional carbon-intensity figures. Multiplied by Container minutes consumed, this gives a coarse CO2-per-build estimate that some audiences find compelling.\n\n**Justification threshold:** when the project starts being asked sustainability questions and a credible-but-coarse number would be more useful than no number. The existing `duration_ms` per `job_phase` event is sufficient input; no schema change required.\n\n### What Will NOT Become A Hero Metric\n\n- **Anything per-project, per-language, per-region, per-team-name.** The privacy floor forbids this. The hero metric story is told without identifying who did the work.\n- **Anything per-passage, per-book, per-verse.** Same reason. Pages aggregates content; book codes identify it.\n- **Engagement metrics.** No \"average session length\", \"return visitor rate\", or similar — the project does not log sessions or visitors and should not start.\n\n---\n\n## Operating Principle\n\n**Compose existing data into the story; never invent new data to make a story prettier.**\n\n- The hero stat exists because `pages_count` already exists. If `pages_count` did not, the right move would be a governance amendment, not a workaround in this article.\n- The X-Ray view exists because `job_phase` events with `duration_ms` already exist. Sub-phase profiling does not exist; this article does not pretend it does.\n- The snapshot strategy preserves what is already collected, against a known retention boundary. It does not collect anything new.\n- The public dashboard surfaces what exists in the most honest, most readable form. It does not embellish, gamify, or rebrand.\n\nIf a story worth telling cannot be told from the current schema, the answer is to amend governance — not to bend the storytelling article around what the schema does and does not allow.\n\n---\n\n## See Also\n\n### PTXprint canon\n\n- [Telemetry Governance — What PTXprint MCP Tracks and Why](klappy://canon/governance/telemetry-governance) — the data contract this article composes over\n- [v1.2 Specification](klappy://canon/specs/ptxprint-mcp-v1-2-spec) — the tool surface the telemetry observes\n- [Failure Mode Taxonomy](klappy://canon/articles/failure-mode-taxonomy) — the source of the `failure_mode` enum used in storytelling queries\n- [Headless Operations Overview](klappy://canon/governance/headless-operations) — the agent-facing operational KB\n\n### Upstream pattern (klappy.dev)\n\n- [Vodka Architecture](klappy://canon/principles/vodka-architecture) — the design pattern the boundary check defends\n- [DRY Canon — Says It Once](klappy://canon/principles/dry-canon-says-it-once) — the principle that justified splitting this article from governance\n- [Maintainability — One Person, Indefinitely](klappy://canon/principles/maintainability-one-person-indefinitely) — the principle the hero stat ultimately serves\n- [Verification Requires Fresh Context](klappy://canon/principles/verification-requires-fresh-context) — why this article carries `status: draft_pending_fresh_review`\n"},{"path":"articles/output-naming.md","uri":"klappy://canon/articles/output-naming","title":"Output Naming — Interpreting PDF and Log URLs","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","output","naming-convention","r2"],"stability":"working","what_it_answers":"What does a v1.2 output URL look like? How does the filename relate to the inputs? How long is it valid?","sections":[{"slug":"the-two-urls-the-agent-receives","heading":"The two URLs the agent receives","start":22,"end":35},{"slug":"the-filename-convention","heading":"The filename convention","start":35,"end":56},{"slug":"the-content-addressed-prefix","heading":"The content-addressed prefix","start":56,"end":71},{"slug":"url-validity","heading":"URL validity","start":71,"end":83},{"slug":"when-to-surface-the-url-to-the-user","heading":"When to surface the URL to the user","start":83,"end":91},{"slug":"output-retention","heading":"Output retention","start":91,"end":103},{"slug":"predicted-vs-actual","heading":"Predicted vs. actual","start":103,"end":116},{"slug":"what-to-do-when-an-old-url-is-shared-with-a-new-agent","heading":"What to do when an old URL is shared with a new agent","start":116,"end":131}],"body":"---\ntitle: \"Output Naming — Interpreting PDF and Log URLs\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"output\", \"naming-convention\", \"r2\"]\nderives_from: \"canon/governance/headless-operations.md (Part 2 Output path convention)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Output Naming\n\n> **What this answers.** What does a v1.2 output URL look like? How does the filename relate to the inputs? How long is it valid?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/file-system-map`\n\n---\n\n## The two URLs the agent receives\n\nA successful `get_job_status` response carries two presigned R2 GET URLs:\n\n```\npdf_url:  https://<account>.r2.cloudflarestorage.com/ptxprint-outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf?<presigned-params>\nlog_url:  https://<account>.r2.cloudflarestorage.com/ptxprint-outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.log?<presigned-params>\n```\n\nBoth follow the same pattern: a content-addressed prefix (`<payload_hash>`) plus a deterministic filename derived from the payload's inputs.\n\n`submit_typeset` also returns a `predicted_pdf_url` synchronously at submission time, before the job runs. The agent can use it to construct user-facing download links in advance — but the file at that URL doesn't exist until the job completes (or the URL is being returned as a cache hit).\n\n## The filename convention\n\n```\n<PRJ>_<Config>_<bks>_ptxp.pdf\n<PRJ>_<Config>_<bks>_ptxp.log\n```\n\n| Slot | Source | Example |\n|---|---|---|\n| `<PRJ>` | payload's `project_id`, uppercased | `WSG` |\n| `<Config>` | payload's `config_name`, exact case | `FancyNT` |\n| `<bks>` | payload's `books`, see below | `JHN` or `MAT-REV` |\n| `_ptxp` | literal | `_ptxp` |\n\n`<bks>` follows two rules:\n\n- **Single book** → the USFM code as-is: `[\"JHN\"]` → `JHN`\n- **Multiple books** → first-last: `[\"MAT\",\"MRK\",\"LUK\",\"JHN\"]` → `MAT-JHN`. The hyphen is the canonical-order separator (PTXprint's USFM book ordering, not alphabetical). For the standard NT, `[\"MAT\", ..., \"REV\"]` → `MAT-REV`.\n\nThis naming is unchanged from the GUI. Users who recognize PDFs from existing PTXprint workflows will see filenames they already understand.\n\n## The content-addressed prefix\n\nThe full R2 path is:\n\n```\nptxprint-outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf\n```\n\n`<payload_hash>` is the sha256 of the canonical JSON of the payload (see `klappy://canon/articles/payload-construction`). Two consequences:\n\n- **Cache hit on identical inputs.** Same payload → same hash → same path → existing PDF returned without re-running PTXprint.\n- **Coexistence of variants.** The same project + config + books with different cfg content produces different `<payload_hash>` values, so multiple variants live side by side in R2.\n\nThe filename portion alone (`<PRJ>_<Config>_<bks>_ptxp.pdf`) is **not** unique across all jobs — it's only unique within a `<payload_hash>` directory. Don't use it as a session-wide identifier; use `<payload_hash>/<filename>` together.\n\n## URL validity\n\nPresigned URLs are time-limited. Default windows:\n\n| URL | Default validity | Configurable per-deployment? |\n|---|---|---|\n| `pdf_url` (in `get_job_status`) | 7 days | yes |\n| `log_url` (in `get_job_status`) | 7 days | yes |\n| `predicted_pdf_url` (in `submit_typeset`) | 7 days | yes |\n\nAfter a presigned URL expires, the underlying object is still in R2 (subject to the bucket's retention policy — 90 days default for outputs). The agent re-mints by polling `get_job_status` again — the response will carry fresh presigned URLs.\n\n## When to surface the URL to the user\n\nSurface the `pdf_url` immediately when the job succeeds. The user typically wants to click and view. Don't wait for additional validation.\n\nFor long-lived sharing (the user wants to email the link), warn that the URL expires in 7 days. If they need permanent access, recommend they download the PDF.\n\nIf `cached: true` was set on the original `submit_typeset` response, the URL points to a PDF that was generated by an earlier (possibly much earlier) submission. The content is byte-identical to what a fresh run would produce — that's what content-addressing guarantees — but the timestamp on the R2 object reflects the original generation time, not now. Useful for tracking authorship; not relevant to most users.\n\n## Output retention\n\nR2 object retention is set per-bucket via lifecycle policy:\n\n| Bucket | Default retention | Purpose |\n|---|---|---|\n| `ptxprint-outputs` | 90 days | content-addressed PDFs and logs |\n\nExpired output objects are deleted by R2; subsequent re-submission of the same payload re-runs the typesetting (cache miss).\n\nThe MCP server is not a long-term PDF host. For archival, the user (or BT Servant) downloads the PDF and stores it in their own system.\n\n## Predicted vs. actual\n\nThe `predicted_pdf_url` returned by `submit_typeset` is computed before the job runs. It will match the eventual `pdf_url` exactly **if the job succeeds**. If the job fails (hard or soft), the predicted URL points at a path that may not exist in R2:\n\n| Submit response | Job outcome | What's at `predicted_pdf_url` |\n|---|---|---|\n| `cached: true` | (job didn't run) | The cached PDF, exists, valid |\n| `cached: false` | success | The new PDF, exists once job completes |\n| `cached: false` | hard failure | Nothing — no PDF was produced |\n| `cached: false` | soft failure | A degraded PDF — exists, but is incomplete |\n\nThe agent should not surface `predicted_pdf_url` to the user as a download link before the job has succeeded. Use it for internal bookkeeping; surface `pdf_url` from `get_job_status` once the job's `state == \"succeeded\"`.\n\n## What to do when an old URL is shared with a new agent\n\nWhen a user pastes a v1.2 PDF URL into a new conversation, the agent can:\n\n1. Extract the `<payload_hash>` from the URL path.\n2. Optionally HEAD the URL to confirm the object still exists.\n3. If the user asks \"what's in this?\", treat the URL as the answer — there's no MCP tool that maps `payload_hash` back to the original payload (intentional; payloads can contain content not appropriate to retain server-side).\n4. If the user wants to \"make another like that\", they need to provide the source materials again (their project state, the cfg they used, etc.); the agent constructs a new payload from those.\n\nThe PDF is the artifact. The payload is the recipe. The server stores PDFs (for retention window) and recipes only as content-addressed paths — not the recipe content itself.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/payload-construction` for how the inputs that produce these names get assembled.*\n"},{"path":"articles/payload-construction.md","uri":"klappy://canon/articles/payload-construction","title":"Payload Construction — How to Build a v1.2 Typeset Job","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","payload","submit-typeset","schema"],"stability":"working","what_it_answers":"How does the agent build a payload for `submit_typeset`? What goes inline, what goes by URL, when do I use `define` vs `config_files`?","sections":[{"slug":"the-contract-in-one-sentence","heading":"The contract in one sentence","start":37,"end":43},{"slug":"skeleton","heading":"Skeleton","start":43,"end":84},{"slug":"what-goes-where","heading":"What goes where","start":84,"end":97},{"slug":"configfiles-keys-relative-paths","heading":"`config_files` keys: relative paths","start":97,"end":118},{"slug":"define-vs-configfiles-which-to-use","heading":"`define` vs `config_files`: which to use","start":118,"end":130},{"slug":"sources-fonts-figures-url-sha256","heading":"Sources, fonts, figures: URL + sha256","start":130,"end":154},{"slug":"where-do-the-urls-come-from","heading":"Where do the URLs come from?","start":154,"end":167},{"slug":"canonical-hashing-and-the-cache","heading":"Canonical hashing and the cache","start":167,"end":179},{"slug":"worked-example-a-simple-draft-of-john","heading":"Worked example: a simple draft of John","start":179,"end":216},{"slug":"what-the-agent-should-not-include","heading":"What the agent should NOT include","start":216,"end":224},{"slug":"when-the-payload-doesn-t-behave-as-expected","heading":"When the payload doesn't behave as expected","start":224,"end":237}],"body":"---\ntitle: \"Payload Construction — How to Build a v1.2 Typeset Job\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"payload\", \"submit-typeset\", \"schema\"]\nderives_from: \"canon/governance/headless-operations.md (Part 0, Part 1, Part 2)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n> **Quickstart.** The smallest valid payload is `{schema_version, project_id, books, sources}` — the Container's bundled default cfg fills in everything else and produces a publication-quality A5 two-column Bible book in Charis. Levels of customization stack on top of this from there:\n>\n> ```json\n> {\n>   \"schema_version\": \"1.0\",\n>   \"project_id\": \"BSB\",\n>   \"books\": [\"JHN\"],\n>   \"sources\": [\n>     { \"book\": \"JHN\", \"filename\": \"44JHNBSB.usfm\", \"url\": \"...\", \"sha256\": \"...\" }\n>   ]\n> }\n> ```\n>\n> The full progression — when to add `define`, when to replace the cfg, when to supply fonts, etc. — lives in `klappy://canon/articles/progressive-customization`. The bundled cfg itself is documented at `klappy://canon/articles/bundled-default-cfg`. The rest of this article is the schema reference.\n\n# Payload Construction\n\n> **What this answers.** How does the agent build a payload for `submit_typeset`? What goes inline, what goes by URL, when do I use `define` vs `config_files`?\n>\n> **Related articles.** `klappy://canon/articles/output-naming` · `klappy://canon/articles/file-system-map` · `klappy://canon/articles/config-construction` · `klappy://canon/articles/font-resolution` · `klappy://canon/articles/failure-mode-taxonomy`\n\n---\n\n## The contract in one sentence\n\nA payload is a JSON object that fully describes one typesetting job: which project and config to use, which books, the inline text content of every config-folder file, and URL+sha256 references for every binary input (USFM sources, fonts, figures). The agent submits it via `submit_typeset` and gets back a `job_id` plus a predicted PDF URL.\n\nRe-submitting the same payload returns the cached PDF URL with no re-typesetting. The cache is content-addressed by `sha256(canonical_json(payload))`.\n\n## Skeleton\n\n```json\n{\n  \"schema_version\": \"1.0\",\n\n  \"project_id\": \"WSG\",\n  \"config_name\": \"Default\",\n  \"books\": [\"JHN\"],\n  \"mode\": \"simple\",\n  \"define\": {},\n\n  \"config_files\": {\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"[paper]\\npagesize = 148mm, 210mm (A5)\\n...\"\n  },\n\n  \"sources\": [\n    {\n      \"book\": \"JHN\",\n      \"filename\": \"44JHNWSG.SFM\",\n      \"url\": \"https://r2.../uploads/abc/44JHNWSG.SFM\",\n      \"sha256\": \"...\"\n    }\n  ],\n\n  \"fonts\": [\n    {\n      \"family_id\": \"charissil\",\n      \"version\": \"7.000\",\n      \"filename\": \"CharisSIL-Regular.ttf\",\n      \"url\": \"https://lff.api.languagetechnology.org/...\",\n      \"sha256\": \"...\"\n    }\n  ],\n\n  \"figures\": []\n}\n```\n\nThe full schema lives in the v1.2 spec §4. This article is the agent-side construction guide.\n\n## What goes where\n\n| Slot | What | Inline or URL? |\n|---|---|---|\n| `project_id`, `config_name`, `books`, `mode` | identifiers and run mode | inline (small) |\n| `define` | one-shot `-D key=value` overrides | inline |\n| `config_files` | every text artifact in the project's config folder (cfg, sty, changes.txt, FRTlocal.sfm, AdjLists, piclist, override files) | **inline text**, keyed by relative path |\n| `sources` | USFM book content | **URL + sha256** |\n| `fonts` | font binaries | **URL + sha256** |\n| `figures` | image binaries (TIF/PNG) | **URL + sha256** |\n\nRule of thumb: text goes inline; binaries go by URL. Multi-MB files in MCP envelopes are a bad idea. Hosting those URLs is the agent's environment concern — the typesetting MCP does not stage or upload input files.\n\n## `config_files` keys: relative paths\n\nThe keys in `config_files` are paths relative to the project root. The worker materializes each file at the corresponding location in its scratch dir before running PTXprint. See `klappy://canon/articles/file-system-map` for the full tree.\n\nThe most common keys, in order of frequency:\n\n```\nshared/ptxprint/<config>/ptxprint.cfg          primary settings (always)\nshared/ptxprint/<config>/ptxprint.sty          style overrides (often)\nshared/ptxprint/<config>/changes.txt           regex transforms (sometimes)\nshared/ptxprint/<config>/FRTlocal.sfm          front matter (when ToC/cover needed)\nshared/ptxprint/<config>/<PRJ>-<config>.piclist\n                                               picture placements (when figures present)\nshared/ptxprint/<config>/AdjLists/<num><BOOK><PRJ>-<config>.SFM.adj\n                                               paragraph adjustments (rare, per-book)\nshared/ptxprint/<config>/ptxprint_override.cfg    config-level overrides (admin)\nshared/ptxprint/ptxprint_project.cfg              project-level overrides (admin)\n```\n\nInheritance: if the config has `[import] config = Parent`, **also** include the parent's `ptxprint.cfg` (and recursively up the chain) at the parent's path. PTXprint resolves inheritance inside the worker the same way it does in the GUI — but only if the parent files are physically present. See `klappy://canon/articles/config-inheritance-and-overrides`.\n\n## `define` vs `config_files`: which to use\n\nThe `define` map is for **transient run-time overrides** that should not mutate the working config. The keys are PTXprint UI widget identifiers (`s_linespacing`, `c_fighiderefs`, etc.); they map to `-D key=value` flags when the worker invokes the CLI.\n\n| Use `define` when... | Use `config_files[\"...ptxprint.cfg\"]` when... |\n|---|---|\n| Producing a one-off draft with different spacing | Committing to a finalised configuration |\n| Exploring options without persisting | The change should round-trip back to the user's project store |\n| Parameterised runs (rotating output formats) | The change is part of the user's saved working state |\n\nA common pattern: explore with `define`, then promote to `config_files` once the user commits.\n\n## Sources, fonts, figures: URL + sha256\n\nEach entry must include a fetchable URL and the sha256 of the file at that URL. The worker hashes the bytes as they arrive and aborts if they don't match.\n\n```json\n{\n  \"book\": \"JHN\",\n  \"filename\": \"44JHNWSG.SFM\",\n  \"url\": \"https://r2.../44JHNWSG.SFM\",\n  \"sha256\": \"9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08\"\n}\n```\n\nURL stability matters for caching. Two payloads pointing at the same content via different URLs hash differently and miss the cache. Prefer:\n\n- Stable HTTPS endpoints the agent's host controls (a static file server, the user's own bucket)\n- Content-addressed CDNs (the sha256 is in the URL)\n- Stable Git-LFS or DBL endpoints\n\nAvoid:\n- Volatile presigned URLs that change every 15 minutes\n- Sources that may move (a Paratext server under maintenance)\n- URLs whose host requires auth that the worker can't replay\n\n## Where do the URLs come from?\n\nThis MCP server does not host or stage input files. There is no `get_upload_url` or equivalent tool. When the user has a USFM or font file on their local disk, the agent's host environment is responsible for making it reachable at an HTTPS URL.\n\nPractical patterns the agent's host can offer:\n\n- A static HTTP server backed by the user's filesystem (typical for desktop dev environments)\n- A tunnel from a local file server to a public URL (ngrok, Cloudflare tunnel, Tailscale Funnel)\n- The user storing inputs in a Git repo, an S3 bucket, or any service they already control\n- The Paratext server already hosting the project's USFM at a stable URL\n\nWhatever pattern the agent's host supports, the agent's job is to land at a final HTTPS URL and a sha256, then include both in the payload. The typesetting MCP is intentionally indifferent to where those URLs originated.\n\n## Canonical hashing and the cache\n\n`payload_hash = sha256(canonical_json(payload))`. The canonicalization (RFC 8785 JCS or equivalent) ensures logically-identical payloads produce identical hashes regardless of key order or whitespace.\n\nWhen the agent submits a payload:\n\n1. The worker hashes it.\n2. If `outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf` exists in R2 → the response includes `cached: true` and the URL points to the existing PDF. **No typesetting runs.**\n3. If miss → a Container is dispatched, the job runs, the PDF lands at the same content-addressed path.\n\nThis is the mechanism behind \"free re-runs of unchanged builds.\" Agents that iterate by changing one cfg key and re-submitting get a cache hit on every key that has been tried before. There is no separate undo button — re-submitting an earlier payload is the undo.\n\n## Worked example: a simple draft of John\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"WSG\",\n  \"config_name\": \"Default\",\n  \"books\": [\"JHN\"],\n  \"mode\": \"simple\",\n  \"define\": { \"output_format\": \"Screen-Quickest\" },\n  \"config_files\": {\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"[config]\\nname = Default\\n[paper]\\npagesize = 148mm, 210mm (A5)\\nmargins = 12\\n[paragraph]\\nlinespacing = 14\\n\",\n    \"shared/ptxprint/Default/ptxprint.sty\": \"\"\n  },\n  \"sources\": [\n    {\n      \"book\": \"JHN\",\n      \"filename\": \"44JHNWSG.SFM\",\n      \"url\": \"https://r2.../uploads/abc/44JHNWSG.SFM\",\n      \"sha256\": \"<sha256 of the USFM file>\"\n    }\n  ],\n  \"fonts\": [\n    {\n      \"family_id\": \"charissil\",\n      \"version\": \"7.000\",\n      \"filename\": \"CharisSIL-Regular.ttf\",\n      \"url\": \"https://lff.../CharisSIL-Regular.ttf\",\n      \"sha256\": \"<sha256 of the font>\"\n    }\n  ],\n  \"figures\": []\n}\n```\n\nSubmit, poll `get_job_status` until `state ∈ {succeeded, failed}`, read the result.\n\n## What the agent should NOT include\n\n- TeX macro files (`*-mods.tex`, `*-premods.tex`) — v1.2 defers these as too risky.\n- Files outside the project sandbox.\n- Multi-megabyte content inline (use URLs).\n- Override files the user hasn't authorized — see `klappy://canon/articles/config-inheritance-and-overrides`.\n- Made-up cfg keys. PTXprint silently ignores unknown keys; the agent's change appears to do nothing. Verify keys against the running tooltip dump or canon's cfg-key index.\n\n## When the payload doesn't behave as expected\n\nRun the diagnostic in `klappy://canon/articles/diagnostic-patterns`. The most common causes:\n\n1. An override file in `config_files` is winning over the cfg change — see `klappy://canon/articles/config-inheritance-and-overrides`.\n2. The cfg key was misspelled — verify against canon.\n3. A cache hit returned an earlier payload's PDF — check `cached: true` in the `submit_typeset` response.\n4. The change was in `define` but `config_files` had a contradicting value — `config_files` wins for keys it sets.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/specs/ptxprint-mcp-v1-2-spec` for the server-side schema definition.*\n"},{"path":"articles/phase-1-poc-scope.md","uri":"klappy://canon/articles/phase-1-poc-scope","title":"Phase 1 PoC Scope — Minimum Fixture Payload, Pipeline Validation","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","phase-1","hackathon","scope","poc","superseded","historical-reference"],"stability":"working","what_it_answers":"What does the agent actually do during hackathon week? What's deferred? When does this article become obsolete?","sections":[{"slug":"revision-note-session-10","heading":"Revision note (session 10)","start":32,"end":38},{"slug":"the-three-phases","heading":"The three phases","start":38,"end":48},{"slug":"the-phase-1-payload-contract","heading":"The Phase 1 payload contract","start":48,"end":124},{"slug":"what-the-container-provides","heading":"What the Container provides","start":124,"end":137},{"slug":"what-phase-1-does-not-do","heading":"What Phase 1 does NOT do","start":137,"end":150},{"slug":"phase-1-definition-of-done","heading":"Phase 1 Definition of Done","start":150,"end":164},{"slug":"when-the-agent-should-escalate","heading":"When the agent should escalate","start":164,"end":172},{"slug":"when-this-article-goes-away","heading":"When this article goes away","start":172,"end":183},{"slug":"old-version-preserved-for-diff-visibility-withdrawn-2026-04-29","heading":"Old version (preserved for diff visibility — withdrawn 2026-04-29)","start":183,"end":195}],"body":"---\ntitle: \"Phase 1 PoC Scope — Minimum Fixture Payload, Pipeline Validation\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"phase-1\", \"hackathon\", \"scope\", \"poc\", \"superseded\", \"historical-reference\"]\nderives_from: \"transcript-encoded-session-7.md (D-025); revised per session-8 H-011, session-9 H-016, session-10 D-019/O-036\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-29\nstatus: superseded\nsuperseded_by: \"klappy://canon/articles/progressive-customization\"\nsupersession_reason: \"Phase 1's anticipated bundling work has been completed. The default-behavior surface (Level 0 in the new ladder) is now publication-quality, not bare-defaults. The progressive-customization article is the live reference for {books, sources} → Level 4+ progression.\"\n---\n\n# Phase 1 PoC Scope\n\n> 🛈 **Superseded.** This article describes the Phase 1 PoC scope as defined on 2026-04-28 (transcript-encoded-session-7 D-025). At that time, the Container was expected to ship with PTXprint's compiled defaults serving as the bundle, with everything else as Phase 2/3 work. Empirical testing on 2026-04-30 found the Container had no usable bundled defaults — Phase 1 strict payloads failed with `\"PTXprint produced no output (silent exit)\"`. The bundling work is being completed (see `klappy://canon/handoffs/bundle-default-cfg-handoff`), and the new default-behavior surface (Level 0 in `klappy://canon/articles/progressive-customization`) is publication-quality.\n>\n> **For current default behavior, see `klappy://canon/articles/progressive-customization`.**\n>\n> **For what the bundle contains, see `klappy://canon/articles/bundled-default-cfg`.**\n>\n> The historical content below is preserved for traceability — the session-7 D-025 boundary, the drift-isolation work in sessions 8–10, and the BSB-render iteration trail are referenced from downstream encodings.\n\n> **What this answers.** What does the agent actually do during hackathon week? What's deferred? When does this article become obsolete?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/output-naming` · `klappy://canon/articles/failure-mode-taxonomy`\n\n---\n\n## Revision note (session 10)\n\nThe earlier revision of this article promised *\"USFM-only payload; the worker materializes a minimal directory tree.\"* That was empirically false — PTXprint exits 1 on a missing `ptxprint-mods.sty` and on missing fonts the cfg references. Session-9 closed the file-completeness gap; session-10 produced the first end-to-end PDF (job `6f37b42b...`, 2026-04-29T01:00:00Z) once the agent supplied the **minimum config_files map** below AND mitigated drift 7 (font compatibility). This article is rewritten to match the empirical reality. Old version preserved at the bottom for diff visibility.\n\n---\n\n## The three phases\n\n| Phase | Scope | Validates |\n|---|---|---|\n| **1 — now** | Minimum-fixture payload (5 config files + ≥1 USFM source); cfg-edit-for-fonts mitigation when fixture requests an unbundled font. | The full pipeline: Worker → Container → PTXprint → R2 → presigned URL → return. |\n| **2 — next** | 3–4 preset config templates the agent picks from; payload `fonts` field documented; `define` widget-ID-to-cfg-key mapping. | The configuration layer actually applies; fonts beyond bundled defaults are usable. |\n| **3 — after** | Resolve zip-vs-structured payload format. | Long-term agent ergonomics for multi-config jobs (diglot, triglot). |\n\nAll session-6 tensions (D-022 templates, O-open-P1-005 zip, O-open-P1-006 preset inventory, H-014 zip resolution) remain real and explicitly sequenced behind Phase 1 pipeline validation.\n\n## The Phase 1 payload contract\n\nThe complete agent-side payload for Phase 1 — empirically minimum, demonstrated working in session 10:\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"minitest\",\n  \"config_name\": \"Default\",\n  \"books\": [\"JHN\"],\n  \"mode\": \"simple\",\n  \"define\": {},\n  \"config_files\": {\n    \"Settings.xml\":                                \"<contents>\",\n    \"custom.sty\":                                  \"<contents>\",\n    \"shared/ptxprint/Default/ptxprint.cfg\":        \"<contents>\",\n    \"shared/ptxprint/Default/ptxprint.sty\":        \"<contents>\",\n    \"shared/ptxprint/Default/ptxprint-mods.sty\":   \"<contents>\"\n  },\n  \"sources\": [\n    {\n      \"book\": \"JHN\",\n      \"filename\": \"44JHNtest.usfm\",\n      \"url\": \"https://raw.githubusercontent.com/.../44JHNtest.usfm\",\n      \"sha256\": \"<sha256 of the file at url>\"\n    }\n  ],\n  \"fonts\": [],\n  \"figures\": []\n}\n```\n\n### Why each `config_files` entry is required\n\n| Path | Why |\n|---|---|\n| `Settings.xml` | Paratext project settings; PTXprint reads `LanguageIsoCode`, `FileNameBookNameForm`, `FileNamePostPart`, etc. to find source USFM. Without it, PTXprint cannot resolve the book filename. |\n| `custom.sty` | Project-level USFM stylesheet additions. Referenced by `shared/ptxprint/Default/ptxprint.cfg` via `..\\..\\..\\custom.sty`; absent → fatal load error. |\n| `shared/ptxprint/Default/ptxprint.cfg` | The 400-key config the `-c Default` flag points at. PTXprint requires this to exist with the named config. |\n| `shared/ptxprint/Default/ptxprint.sty` | Config-level stylesheet overrides. Referenced by the cfg's stylesheet chain. |\n| `shared/ptxprint/Default/ptxprint-mods.sty` | Additional config-level stylesheet modifications. **Empirically required, not optional** (drift 6, session 9). The asymmetric companions `ptxprint-mods.tex` and `ptxprint-premods.tex` are loaded as optional and may be omitted. |\n\n### project_id constraint\n\n`project_id` is capped at 8 characters by the v1.2 schema (validated at submit time). Paratext convention is project-id = folder-name; the upstream `minitests` fixture (9 chars) must be renamed to `\"minitest\"` (8 chars) when used as a payload. This is a known asymmetry between the schema and Paratext convention; an open question for the v1.2 spec is whether to lift the cap.\n\n### Drift-7 mitigation for fixtures requesting unbundled fonts\n\nThe container bundles **Charis SIL** + **Source Code Pro** only (per session-9 PR #10). Any fixture whose `ptxprint.cfg` declares a font outside that set (e.g., the upstream minitests fixture's `Gentium Plus`) will fail with:\n\n```\n! Font \\font<p-12.0>=\"Family Name:script=latn\" at Npt not loadable: Metric (TFM) file or installed font not found.\n```\n\n**Mitigation for Phase 1:** Edit the cfg's `[document]` section in your `config_files` payload to substitute Charis SIL for the four font-role keys before submission. Example transformation:\n\n```ini\n# Before (in ptxprint.cfg [document])\nfontregular = Gentium Plus||false|false|\nfontbold = Gentium Plus| Bold|false|false|\nfontitalic = Gentium Plus| Italic|false|false|\nfontbolditalic = Gentium Plus| Italic Bold|false|false|\n\n# After\nfontregular = Charis SIL||false|false|\nfontbold = Charis SIL|Bold|false|false|\nfontitalic = Charis SIL|Italic|false|false|\nfontbolditalic = Charis SIL|Bold Italic|false|false|\n```\n\n**Do NOT use `define`** for this purpose. PTXprint's `-D` widget IDs are not the same strings as `ptxprint.cfg` keys (session-1 O-003 still open; session-10 D-019). `define: { fontregular: ... }` was empirically tested and did not affect rendering.\n\nThis mitigation produces a pipeline-validation PDF, **not** a faithful render of the fixture. Phase 2 introduces the `fonts` payload field for proper font supply.\n\n**Update (session 11, 2026-04-29):** the Phase-2 path was demonstrated end-to-end via `smoke/fonts-payload.json` (job `802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd`). The minitests fixture's cfg was left **unmodified** (still references Gentium Plus); the four Gentium Plus 6.200 TTFs were staged in R2 and supplied via the payload's `fonts` array. PTXprint discovered the materialised fonts in `<project>/shared/fonts/` without any container changes — `fc-cache` and `OSFONTDIR` were not required, contradicting the session-10 handoff's prediction. The resulting PDF embeds GentiumPlus / GentiumPlus-Bold / GentiumPlus-Italic per `pdffonts` (faithful render), not Charis. Both the cfg-edit mitigation above (simpler) and the fonts-payload approach (faithful) now work for fixtures that request unbundled fonts; choose based on whether the user needs faithful rendering. See `klappy://canon/articles/font-resolution` §\"A worked-minimum English-Bible payload\" for the verified URLs and sha256s.\n\n## What the Container provides\n\nFor Phase 1, the Container image bundles:\n\n- PTXprint 3.0.20 (pinned per session-9 PR #9 for reproducibility)\n- ptx2pdf TeX macros (installed via upstream's documented pattern, not the broken `pip install` package-data path — drift 4)\n- XeTeX (`texlive-xetex`)\n- **SIL Charis** font (bundled per session-9 PR #10; serves the Phase-1 drift-7 mitigation)\n- **Source Code Pro** font (bundled per PR #10; PTXprint's `ptx-cropmarks.tex` line 38 hardcodes it)\n- The MCP worker code\n\nThis is not the long-term architecture — Phase 2/3 will move agent-supplied fonts into the payload — but for hackathon week it gets the pipeline shippable.\n\n## What Phase 1 does NOT do\n\n- Custom-built configurations beyond the supplied fixture's\n- Multi-script support (English Bibles only; Charis covers them; non-Charis fonts require Phase-2 `fonts` field)\n- Override files (apart from cfg-edit-for-fonts mitigation)\n- AdjLists, piclists, changes.txt, FRTlocal — read but not authored by the agent\n- Cover periphs\n- TeX macro injection (`*-mods.tex`, `*-premods.tex`)\n- Diglot or triglot setups\n- Reliable `define` (-D) overrides — until session-1 O-003 / session-10 H-019 closes\n\nThe agent that drives Phase 1 reads three articles to function: `klappy://canon/articles/payload-construction` (full schema), this article (the minimum-payload contract), and `klappy://canon/articles/output-naming` (to interpret result URLs). When Phase 1 produces a hard or soft failure, the agent reads `klappy://canon/articles/failure-mode-taxonomy` and `klappy://canon/articles/diagnostic-patterns`.\n\n## Phase 1 Definition of Done\n\nThe DoD reduces to:\n\n1. Agent constructs the minimum payload above (5 config files + ≥1 USFM source) and submits via `submit_typeset`.\n2. Server returns `job_id` and `predicted_pdf_url` synchronously.\n3. Container picks up the job, materialises the project tree, runs `ptxprint -P <project_id> -c <config_name> -b \"<books>\" -p <scratch> -q`, uploads the resulting PDF and log to R2.\n4. Agent polls `get_job_status`, receives `failure_mode: \"success\"` and a working `pdf_url`.\n5. The PDF, when downloaded, contains typeset content from the source USFM in the fixture's intended layout (modulo drift-7 mitigation if a non-bundled font was substituted).\n\n**Empirically demonstrated session 10**, job `6f37b42b9c73ad5e4f7b8a576de8144bcc6c87fad944051e285a33d21de25059`: 4.1s wall-clock, 2-page XDV → 1-page PDF, 66966 bytes, `Creator: PTXprint 3.0.20 (Default)`.\n\nAnything beyond this — settings the user wants changed, fonts the user prefers (without cfg-edit), layout adjustments, multiple custom configs — is Phase 2.\n\n## When the agent should escalate\n\nPhase 1 is deliberately narrow. If a user asks for anything beyond \"typeset this fixture with the bundled fonts,\" surface honestly:\n\n> \"Phase 1 of this PoC supports typesetting from a fixture's full config_files map plus USFM source, with the container's bundled fonts (Charis SIL, Source Code Pro). Custom configurations, agent-supplied fonts, layout tuning, and reliable widget-ID overrides are coming in Phase 2. For today, I can typeset your fixture with PTXprint's defaults — and if your cfg requests Gentium Plus or another non-bundled font, I'll substitute Charis SIL via cfg-edit. Would you like to proceed?\"\n\nDo not fabricate Phase 2 capabilities. The v1.2 server accepts arbitrary `config_files` maps, `fonts` arrays, and `define` overrides; Phase 1's *validated* tooling around them is narrower than the schema's surface.\n\n## When this article goes away\n\nThis article is **temporary**. It bridges v1.2's full architecture and the hackathon-week minimum. Once Phase 2 ships (preset templates, agent-supplied fonts, widget-ID mapping), this article gets either:\n\n- **Deleted**, if its content is fully obsolete.\n- **Kept as historical reference**, marked `canonical_status: superseded`, with a `superseded_by` pointer to the Phase 2 reference article.\n\nThe decision lives with whoever ships Phase 2.\n\n---\n\n## Old version (preserved for diff visibility — withdrawn 2026-04-29)\n\nThe withdrawn promise was:\n\n> *\"USFM-only payload; PTXprint runs with built-in defaults. ... `project_id` can be a placeholder like `\\\"DEFAULT\\\"` — the worker materializes a minimal directory tree under that name. PTXprint runs with built-in defaults for everything else.\"*\n\nThis was incorrect. PTXprint requires a populated `<project>/shared/ptxprint/<config>/` directory or it silently bails (session-3 C-009) or — once the cfg is supplied but its referenced files aren't — exits 1 with a \"Paratext stylesheet not found\" fatal (session-9 drift 6). The minimum is what the empirical session-10 success ran with.\n\n---\n\n*This article is the operational scope statement for hackathon-week PoC. It supersedes (for hackathon week only) the broader DoD in `klappy://canon/specs/ptxprint-mcp-v1-2-spec` §10 item 2.*\n"},{"path":"articles/piclist-format.md","uri":"klappy://canon/articles/piclist-format","title":"Piclist Format — Picture Placement Files","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","piclist","pictures","figures","supporting-files"],"stability":"working","what_it_answers":"What's a piclist? How does it work with the payload's `figures` array? What are the line fields?","sections":[{"slug":"what-a-piclist-is","heading":"What a piclist is","start":22,"end":28},{"slug":"file-location-and-name","heading":"File location and name","start":28,"end":40},{"slug":"line-syntax","heading":"Line syntax","start":40,"end":58},{"slug":"fields","heading":"Fields","start":58,"end":95},{"slug":"worked-examples","heading":"Worked examples","start":95,"end":123},{"slug":"coordination-with-the-figures-array","heading":"Coordination with the `figures` array","start":123,"end":146},{"slug":"reading-an-existing-piclist","heading":"Reading an existing piclist","start":146,"end":155},{"slug":"hiding-pictures-globally","heading":"Hiding pictures globally","start":155,"end":165},{"slug":"picture-clearance-metadata","heading":"Picture clearance metadata","start":165,"end":178},{"slug":"when-picture-rendering-fails","heading":"When picture rendering fails","start":178,"end":194}],"body":"---\ntitle: \"Piclist Format — Picture Placement Files\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"piclist\", \"pictures\", \"figures\", \"supporting-files\"]\nderives_from: \"canon/governance/headless-operations.md (Part 6 piclist subsection)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Piclist Format\n\n> **What this answers.** What's a piclist? How does it work with the payload's `figures` array? What are the line fields?\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/file-system-map`\n\n---\n\n## What a piclist is\n\nA piclist is a per-config plain-text file that places pictures into the typeset output. Each line anchors one picture to a USFM reference (book + chapter + verse) and specifies how PTXprint should size, position, and caption it.\n\nThe piclist is text content in the payload's `config_files` map. The actual binary image files referenced by the piclist live in the payload's `figures` array (URL + sha256). The two are coordinated: each piclist line's `src` matches a `filename` in `figures`.\n\n## File location and name\n\n```\nshared/ptxprint/<config>/<PRJ>-<Config>.piclist\n```\n\nExamples:\n- `shared/ptxprint/Default/WSG-Default.piclist`\n- `shared/ptxprint/FancyNT/WSG-FancyNT.piclist`\n\nOne piclist per config. Note `<PRJ>` is uppercase, `<Config>` is case-as-named, separated by a hyphen.\n\n## Line syntax\n\n```\nBOOK c.v caption text|src=\"filename\" size=\"col|span\" pgpos=\"<position>\" ref=\"c:v\" [scale=\"<n>\"] [copy=\"<text>\"]\n```\n\nAnatomy:\n- Everything before `|` is the **anchor and caption text** (visible in the rendered output's caption).\n- Everything after `|` is **key=value attributes** (controlling rendering).\n\nWorked example:\n\n```\nJHN 3.16 God so loved the world|src=\"AB01234.tif\" size=\"col\" pgpos=\"br\" ref=\"3:16\"\n```\n\nReads as: at John 3:16, place the image `AB01234.tif`, size `col` (column-width), position `br` (bottom-right), with caption \"God so loved the world\" referenced as \"3:16\".\n\n## Fields\n\n| Field | Required | Common values | Meaning |\n|---|---|---|---|\n| `BOOK c.v` | yes | `JHN 3.16` | Book code + chapter.verse anchor (text before the `\\|`) |\n| caption | yes | \"God so loved the world\" | Caption text shown under the picture (may be vernacular Unicode) |\n| `src` | yes | `\"AB01234.tif\"` | Image filename (must match a `figures[].filename` in the payload) |\n| `size` | yes | `\"col\"` or `\"span\"` | Column-width or page-width |\n| `pgpos` | yes | `\"tl\"`, `\"tr\"`, `\"bl\"`, `\"br\"`, `\"t\"`, `\"b\"`, `\"h\"`, `\"p\"` | Page position (see below) |\n| `ref` | yes | `\"3:16\"` | Caption reference in `c:v` format |\n| `scale` | no | `\"0.8\"`, `\"1.2\"` | Scale factor relative to default size |\n| `copy` | no | `\"Image © Copyright Holder Name\"` | Copyright attribution string |\n\n### `pgpos` values\n\n| Code | Position |\n|---|---|\n| `tl` | top-left |\n| `tr` | top-right |\n| `bl` | bottom-left |\n| `br` | bottom-right |\n| `t` | top, full-width (only with `size=\"span\"`) |\n| `b` | bottom, full-width (only with `size=\"span\"`) |\n| `h` | here (inline, in flow) |\n| `p` | full-page |\n\nSome PTXprint versions accept additional 1-2 letter codes; verify against the running tooltip dump for the project's PTXprint version.\n\n### `size` values\n\n| Value | Width |\n|---|---|\n| `col` | one column of the layout (most common) |\n| `span` | spans all columns (full text width) |\n\nFor single-column layouts, `col` and `span` produce the same width.\n\n## Worked examples\n\n### Single picture in John 3\n\n```\nJHN 3.16 God so loved the world|src=\"AB01234.tif\" size=\"col\" pgpos=\"br\" ref=\"3:16\"\n```\n\n### Full-page picture at the start of Matthew\n\n```\nMAT 1.1 The Genealogy of Jesus|src=\"genealogy.png\" size=\"span\" pgpos=\"p\" ref=\"1:1\"\n```\n\n### Multiple pictures in Genesis\n\n```\nGEN 1.1 The creation of the heavens and earth|src=\"creation.tif\" size=\"span\" pgpos=\"t\" ref=\"1:1\"\nGEN 3.6 Adam and Eve eat the forbidden fruit|src=\"fall.tif\" size=\"col\" pgpos=\"br\" ref=\"3:6\"\nGEN 6.13 Noah builds the ark|src=\"ark.tif\" size=\"col\" pgpos=\"tr\" ref=\"6:13\"\n```\n\n### With scale and copyright\n\n```\nLUK 2.7 The birth of Jesus|src=\"nativity.tif\" size=\"col\" pgpos=\"bl\" ref=\"2:7\" scale=\"0.85\" copy=\"© Sweet Publishing / FreeBibleimages.org\"\n```\n\n## Coordination with the `figures` array\n\nEvery `src` value in the piclist must correspond to a `filename` in the payload's `figures` array:\n\n```json\n\"figures\": [\n  {\n    \"filename\": \"AB01234.tif\",\n    \"url\": \"https://r2.../uploads/abc/AB01234.tif\",\n    \"sha256\": \"<sha256>\"\n  },\n  {\n    \"filename\": \"creation.tif\",\n    \"url\": \"https://r2.../uploads/abc/creation.tif\",\n    \"sha256\": \"<sha256>\"\n  }\n]\n```\n\nThe worker fetches each URL, verifies the sha256, and places the file where PTXprint can find it via the piclist's `src` reference.\n\nA piclist line referencing a `src` that has no matching `figures` entry produces a soft failure (\"image not found\") — the PDF renders without that picture.\n\n## Reading an existing piclist\n\nMost piclists are generated by the GUI's picture-list editor. The agent reads from project state, modifies as needed, and includes the new content in the next payload. Don't reorder lines arbitrarily — line order matters for some tools that diff against earlier versions.\n\nFor an unfamiliar piclist, parse line by line:\n- Split on the first `|` to separate anchor+caption from attributes\n- Parse attributes as `key=\"value\"` pairs (quoted strings, separated by whitespace)\n- Validate `src` against the `figures` array\n\n## Hiding pictures globally\n\nTo suppress all pictures for a run without modifying the piclist, use `define`:\n\n```json\n\"define\": { \"c_fighiderefs\": \"True\" }\n```\n\nThis is a runtime-only override. The piclist content stays in `config_files` and re-enables on the next run that doesn't set the override.\n\n## Picture clearance metadata\n\nTwo related files (`picInfo.txt` and `picChecks.txt`) carry copyright and approval metadata for pictures. They live in:\n\n```\nshared/ptxprint/picInfo.txt              project-wide\nshared/ptxprint/picChecks.txt            project-wide\nshared/ptxprint/<config>/picInfo.txt     config-level (override)\nshared/ptxprint/<config>/picChecks.txt   config-level (override)\n```\n\nThe agent typically doesn't modify these — the GUI's picture management UI tracks clearances. Include them in the payload's `config_files` if they exist in project state and the user has explicitly authorized; omit otherwise.\n\n## When picture rendering fails\n\nThe seven-point checklist for \"PDF was produced but pictures are missing\" — see `klappy://canon/articles/diagnostic-patterns` for the full diagnostic flow. Common causes:\n\n1. Piclist entry references a `src` not in `figures`.\n2. `figures[].sha256` doesn't match the bytes at the URL.\n3. URL is unreachable from the worker.\n4. `pgpos` invalid for the given `size`.\n5. Anchor reference (BOOK c.v) doesn't match a verse in the source USFM.\n6. Picture too large for the target position.\n7. Too many pictures on one page (PTXprint pushes some off).\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/adjlist-format` for the related single-file-per-config pattern.*\n"},{"path":"articles/progressive-customization.md","uri":"klappy://canon/articles/progressive-customization","title":"Progressive Customization — From {books, sources} to Full Control","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","progressive-customization","progressive-disclosure","default-behavior","override","ladder","bootstrap","quickstart"],"stability":"working","what_it_answers":"I'm an agent. I want to render a Bible book. What is the *smallest* thing I can send? And when I want to customize something, where do I go next?","sections":[{"slug":"the-ladder","heading":"The ladder","start":37,"end":52},{"slug":"level-0-default-render","heading":"Level 0 — Default render","start":52,"end":80},{"slug":"level-1-define-for-one-shot-tweaks","heading":"Level 1 — `define` for one-shot tweaks","start":80,"end":119},{"slug":"level-2-replace-the-cfg","heading":"Level 2 — Replace the cfg","start":119,"end":147},{"slug":"level-3-different-fonts","heading":"Level 3 — Different fonts","start":147,"end":183},{"slug":"level-4-custom-stylesheets-settings-xml-mods","heading":"Level 4 — Custom stylesheets, Settings.xml, mods","start":183,"end":211},{"slug":"level-5-figures","heading":"Level 5 — Figures","start":211,"end":244},{"slug":"composition-layering-levels","heading":"Composition: layering levels","start":244,"end":252},{"slug":"what-about-partial-cfg-overlay","heading":"What about partial cfg overlay?","start":252,"end":267},{"slug":"discovery-via-docs","heading":"Discovery via docs","start":267,"end":293},{"slug":"see-also","heading":"See also","start":293,"end":308}],"body":"---\ntitle: \"Progressive Customization — From {books, sources} to Full Control\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - agent-kb\n  - v1.2-aligned\n  - non-canonical\n  - progressive-customization\n  - progressive-disclosure\n  - default-behavior\n  - override\n  - ladder\n  - bootstrap\n  - quickstart\ncompanion_to:\n  - canon/articles/bundled-default-cfg.md\n  - canon/articles/payload-construction.md\n  - canon/articles/settings-cookbook.md\ncanonical_status: non_canonical\ndate: 2026-04-30\nstatus: draft_for_operator_review\nsupersedes_partial: \"canon/articles/phase-1-poc-scope.md (the Phase 1 minimum-payload guidance)\"\n---\n\n# Progressive Customization\n\n> **What this answers.** I'm an agent. I want to render a Bible book. What is the *smallest* thing I can send? And when I want to customize something, where do I go next?\n>\n> **The short answer.** Send `{books, sources}`. The Container's bundled default cfg produces a publication-quality A5 two-column Bible book in Charis. Add fields when you need them; this article is the map of which field unlocks which capability.\n\n---\n\n## The ladder\n\n| Level | Add to payload | Unlocks | Where to learn |\n|---|---|---|---|\n| **0** | nothing — just `{schema_version, project_id, books, sources}` | Default render: A5 two-column, Charis, parallel refs, footnotes, mirror header | `klappy://canon/articles/bundled-default-cfg` |\n| **1** | `define: {key: value, ...}` | One-shot tweaks (line spacing, hide xrefs, set title, etc.) without replacing the cfg | `klappy://canon/articles/settings-cookbook` |\n| **2** | `config_files[\"shared/ptxprint/Default/ptxprint.cfg\"]` | Full cfg replacement (different layout, multi-book mode, scope changes) | `klappy://canon/articles/config-construction` |\n| **3** | `fonts: [...]` | Different typefaces (Gentium, Andika, language-specific fonts) | `klappy://canon/articles/font-resolution` |\n| **4** | `config_files[\"shared/ptxprint/Default/ptxprint.sty\"]`, `Settings.xml`, `*-mods.sty` | Custom styling, Paratext settings, stylesheet overrides | `klappy://canon/articles/stylesheet-format` · `klappy://canon/articles/file-system-map` |\n| **5** | `figures: [...]` + `config_files[\".../<PRJ>-<config>.piclist\"]` | Pictures and illustrations | `klappy://canon/articles/piclist-format` |\n\nEach level is purely additive. Level 1 doesn't require anything from Level 2. Level 3 doesn't require anything from Level 4. The bundle is the floor; you climb only the rungs you need.\n\n---\n\n## Level 0 — Default render\n\nThe smallest possible payload:\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"BSB\",\n  \"books\": [\"JHN\"],\n  \"sources\": [\n    {\n      \"book\": \"JHN\",\n      \"filename\": \"44JHNBSB.usfm\",\n      \"url\": \"https://...\",\n      \"sha256\": \"<hex64>\"\n    }\n  ]\n}\n```\n\nThat's it. No `config_files`, no `fonts`, no `figures`, no `define`, no `mode` (defaults to `simple`). The Container stages its bundled cfg at the project's Default config path, runs PTXprint, returns a content-addressed PDF URL.\n\nWhat the bundle produces is described in `klappy://canon/articles/bundled-default-cfg`. Empirically: BSB John → 37 pages, ~377 KB PDF, ~15 seconds container time, 0 errors.\n\n**When Level 0 is the right answer:** the agent has a USFM file and just wants to see it as a book. No styling decisions, no font requirements beyond Latin script, no figures. Most \"render this for me\" intents start here.\n\n---\n\n## Level 1 — `define` for one-shot tweaks\n\nWhen the agent wants to change one knob without touching the cfg:\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"BSB\",\n  \"books\": [\"JHN\"],\n  \"sources\": [...],\n  \"define\": {\n    \"s_linespacing\": \"13\",\n    \"c_iffighiderefs\": \"True\",\n    \"vars_maintitle\": \"John\"\n  }\n}\n```\n\n`define` keys are PTXprint UI widget identifiers (`s_linespacing`, `c_iffighiderefs`, `vars_maintitle`, etc.). The Container passes them as `-D key=value` flags to the PTXprint CLI. They override the cfg for this render only — nothing persists.\n\n**When Level 1 is the right answer:** \"change line spacing to 13\"; \"hide cross-references\"; \"set the title to John\"; \"use 14pt margins instead of 12pt\"; any single-knob adjustment.\n\n**How to find the right key.** Use `docs(query=\"<your intent>\", depth=2)` against the settings cookbook:\n\n| Intent | Likely query |\n|---|---|\n| \"make text bigger\" | `docs(\"body font size\")` |\n| \"tighter line spacing\" | `docs(\"line spacing\")` |\n| \"hide cross-reference numbers\" | `docs(\"hide cross references\")` |\n| \"set the title\" | `docs(\"title page title\")` |\n| \"single column\" | `docs(\"single column body\")` |\n| \"different page size\" | `docs(\"page size\")` |\n\nThe cookbook (`klappy://canon/articles/settings-cookbook`) maps intents to widget IDs.\n\n**Limit:** `define` can change cfg-key behavior, but it can't change the *file shape* of the project (e.g., it can't add a stylesheet, can't add a Settings.xml, can't add fonts). For those, climb to Level 2+.\n\n---\n\n## Level 2 — Replace the cfg\n\nWhen the agent needs many cfg changes, or changes that don't fit the `define` model (e.g., `[vars]` section additions, multi-book layout decisions):\n\n```json\n{\n  ...\n  \"config_files\": {\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"[paper]\\npagesize = 210mm, 297mm (A4)\\ncolumns = False\\n[document]\\nfontregular = Gentium Plus||false|false|\\n...\"\n  }\n}\n```\n\nThe caller's cfg fully replaces the bundled default. The Container does not merge — it stages exactly what the caller sent.\n\n**Reasoning principle:** if the caller supplies a cfg, they own it. Mixing the bundled default with a partial cfg would create silent surprises (which keys take precedence? which sections get filled in? what about typo'd section names?). Replace-only keeps the contract obvious.\n\n**Workflow pattern:**\n1. `docs(query=\"bundled default cfg\")` to retrieve the bundle as a starting point\n2. Modify the cfg in-place\n3. Submit the modified cfg under `config_files`\n\nFor first-time cfg authors: `klappy://canon/articles/config-construction` walks through the structural anatomy of a working cfg.\n\n**When Level 2 is the right answer:** \"render with a 6×9 page size, single column, larger fonts\" (multi-knob restructure); \"render the whole NT\" (needs `bookscope = multiple` and `booklist`); \"add a custom `[vars]` block for a publication's title metadata\"; \"use a different versification\".\n\n---\n\n## Level 3 — Different fonts\n\nWhen the agent's content is non-Latin, or the publication style requires a specific typeface:\n\n```json\n{\n  ...\n  \"fonts\": [\n    {\n      \"family_id\": \"gentiumplus\",\n      \"version\": \"6.200\",\n      \"filename\": \"GentiumPlus-Regular.ttf\",\n      \"url\": \"https://lff.api.languagetechnology.org/.../GentiumPlus-Regular.ttf\",\n      \"sha256\": \"<hex64>\"\n    },\n    {\n      \"family_id\": \"gentiumplus\",\n      \"filename\": \"GentiumPlus-Bold.ttf\",\n      \"url\": \"...\",\n      \"sha256\": \"...\"\n    }\n  ]\n}\n```\n\n**Coordination with Level 2:** the cfg's `[document] fontregular` (and `fontbold`, `fontitalic`, `fontbolditalic`) must name a font that's findable. Either:\n\n- (a) name a font the Container has bundled (Charis, in current state) — no `fonts` array needed\n- (b) name a font supplied via the `fonts` array — Container fetches via URL+sha256, places in font path, fontconfig finds it by family name\n\nFor (b), the cfg in `config_files` must reference the family name the font's metadata advertises. Use `klappy://canon/articles/font-resolution` for the URL/hash sourcing pattern via SIL's Language Font Finder (LFF).\n\n**When Level 3 is the right answer:** non-Latin script (Arabic, Devanagari, CJK); specific style requirements (Gentium for academic publishing); language-tagged font fallback for vernacular content.\n\n---\n\n## Level 4 — Custom stylesheets, Settings.xml, mods\n\nWhen the agent's publication needs custom paragraph styles, peripherals (sidebars, study notes), or non-default Paratext metadata:\n\n```json\n{\n  ...\n  \"config_files\": {\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"...\",\n    \"shared/ptxprint/Default/ptxprint.sty\": \"\\\\Marker s1\\n\\\\Bold\\n\\\\Justification Center\\n...\",\n    \"shared/ptxprint/Default/ptxprint-mods.sty\": \"...\",\n    \"Settings.xml\": \"<ScriptureText>...</ScriptureText>\"\n  }\n}\n```\n\n**Reference articles:**\n\n- `klappy://canon/articles/stylesheet-format` — sty file syntax\n- `klappy://canon/articles/file-system-map` — full project tree (where each file goes)\n- `klappy://canon/articles/usfm-markers-headless` — what markers do and how styles target them\n- `klappy://canon/articles/frt-local-and-cover-periphs` — front matter content\n- `klappy://canon/articles/changes-txt-format` — regex USFM transforms\n\n**When Level 4 is the right answer:** publication-specific styling (paragraph indents, drop-cap rules, peripheral box styling); diglot publishing; cross-references with custom layouts; front matter (TOC, copyright page); custom versification declarations.\n\n---\n\n## Level 5 — Figures\n\nWhen the publication has illustrations, maps, or photos:\n\n```json\n{\n  ...\n  \"config_files\": {\n    ...,\n    \"shared/ptxprint/Default/BSB-Default.piclist\": \"JHN 1.1 ...|src=\\\"jesus.tif\\\" size=\\\"col\\\" pgpos=\\\"tr\\\" ref=\\\"1:1\\\"\"\n  },\n  \"figures\": [\n    {\n      \"filename\": \"jesus.tif\",\n      \"url\": \"https://.../jesus.tif\",\n      \"sha256\": \"<hex64>\"\n    }\n  ]\n}\n```\n\nThe piclist (config-folder text file) declares placement; the `figures` array supplies the binaries. Both must agree on filenames.\n\n**Reference articles:**\n\n- `klappy://canon/articles/piclist-format` — piclist syntax\n- `klappy://canon/articles/file-system-map` — where figures get materialized\n- `klappy://canon/articles/payload-construction` — schema details for the `figures` array\n\n**When Level 5 is the right answer:** the publication has any visual content — Bible illustrations, maps, charts, photos. Figures can be added to any earlier level.\n\n---\n\n## Composition: layering levels\n\nLevels stack. A Level-3 payload (`fonts`) typically also includes Level-2 (`config_files` with a cfg that names the new font family). A Level-5 payload (`figures`) typically also includes Level-4 (`config_files` with a piclist). The `define` map (Level 1) can ride on top of any level.\n\nWhat does NOT stack: `define` entries that contradict the cfg in `config_files` for the same key. The cfg wins for keys it explicitly sets; `define` wins for keys the cfg leaves at PTXprint's compiled default. To force a `define` value to override a cfg key, leave that key out of the cfg or comment it.\n\n---\n\n## What about partial cfg overlay?\n\nA reasonable instinct: \"I want to keep the bundle's `[paper]` and `[notes]` sections but change `[document] fontregular` only — can I send a cfg with just `[document]`?\"\n\nThe current architectural answer: **no, the worker stages exactly what you send.** If you send a cfg with only `[document]`, PTXprint sees a cfg with only `[document]` — every other section reverts to PTXprint's compiled defaults (which differ from the bundle's defaults). The result is unpredictable layout drift.\n\nTwo clean paths instead:\n\n1. **`define` for one or two cfg-key changes** (Level 1) — no cfg replacement at all.\n2. **Full cfg replacement** (Level 2) — start from the bundle (retrieve via `docs(\"bundled default cfg\")`), edit, send the whole thing.\n\nFuture option (deferred): PTXprint's native `[import] config = Parent` inheritance would allow a child cfg with only its overrides, with the bundle as parent. This requires the Container to keep the bundle accessible at a parent-config path AND callers to declare the import correctly. Tracked as a deferred enhancement; not active in current Container behavior.\n\n---\n\n## Discovery via docs\n\nMost Level-0 → Level-1 → Level-2 transitions are agent-driven:\n\n```\nagent: \"render BSB John\"\n→ Level 0 — submit {books, sources}\n→ render succeeds, agent shows PDF to user\n\nuser: \"but make the lines a bit tighter\"\nagent: docs(\"change line spacing\", depth=2)\n→ settings-cookbook returns: define = {\"s_linespacing\": \"<value>\"}\n→ Level 1 — submit with define added\n\nuser: \"and switch to 6x9 trim with single column\"\nagent: docs(\"page size\", depth=2)\nagent: docs(\"single column\", depth=2)\n→ multi-knob, doesn't fit cleanly in define\nagent: docs(\"bundled default cfg\", depth=2)\n→ retrieve full cfg, edit [paper] columns/pagesize, climb to Level 2\n```\n\nThe pattern: each customization request is a docs query. The query surfaces the right field; the agent climbs the ladder one level at a time, only when needed.\n\n---\n\n## See also\n\n- `klappy://canon/articles/bundled-default-cfg` — what the bundle produces\n- `klappy://canon/articles/payload-construction` — schema reference\n- `klappy://canon/articles/settings-cookbook` — Level-1 intent-to-key map\n- `klappy://canon/articles/config-construction` — Level-2 cfg authoring\n- `klappy://canon/articles/font-resolution` — Level-3 font sourcing\n- `klappy://canon/articles/stylesheet-format` — Level-4 sty authoring\n- `klappy://canon/articles/piclist-format` — Level-5 figure placement\n- `klappy://canon/articles/diagnostic-patterns` — when a level produces unexpected output\n\n---\n\n*The bundle is the floor. Climb only the rungs you need. Each rung has a docs query. Discovery is tool-driven; canon documents the patterns; the worker stays opinion-free.*\n"},{"path":"articles/settings-cookbook.md","uri":"klappy://canon/articles/settings-cookbook","title":"Settings Cookbook — Intent-Keyed Reference","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","settings","cookbook","intent","by-task"],"stability":"working","what_it_answers":"\"User said X — what setting controls that?\" Quick lookup table by user intent.","sections":[{"slug":"how-to-use-this-article","heading":"How to use this article","start":22,"end":28},{"slug":"make-the-page-a-different-size","heading":"\"Make the page a different size\"","start":28,"end":36},{"slug":"wider-narrower-margins","heading":"\"Wider/narrower margins\"","start":36,"end":44},{"slug":"bigger-smaller-body-text","heading":"\"Bigger / smaller body text\"","start":44,"end":53},{"slug":"tighter-looser-line-spacing","heading":"\"Tighter / looser line spacing\"","start":53,"end":61},{"slug":"two-columns-one-column-different-gutter","heading":"\"Two columns / one column / different gutter\"","start":61,"end":70},{"slug":"hide-show-footnotes","heading":"\"Hide / show footnotes\"","start":70,"end":82},{"slug":"add-cross-references","heading":"\"Add cross-references\"","start":82,"end":92},{"slug":"show-hide-parallel-passage-references","heading":"\"Show / hide parallel-passage references\"","start":92,"end":104},{"slug":"hide-show-pictures-globally","heading":"\"Hide / show pictures globally\"","start":104,"end":114},{"slug":"custom-header-text","heading":"\"Custom header text\"","start":114,"end":123},{"slug":"generate-a-table-of-contents","heading":"\"Generate a Table of Contents\"","start":123,"end":136},{"slug":"make-a-draft-low-quality-fast","heading":"\"Make a draft (low-quality, fast)\"","start":136,"end":145},{"slug":"output-as-black-and-white-colour-spot-colour","heading":"\"Output as black-and-white / colour / spot-colour\"","start":145,"end":153},{"slug":"add-a-watermark","heading":"\"Add a watermark\"","start":153,"end":157},{"slug":"booklet-pagination","heading":"\"Booklet pagination\"","start":157,"end":167},{"slug":"set-up-a-diglot","heading":"\"Set up a diglot\"","start":167,"end":171},{"slug":"lock-down-a-setting-for-a-cluster","heading":"\"Lock down a setting for a cluster\"","start":171,"end":175},{"slug":"when-the-cookbook-doesn-t-have-it","heading":"When the cookbook doesn't have it","start":175,"end":192}],"body":"---\ntitle: \"Settings Cookbook — Intent-Keyed Reference\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"settings\", \"cookbook\", \"intent\", \"by-task\"]\nderives_from: \"canon/governance/headless-operations.md (Part 5 Settings Cookbook)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Settings Cookbook\n\n> **What this answers.** \"User said X — what setting controls that?\" Quick lookup table by user intent.\n>\n> **Related articles.** `klappy://canon/articles/config-construction` · `klappy://canon/articles/cli-reference` · `klappy://canon/articles/workflow-recipes`\n\n---\n\n## How to use this article\n\nSections are by **user intent**, not by INI section. For each intent, the table lists the relevant cfg section, the typical key, common values, and what to watch for. Treat as starting points; verify exact key names against canon's tooltip-derived index before relying on a name.\n\nWhen a key name is uncertain (the deck doesn't always name them consistently), this article says so. Don't fabricate.\n\n## \"Make the page a different size\"\n\n| Section | Key | Common values | Notes |\n|---|---|---|---|\n| `[paper]` | `pagesize` | `148mm, 210mm (A5)`, `210mm, 297mm (A4)`, `5.5in, 8.5in (Half Letter)` | Format is `width, height (label)`. Confirm with printer before committing. |\n\nWatch for: page-size change usually invalidates margin and column tuning; user may need to re-tune. Booklet output formats constrain physical paper separately.\n\n## \"Wider/narrower margins\"\n\n| Section | Key | Common values | Notes |\n|---|---|---|---|\n| `[paper]` | `margins` | `12` (mm), `0.5` (in) | Often a single value applied to all four sides; some configs split top/bottom/inner/outer |\n\nWatch for: binding gutter is usually separate; tight margins collide with header/footer space.\n\n## \"Bigger / smaller body text\"\n\n| Section | Key | Common values | Notes |\n|---|---|---|---|\n| `[document]` | (font-size key — verify) | `9.5`, `10.5`, `12` (pt) | Deck doesn't name the exact key consistently; query canon |\n| `[paragraph]` | `linespacing` | font size × 1.2–1.3 | Adjust together with font size |\n\nWatch for: font changes can introduce missing-glyph problems in non-Latin scripts; if page count was optimised, font-size change throws off line counts.\n\n## \"Tighter / looser line spacing\"\n\n| Section | Key | Common values |\n|---|---|---|\n| `[paragraph]` | `linespacing` | typically 1.2–1.3 × the font size |\n\nWatch for: Layout tab has three \"Optimize Lines Per Page\" options the agent can probe by submitting candidate payloads with different `define` values. A 5% body-text shrink often resolves overfull-box issues without touching line spacing.\n\n## \"Two columns / one column / different gutter\"\n\n| Section | Key | Common values |\n|---|---|---|\n| `[document]` | `columns` (or similar) | `1`, `2`, `3` |\n| `[paper]` | column-gutter key | mm or em |\n\nWatch for: per-book column toggles override the global value (Psalms, Glossary, Strong's index commonly invert). Column count change affects every other layout decision.\n\n## \"Hide / show footnotes\"\n\n| Section | Key | Common values |\n|---|---|---|\n| `[notes]` | `includefootnotes` | `True` / `False` — the translation's `\\f` notes |\n| `[notes]` | `includextfn` | `True` / `False` — USFM 3 extended study notes `\\ef` |\n| `[notes]` | `fneachnewline` | `True` = one note per line; `False` = notes run on as a paragraph |\n| `[project]` | `usechangesfile` | `True` — required for any `changes.txt` rule to apply |\n| `[notes]` | `fnpos` | `page` (observed default) — placement of the note band |\n\nFootnote `\\f` style (in `ptxprint.sty`) controls caller appearance independently.\n\n## \"Add cross-references\"\n\n| Section | Key | Notes |\n|---|---|---|\n| `[notes]` | xref-list-source key | `Standard`, `Comprehensive`, `Alphabetical`, project-internal |\n| `[notes]` | xref-placement key | `below-footnotes`, `side-aligned`, `inner-aligned`, `centre-column`, `column-aligned` |\n| `[notes]` | xref-filter-to-published key | `True` / `False` |\n\nExternal cross-reference lists are powerful. Walk filter + placement + alignment as three orthogonal choices.\n\n## \"Show / hide parallel-passage references\"\n\n> **Disambiguation.** This is a different concept from \"Add cross-references\" above. The recipe above controls the `\\x` cross-reference marker (footnote-style verse-to-verse links built from an external xref list). This recipe controls the `\\r` marker (the parallel-passage line that sits under a section heading, like *(Genesis 1:1–2; Hebrews 11:1–3)*). Both are sometimes informally called \"cross-references\" in English; the underlying USFM markers and rendering paths are distinct. See `klappy://canon/articles/usfm-markers-headless` for the `\\r` marker definition.\n\n| Section | Key | Common values | Notes |\n|---|---|---|---|\n| `[document]` | `parallelrefs` | `True` / `False` | When `True`, `\\r` lines render as italic centered text under the preceding section heading. When `False`, `\\r` content is silently dropped from output (no warning, no error). |\n\nWatch for: `parallelrefs = False` is the canon-grounded cause of the most-common \"my section headings lost their cross-references\" symptom. Easy to flip off accidentally during a config cleanup pass — once flipped, the regression is invisible without visual comparison against a known-good render. Confirmed empirically in session 12 (see `canon/encodings/transcript-encoded-session-12.md` H-026 / O-046) where the v3 → v4 → v5 cleanup iteration spent four versions trying stylesheet hacks before diff'ing the cfg and finding the one cfg-key change that mattered.\n\nThe `\\r` marker's appearance (italic, centered, etc.) is controlled separately by the `\\Marker r` block in `ptxprint.sty`. To suppress the marker entirely, set `parallelrefs = False`. To show it but restyle, leave `parallelrefs = True` and edit the `\\Marker r` block.\n\n## \"Hide / show pictures globally\"\n\nUse the payload's `define` field for run-time-only override:\n\n```json\n\"define\": { \"c_fighiderefs\": \"True\" }\n```\n\nFor per-picture control, modify the piclist content in `config_files`. See `klappy://canon/articles/piclist-format`.\n\n## \"Custom header text\"\n\n| Section | Key | Notes |\n|---|---|---|\n| `[header]` | header-text key | Often supports `\\zvar` substitution |\n| `[vars]` | user-defined variables | Define `headertext = MyTitle` and reference in header settings |\n\nFor diglot or RTL-mixed headers, additional code-snippet patterns exist; refer the user there.\n\n## \"Generate a Table of Contents\"\n\nTwo parts:\n\n1. Enable front matter in the configuration (so FRT gets included; add `FRT` to the payload's `books`).\n2. Add the ToC marker to `FRTlocal.sfm` content:\n\n```\n\\ip \\ztoc|main\\*\n```\n\nThe `|main\\*` parameter accepts variants: `|nt\\*`, `|ot\\*`, `|sortc\\*`, `|sorta\\*`. See `klappy://canon/articles/usfm-markers-headless`.\n\n## \"Make a draft (low-quality, fast)\"\n\n| Approach | What it does |\n|---|---|\n| `mode: \"simple\"` in the payload | One-pass typesetting; skips multi-pass autofill |\n| `define: { \"output_format\": \"Screen-Quickest\" }` | Skips post-processing for fastest preview |\n\nUse for fast iteration; don't use for final output.\n\n## \"Output as black-and-white / colour / spot-colour\"\n\n| Section | Key | Common values |\n|---|---|---|\n| `[finishing]` | output format key | `Print-CMYK`, `Print-Gray`, `Print-Spot`, `Screen-Quickest`, `Digital-RGB`, `Digital-CMYK` |\n\nPrint-CMYK = full-colour offset; Print-Gray = B/W; Print-Spot = two-colour. Screen formats are faster; switch to print formats only for final.\n\n## \"Add a watermark\"\n\nThe deck mentions a Watermark setting but doesn't surface the exact key in plain text. Query canon's tooltip-dump-derived index, or surface to user as a known gap.\n\n## \"Booklet pagination\"\n\n| Section | Key | Common values | Notes |\n|---|---|---|---|\n| `[finishing]` | booklet-up key | `2-up`, `4-up`, `8-up` | |\n| `[finishing]` | booklet-flip key | `short-edge`, `long-edge` | Depends on imposition |\n| `[finishing]` | fold-first key | `True` / `False` | For some bind orders |\n\nTest pattern: 16-page PDF, 4-up, A3 paper, fold top-to-bottom then left-to-right, staple, trim.\n\n## \"Set up a diglot\"\n\nMulti-step workflow, not a single setting. See `klappy://canon/articles/workflow-recipes` (Recipe: Set up a diglot publication) for the axes and order.\n\n## \"Lock down a setting for a cluster\"\n\nSee `klappy://canon/articles/config-inheritance-and-overrides`. The agent edits `ptxprint_project.cfg` (project-wide) or `ptxprint_override.cfg` (config-specific) in project state, with cluster-propagation implications spelled out to the user before changes.\n\n## When the cookbook doesn't have it\n\nThe escalation ladder:\n\n1. Search canon for the user's intent or the suspected setting name.\n2. Check the running tooltip dump (canon-seeded).\n3. Look at an existing project that does the thing the user wants — settings can be reverse-engineered from working configs.\n4. Recommend the user check the running PTXprint UI (the tooltip is canonical).\n5. Recommend the community forum at `support.bible/ptxprint` for project-specific questions.\n6. Acknowledge the gap honestly: \"I don't have this in my reference. Here's where to look.\"\n\nNever fabricate settings names.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`.*\n"},{"path":"articles/snapshot-operations.md","uri":"klappy://canon/articles/snapshot-operations","title":"Snapshot Operations — Track A Telemetry Archive","audience":"project","tags":["ptxprint","mcp","telemetry","snapshot","operations","long-term-retention","r2","cron","bootstrap","v1.3-aligned"],"stability":"working","what_it_answers":"The snapshot mechanism designed in `klappy://canon/articles/hero-metrics-and-storytelling` § \"Long-Term Retention Strategy\" is implemented in `src/snapshot.ts` and wired into the Worker. This article is the operations companion: how to set ","sections":[{"slug":"what-the-snapshot-system-does","heading":"What the snapshot system does","start":21,"end":43},{"slug":"one-time-setup-per-environment","heading":"One-time setup (per environment)","start":43,"end":71},{"slug":"bootstrap-backfill-the-archive-after-deploy","heading":"Bootstrap — backfill the archive after deploy","start":71,"end":115},{"slug":"manual-single-week-snapshot","heading":"Manual single-week snapshot","start":115,"end":132},{"slug":"smoke-test-post-deploy","heading":"Smoke test (post-deploy)","start":132,"end":173},{"slug":"reading-the-lifetime-hero-stat","heading":"Reading the lifetime hero stat","start":173,"end":220},{"slug":"what-the-cron-does-under-the-hood","heading":"What the cron does (under the hood)","start":220,"end":254},{"slug":"operating-principle","heading":"Operating principle","start":254,"end":262},{"slug":"see-also","heading":"See Also","start":262,"end":268}],"body":"---\ntitle: \"Snapshot Operations — Track A Telemetry Archive\"\naudience: project\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"telemetry\", \"snapshot\", \"operations\", \"long-term-retention\", \"r2\", \"cron\", \"bootstrap\", \"v1.3-aligned\"]\ndate: 2026-04-30\nderives_from: \"klappy://canon/articles/hero-metrics-and-storytelling, klappy://canon/governance/telemetry-governance\"\ncompanion_to: \"klappy://canon/articles/hero-metrics-and-storytelling, klappy://canon/specs/ptxprint-mcp-v1.3-spec\"\ncanonical_status: non_canonical\ngoverns: \"How operators run, bootstrap, smoke-test, and read the Track A telemetry snapshot archive.\"\nstatus: working\n---\n\n# Snapshot Operations — Track A Telemetry Archive\n\n> The snapshot mechanism designed in `klappy://canon/articles/hero-metrics-and-storytelling` § \"Long-Term Retention Strategy\" is implemented in `src/snapshot.ts` and wired into the Worker. This article is the operations companion: how to set up the R2 bucket, how to bootstrap the archive after deploy, how to smoke-test a snapshot run, and how to read the lifetime hero stat. It is operational, not architectural — the architectural decisions live in the hero-metrics article and stay there.\n\n---\n\n## What the snapshot system does\n\nA Cloudflare Worker Cron Trigger fires at Monday 00:00 UTC every week. It runs five aggregate queries against the `ptxprint_telemetry` Analytics Engine dataset, scoped to the just-completed week (`[Mon 00:00 UTC, next Mon 00:00 UTC)`). Each query result becomes one or more JSONL lines in an R2 bucket named `ptxprint-telemetry-snapshots`. The archive persists past the 90-day Analytics Engine retention window. Lifetime totals are computed by reading the archive and adding the current incomplete week from raw Analytics Engine.\n\nThe five metrics:\n\n| Metric | R2 object | Rows per snapshot |\n|---|---|---|\n| `pages_typeset_weekly` | `pages-typeset-weekly.jsonl` | 1 per week |\n| `successful_builds_weekly` | `successful-builds-weekly.jsonl` | 1 per week |\n| `cache_hits_weekly` | `cache-hits-weekly.jsonl` | 1 per week |\n| `cache_misses_weekly` | `cache-misses-weekly.jsonl` | 1 per week |\n| `failure_mode_distribution_weekly` | `failure-mode-distribution-weekly.jsonl` | 1 per (week, failure_mode) |\n\nThe article enumerates four query buckets; `cache_hits` and `cache_misses` are materialized as two distinct metrics so each metric has exactly one R2 object (matching the article's \"one object per metric\" rule).\n\n### The `failure_mode` dimension\n\nThe hero-metrics article schema example shows `{metric, week_start, value, snapshotted_at, source}`. For the failure-mode distribution, each row carries an additional `failure_mode` field that names the bucket (`success`, `soft`, `hard`, `cancelled`, `timeout`). This is additive — `failure_mode` is already a public schema dimension per `klappy://canon/governance/telemetry-governance` § \"Structural Dimensions\" — and it preserves the article's \"one R2 object per metric\" rule. The replacement key for idempotent merges is `(metric, week_start, failure_mode ?? \"\")`.\n\n---\n\n## One-time setup (per environment)\n\n### 1. Create the R2 bucket\n\n```bash\nwrangler r2 bucket create ptxprint-telemetry-snapshots\n```\n\nThe bucket binding (`TELEMETRY_SNAPSHOTS`) is already declared in `wrangler.jsonc`. After bucket creation, the next deploy picks it up automatically.\n\n### 2. (Optional) Set a manual-run token\n\nThe cron handles routine weekly snapshots. For bootstrap and recovery, the Worker exposes `POST /internal/snapshot/run`, gated by a header token. To enable it:\n\n```bash\nopenssl rand -hex 32 | wrangler secret put SNAPSHOT_BOOTSTRAP_TOKEN\n```\n\nIf the secret is not set, the route returns `503` with a clear message. The Cron Trigger does not depend on this token.\n\n### 3. Make the bucket public-readable (optional, recommended)\n\nSame transparency principle as `telemetry_public` — the snapshot archive contains only public aggregates. Enable public read access in the Cloudflare dashboard (R2 → Bucket → Settings → Custom domains / Public access).\n\nIf the bucket is left private, the read recipes in this article still work via the Worker's `/diagnostics/snapshot/lifetime` endpoint (which reads R2 server-side).\n\n---\n\n## Bootstrap — backfill the archive after deploy\n\nUntil the cron has run at least once, the archive is empty. To capture every week within the 90-day Analytics Engine retention window:\n\n```bash\nTOKEN=<your SNAPSHOT_BOOTSTRAP_TOKEN>\n\ncurl -X POST https://ptxprint.klappy.dev/internal/snapshot/run \\\n  -H \"x-snapshot-bootstrap-token: $TOKEN\" \\\n  -H \"content-type: application/json\" \\\n  -d '{\"weeks_back\": 13}'\n```\n\n`weeks_back: 13` snapshots the last 13 completed weeks (≈ 90 days). The route caps `weeks_back` at 26.\n\nThe response shape:\n\n```json\n{\n  \"weeks_processed\": 13,\n  \"results\": [\n    {\n      \"week_start\": \"2026-01-26\",\n      \"snapshotted_at\": \"2026-04-30T03:14:15.926Z\",\n      \"metrics\": [\n        { \"name\": \"pages_typeset_weekly\", \"object_key\": \"pages-typeset-weekly.jsonl\", \"records_written\": 1, \"records_replaced\": 0 },\n        ...\n      ],\n      \"ok\": true\n    },\n    ...\n  ],\n  \"ok\": true\n}\n```\n\nPer-metric errors do not abort other metrics or other weeks; they appear as `error: \"...\"` strings in the response. Re-running with the same range is safe — it overwrites with the same values.\n\n### Schedule the bootstrap as soon as the snapshot mechanism deploys\n\nPer the hero-metrics article: \"every week of delay between v1.2 telemetry shipping and snapshot bootstrapping is a week of pages that will not survive the retention window.\" Run the bootstrap once immediately after the first deploy that includes Track A.\n\n---\n\n## Manual single-week snapshot\n\nFor recovery, smoke testing, or backfilling a specific week:\n\n```bash\ncurl -X POST https://ptxprint.klappy.dev/internal/snapshot/run \\\n  -H \"x-snapshot-bootstrap-token: $TOKEN\" \\\n  -H \"content-type: application/json\" \\\n  -d '{\"week\": \"2026-04-27\"}'\n```\n\nThe week must be a Monday in `YYYY-MM-DD` form. The route refuses non-Monday dates with an explicit error message naming the correct Monday.\n\nWith no body (or `{}`), the route runs the same week the cron would have run on its next firing — the just-completed week.\n\n---\n\n## Smoke test (post-deploy)\n\nAfter bootstrap, verify the pipeline end-to-end:\n\n1. **List the archive contents.**\n   ```bash\n   curl https://ptxprint.klappy.dev/diagnostics/snapshot\n   ```\n   Expect `objects_present` to contain the five expected `.jsonl` files (or fewer if some metrics had zero data — empty queries still produce one row with `value: 0`, so all five files should normally be present after bootstrap).\n\n2. **Fetch one JSONL file directly from R2.**\n\n   If the bucket is public:\n   ```bash\n   curl https://pub-<bucket-id>.r2.dev/pages-typeset-weekly.jsonl\n   ```\n\n   If private, the file contents are visible to the Worker only — use `wrangler r2 object get`:\n   ```bash\n   wrangler r2 object get ptxprint-telemetry-snapshots/pages-typeset-weekly.jsonl\n   ```\n\n   Expect one JSON object per line, each matching:\n   ```json\n   {\"metric\":\"pages_typeset_weekly\",\"week_start\":\"2026-04-27\",\"value\":...,\"snapshotted_at\":\"...\",\"source\":\"ptxprint_telemetry\"}\n   ```\n\n3. **Verify idempotency.** Re-run the snapshot for the same week. The line count in R2 should not change. The `snapshotted_at` field on the affected row should advance to the new run's timestamp; the `value` should match (or differ only because new events arrived in raw AE since the last run).\n\n4. **Verify the lifetime composite.**\n   ```bash\n   curl https://ptxprint.klappy.dev/diagnostics/snapshot/lifetime\n   ```\n   Returns the lifetime hero stat as JSON. See \"Reading the lifetime hero stat\" below.\n\n### Bootstrap smoke without real telemetry\n\nIf the deploy is fresh and `ptxprint_telemetry` has no rows yet, every snapshot value will be `0` or `null`. That is a real smoke result — it proves the R2 write path works even when the data is empty. The `pages-typeset-weekly.jsonl` file will contain one row per snapshot week, each with `value: 0`. As real telemetry accumulates, future snapshots replace those zeros with real counts (idempotent merge by week).\n\n---\n\n## Reading the lifetime hero stat\n\nThe hero-metrics article specifies the composite query:\n\n```text\nlifetime_pages_typeset =\n  SUM(value FROM R2 ptxprint-telemetry-snapshots/pages-typeset-weekly.jsonl)\n  +\n  SELECT SUM(pages_count * _sample_interval)\n  FROM ptxprint_telemetry\n  WHERE event_type = 'job_terminal'\n    AND failure_mode = 'success'\n    AND timestamp >= toStartOfWeek(NOW() - INTERVAL '7' DAY)\n```\n\nThe Worker exposes this directly at `GET /diagnostics/snapshot/lifetime` (no auth, public, 5-minute browser cache). The response:\n\n```json\n{\n  \"lifetime_pages\": 152340,\n  \"archive_pages\": 138917,\n  \"current_week_pages\": 13423,\n  \"current_week_start\": \"2026-04-27\",\n  \"archive_weeks_counted\": 41,\n  \"computed_at\": \"2026-04-30T03:14:15.926Z\",\n  \"archive_source\": \"r2:ptxprint-telemetry-snapshots/pages-typeset-weekly.jsonl\",\n  \"raw_source\": \"ptxprint_telemetry (analytics engine)\"\n}\n```\n\nThis URL is what the eventual public homepage hero number queries. The endpoint lives on the Worker so the recipe does not need to be re-implemented per consumer.\n\n### Doing the composition by hand\n\nFor Grafana, a custom dashboard, or a one-off audit:\n\n1. Read the archive:\n   - Public bucket: `curl https://pub-<bucket-id>.r2.dev/pages-typeset-weekly.jsonl`\n   - Private bucket: `wrangler r2 object get ptxprint-telemetry-snapshots/pages-typeset-weekly.jsonl --file=archive.jsonl`\n2. Sum the `value` fields across rows where `metric == \"pages_typeset_weekly\"`.\n3. Run the current-week SQL via `telemetry_public` or directly through the Analytics Engine SQL API.\n4. Add the two numbers.\n\nThe function `getLifetimeHeroStat()` in `src/snapshot.ts` is the single source of truth. If the recipe ever changes (e.g., a different cutoff for the current week), update the function and this section together.\n\n---\n\n## What the cron does (under the hood)\n\n```typescript\n// src/index.ts default export\nasync scheduled(event, env, ctx) {\n  const fireTime = new Date(event.scheduledTime);\n  const justCompletedWeek = addDays(weekStartFor(fireTime), -7);\n  ctx.waitUntil(runSnapshot(env, justCompletedWeek));\n}\n```\n\nWhen Cron fires Monday 00:00 UTC, `weekStartFor(fireTime)` returns the *current* week's Monday (which is `fireTime` itself). Subtracting 7 days yields the prior Monday — the start of the week that just ended at `fireTime - 1 second`.\n\n`runSnapshot()` iterates over the `METRICS` array in `src/snapshot.ts`. Per-metric errors are captured and logged; they do not abort other metrics. The whole run is wrapped in `ctx.waitUntil` so the cron itself returns immediately and the Workers runtime keeps the snapshot work alive in the background.\n\n### Verifying the cron registration\n\nAfter deploy:\n\n```bash\nwrangler deployments list   # verify latest deploy includes the cron trigger\n```\n\nOr via the Cloudflare API:\n\n```bash\ncurl -H \"Authorization: Bearer $CF_API_TOKEN\" \\\n  \"https://api.cloudflare.com/client/v4/accounts/$CF_ACCOUNT_ID/workers/scripts/ptxprint-mcp/schedules\"\n```\n\nExpect `{\"data\":[{\"cron\":\"0 0 * * 1\",\"created_on\":\"...\",\"modified_on\":\"...\"}], \"success\":true}`.\n\n---\n\n## Operating principle\n\nThe snapshot system is generic infrastructure. The five metrics it archives are the canonical \"what gets snapshotted\" list from the hero-metrics article — adding a new metric means appending one entry to the `METRICS` array in `src/snapshot.ts`, with no other code changes. Removing a metric means deleting the entry; the orphaned R2 object stays in place (data is precious, code is cheap) until manually deleted.\n\nThe privacy floor (`klappy://canon/governance/telemetry-governance` § \"Privacy Floor\") is preserved by construction: the snapshot writer only reads aggregate query results that themselves only reference public schema slots. There is no path by which project IDs, USFM bytes, or any content data could appear in the archive.\n\n---\n\n## See Also\n\n- [Hero Metrics & Storytelling](klappy://canon/articles/hero-metrics-and-storytelling) — the design that this article operationalizes.\n- [Telemetry Governance](klappy://canon/governance/telemetry-governance) — the data contract the snapshot reads from.\n- [v1.3 Specification](klappy://canon/specs/ptxprint-mcp-v1.3-spec) — the tool surface telemetry observes.\n"},{"path":"articles/study-notes-and-footnotes.md","uri":"klappy://canon/articles/study-notes-and-footnotes","title":"Study Notes and Footnotes — Putting Notes Under the Text, and Keeping Them There","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.3-aligned","footnotes","study-notes","changes-txt","layout"],"stability":"working","what_it_answers":"How do I add study notes (or any notes) to a book without editing the source USFM? Why did my `changes.txt` do nothing? What is the difference between `\\f` and `\\ef`? Why does a book with many notes fail with `! Dimension too large`, and what layout settings make a study Bible fit?","sections":[{"slug":"the-shape-of-a-study-bible","heading":"The shape of a study Bible","start":19,"end":25},{"slug":"getting-the-notes-into-the-text-without-touching-the-source","heading":"Getting the notes into the text without touching the source","start":25,"end":42},{"slug":"the-setting-you-must-flip","heading":"The setting you must flip","start":42,"end":53},{"slug":"f-or-ef","heading":"`\\f` or `\\ef`?","start":53,"end":62},{"slug":"why-a-full-book-fails-dimension-too-large","heading":"Why a full book fails: `! Dimension too large`","start":62,"end":81},{"slug":"diagnostic-checklist","heading":"Diagnostic checklist","start":81,"end":89},{"slug":"recipe-study-notes-from-an-open-notes-source","heading":"Recipe: study notes from an open notes source","start":89,"end":97}],"body":"---\ntitle: \"Study Notes and Footnotes — Putting Notes Under the Text, and Keeping Them There\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.3-aligned\", \"footnotes\", \"study-notes\", \"changes-txt\", \"layout\"]\ndate: 2026-09-05\ncanonical_status: working\n---\n\n# Study Notes and Footnotes — Putting Notes Under the Text, and Keeping Them There\n\n> **What this answers.** How do I add study notes (or any notes) to a book without editing the source USFM? Why did my `changes.txt` do nothing? What is the difference between `\\f` and `\\ef`? Why does a book with many notes fail with `! Dimension too large`, and what layout settings make a study Bible fit?\n>\n> **Related articles.** `klappy://canon/articles/changes-txt-format` · `klappy://canon/articles/config-construction` · `klappy://canon/articles/settings-cookbook` · `klappy://canon/articles/failure-mode-taxonomy`\n\n---\n\n## The shape of a study Bible\n\nScripture in the text block (usually two columns), notes in a band at the foot of the **same page** as the verses they explain. In USFM the notes are footnotes anchored in the verse; PTXprint/XeTeX places each page's notes under that page's text and shrinks the text block to make room. Nothing else has to be built: the layout is a footnote layout with more footnotes.\n\nVerified 2026-09-05: Titus, BSB, 14 Aquifer Open Study Notes as `\\f` footnotes at their verses, A5 two-column, Gentium — every note landed on the page of its verse (kitchen ticket `2026-09-05-titus-study-bible-ptxprint`).\n\n## Getting the notes into the text without touching the source\n\n`sources` are URL + sha256 — the worker verifies the bytes, so the agent cannot edit the USFM in place and cannot host a modified copy without infrastructure it may not have. Use `changes.txt` instead: a config-scoped file in `config_files` whose rules insert the note after the verse marker.\n\n```\n# shared/ptxprint/<config>/changes.txt\nat TIT 3:1 \"\\\\v 1 \" > \"\\\\v 1 \\\\f + \\\\fr 3:1 \\\\ft Paul might be telling the believers to make a clear distinction… \\\\f* \"\nat TIT 3:4 \"\\\\v 4 \" > \"\\\\v 4 \\\\f + \\\\fr 3:4–7 \\\\ft This passage might be a summary or quote… \\\\f* \\\\f + \\\\fr 3:4 \\\\ft We should behave toward…\\\\f* \"\n```\n\nRules of the road (all bitten in practice):\n\n- **Anchor on the verse marker with its trailing space** (`\"\\\\v 1 \"`), scoped with `at BKK c:v`. Without the space, `\\v 1` also matches `\\v 10`–`\\v 19`.\n- **Several notes on one verse** (a section note and a verse note) go in one rule, in reading order, separated by a space.\n- **Note text must be plain USFM text.** Strip HTML; turn `<data class=\"bible-ref\">` cross-references into plain refs; never let a `\"` or `\\` from the note into the rule — `\"` ends the rule string, `\\` starts a marker. Replace `\"` with `”`.\n- `\\fr` carries the note's own range (`3:4–7`), which may differ from the anchoring verse.\n\n## The setting you must flip\n\n`changes.txt` is **ignored unless the config enables it**:\n\n```\n[project]\nusechangesfile = True\n```\n\nThe John fixture (`smoke/bsb-jhn-empirical.json`) ships `usechangesfile = False`. A payload that adds a `changes.txt` without flipping this renders a perfect book with no notes and no error — the first Titus run did exactly that. Check this line first when a rule \"did nothing\".\n\n## `\\f` or `\\ef`?\n\n| Marker | What it is | When |\n|---|---|---|\n| `\\f + \\fr … \\ft … \\f*` | The translation's own footnote stream — textual variants, literal renderings | The BSB source already carries these (`† 1:2 Literally before times eternal`) |\n| `\\ef + \\fr … \\ft … \\ef*` | USFM 3 **extended (study) note** — a separate note class, set apart from the translation's footnotes | Study notes, so a reader can tell the publisher's comment from the translator's |\n\nTo print `\\ef` notes the config needs `includextfn = True` (`[notes]`); with it off they are silently dropped. Both streams share the footnote area; `\\ef` lets the stylesheet give study notes their own size, caller, and rule. As of 2026-09-05 the `\\ef` path is documented but **not yet rendered in this kitchen** — the `\\f` path is the verified one. Treat `\\ef` as the target, `\\f` as the proven fallback.\n\n## Why a full book fails: `! Dimension too large`\n\nXeTeX places a page's footnotes as an *insert*. The text block shrinks to make room, but a page cannot shrink below zero text: when the notes anchored on one page are together **taller than the page**, the insert cannot be placed and TeX fails — `! Dimension too large` in the log (`failure_mode: hard`, `errors: [\"! Dimension too large.\"]`). Observed on Titus 1:5, where three long notes stack on one A5 page at 12 pt body with body-sized notes; chapter 3 alone (fewer, shorter notes) rendered.\n\nThe fixture layout is a *reading* Bible. A study Bible squashes the text and shrinks the notes so more of both fit:\n\n| Setting (`ptxprint.cfg`) | Reading Bible (fixture) | Study Bible |\n|---|---|---|\n| `[paper] pagesize` | `148mm, 210mm (A5)` | `6in, 9in` (standard study trim) or `8.5in, 11in` |\n| `[paper] fontsize` / `displayfontsize` | 12 | 9.5–10 |\n| `[notes] fnfontsize`* | body size | 7–7.5 |\n| `[notes] fneachnewline` | `False` (notes run on as one paragraph) | `True` (one note per line) |\n| `[notes] fnpos` | `page` | `page` |\n| note splitting | not allowed → fails | allow a note to continue onto the next page (head stays with the verse) |\n\n\\* Key names marked with the section they live in as observed in the bundled default cfg (`klappy://canon/articles/bundled-default-cfg`); where a key is named here but not present in the dump, confirm against a render before relying on it — the cfg surface has ~400 keys and this article was cut from one book's worth of them.\n\nPractical ceiling to plan for: at 7.5 pt in two columns on 6×9, a page carries roughly 3,000–3,500 characters of notes below a third-page text block. A verse whose notes exceed that must split, or the notes must be edited down.\n\n## Diagnostic checklist\n\n1. Notes missing, no error → `usechangesfile = True`? (`\\ef`: `includextfn = True`?)\n2. Notes on the wrong verse → anchor has the trailing space; `at BKK c:v` scope present.\n3. `! Dimension too large` → count characters of notes anchored on the failing page; shrink notes / enlarge page / split.\n4. Notes present but the translation's own footnotes vanished → the rule replaced a `\\v` that already carried a `\\f`; anchor after the existing note or insert before the next `\\v`.\n5. Rule string looks fine, still nothing → a `\"` inside the note text ended the rule early. Look for `”` substitution.\n\n## Recipe: study notes from an open notes source\n\n1. Fetch the notes for the book (e.g. Aquifer Open Study Notes via the Aquifer MCP: `search \"TIT 3\"` → `get`), keep `content_id`, passage range, HTML body.\n2. Convert each note's HTML to plain USFM text; group by anchoring verse; write one `at BKK c:v` rule per verse.\n3. Start from a proven config (the John fixture), set `usechangesfile = True`, add the study-layout settings above, add `changes.txt` and a `FRTlocal.sfm` licence page to `config_files`.\n4. Submit; on `! Dimension too large`, bisect by chapter (`PTX_ONLY`-style filters) to find the page, then apply the layout table.\n5. Keep the payload and the changes file with the PDF — they are the provenance of every note on the page.\n"},{"path":"articles/stylesheet-format.md","uri":"klappy://canon/articles/stylesheet-format","title":"Stylesheet Format — ptxprint.sty and ptxprint-mods.sty","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","stylesheet","sty","marker-styles","supporting-files"],"stability":"working","what_it_answers":"What's a `.sty` file? When does the agent edit one? How do marker-style declarations work?","sections":[{"slug":"what-sty-files-are","heading":"What `.sty` files are","start":22,"end":34},{"slug":"marker-style-declarations","heading":"Marker-style declarations","start":34,"end":56},{"slug":"common-properties","heading":"Common properties","start":56,"end":75},{"slug":"category-and-id-qualifier","heading":"Category-and-id qualifier","start":75,"end":94},{"slug":"when-to-edit-a-stylesheet","heading":"When to edit a stylesheet","start":94,"end":101},{"slug":"when-not-to-edit-a-stylesheet","heading":"When NOT to edit a stylesheet","start":101,"end":107},{"slug":"layering-sty-and-mods-sty","heading":"Layering: `.sty` and `-mods.sty`","start":107,"end":120},{"slug":"stable-order-don-t-reorder","heading":"Stable order: don't reorder","start":120,"end":124},{"slug":"adding-a-custom-marker","heading":"Adding a custom marker","start":124,"end":137},{"slug":"including-in-the-payload","heading":"Including in the payload","start":137,"end":149},{"slug":"worked-example-cover-styling","heading":"Worked example: cover styling","start":149,"end":180}],"body":"---\ntitle: \"Stylesheet Format — ptxprint.sty and ptxprint-mods.sty\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"stylesheet\", \"sty\", \"marker-styles\", \"supporting-files\"]\nderives_from: \"canon/governance/headless-operations.md (Part 6 ptxprint.sty subsection)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Stylesheet Format\n\n> **What this answers.** What's a `.sty` file? When does the agent edit one? How do marker-style declarations work?\n>\n> **Related articles.** `klappy://canon/articles/usfm-markers-headless` · `klappy://canon/articles/payload-construction`\n\n---\n\n## What `.sty` files are\n\nUSFM stylesheets — text files that declare how PTXprint should render each USFM marker. Three locations:\n\n```\nshared/ptxprint/ptxprint.sty                 project-wide (often blank or minimal)\nshared/ptxprint/<config>/ptxprint.sty        config-level overrides\nshared/ptxprint/<config>/ptxprint-mods.sty   additional modifications layered on top\n```\n\nEach is included in the payload's `config_files` map at its respective path.\n\n## Marker-style declarations\n\nThe basic form:\n\n```\n\\Marker mt1\n\\Color 333333\n\\FontSize 1.4\n\\Bold\n\n\\Marker p\n\\FirstLineIndent 0.125\n\\SpaceBefore 0\n\\SpaceAfter 0\n\n\\Marker f\n\\TextProperties footnote\n\\FontSize 0.85\n```\n\nEach `\\Marker <name>` block declares properties for one USFM marker. Properties depend on the marker type (paragraph, character, footnote, etc.).\n\n## Common properties\n\n| Property | Type | Example | Meaning |\n|---|---|---|---|\n| `\\Color` | hex | `\\Color 333333` | RGB color (no `#` prefix) |\n| `\\FontSize` | factor | `\\FontSize 1.4` | Multiplier of body font size |\n| `\\Bold` | flag | `\\Bold` | Bold weight |\n| `\\Italic` | flag | `\\Italic` | Italic style |\n| `\\Underline` | flag | `\\Underline` | Underline |\n| `\\Justification` | enum | `\\Justification center` | `left` / `center` / `right` / `both` |\n| `\\FirstLineIndent` | length | `\\FirstLineIndent 0.125` | First-line indent (often inches) |\n| `\\SpaceBefore` | length | `\\SpaceBefore 0.5` | Vertical space before |\n| `\\SpaceAfter` | length | `\\SpaceAfter 0` | Vertical space after |\n| `\\LeftMargin` | length | `\\LeftMargin 0.5` | Left margin |\n| `\\RightMargin` | length | `\\RightMargin 0` | Right margin |\n| `\\Endmarker` | name | `\\Endmarker f*` | Closing marker for character styles |\n\nThe full property set is documented in PTXprint's stylesheet reference. For the agent, the listed properties cover the most common edits.\n\n## Category-and-id qualifier\n\nThe same marker can style differently in different contexts using a category prefix:\n\n```\n\\Marker mt1\n\\Color 333333\n\\FontSize 1.4\n\\Bold\n\n\\Marker cat:coverfront|mt1\n\\Color FFFFFF\n\\FontSize 2.0\n```\n\nThe first block applies to `\\mt1` everywhere. The second overrides `\\mt1` only when it appears in the `coverfront` category (e.g., inside a `\\periph Front Cover|id=\"coverfront\"` block).\n\nThis pattern lets a cover use larger, white titles without affecting body-text titles.\n\n## When to edit a stylesheet\n\n- The user needs styling beyond what the GUI Styles tab exposes.\n- Defining ornament categories (e.g., a custom rule pattern for chapter starts).\n- Style overrides for specific peripheral books or cover sections.\n- Declaring custom markers added via `changes.txt` or in source USFM.\n\n## When NOT to edit a stylesheet\n\n- The user is in the GUI — let them use the Style Editor (preserves UI integration).\n- The change is generic styling that the GUI Styles tab covers — modify `ptxprint.cfg` content instead.\n- Agent is uncertain about properties — verify against an existing `.sty` file or canon's marker reference before writing.\n\n## Layering: `.sty` and `-mods.sty`\n\nPTXprint reads `.sty` files in order:\n\n1. Standard library defaults (built into PTXprint)\n2. `shared/ptxprint/ptxprint.sty` (project-wide)\n3. `shared/ptxprint/<config>/ptxprint.sty` (config-level)\n4. `shared/ptxprint/<config>/ptxprint-mods.sty` (additional mods)\n\nLater layers override earlier ones for matching marker declarations.\n\nThe `-mods.sty` file is typically used for layered modifications the user wants to keep separate from the main config-level stylesheet — e.g., one-off experiments that the user can remove cleanly.\n\n## Stable order: don't reorder\n\nOlder PTXprint versions reordered marker declarations on save (the GUI Style Editor would re-sort). The agent should preserve order when reading and including existing stylesheets — diffs against earlier versions get confusing if order changes.\n\n## Adding a custom marker\n\nWhen the agent (or `changes.txt`) introduces a custom USFM marker not in the standard library, declare it:\n\n```\n\\Marker mycustom\n\\TextProperties paragraph\n\\FontSize 1.0\n\\Bold\n```\n\nWithout a declaration, PTXprint logs `! Undefined control sequence \\mycustom` and the run fails hard.\n\n## Including in the payload\n\n```json\n\"config_files\": {\n  \"shared/ptxprint/Default/ptxprint.cfg\": \"...\",\n  \"shared/ptxprint/Default/ptxprint.sty\": \"\\\\Marker mt1\\n\\\\Color 333333\\n\\\\FontSize 1.4\\n\\\\Bold\\n\\n\\\\Marker p\\n\\\\FirstLineIndent 0.125\\n\",\n  \"shared/ptxprint/Default/ptxprint-mods.sty\": \"\\\\Marker s1\\n\\\\FontSize 1.1\\n\\\\Color 666666\\n\"\n}\n```\n\nIf `ptxprint.sty` already exists in project state, read and include it; modify only what's changing. Don't replace established declarations the user hasn't asked to change.\n\n## Worked example: cover styling\n\n```\n% Standard mt1 in body\n\\Marker mt1\n\\Color 000000\n\\FontSize 1.6\n\\Bold\n\\SpaceBefore 1.0\n\\SpaceAfter 0.3\n\n% Override mt1 on the front cover (white text on dark background)\n\\Marker cat:coverfront|mt1\n\\Color FFFFFF\n\\FontSize 2.4\n\\Bold\n\\SpaceBefore 2.0\n\n% Override mt1 on the back cover (smaller, less prominent)\n\\Marker cat:coverback|mt1\n\\Color FFFFFF\n\\FontSize 1.2\n\\Bold\n```\n\nThree styles for one marker, selected by category. The cover periphs in `FRTlocal.sfm` set the category context.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/usfm-markers-headless` for the marker reference.*\n"},{"path":"articles/usfm-markers-headless.md","uri":"klappy://canon/articles/usfm-markers-headless","title":"USFM Markers in Headless Context","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","usfm","markers","book-codes","z-extensions"],"stability":"working","what_it_answers":"What are the USFM book codes? Which markers will the agent encounter in source content and supporting files? What are the PTXprint-specific `\\z*` extensions?","sections":[{"slug":"usfm-book-codes","heading":"USFM book codes","start":22,"end":73},{"slug":"building-the-books-field-of-the-payload","heading":"Building the `books` field of the payload","start":73,"end":85},{"slug":"standard-usfm-markers-the-agent-will-encounter","heading":"Standard USFM markers the agent will encounter","start":85,"end":113},{"slug":"ptxprint-extensions-the-z-markers","heading":"PTXprint extensions: the `\\z*` markers","start":113,"end":180},{"slug":"tex-hooks-in-mods-tex","heading":"TeX hooks (in `*-mods.tex`)","start":180,"end":193},{"slug":"cover-periphs","heading":"Cover periphs","start":193,"end":213},{"slug":"common-patterns-the-agent-will-write-or-modify","heading":"Common patterns the agent will write or modify","start":213,"end":246}],"body":"---\ntitle: \"USFM Markers in Headless Context\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"usfm\", \"markers\", \"book-codes\", \"z-extensions\"]\nderives_from: \"canon/governance/headless-operations.md (Part 7 USFM in Headless Context)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# USFM Markers in Headless Context\n\n> **What this answers.** What are the USFM book codes? Which markers will the agent encounter in source content and supporting files? What are the PTXprint-specific `\\z*` extensions?\n>\n> **Related articles.** `klappy://canon/articles/changes-txt-format` · `klappy://canon/articles/payload-construction`\n\n---\n\n## USFM book codes\n\nThree-letter codes for canonical books plus peripherals.\n\n### Canonical (subset shown; standard 66)\n\n| Code | Book | Code | Book |\n|---|---|---|---|\n| GEN | Genesis | MAT | Matthew |\n| EXO | Exodus | MRK | Mark |\n| LEV | Leviticus | LUK | Luke |\n| NUM | Numbers | JHN | John |\n| DEU | Deuteronomy | ACT | Acts |\n| JOS | Joshua | ROM | Romans |\n| JDG | Judges | 1CO | 1 Corinthians |\n| RUT | Ruth | 2CO | 2 Corinthians |\n| 1SA | 1 Samuel | GAL | Galatians |\n| 2SA | 2 Samuel | EPH | Ephesians |\n| ... | ... | PHP | Philippians |\n| PSA | Psalms | COL | Colossians |\n| PRO | Proverbs | 1TH | 1 Thessalonians |\n| ECC | Ecclesiastes | 2TH | 2 Thessalonians |\n| SNG | Song of Solomon | 1TI | 1 Timothy |\n| ISA | Isaiah | 2TI | 2 Timothy |\n| JER | Jeremiah | TIT | Titus |\n| ... | ... | PHM | Philemon |\n| MAL | Malachi | HEB | Hebrews |\n|   |   | JAS | James |\n|   |   | 1PE | 1 Peter |\n|   |   | 2PE | 2 Peter |\n|   |   | 1JN | 1 John |\n|   |   | 2JN | 2 John |\n|   |   | 3JN | 3 John |\n|   |   | JUD | Jude |\n|   |   | REV | Revelation |\n\nFor the full canonical list, including OT books not shown above, refer to USFM 3.0 documentation.\n\n### Peripherals (used by PTXprint)\n\n| Code | Book |\n|---|---|\n| FRT | Front matter |\n| BAK | Back matter |\n| GLO | Glossary |\n| TDX | Topical index |\n| NDX | Names index |\n| CNC | Concordance |\n| XXG | Generic appendix |\n| XXS | Strong's index (auto-generated) |\n\n## Building the `books` field of the payload\n\nThe payload's `books` field is an array of USFM codes:\n\n```json\n\"books\": [\"JHN\"]                                                // single\n\"books\": [\"MAT\", \"MRK\", \"LUK\", \"JHN\"]                          // gospels\n\"books\": [\"MAT\", \"MRK\", \"LUK\", \"JHN\", \"ACT\", \"ROM\", ..., \"REV\"]  // full NT\n```\n\nThe worker translates this to PTXprint's `-b \"MAT MRK LUK JHN\"` argument. PTXprint also accepts ranges (`MAT-JHN`) but explicit codes are clearer.\n\n## Standard USFM markers the agent will encounter\n\nThese are the core USFM 3.0 markers most relevant to typesetting. Used in source files, in `changes.txt` patterns, and in `FRTlocal.sfm` content.\n\n| Marker | Purpose |\n|---|---|\n| `\\id <CODE>` | Book identifier (first line of every book) |\n| `\\h <name>` | Running header text for the book |\n| `\\toc1` / `\\toc2` / `\\toc3` | ToC fields (long, short, abbreviated) |\n| `\\mt1` / `\\mt2` / `\\mt3` | Main title hierarchy |\n| `\\c <num>` | Chapter |\n| `\\v <num>` | Verse |\n| `\\p` | Paragraph |\n| `\\nb` | No-break paragraph (continuation) |\n| `\\m` | Margin paragraph (no first-line indent) |\n| `\\q1` / `\\q2` / `\\q3` | Poetry levels |\n| `\\s` / `\\s1` / `\\s2` | Section headings |\n| `\\r` | Parallel passage reference |\n| `\\f ... \\f*` | Footnote |\n| `\\x ... \\x*` | Cross-reference |\n| `\\fig ...\\fig*` | Figure/picture (legacy; PTXprint prefers piclist) |\n| `\\w word\\|target\\w*` | Glossary word reference |\n| `\\ip` | Introduction paragraph |\n| `\\io1` / `\\io2` | Introduction outline |\n| `\\b` | Blank line |\n\nFor complete USFM specification, refer to ubsicap.github.io/usfm.\n\n## PTXprint extensions: the `\\z*` markers\n\nThese are NOT standard USFM. They are PTXprint-specific extensions, recognized only by PTXprint's typesetting pipeline.\n\n| Marker | Purpose | Example |\n|---|---|---|\n| `\\zrule` | Decorative rule | `\\zrule\\|cat=\"ornaments3\" width=\".5\" align=\"c\" thick=\"8pt\"\\*` |\n| `\\zqrcode` | QR code | `\\zqrcode\\|pgpos=\"cr\" size=\"1.2cm\" data=\"https://...\"\\*` |\n| `\\zccimg` | Creative Commons logo | `\\zccimg by-nd\\|size=\"col\" pgpos=\"p\" scale=\"0.20\"\\*` |\n| `\\zvar` | Variable substitution from `[vars]` cfg section | `\\zvar\\|booktitle\\*` |\n| `\\zbl` | Blank lines | `\\zbl\\|3\\*` |\n| `\\zgap` | Vertical gap | `\\zgap\\|1.3cm\\*` |\n| `\\ztoc` | Generate Table of Contents | `\\ztoc\\|main\\*` |\n| `\\zglot` | Diglot side selector | `\\zglot\\|R\\*` |\n| `\\zthumbtab` | Thumb tab definition | `\\zthumbtab bookname` |\n| `\\zifvarset` | Conditional content (gate on `[vars]` value) | `\\zifvarset\\|var=\"varname\"\\*` |\n| `\\zlabel` / `\\zpage` / `\\zref` | Cross-references in front matter | `\\zref\\|id=\"uniquelabel\" show=\"b_c:v\"\\*` |\n| `\\zcopyright` / `\\zlicense` | Copyright/license blocks (no `\\|` arg, no closing `\\*`) | `\\zcopyright` |\n| `\\zimagecopyrights[LANG]` | Image copyright block in language | `\\zimagecopyrights[en]` |\n| `\\nopagenums` / `\\dopagenums` / `\\resetpagenums` | Page numbering control | `\\resetpagenums -1` |\n\n### `\\ztoc` parameters\n\n| Parameter | Effect |\n|---|---|\n| `\\|main\\*` | Default ToC of the publication's main books |\n| `\\|nt\\*` | NT books only |\n| `\\|ot\\*` | OT books only |\n| `\\|sortc\\*` | Sort by `\\toc1` short title alphabetically |\n| `\\|sorta\\*` | Sort alphabetically by `\\toc1` long title |\n\nUsed inside `FRTlocal.sfm`:\n\n```\n\\ip \\ztoc|main\\*\n```\n\n### `\\zvar` and the `[vars]` cfg section\n\nThe cfg's `[vars]` section defines variables the agent can reference from USFM:\n\n```ini\n[vars]\nbooktitle = New Testament\nimprint_year = 2026\n```\n\nIn `FRTlocal.sfm`:\n\n```\n\\mt1 \\zvar|booktitle\\*\n\\ip Published \\zvar|imprint_year\\*\n```\n\nPTXprint substitutes the values at typeset time.\n\n### `\\zglot` for diglot column selection\n\nIn a diglot config, the same source USFM can mark which content goes in which column:\n\n```\n\\zglot|L\\* This text goes in the left column.\n\\zglot|R\\* This text goes in the right column.\n```\n\nFor most diglots, the L/R selection is implicit (each column has its own source project), but `\\zglot` is available for inline overrides.\n\n## TeX hooks (in `*-mods.tex`)\n\nFor low-level layout control:\n\n| Hook | Pattern | Purpose |\n|---|---|---|\n| `\\sethook{pos}{mrkr}{code}` | `pos` = `start`/`end` | Run code at marker boundaries |\n| `\\setcvhook{ref}{code}` | `ref` = `BOOK c.v` | Run code at a chapter:verse |\n| `\\setbookhook{pos}{book}{code}` | `pos` = `start`/`end`/`final` | Run code at book boundaries |\n| `\\setbetweenhook{m1}{m2}{code}` | | Run code between consecutive markers |\n\nThe deck lists dozens of snippet patterns using these hooks. **The v1.2 spec defers `*-mods.tex` editing** as too risky for automated use without deeper validation. Respect that boundary unless the user explicitly asks and accepts the risk.\n\n## Cover periphs\n\nInside `FRTlocal.sfm` content (or as separate periph definitions):\n\n```\n\\periph Front Cover|id=\"coverfront\"\n... content ...\n\n\\periph Back Cover|id=\"coverback\"\n... content ...\n\n\\periph Spine|id=\"coverspine\"\n... content ...\n\n\\periph Whole Cover|id=\"coverwhole\"\n... content ...\n```\n\nEach carries its own content. The Cover Wizard generates these in the GUI; in headless, the agent constructs them as USFM directly in the payload's `FRTlocal.sfm` content.\n\n## Common patterns the agent will write or modify\n\n### Add a ToC to FRTlocal.sfm\n\n```\n\\id FRT\n\\mt1 New Testament\n\\ip \\ztoc|main\\*\n```\n\n### Add a copyright block\n\n```\n\\zcopyright\n\\zlicense\n```\n\n### Insert a vertical gap before a verse\n\nIn `changes.txt`:\n\n```\nat MAT 5:1 \"\\\\v 1\" > \"\\\\zgap|2cm\\\\*\\\\v 1\"\n```\n\n### Variable substitution in headers\n\nIn cfg's `[vars]` section: `imprintyear = 2026`. In header text setting: `New Testament \\zvar|imprintyear\\*`.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`. See also: `klappy://canon/articles/changes-txt-format` (USFM markers in regex patterns) and `klappy://canon/articles/frt-local-and-cover-periphs` (front matter content).*\n"},{"path":"articles/using-custom-fonts.md","uri":"klappy://canon/articles/using-custom-fonts","title":"Using Custom Fonts — Step-by-Step Recipe for Agents","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","fonts","custom-fonts","font-swap","font-family","font-recipe","level-2","level-3","config_files","fontregular","fontbold","fontitalic","fontbolditalic","cfg-document-section","sha256","google-fonts","lff","sil-ofl","ofl","comic-neue","im-fell-dw-pica","howto","recipe","quickstart"],"stability":"working","what_it_answers":"A user wants the rendered Bible in a custom font — Gentium, Andika, a different language-specific font, or just an aesthetic choice. The agent has font URLs and sha256 hashes in hand. What payload does the agent send?","sections":[{"slug":"tl-dr","heading":"TL;DR","start":56,"end":67},{"slug":"the-four-cfg-keys-you-must-set","heading":"The four cfg keys you must set","start":67,"end":85},{"slug":"the-recipe-7-steps","heading":"The recipe (7 steps)","start":85,"end":201},{"slug":"the-level-1-define-trap","heading":"The Level-1 `define` trap","start":201,"end":232},{"slug":"worked-example-1-comic-neue-comic-sans-surrogate","heading":"Worked example 1 — Comic Neue (Comic Sans surrogate)","start":232,"end":283},{"slug":"worked-example-2-im-fell-dw-pica-textured-historical-surrogate","heading":"Worked example 2 — IM FELL DW Pica (textured/historical surrogate)","start":283,"end":315},{"slug":"handling-missing-variants-quick-guide","heading":"Handling missing variants — quick guide","start":315,"end":327},{"slug":"cosmetic-warnings-to-expect","heading":"Cosmetic warnings to expect","start":327,"end":339},{"slug":"pitfalls","heading":"Pitfalls","start":339,"end":350},{"slug":"sourcing-patterns-a-quick-directory","heading":"Sourcing patterns — a quick directory","start":350,"end":363},{"slug":"see-also","heading":"See also","start":363,"end":376}],"body":"---\ntitle: \"Using Custom Fonts — Step-by-Step Recipe for Agents\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - agent-kb\n  - v1.2-aligned\n  - non-canonical\n  - fonts\n  - custom-fonts\n  - font-swap\n  - font-family\n  - font-recipe\n  - level-2\n  - level-3\n  - config_files\n  - fontregular\n  - fontbold\n  - fontitalic\n  - fontbolditalic\n  - cfg-document-section\n  - sha256\n  - google-fonts\n  - lff\n  - sil-ofl\n  - ofl\n  - comic-neue\n  - im-fell-dw-pica\n  - howto\n  - recipe\n  - quickstart\ncompanion_to:\n  - canon/articles/font-resolution.md\n  - canon/articles/progressive-customization.md\n  - canon/articles/bundled-default-cfg.md\n  - canon/articles/payload-construction.md\n  - canon/articles/agent-faq-customization.md\ncanonical_status: non_canonical\ndate: 2026-04-30\nstatus: draft_for_operator_review\n---\n\n# Using Custom Fonts — Step-by-Step Recipe for Agents\n\n> 🟢 **Live-validated 2026-04-30.** This recipe was end-to-end tested against the deployed `https://ptxprint-mcp.klappy.workers.dev` worker on BSB James (`60JASBSB.usfm`) with two distinct font families: **Comic Neue** (6-variant OFL family from Google Fonts) and **IM FELL DW Pica** (2-variant OFL family from Google Fonts). Both produced clean A5 PDF/X-4 output (5 pages each, 0 errors, 0 overfull boxes, jobs completed in 4.5 s and 6.3 s respectively). Embedded font subsets were verified via `pdffonts`. **The pattern in this article is what actually works on the live server.** If you find guidance elsewhere in canon that contradicts the recipe below for current-phase behavior, this article wins.\n>\n> **What this answers.** A user wants the rendered Bible in a custom font — Gentium, Andika, a different language-specific font, or just an aesthetic choice. The agent has font URLs and sha256 hashes in hand. What payload does the agent send?\n>\n> **What this is not.** This is not the contract reference for how fonts reach the worker (that's `klappy://canon/articles/font-resolution`). This is the practical recipe — copy-paste commands, the Level-1 trap to avoid, and two worked examples drawn from real runs.\n\n---\n\n## TL;DR\n\nTo swap fonts on the bundled default cfg you need two payload fields:\n\n1. **`fonts: [...]`** — one entry per `.ttf`, each with `family_id`, `version`, `filename`, `url`, `sha256`. (Level 3 on the ladder.)\n2. **`config_files: { \"shared/ptxprint/Default/ptxprint.cfg\": \"<full cfg text>\" }`** — start from the bundled default cfg verbatim, then override the four font keys in `[document]`. (Level 2 on the ladder.)\n\nYou **cannot** swap fonts via `define` alone. The `define` map turns into `-D key=value` CLI flags, and font values like `Comic Neue||false|false|` contain spaces and pipe characters that the flag parser silently mangles, producing a 0-exit-code with no PDF. **Level 2 cfg replacement is the right tool here.**\n\n---\n\n## The four cfg keys you must set\n\nInside `[document]` of `ptxprint.cfg`:\n\n```ini\n[document]\nfontregular    = <Family Name>||false|false|\nfontbold       = <Family Name>|Bold|false|false|\nfontitalic     = <Family Name>|Italic|false|false|\nfontbolditalic = <Family Name>|Bold Italic|false|false|\n```\n\nThe pipe-delimited value is `family|subfamily|<feature flags>`. The first segment is the **display family name as it appears in the TTF's `name` table (NameID 1)**, not your `family_id` slug. Subfamily values are typically `Regular` (empty), `Bold`, `Italic`, `Bold Italic`. Get them from the TTF directly — see step 4 of the recipe below.\n\nIf a font ships fewer variants than the four keys want, see the \"Handling missing variants\" section below.\n\n---\n\n## The recipe (7 steps)\n\n### 1. Pick a font you can legally distribute\n\nThe worker fetches font binaries by URL on every cache-miss job. Whatever URL you provide must serve a font you have the right to redistribute (or that's already publicly licensed for redistribution). **Fonts that are NOT safe:** Comic Sans MS (Microsoft proprietary), Papyrus (Linotype/Monotype proprietary), Times New Roman (Monotype proprietary), and most foundry fonts. **Fonts that ARE safe:** SIL OFL fonts (Charis SIL, Andika, Gentium Plus, Doulos SIL), Google Fonts under OFL or Apache 2.0, and any font whose license explicitly permits redistribution. Apple System Font, Helvetica from macOS, Arial — all proprietary.\n\nIf a user asks for a proprietary font, either (a) the user has licensed it and is hosting the binary on a URL the worker can reach, or (b) the agent suggests an OFL lookalike. Comic Neue → Comic Sans, Liberation Serif → Times, etc.\n\n### 2. Find a stable URL\n\nBest sources, in rough order of preference:\n\n- **SIL Language Font Finder** (`lff.api.languagetechnology.org`) — versioned, content-addressed, the canonical source for SIL fonts. Already documented in `klappy://canon/articles/font-resolution`.\n- **`raw.githubusercontent.com/google/fonts/main/ofl/<family>/<file>.ttf`** — Google Fonts has a strict no-rewrite policy on the `main` branch; URLs are effectively immutable.\n- **Upstream foundry repo `raw.githubusercontent.com/<owner>/<repo>/<branch>/<path>.ttf`** — fine if the repo is dormant or release-tagged.\n- **User-controlled HTTPS** — any URL the worker can reach without auth. The worker has no credential store, so URLs that 401 will fail.\n\nAvoid presigned URLs that expire (the worker may not fetch immediately on a cache miss), CDNs that serve different bytes from different edges, or URLs that require auth.\n\n### 3. Download and hash each TTF\n\n```bash\ncurl -sSL -o ComicNeue-Regular.ttf \\\n  https://raw.githubusercontent.com/google/fonts/main/ofl/comicneue/ComicNeue-Regular.ttf\nsha256sum ComicNeue-Regular.ttf\n```\n\nThe sha256 goes in the payload as the `sha256` field on each `fonts[]` entry. The worker re-fetches and verifies on every cache-miss job; sha256 mismatch is a hard failure with a clear error (`sha256 mismatch on fonts[N]: <filename>`).\n\n### 4. Extract the family/subfamily names from each TTF\n\nThe values you put in `fontregular`, `fontbold`, etc. must match what's in the TTF's `name` table. Get them with `fonttools`:\n\n```python\nfrom fontTools.ttLib import TTFont\ntt = TTFont(\"ComicNeue-Regular.ttf\", lazy=True)\nnt = tt['name']\nprint(\"family:   \", nt.getDebugName(1))   # NameID 1: family name\nprint(\"subfamily:\", nt.getDebugName(2))   # NameID 2: subfamily / style\nprint(\"fullname: \", nt.getDebugName(4))   # NameID 4: full font name\ntt.close()\n```\n\nFor a typical 4-style family you'll get something like:\n\n```\nComicNeue-Regular.ttf     family: Comic Neue   subfamily: Regular\nComicNeue-Bold.ttf        family: Comic Neue   subfamily: Bold\nComicNeue-Italic.ttf      family: Comic Neue   subfamily: Italic\nComicNeue-BoldItalic.ttf  family: Comic Neue   subfamily: Bold Italic\n```\n\nUse the family value verbatim in the cfg keys. Don't lowercase, don't slugify. PTXprint matches via fontconfig at typeset time and is case-sensitive.\n\n### 5. Build the cfg by starting from the bundle\n\nThe container ships with a publication-quality default cfg. Don't write a cfg from scratch; **start from the bundle and modify only the four font keys**. The bundle is documented at `klappy://canon/articles/bundled-default-cfg` — its full INI block lives at the bottom of that article and is the source of truth.\n\nA simple sed-style modification works:\n\n```python\nimport re\ntext = open(\"canon/articles/bundled-default-cfg.md\").read()\nini_start = text.find(\"```ini\") + 7\nini_end   = text.find(\"```\", ini_start)\ncfg = text[ini_start:ini_end]\n\ndef sub(rx, replacement, s):\n    return re.sub(rx, replacement, s, flags=re.MULTILINE)\n\ncfg = sub(r\"^fontregular\\s*=.*$\",    \"fontregular = Comic Neue||false|false|\", cfg)\ncfg = sub(r\"^fontbold\\s*=.*$\",       \"fontbold = Comic Neue|Bold|false|false|\", cfg)\ncfg = sub(r\"^fontitalic\\s*=.*$\",     \"fontitalic = Comic Neue|Italic|false|false|\", cfg)\ncfg = sub(r\"^fontbolditalic\\s*=.*$\", \"fontbolditalic = Comic Neue|Bold Italic|false|false|\", cfg)\n```\n\nIf the user is rendering a book other than John (the bundle's default), also override `[project] book = <CODE>` to keep PDF metadata coherent. Cosmetic but cleaner.\n\n### 6. Assemble the payload\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"BSB\",\n  \"config_name\": \"Default\",\n  \"books\": [\"JAS\"],\n  \"config_files\": {\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"<full cfg from step 5, including your font overrides>\"\n  },\n  \"sources\": [\n    {\n      \"book\": \"JAS\",\n      \"filename\": \"60JASBSB.usfm\",\n      \"url\": \"https://...\",\n      \"sha256\": \"<sha256 of usfm>\"\n    }\n  ],\n  \"fonts\": [\n    {\n      \"family_id\": \"comicneue\",\n      \"version\": \"2.5\",\n      \"filename\": \"ComicNeue-Regular.ttf\",\n      \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/comicneue/ComicNeue-Regular.ttf\",\n      \"sha256\": \"<from step 3>\"\n    },\n    \"...one entry per TTF, all variants the cfg references...\"\n  ]\n}\n```\n\n### 7. Submit, poll, fetch the PDF\n\nSame as any other job — `submit_typeset` returns a `job_id` and a `predicted_pdf_url`; `get_job_status` polls until `state ∈ {succeeded, failed, cancelled}`. On success the worker writes the PDF to R2 at the predicted URL. The full flow is documented in `klappy://canon/articles/payload-construction`.\n\n---\n\n## The Level-1 `define` trap\n\nIt is tempting to think a font swap is a trivial Level-1 override:\n\n```json\n{\n  \"define\": {\n    \"fontregular\": \"Comic Neue||false|false|\"\n  }\n}\n```\n\nThis **does not work** for font values. It produces a hard failure that looks like success at exit-code level:\n\n```\n[container diagnostic] PTXprint exited 0 with no PDF and no log file.\n[container diagnostic] argv: ptxprint -P BSB -b JAS ... -D fontregular=Comic Neue||false|false| ...\n[container diagnostic] stderr_tail: (empty)\nerrors: [\"PTXprint produced no output (silent exit). Likely cause: missing config_files or invalid project layout.\"]\n```\n\n(That diagnostic is from a real failed run on 2026-04-30 — job `833fd280…`.)\n\nThe cause: `define` keys are passed to the PTXprint CLI as `-D key=value` flags. Values containing **spaces** (`Comic Neue`) or **pipe characters** (`||false|false|`) — both of which are required for any font value — get tokenized incorrectly by the flag parser. PTXprint then sees malformed input, silently abandons the typeset, and exits 0 with no log file. The worker correctly reports this as a hard failure, but the agent's diagnostic point-of-view (\"I just told it to use Comic Neue, what's wrong?\") doesn't include the shell-tokenization step that broke the value.\n\n**The bypass is Level 2.** The cfg goes through the payload as JSON, not the CLI, so spaces and pipes inside the cfg value are preserved verbatim. The worker writes the cfg to disk; PTXprint reads the cfg from disk; pipes and spaces survive intact.\n\n(There is also a deeper issue: the `define` keys are nominally PTXprint **widget identifiers** — the GUI control names, like `s_linespacing`, not the cfg keys themselves. The widget-ID-to-cfg-key mapping is partially documented but incomplete. For values that are simple scalars and whose cfg key happens to coincide with a widget ID, `define` works. For font values, even if the widget mapping were perfect, the shell-tokenization issue would still bite. Use Level 2.)\n\n---\n\n## Worked example 1 — Comic Neue (Comic Sans surrogate)\n\nValidated 2026-04-30. Job `7b434f262aa76256…` succeeded in 4.5 s producing a 5-page A5 PDF/X-4 with three Comic Neue subsets embedded (Regular, Bold, Italic).\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"BSB\",\n  \"config_name\": \"Default\",\n  \"books\": [\"JAS\"],\n  \"config_files\": {\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"<bundled default with these four lines replaced>\"\n  },\n  \"sources\": [\n    {\n      \"book\": \"JAS\",\n      \"filename\": \"60JASBSB.usfm\",\n      \"url\": \"https://raw.githubusercontent.com/usfm-bible/examples.bsb/main/60JASBSB.usfm\",\n      \"sha256\": \"59cc6f5e7b2404926932c415f71eb6a376bcbb4423ca1b2cda0e00192df47a12\"\n    }\n  ],\n  \"fonts\": [\n    { \"family_id\": \"comicneue\", \"version\": \"2.5\", \"filename\": \"ComicNeue-Regular.ttf\",\n      \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/comicneue/ComicNeue-Regular.ttf\",\n      \"sha256\": \"a0ee5a37c8b27c4db0700137d928598b1e23b0089e1546a8961909176b779360\" },\n    { \"family_id\": \"comicneue\", \"version\": \"2.5\", \"filename\": \"ComicNeue-Bold.ttf\",\n      \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/comicneue/ComicNeue-Bold.ttf\",\n      \"sha256\": \"3e7e5fccfd7e0788f317b43312151c1bd5cf058c9697a8d83eac3939050bd61e\" },\n    { \"family_id\": \"comicneue\", \"version\": \"2.5\", \"filename\": \"ComicNeue-Italic.ttf\",\n      \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/comicneue/ComicNeue-Italic.ttf\",\n      \"sha256\": \"e06bfd1552f5c9464c5665733ffd69239b0593885dbb9e059688a5900f78cf98\" },\n    { \"family_id\": \"comicneue\", \"version\": \"2.5\", \"filename\": \"ComicNeue-BoldItalic.ttf\",\n      \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/comicneue/ComicNeue-BoldItalic.ttf\",\n      \"sha256\": \"5c312c2a2fa64eee82f3b87fcfab8f3b12a5e59b043124401d322eb323cfbf16\" }\n  ]\n}\n```\n\nThe four cfg key overrides for this run:\n\n```ini\nfontregular    = Comic Neue||false|false|\nfontbold       = Comic Neue|Bold|false|false|\nfontitalic     = Comic Neue|Italic|false|false|\nfontbolditalic = Comic Neue|Bold Italic|false|false|\n```\n\nOutput: BSB James, two-column A5, body in Comic Neue Regular, parallel-passage refs in Comic Neue Italic, drop cap on chapter 1, footnote callers `a, b, c, …`. Identical layout to the bundled-default render — only the typeface changed.\n\n---\n\n## Worked example 2 — IM FELL DW Pica (textured/historical surrogate)\n\nValidated 2026-04-30. Job `5ead41b366d6d17a…` succeeded in 6.3 s. Same 5-page A5 PDF/X-4, two embedded subsets (Roman + Italic).\n\nThis family illustrates **handling missing variants**. IM FELL DW Pica only ships Roman (Regular) and Italic — there is no native Bold and no Bold Italic in the family. PTXprint's cfg requires all four keys to be set; leaving them blank or omitting them produces unpredictable behavior. The pragmatic answer is to map missing weights to the closest available variant:\n\n```ini\nfontregular    = IM FELL DW Pica||false|false|\nfontbold       = IM FELL DW Pica||false|false|\nfontitalic     = IM FELL DW Pica|Italic|false|false|\nfontbolditalic = IM FELL DW Pica|Italic|false|false|\n```\n\nBold falls back to Roman; Bold Italic falls back to Italic. Visually this means bold passages don't appear bold, but the run completes cleanly and the PDF is valid. The alternative — letting PTXprint try to synthesize a faux-bold from Roman — is worse (smudgy artifacts) and not all XeTeX font setups support it.\n\nIf the user genuinely needs a bold weight, either (a) pick a different family that has one, or (b) source a Bold variant from a sibling family (e.g. the `imfellgreatprimer` family also has only Regular + Italic, but `imfelldoublepica` ships a slightly different aesthetic). There is no synthetic-bold escape hatch worth chasing.\n\nThe fonts payload:\n\n```json\n\"fonts\": [\n  { \"family_id\": \"imfelldwpica\", \"version\": \"2.000\", \"filename\": \"IMFePIrm28P.ttf\",\n    \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/imfelldwpica/IMFePIrm28P.ttf\",\n    \"sha256\": \"f65e54016dfab4222ba552cfb82260b14a7df6527cce66430f5f66022addb052\" },\n  { \"family_id\": \"imfelldwpica\", \"version\": \"2.000\", \"filename\": \"IMFePIit28P.ttf\",\n    \"url\": \"https://raw.githubusercontent.com/google/fonts/main/ofl/imfelldwpica/IMFePIit28P.ttf\",\n    \"sha256\": \"e09a00654b5dd266aee743c0c43d129c8404a2d5e0ebf27e5a0e472bd1900b8d\" }\n]\n```\n\n---\n\n## Handling missing variants — quick guide\n\n| Situation | What to set in cfg |\n|---|---|\n| Family has all four (Regular/Bold/Italic/Bold Italic) | Use each in its own slot — the canonical case. |\n| Family has only Regular + Italic | `fontbold` and `fontbolditalic` map to Regular and Italic respectively. |\n| Family has only Regular | All four keys point at the same variant. The render won't show weight or slant variation, but it will complete. |\n| Family has Light, Regular, Medium, Bold | Pick which weight is body. Common pattern: `fontregular = Family|Light` for a delicate look, or `fontregular = Family\\|\\|false\\|false\\|` for default Regular. |\n| Family has italics-only or bolds-only siblings (display fonts) | These are rarely appropriate as Bible body fonts. Use them only for headings via custom stylesheet. |\n\n---\n\n## Cosmetic warnings to expect\n\nThese show up in the log but don't fail the job:\n\n- **`Missing character: There is no Ê¼ (U+02BC) in font ...`** — many BSB and other modern Bible texts use U+02BC (Modifier Letter Apostrophe) for typographic apostrophes. Many display fonts (including Comic Neue and IM FELL DW Pica) don't include this glyph; the character drops silently from the rendered text. SIL Charis, Gentium Plus, and Andika cover U+02BC. If U+02BC matters for the user's edition, pick a font with the relevant Unicode coverage.\n- **`Underfill[A]: [N] ht=...pt, space=...pt`** — page didn't fill its column. The bundled default cfg is conservative on line-spacing and font factor; pages with light content (start of a book, end of a chapter) underfill. Cosmetic in v1; the canonical fix is `mode: \"autofill\"` (deferred — see `klappy://canon/articles/agent-faq-customization`).\n- **`IF CHECK mnote Failed. Entered at 2 != exit at 1`** — XeTeX margin-note rebalancing diagnostic. Cosmetic noise on first-pass typesetting; doesn't affect output.\n\nIf your `errors` array is empty and `failure_mode` is `success`, ignore log warnings of these classes.\n\n---\n\n## Pitfalls\n\n1. **Don't put font cfg keys in `define`.** Per the trap section above. Use `config_files` for any value containing spaces, pipes, or special chars. `define` is for simple scalar values only (booleans, integers, single-token strings).\n2. **Don't slugify the family name in the cfg.** `comicneue||false|false|` will silently fail to match; use `Comic Neue||false|false|` exactly as it appears in the TTF.\n3. **Don't omit variants from the `fonts` array.** Every variant referenced in the cfg must be in the array. If `fontbold = Comic Neue|Bold|...`, then `ComicNeue-Bold.ttf` must be in `fonts`. PTXprint won't synthesize a missing weight; the run will fail with a clearer error than the Level-1 trap (the log will say font not found).\n4. **Don't ship proprietary fonts via your URLs.** Comic Sans MS, Papyrus, Times New Roman, Arial, Helvetica — these are proprietary and you do not have the right to host them on a public URL. Either license + self-host (private), or use OFL lookalikes (Comic Neue, IM FELL DW Pica, Liberation Serif, Liberation Sans, Nimbus Sans).\n5. **Don't trust upstream URL stability without checking.** Re-derive sha256 if your job fails on `sha256 mismatch`. Google Fonts `main` is stable in practice; smaller upstream repos may rewrite history. Pin to release tags (`?ref=v2.5`) when available.\n6. **Don't include both `define: {fontregular: ...}` AND `config_files: { ptxprint.cfg: ...with fontregular set... }`.** The `config_files` value wins for the cfg key, but the `define` flag still gets passed to the CLI and may produce the silent-exit failure mode. Pick one path; for fonts, that's `config_files`.\n\n---\n\n## Sourcing patterns — a quick directory\n\n| Source | URL pattern | Stability | Tested |\n|---|---|---|---|\n| SIL Language Font Finder | `https://lff.api.languagetechnology.org/...` | Versioned, stable | Charis SIL (production) |\n| Google Fonts (OFL families) | `https://raw.githubusercontent.com/google/fonts/main/ofl/<family>/<file>.ttf` | `main` is no-rewrite policy | Comic Neue, IM FELL DW Pica (this article) |\n| Google Fonts (Apache families) | `https://raw.githubusercontent.com/google/fonts/main/apache/<family>/<file>.ttf` | Same policy | not yet tested |\n| Upstream foundry repos | `https://raw.githubusercontent.com/<owner>/<repo>/<branch>/...` | Varies — pin tags when possible | Comic Neue Angular (from `crozynski/comicneue`) |\n| User-controlled HTTPS | any | depends on host | Various — see `klappy://canon/articles/font-resolution` |\n| R2-staged fixtures | `https://ptxprint-mcp.klappy.workers.dev/r2/outputs/fixtures/fonts/...` | Worker-controlled, very stable | Gentium Plus 6.200 (session 11) |\n\n---\n\n## See also\n\n- `klappy://canon/articles/font-resolution` — the contract: how fonts reach the worker, sha256 verification semantics, what happens when a font is missing.\n- `klappy://canon/articles/progressive-customization` — the full Level 0–5 ladder. Fonts are Level 3; cfg replacement is Level 2.\n- `klappy://canon/articles/bundled-default-cfg` — the source of truth for the cfg you're starting from.\n- `klappy://canon/articles/payload-construction` — payload schema reference.\n- `klappy://canon/articles/agent-faq-customization` — FAQ on what's live vs deferred.\n- `klappy://canon/articles/settings-cookbook` — for non-font cfg overrides (line spacing, page size, hide xrefs, etc.) — many of these *do* work via `define` because their values are simple scalars.\n\n---\n\n*Created 2026-04-30 from two empirically-validated end-to-end runs against the live deploy. Status `draft_for_operator_review` because same-session review is not valid canon per `klappy://canon/principles/verification-requires-fresh-context` — a fresh-session cross-check is owed before promoting to `status: working`.*\n"},{"path":"articles/workflow-recipes.md","uri":"klappy://canon/articles/workflow-recipes","title":"Workflow Recipes — Common End-to-End Flows","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","recipes","workflows","playbooks"],"stability":"working","what_it_answers":"Common end-to-end flows the agent runs: draft a PDF, typeset an NT, set up a diglot, recover from a failure, lock down house style.","sections":[{"slug":"recipe-draft-pdf-for-review","heading":"Recipe: Draft PDF for review","start":22,"end":42},{"slug":"recipe-print-ready-new-testament","heading":"Recipe: Print-ready New Testament","start":42,"end":68},{"slug":"recipe-modify-a-single-setting-and-re-run","heading":"Recipe: Modify a single setting and re-run","start":68,"end":85},{"slug":"recipe-set-up-a-diglot-publication","heading":"Recipe: Set up a diglot publication","start":85,"end":105},{"slug":"recipe-lock-down-house-style-for-a-cluster","heading":"Recipe: Lock down house style for a cluster","start":105,"end":127},{"slug":"recipe-diagnose-pdf-was-produced-but-pictures-are-missing","heading":"Recipe: Diagnose \"PDF was produced but pictures are missing\"","start":127,"end":145},{"slug":"recipe-i-changed-it-but-nothing-happened","heading":"Recipe: \"I changed it but nothing happened\"","start":145,"end":166}],"body":"---\ntitle: \"Workflow Recipes — Common End-to-End Flows\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"agent-kb\", \"v1.2-aligned\", \"non-canonical\", \"recipes\", \"workflows\", \"playbooks\"]\nderives_from: \"canon/governance/headless-operations.md (Part 8 Workflow Recipes)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ncanonical_status: non_canonical\ndate: 2026-04-28\nstatus: draft\n---\n\n# Workflow Recipes\n\n> **What this answers.** Common end-to-end flows the agent runs: draft a PDF, typeset an NT, set up a diglot, recover from a failure, lock down house style.\n>\n> **Related articles.** `klappy://canon/articles/payload-construction` · `klappy://canon/articles/diagnostic-patterns` · `klappy://canon/articles/settings-cookbook`\n\n---\n\n## Recipe: Draft PDF for review\n\n**Context.** User has a project with USFM source. Wants to see what it looks like in print, fast.\n\n```\n1. List configurations (from project state). If none beyond Default, use Default.\n2. Ask: which book(s)? Suggest a single short book (JUD, 3JN, PHM) if user is unsure.\n3. Ask: any specific concerns? (Page size, paper, print or screen?)\n4. Construct payload:\n     project_id, config_name=\"Default\", books=[<book>],\n     mode=\"simple\",\n     define={\"output_format\": \"Screen-Quickest\"}\n   Submit via submit_typeset. Receive job_id.\n5. Poll get_job_status until state ∈ {succeeded, failed}.\n6. Read log_tail and errors[] for warnings.\n7. Surface pdf_url plus any non-trivial warnings.\n```\n\nRefinements: walk multi-config users through which to use; mention overfulls but don't block; run the seven-point picture checklist if pictures missing.\n\n## Recipe: Print-ready New Testament\n\n**Context.** User has a working draft and wants the full NT typeset for print.\n\n```\n1. Confirm: which configuration represents production layout?\n   If none, walk through \"create a new config\" first.\n2. Confirm books: standard NT is\n   \"MAT MRK LUK JHN ACT ROM 1CO 2CO GAL EPH PHP COL 1TH 2TH\n    1TI 2TI TIT PHM HEB JAS 1PE 2PE 1JN 2JN 3JN JUD REV\".\n3. Confirm output format:\n     Print-CMYK for colour, Print-Gray for B/W, Print-Spot for two-colour.\n4. Confirm page size and paper match printer's requirements.\n5. Construct payload at full quality:\n     mode=\"autofill\" if the team uses autofill, else \"simple\".\n     output_format set in define or in cfg.\n   Submit.\n6. Poll get_job_status with reasonable interval.\n   Autofill on an NT can take 30+ minutes; set timeout=1800 in payload.\n7. On completion: check failure_mode, page count (NT typically 250–500),\n   overfull_count, picture rendering.\n8. Surface pdf_url, page count, log_url, warnings.\n```\n\nMulti-pass typesetting plus autofill takes longer. Set `timeout: 1800` for an NT-scale autofill.\n\n## Recipe: Modify a single setting and re-run\n\n**Context.** User wants to change one thing in an existing config and see the result.\n\n```\n1. Read current value from project state (or working payload state if iterating).\n2. Surface: \"Current value is X. Change to Y?\" If unclear, ask.\n3. Check both override files for this key. If locked, surface and stop.\n4. Update working ptxprint.cfg content — modify project state or stage in memory.\n5. Construct new payload with updated config_files content. Submit.\n6. Poll for completion. Receive new pdf_url.\n7. Surface change visually (compare to previous pdf_url) or numerically\n   (page count delta, overfull count delta).\n```\n\nFor exploratory changes, prefer `define` over mutating `config_files[\"...ptxprint.cfg\"]` — preserves saved state.\n\n## Recipe: Set up a diglot publication\n\n**Context.** User has two projects (primary, secondary) and wants them side by side.\n\n```\n1. Verify both projects exist in project state (Claude Desktop FS, Git, etc.).\n2. Verify the secondary project has the books the user wants.\n3. Discuss merge strategy: chapter / paragraph / verse merge.\n4. Create a new configuration in the primary project (e.g. \"DiglotEN-FR\").\n5. Set [diglot_L] → primary project + config + column fraction.\n6. Set [diglot_R] → secondary project + config + column fraction.\n7. Address font sizing — different scripts may need different sizes.\n8. Submit a payload covering both projects' configs and a small range\n   (single chapter) to start.\n9. Diagnose alignment. If versification differs, address that next.\n10. Iterate by submitting new payloads with adjusted config_files.\n```\n\nDiglot is multi-axis. Don't promise a one-shot setup. Walk axes in order: project pair → merge strategy → fonts → fractions → versification → pictures → headers.\n\n## Recipe: Lock down house style for a cluster\n\n**Note:** this operates on project state (filesystem, Git, Paratext server), not on the typesetting MCP. The MCP only sees resulting payloads at typesetting time.\n\n**Context.** Cluster admin wants to enforce settings across multiple projects.\n\n```\n1. Confirm with admin which settings should be locked.\n2. Per setting, decide hard lock (no user change) or soft lock\n   (* prefix, user can change temporarily).\n3. Project-wide settings → write to shared/ptxprint/ptxprint_project.cfg\n   in project state.\n4. Config-specific → write to shared/ptxprint/<config>/ptxprint_override.cfg.\n5. Submit a payload that includes the override files; verify the settings\n   take effect.\n6. Regression check: try to override a locked setting via the payload's\n   define field; confirm the override file wins.\n7. Document for admin which keys are locked and where.\n```\n\nRepeat per project in the cluster. The admin propagates via Send/Receive (Paratext, Git, etc.) — not via the MCP.\n\n## Recipe: Diagnose \"PDF was produced but pictures are missing\"\n\n**Context.** Soft failure — `failure_mode: \"soft\"`, PDF exists, pictures missing.\n\nWalk the seven-point checklist. See `klappy://canon/articles/diagnostic-patterns` for the full version.\n\n```\n1. Pictures listed in the piclist?         → inspect config_files\n2. Picture files in the figures array?     → inspect payload\n3. Can PTXprint see them?                  → grep \"image not found\" in log\n4. Are all missing or just some?           → diff piclist vs log render count\n5. Anchor Refs valid?                      → verify against source USFM\n6. Pictures too large?                     → check piclist scale values\n7. Too many on one page?                   → spread across more verses\n```\n\nSurface the specific fix. Multiple issues → fix one at a time and re-submit.\n\n## Recipe: \"I changed it but nothing happened\"\n\n**Context.** User edited a setting (or agent did) and output didn't change.\n\n```\n1. Confirm cfg key was actually changed in the submitted payload.\n2. Check ptxprint_override.cfg in payload's config_files (and project state).\n3. Check ptxprint_project.cfg in payload's config_files (and project state).\n4. If neither override has the key, issue is elsewhere:\n   - Was the latest payload submitted? Check payload_hash.\n   - Was it a cache hit on a stale payload? Check the `cached` field.\n   - Is parent config's value being inherited and child not overriding?\n5. If override file has the key, surface and ask whether to edit override.\n```\n\nMost common cause: an override file silently winning. See `klappy://canon/articles/config-inheritance-and-overrides`.\n\n---\n\n*This article is part of the v1.2-aligned KB split from `canon/governance/headless-operations.md`.*\n"},{"path":"governance/headless-operations.md","uri":"klappy://canon/governance/headless-operations","title":"PTXprint Headless Operations — Knowledge Base for AI Agents and Assistants (Overview)","audience":"agent","tags":["ptxprint","headless","mcp","agent-kb","operations","v1.2-aligned","non-canonical","overview","map"],"stability":"working","what_it_answers":"**What this KB is.** Operational knowledge for an AI working with PTXprint through the v1.2 MCP server — not through the GUI. Two audiences share it: (1) an autonomous agent that submits jobs to the MCP server to produce PDFs, and (2) an AI","sections":[{"slug":"how-to-use-this-kb","heading":"How to use this KB","start":30,"end":36},{"slug":"chapter-map","heading":"Chapter map","start":36,"end":81},{"slug":"authorship-and-updates","heading":"Authorship and updates","start":81,"end":89},{"slug":"open-gaps","heading":"Open gaps","start":89,"end":119},{"slug":"provenance","heading":"Provenance","start":119,"end":128}],"body":"---\ntitle: \"PTXprint Headless Operations — Knowledge Base for AI Agents and Assistants (Overview)\"\naudience: agent\nexposure: nav\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"headless\", \"mcp\", \"agent-kb\", \"operations\", \"v1.2-aligned\", \"non-canonical\", \"overview\", \"map\"]\nderives_from: \"canon/governance/headless-operations.md (pre-2026-04-28 monolith), transcript-encoded-session-6.md (H-011)\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1-2-spec.md\"\ngenerator: \"claude-opus-4.7 (interactive session)\"\ncreated_at: 2026-04-28T02:50:00Z\nupdated_at: 2026-04-28T20:30:00Z\ncanonical_status: non_canonical\nintended_consumer: \"AI agent submitting jobs to the PTXprint MCP server, or AI assistant helping a user create configs and produce PDFs conversationally via the MCP server\"\n---\n\n# PTXprint Headless Operations — Overview\n\n> **What this KB is.** Operational knowledge for an AI working with PTXprint through the v1.2 MCP server — not through the GUI. Two audiences share it: (1) an autonomous agent that submits jobs to the MCP server to produce PDFs, and (2) an AI assistant guiding a human user conversationally through MCP-mediated config creation. Both touch the same surface; both need the same knowledge.\n\n> **What this KB is not.** Not the MCP server's API specification (that lives in `klappy://canon/specs/ptxprint-mcp-v1-2-spec`). Not the catalog of all 400+ settings (that lives in canon, derived from the running tooltip dump). Not the user-facing training manual. This document is the operational layer between the spec and the user.\n\n> **Architecture context.** The PTXprint MCP server is a stateless content-addressed build system. The agent does not edit files inside a sandboxed project tree on the server — it reads project state from wherever the user keeps it (Claude Desktop's filesystem access, Git, DBL, the user's Paratext server) and constructs a payload describing one typesetting job. The payload is submitted to the server, which dispatches it to an ephemeral worker container that materializes the inputs, runs PTXprint, and uploads the PDF to R2. The agent receives a job_id and polls for status.\n>\n> Iterative editing in this model means re-submitting an updated payload, not editing files in place. Each typesetting run is fully reproducible from its payload; outputs are content-addressed by sha256 of the payload, so re-submitting an unchanged build returns the cached result for free.\n\n> **Containment.** Non-canonical. Where this disagrees with the running PTXprint product, the product wins. Where this disagrees with canon, canon wins. Verify settings names against the running UI before encoding into production code.\n\n---\n\n## How to use this KB\n\nThis document is an **index**. Detail lives in chapter articles under `canon/articles/`. Fetch the chapter that answers your immediate question via `oddkit_get`.\n\nThe articles are designed for progressive disclosure — read what you need, ignore the rest. Cross-references between articles use `klappy://` URIs.\n\n## Chapter map\n\n### Building and submitting jobs\n\n| Chapter | Use when |\n|---|---|\n| `klappy://canon/articles/payload-construction` | Building a `submit_typeset` payload from scratch |\n| `klappy://canon/articles/file-system-map` | Choosing relative-path keys in `config_files` |\n| `klappy://canon/articles/output-naming` | Interpreting `pdf_url`/`log_url` and the filename convention |\n| `klappy://canon/articles/cli-reference` | Understanding what the worker invokes; mapping payload → flags |\n\n### Configuration\n\n| Chapter | Use when |\n|---|---|\n| `klappy://canon/articles/config-construction` | Modifying or creating a `ptxprint.cfg` |\n| `klappy://canon/articles/config-inheritance-and-overrides` | Walking `[import]` chains; debugging override files |\n| `klappy://canon/articles/settings-cookbook` | Translating user intent to specific cfg keys |\n\n### Supporting file formats\n\n| Chapter | Use when |\n|---|---|\n| `klappy://canon/articles/font-resolution` | Building the `fonts` array; sourcing font URLs |\n| `klappy://canon/articles/adjlist-format` | Writing paragraph adjustments per book |\n| `klappy://canon/articles/piclist-format` | Placing pictures via `<PRJ>-<Config>.piclist` |\n| `klappy://canon/articles/changes-txt-format` | Regex USFM transforms |\n| `klappy://canon/articles/stylesheet-format` | Marker style declarations in `.sty` |\n| `klappy://canon/articles/usfm-markers-headless` | USFM book codes, markers, PTXprint `\\z*` extensions |\n| `klappy://canon/articles/frt-local-and-cover-periphs` | Front matter content; cover periphs |\n\n### Running, diagnosing, recovering\n\n| Chapter | Use when |\n|---|---|\n| `klappy://canon/articles/failure-mode-taxonomy` | Interpreting `failure_mode: hard \\| soft \\| success` |\n| `klappy://canon/articles/diagnostic-patterns` | Reading XeTeX logs; common error signatures; recovery loop |\n| `klappy://canon/articles/workflow-recipes` | End-to-end flows (draft, NT, diglot, lockdown, recovery) |\n\n### Scope\n\n| Chapter | Use when |\n|---|---|\n| `klappy://canon/articles/phase-1-poc-scope` | What's in/out for hackathon week (USFM-only payload) |\n\n## Authorship and updates\n\nThis overview replaces the prior monolithic `headless-operations.md` (68kb, single document). The split into chapter articles was H-011 from session 6, motivated by progressive-disclosure ergonomics — agents fetch only the chapter they need rather than the whole KB.\n\nSource content for the chapters comes from the prior monolith (sessions 1–5 derivation), the v1.2 spec, the master deck, and session 6 transcript encoding. Each chapter cites `derives_from` in its frontmatter.\n\nWhen adding new operational knowledge, add it to the most-relevant chapter article — not back into this overview. This overview stays a map; chapters are the single source of truth for their topic, per `klappy://canon/principles/dry-canon-says-it-once`.\n\n## Open gaps\n\nItems not yet covered by any chapter article. Surface to the user honestly when their request lands here.\n\n1. **Exact widget-identifier names for `define` overrides.** The deck shows examples (`s_linespacing`, `c_fighiderefs`) but doesn't enumerate them. A tooltip-dump-derived index belongs in canon.\n\n2. **The 400+ settings catalog.** Chapters name sections and give intent-keyed pointers. A complete per-key article set requires the running tooltip dump as source.\n\n3. **Picture catalogue and download workflows.** The high-res download lifecycle (request → wait → email → download → don't-unzip) doesn't have a clean MCP path yet — it's a project-state concern that happens before payload construction.\n\n4. **Cover-wizard equivalents in headless.** The agent constructs cover periphs directly in `FRTlocal.sfm`. The deck's 6-step wizard has no documented headless analogue.\n\n5. **TeX-macro injection (`*-mods.tex`, `*-premods.tex`).** v1.2 defers as too risky for automation. Until canon documents validation patterns, the agent shouldn't include these in payloads.\n\n6. **Programmatic PDF Compare.** Available in GUI; not exposed via MCP yet.\n\n7. **Diglot two-config concurrency.** When editing a diglot setup, two project-config pairs are in play simultaneously. Constructing payloads that reference both correctly is the agent's responsibility — and not yet documented in a single chapter.\n\n8. **Project state read/write mechanism.** The agent needs read/write access to the user's project storage (filesystem, Git, Paratext server) but the typesetting MCP doesn't provide it. Implicit for Claude Desktop with native FS access; needs documentation per host elsewhere.\n\n9. **Payload history persistence.** Whether the agent maintains payload history in volatile memory, persistent agent state, or external storage — and how to surface that history for rollback purposes.\n\n10. **Cache-hit observability.** When to surface \"this PDF was returned from cache\" vs. \"this PDF was just generated\" in user-facing messaging.\n\n11. **Hackathon-week preset templates** (session 6 D-022 / H-012). The 3–4 presets the agent picks from haven't been enumerated. Operator + Martin own this.\n\n12. **Zip-payload vs structured-payload tension** (session 6 O-open-P1-005 / H-014). Martin pushed for zip-payload during session 6; v1.2 D-021 chose structured. Resolution required before next spec rev.\n\nWhen the user's request lands in one of these gaps: surface honestly, recommend canon search or the running tooltip, and proceed only with explicit user direction.\n\n## Provenance\n\n- **Source materials:** `ptxprint-master-slides.surface.json`, `ptx2pdf.surface.json`, `transcript-encoded.md` (sessions 1–7), `ptxprint-mcp-v1.2-spec.md`, the operator's PDF deck extraction.\n- **Generated:** 2026-04-28T02:50:00Z by claude-opus-4.7 (monolith).\n- **Updated:** 2026-04-28T04:06:00Z (v1.2 alignment).\n- **Restructured:** 2026-04-28T20:30:00Z (split into chapter articles per session 6 H-011).\n- **Human review status:** not reviewed.\n- **Recommended next passes:** resolution of the open gaps in companion canon articles; canon-seeding from the running tooltip dump; v1.2 spec rev addressing session 6 O-open-P1-005 (zip vs structured) and rescope of §10 DoD per session 6 D-022 (hackathon = 3–4 presets, not canon-driven generation).\n"},{"path":"governance/telemetry-governance.md","uri":"klappy://canon/governance/telemetry-governance","title":"Telemetry Governance — What PTXprint MCP Tracks and Why","audience":"project","tags":["canon","governance","constraint","telemetry","transparency","privacy","vodka-architecture","maintainability","analytics-engine","ptxprint","mcp","v1.2-aligned","ptxprint_telemetry"],"stability":"working","what_it_answers":"The PTXprint MCP server runs Bible-translation typesetting jobs on shared infrastructure. Each job costs real Container minutes and produces artifacts that belong to translation teams operating in sensitive contexts. Telemetry exists so the","sections":[{"slug":"summary-higher-privacy-floor-same-transparency-discipline","heading":"Summary — Higher Privacy Floor, Same Transparency Discipline","start":24,"end":52},{"slug":"privacy-floor-what-is-never-logged","heading":"Privacy Floor — What Is Never Logged","start":52,"end":89},{"slug":"what-is-tracked-automatic-every-request-and-every-job-phase","heading":"What Is Tracked (Automatic, Every Request and Every Job Phase)","start":89,"end":158},{"slug":"job-lifecycle-events-container-side-forwarded-through-the-worker","heading":"Job Lifecycle Events (Container-Side, Forwarded Through the Worker)","start":158,"end":198},{"slug":"consumer-identification-model","heading":"Consumer Identification Model","start":198,"end":216},{"slug":"transparency-leaderboard","heading":"Transparency Leaderboard","start":216,"end":251},{"slug":"storage-and-infrastructure","heading":"Storage and Infrastructure","start":251,"end":279},{"slug":"vodka-architecture-compliance","heading":"Vodka Architecture Compliance","start":279,"end":294},{"slug":"query-security-boundary","heading":"Query Security Boundary","start":294,"end":321},{"slug":"schema-hygiene","heading":"Schema Hygiene","start":321,"end":334},{"slug":"canned-queries-the-dashboard-library","heading":"Canned Queries — The Dashboard Library","start":334,"end":462},{"slug":"the-social-contract","heading":"The Social Contract","start":462,"end":476},{"slug":"operating-principle","heading":"Operating Principle","start":476,"end":488},{"slug":"see-also","heading":"See Also","start":488,"end":509}],"body":"---\nuri: klappy://canon/governance/telemetry-governance\ntitle: \"Telemetry Governance — What PTXprint MCP Tracks and Why\"\naudience: project\nexposure: nav\nvoice: instructional\nstability: working\ntags: [\"canon\", \"governance\", \"constraint\", \"telemetry\", \"transparency\", \"privacy\", \"vodka-architecture\", \"maintainability\", \"analytics-engine\", \"ptxprint\", \"mcp\", \"v1.2-aligned\", \"ptxprint_telemetry\"]\ndate: 2026-04-30\nderives_from: \"klappy://canon/constraints/telemetry-governance (klappy.dev / oddkit), canon/encodings/telemetry-feature-planning-ledger.md\"\ncompanion_to: \"canon/specs/ptxprint-mcp-v1.2-spec.md, canon/articles/failure-mode-taxonomy.md, canon/articles/hero-metrics-and-storytelling.md\"\ncanonical_status: canonical\ngoverns: \"All telemetry collection in the PTXprint MCP server (Worker + Container + Durable Objects + R2)\"\nstatus: reviewed\nreviewed_by: \"managed-agent fresh session, PR #25 (commit bec765d)\"\nrevisions_applied: \"B-1, B-2, N-1, N-2 of the H-T2 review applied in fix/h-t2-revisions\"\n---\n\n# Telemetry Governance — What PTXprint MCP Tracks and Why\n\n> The PTXprint MCP server runs Bible-translation typesetting jobs on shared infrastructure. Each job costs real Container minutes and produces artifacts that belong to translation teams operating in sensitive contexts. Telemetry exists so the maintainer can make informed decisions about cache effectiveness, failure mix, and where time goes — never to identify which team is doing what work. The system tracks the **shape** of usage; it never records the **substance**. The data is public. If you wouldn't show a Bible translator the dashboard showing what they did, you shouldn't be collecting that data.\n\n---\n\n## Summary — Higher Privacy Floor, Same Transparency Discipline\n\nThis governance forks the proven pattern from `klappy://canon/constraints/telemetry-governance` (oddkit's hosted service) and adapts it to one materially different context: PTXprint MCP serves Bible translation teams whose project metadata can reveal who they are, where they work, and what languages they translate into. That context demands a higher privacy floor than oddkit's \"which canon doc was retrieved\" model required.\n\nFive operational questions justify the telemetry. Anything that doesn't help answer one of them is out of scope:\n\n1. **Adoption** — how many distinct agents/teams are submitting jobs, and is that growing?\n2. **Cache effectiveness** — what fraction of `submit_typeset` calls hit the content-addressed cache (free) vs. dispatch the Container (~30 minutes of CPU)? The maintainer needs this number weekly.\n3. **Failure mix** — of dispatched jobs, what's the hard / soft / success distribution? A rising soft-failure rate is the silent-degradation signal v1.2 was designed to surface.\n4. **Where time goes** — within a job, how much wall-clock is fetching inputs vs. typesetting vs. autofill vs. uploading? Tells us whether the bottleneck is PTXprint or our infrastructure.\n5. **Which canon docs the `docs` tool actually serves** — same value as oddkit's document leaderboard; tells us which agent-facing articles do real work.\n\nTelemetry that doesn't answer at least one of these isn't worth shipping.\n\nThe design follows four rules:\n\n1. **Track structural identifiers, never content.** The server counts which tools were called, which event types fired, which phases completed, when, and how often. It never records project IDs, source URLs, payload contents, USFM bytes, log lines, or PDF bytes.\n\n2. **Project-identity exclusion is enforced in code.** The exclusion is not a reviewer-discipline policy. Schema slots are typed structurally (counts, sizes, durations, enums) so a leak would require an explicit code change visible in PR diff.\n\n3. **The data is public, not private.** Any consumer can call `telemetry_public` and see the same dashboard the maintainer sees. Same leaderboard. Same numbers. Same trends. There is no information asymmetry between host and consumer.\n\n4. **Participation is rewarded, not extracted.** Consumers who identify themselves — via header, query parameter, or MCP `clientInfo` — appear on the transparency leaderboard. Identification is encouraged and scored, never coerced.\n\nThis document is the authoritative reference for what is tracked, what is excluded, and why. It is fetched at runtime by `telemetry_policy` — not hardcoded in server logic. If the policy changes, this document changes. The server stays the same.\n\n---\n\n## Privacy Floor — What Is Never Logged\n\nThis section leads, not trails, the rest of the document. The privacy floor for PTXprint MCP telemetry is materially higher than oddkit's because the workload involves Bible translation projects, and project metadata can identify translation teams operating in sensitive contexts.\n\nThe following are **never collected, under any circumstance**:\n\n### Project identity\n\n- **Paratext project IDs** (e.g., `WSG`, `WSGNT`, `BSB`)\n- **Configuration names** (e.g., `Default`, `FancyNT`, `Long`)\n- **USFM book codes** (e.g., `MAT`, `JHN`, `ROM`)\n- **Hostnames or URLs** of source repositories, fonts, figures, or any other input\n\n### Content\n\n- **USFM source bytes**\n- **Payload contents** (the JSON document submitted to `submit_typeset`)\n- **Configuration file contents** (any `ptxprint.cfg`, `.sty`, `changes.txt`, AdjList, or piclist)\n- **XeTeX log content**\n- **PDF bytes**\n- **`docs` tool query strings** (the natural-language question is treated as content, not metadata)\n\n### Caller identity beyond opt-in\n\n- **IP addresses** — never logged for telemetry purposes\n- **Browser or device fingerprinting** — never collected\n- **Authentication tokens, API keys, or any credential material** — never logged\n- **User account IDs** beyond the self-declared `consumer_label`\n\n### The pseudonymous boundary\n\nThe single pseudonymous dimension is `payload_hash_prefix` — the **first 8 hex characters** of the sha256 of the canonical payload JSON. Never the full hash. Never any field of the payload itself. Two teams typesetting the same public Bible (e.g., BSB) will share a prefix; this collision is privacy-positive and intentional.\n\nThe principle: if a field reveals **who** is doing the work or **what specific text** they are working on, it is excluded. If it reveals **how much** work happened, **how long** it took, or **what shape** it was, it is included.\n\n---\n\n## What Is Tracked (Automatic, Every Request and Every Job Phase)\n\nTwo event classes are written:\n\n- **MCP-side events** are written from the Worker for every JSON-RPC envelope the server handles. One data point per `mcp_request`; an additional `tool_call` data point when the method is `tools/call`.\n- **Job-lifecycle events** are written from the Worker on behalf of the Container. The Container POSTs a small telemetry envelope to a Worker endpoint at each phase transition; the Worker forwards to Analytics Engine. See the next section for the full lifecycle.\n\nAll writes go to the `ptxprint_telemetry` Cloudflare Workers Analytics Engine dataset via `env.PTXPRINT_TELEMETRY.writeDataPoint()`. Writes are non-blocking and add zero latency to request handling.\n\n### Structural Dimensions (Blobs)\n\n| # | Field | Populated For | Example |\n|---|---|---|---|\n| 1 | `event_type` | all | `mcp_request \\| tool_call \\| job_phase \\| job_terminal` |\n| 2 | `method` | mcp_request, tool_call | `tools/call`, `initialize` |\n| 3 | `tool_name` | mcp_request, tool_call | `submit_typeset \\| get_job_status \\| cancel_job \\| docs \\| telemetry_public \\| telemetry_policy` |\n| 4 | `consumer_label` | all | `claude-desktop`, `bt-servant`, `unknown` |\n| 5 | `consumer_source` | all | `header \\| query \\| client_info \\| user_agent \\| oauth` (the last reserved for future authenticated deployments; v1.2 has no auth so this value is never emitted today) |\n| 6 | `worker_version` | all | `1.2.3` |\n| 7 | `phase` | job_* | `queued \\| fetching_inputs \\| typesetting \\| autofill \\| uploading \\| done` |\n| 8 | `failure_mode` | job_terminal | `success \\| soft \\| hard \\| cancelled \\| timeout` |\n| 9 | `cache_outcome` | mcp_request (when `tool_name = submit_typeset`) | `hit \\| miss \\| n/a` |\n| 10 | `payload_hash_prefix` | submit_typeset, job_* | first 8 hex chars only (see Privacy Floor §) |\n| 11 | `docs_audience` | docs | `headless \\| gui` |\n| 12 | `docs_top_uri` | docs | `klappy://canon/...` URI of the top hit (canon URIs are public; same logic as oddkit's `document_uri`) |\n\nSlots 13+ are reserved as empty until a real operational question demands them. Do not populate speculatively. (See Schema Hygiene §.)\n\n### Numeric Values (Doubles)\n\n| # | Value | What It Records |\n|---|---|---|\n| 1 | `count` | always `1`, for SUM aggregation |\n| 2 | `duration_ms` | wall-clock at the layer that wrote the event — Worker edge for `mcp_*`, Container-reported for `job_*` |\n| 3 | `bytes_in` | for `mcp_*`: UTF-8 byte length of the JSON-RPC request body. For `submit_typeset`: payload size |\n| 4 | `bytes_out` | for `mcp_*`: response body size. For `job_terminal` success: the produced PDF byte count |\n| 5 | `sources_count` | number of source URLs in the payload (USFM and other text inputs) |\n| 6 | `fonts_count` | number of font URLs in the payload |\n| 7 | `figures_count` | number of figure URLs in the payload |\n| 8 | `passes_completed` | for autofill jobs, the pass count when the job stopped |\n| 9 | `overfull_count` | XeTeX `Overfull \\hbox` warning count from the run log |\n| 10 | `pages_count` | page count in the produced PDF (`job_terminal` with `failure_mode = success` only) |\n\nSlots 11+ are reserved as empty.\n\n### Automatic Properties\n\n| Property | Source |\n|---|---|\n| Timestamp | Cloudflare Analytics Engine (automatic per data point) |\n| Sampling key | `consumer_label` (for Analytics Engine sampling consistency) |\n\n### What This Enables\n\n- **Adoption signal** — distinct consumer count by week (`SELECT DISTINCT consumer_label`)\n- **Cache hit rate** — `SUM(IF(cache_outcome = 'hit', 1, 0)) / SUM(_sample_interval)` filtered to `tool_name = 'submit_typeset'`\n- **Failure mix** — `GROUP BY failure_mode` over `job_terminal` events\n- **Where time goes** — average `duration_ms` per `phase` over `job_phase` events\n- **Tool leaderboard** — `GROUP BY tool_name` over `tool_call` events\n- **Document leaderboard** — `GROUP BY docs_top_uri` over `tool_call` events with `tool_name = 'docs'`\n- **Pages-per-success ratio** — average `pages_count` over `job_terminal` with `failure_mode = 'success'`\n- **Soft-failure rate trend** — `WHERE failure_mode = 'soft'` over time\n\n### Per-Request Diagnostics (Ephemeral, Not Stored)\n\nLike oddkit, the Worker MAY return per-request span detail in a debug field on tool responses (cache lookups, sub-fetches, action timing). This is ephemeral diagnostic data — present in the HTTP response and nowhere else. It is not written to Analytics Engine. The Container forwards similar per-job diagnostic spans through the Worker telemetry endpoint; only the structured aggregate data documented above lands in the dataset.\n\n---\n\n## Job Lifecycle Events (Container-Side, Forwarded Through the Worker)\n\nThe MCP-side events alone are insufficient to answer the operational questions. A typeset job runs for seconds (cache hit), minutes (simple typesetting), or up to ~30 minutes (autofill of a New Testament). The Worker sees only the `submit_typeset` call (cache hit or dispatch) and subsequent `get_job_status` polls — it cannot see what happens inside the Container.\n\nThe Container fills the gap by emitting two kinds of events:\n\n### `job_phase` — written at every phase transition\n\nThe phase enum is a superset of `progress.current_phase` from `get_job_status`. The spec exposes four in-flight phases the agent polls (`fetching_inputs | typesetting | autofill | uploading`); telemetry adds `queued` (written by the Worker on dispatch, before the Container picks the job up) and `done` (written by the Container as the terminal phase) so dashboards can reason about end-to-end wall-clock from submission to release, not just the in-flight portion. Full enum:\n\n1. `queued` — telemetry-only. Worker has dispatched to the Container; the Container has not picked it up yet\n2. `fetching_inputs` — Container is parallel-fetching `sources`, `fonts`, `figures` from URLs\n3. `typesetting` — PTXprint is running the simple-typeset pass\n4. `autofill` — PTXprint is running optimization passes (only present when the payload requested autofill)\n5. `uploading` — Container is uploading the PDF and run log to R2\n6. `done` — telemetry-only. Terminal phase; the Container is releasing the job\n\n`duration_ms` on a `job_phase` event records how long the **just-completed** phase took.\n\n### `job_terminal` — written exactly once per dispatched job\n\nCarries the final outcome and the totals worth keeping:\n\n- `failure_mode` ∈ `{success, soft, hard, cancelled, timeout}` — the three values `success | soft | hard` per `klappy://canon/articles/failure-mode-taxonomy`, extended with two terminal states (`cancelled`, `timeout`) per planning-ledger D-T1. The taxonomy's three-value enum is what `get_job_status.failure_mode` returns to the agent (and is null when the job ended in a non-typesetting terminal state); the telemetry blob flattens the typesetting classification and the terminal state into one queryable dimension so dashboards can ask \"of dispatched jobs, what fraction succeeded vs. soft-failed vs. hard-failed vs. were cancelled vs. timed out?\" in a single `GROUP BY`.\n- `passes_completed` — for autofill jobs, the count when the job ended\n- `overfull_count` — count of `Overfull \\hbox` warnings in the run log\n- `pages_count` — populated only when `failure_mode = success`\n- `bytes_out` — PDF byte count (success only)\n\n### Routing constraint\n\nAll Container telemetry routes through the Worker via the existing service binding. The Container holds **no** Analytics Engine credentials and **no** Cloudflare Account ID. This is both:\n\n- a **redaction-surface** constraint — one place to enforce the privacy floor\n- a **credential-management** constraint — one binding to rotate\n\nDirect Container-to-Analytics-Engine writes are explicitly rejected until the forward path is shown to bottleneck. As of this version, no such bottleneck has been observed.\n\n---\n\n## Consumer Identification Model\n\nPTXprint MCP's hosted service is open. No authentication is required. Consumer labels are resolved from whatever the request provides, in priority order:\n\n1. `?consumer=` query parameter (URL-level, highest priority — works on every MCP client)\n2. `x-ptxprint-client` header (explicit)\n3. MCP `initialize` → `clientInfo.name` (protocol-native)\n4. `User-Agent` header (fallback)\n5. `\"unknown\"` (default)\n\nThe query parameter is the recommended identification method because every MCP client lets users edit the URL, while not all platforms expose custom headers. Unidentified consumers see a one-line footer on tool responses linking to this policy and explaining how to identify themselves.\n\n### Verified Clients\n\nA server-side allowlist (env var `TELEMETRY_VERIFIED_CLIENTS`) designates verified consumer labels. Verified clients receive weighted leaderboard scoring. Verification increases visibility but never blocks participation.\n\n---\n\n## Transparency Leaderboard\n\nModeled after the oddkit pattern, which itself derives from Aquifer MCP.\n\n### Self-Report Headers (Optional, Incentivized)\n\n| Field | Header | Description |\n|---|---|---|\n| Client name | `x-ptxprint-client` | Your client name (highest priority identifier). Examples: `claude-desktop`, `bt-servant`, `your-organization-agent`. |\n| Client version | `x-ptxprint-client-version` | Version string for the client. Semver recommended but any stable identifier works. |\n| Agent name | `x-ptxprint-agent-name` | The AI agent or model name when distinct from the client. Example: `claude-opus-4-7`. |\n| Agent version | `x-ptxprint-agent-version` | Version string for the agent/model. |\n| Surface | `x-ptxprint-surface` | Where this is running. Examples: `claude.ai`, `vscode`, `cli`, `ci`, `production`. |\n| Contact URL | `x-ptxprint-contact-url` | URL for your project or organization. Appears on the transparency leaderboard. |\n| Policy URL | `x-ptxprint-policy-url` | Your privacy or telemetry policy URL. Signals reciprocal transparency. |\n| Capabilities | `x-ptxprint-capabilities` | Comma-separated capability list. Example: `submit,poll,docs`. |\n\n### Completeness Scoring\n\nEach `tools/call` scores the number of self-report fields present (0–8). Completeness percentage drives badge assignment:\n\n| Badge | Threshold |\n|---|---|\n| Open Ledger | ≥ 90% |\n| Clear Reporter | ≥ 70% |\n| Starter Reporter | ≥ 40% |\n| Hint Reporter | > 0% |\n| Silent Reporter | 0% |\n\n### Leaderboard Integrity\n\nConsumer labels are transparent self-declarations. They are not identity proof unless verified by the server-side allowlist. Treat labels as honest claims, not authentication.\n\n---\n\n## Storage and Infrastructure\n\n### Cloudflare Workers Analytics Engine\n\nTelemetry data points are written via `env.PTXPRINT_TELEMETRY.writeDataPoint()`. Writes are non-blocking and add zero latency to request handling.\n\n- **Dataset:** `ptxprint_telemetry`\n- **Retention:** 3 months (Cloudflare default)\n- **Querying:** SQL API via `CF_ACCOUNT_ID` and `CF_API_TOKEN` (read-only)\n- **Visualization:** Grafana-compatible\n- **Pricing:** 100,000 data points/day free; 10,000 queries/day free\n\n### Long-Term Retention\n\nAnalytics Engine data expires after 3 months. Ad-hoc aggregate snapshots may be persisted to a durable store (KV, R2, or a canon article) before expiration if long-term trend analysis is wanted. No automated weekly snapshot job exists in v1; this matches the oddkit retention model.\n\n### Policy Resolution at Runtime\n\n`telemetry_policy` returns this document, but it does not hardcode it. The tool resolves the policy through a three-tier fallback chain (per planning-ledger D-T7, mirroring oddkit's `telemetry_policy` pattern verbatim):\n\n1. **`knowledge_base`** — fetch this document live from `klappy/ptxprint-mcp` via the canon retrieval path. This is the primary source; if it succeeds the response carries `governance_source: \"knowledge_base\"` and the freshest text the maintainer has shipped.\n2. **`bundled`** — if the canon fetch fails (network error, 404, malformed response), serve a copy bundled into the Worker at deploy time. Marked `governance_source: \"bundled\"`. Slightly stale by definition; never silently misleading.\n3. **`minimal`** — if even the bundled copy is missing or unreadable, return a one-paragraph minimal policy that lists the dataset name, the privacy-floor non-negotiables, and a pointer to this document's URI. Marked `governance_source: \"minimal\"`.\n\nThe fallback chain is observable: every `telemetry_policy` response includes the `governance_source` field, so a consumer can tell which tier served their copy and how stale it might be. The server holds no governance opinions in code at any tier — even the `minimal` tier is a static string defined in the deploy artifact, not derived from server logic.\n\n---\n\n## Vodka Architecture Compliance\n\nThis telemetry implementation is infrastructure serving the Maintainability principle. It passes the three-question constraint test from `klappy://canon/principles/vodka-architecture`:\n\n1. **Has the server grown thick?**  \n   No. One `writeDataPoint()` per request at the Worker, plus a small Worker endpoint that receives Container-emitted phase events and forwards them. Two new tools (`telemetry_public`, `telemetry_policy`). Combined module is self-contained.\n\n2. **Has the server acquired domain opinions?**  \n   The borderline answer surfaced in planning: `failure_mode` and `phase` are PTXprint-shaped enums. The resolution rule applied: **if the field is already in the public response shape**, persisting it is not new opinion — it is making existing opinion queryable over time. Both fields are in the `get_job_status` response surface (per `canon/specs/ptxprint-mcp-v1.2-spec.md` §3), so logging them does not move the vodka line. New blob dimensions that are NOT already in a public response would warrant fresh scrutiny.\n\n3. **Can the server be removed without consequence?**  \n   No. Without telemetry the maintainer has no signal on cache effectiveness, no evidence of soft-failure rate, and no way to know which `docs` queries agents actually run. All three drive prioritization decisions for a single-maintainer system. The telemetry is load-bearing for infrastructure sustainability.\n\n---\n\n## Query Security Boundary\n\n`telemetry_public` accepts raw SQL and forwards it to Cloudflare Analytics Engine. The data is public by design — there is nothing to steal. But the query interface requires infrastructure guards to prevent abuse without adding domain opinions.\n\n### Threat Model\n\nThe data is public. The API token is read-only. The risk is not data exfiltration — it's resource exhaustion and information leak about other account resources.\n\n### Guards (Infrastructure, Not Domain Opinion)\n\n**1. Dataset allowlist.** The server validates that the SQL query targets only the `ptxprint_telemetry` dataset. Any query referencing a different dataset is rejected before reaching the Analytics Engine API. This prevents cross-dataset access to other Analytics Engine data on the same Cloudflare account.\n\n**2. Rate limiting.** `telemetry_public` calls are rate-limited per consumer label. The limit protects the Analytics Engine query quota (10,000 queries/day free tier). Rate limiting is infrastructure — it protects the resource, not the data.\n\n**3. Error sanitization.** Analytics Engine API errors are caught and returned as generic failure messages. Raw error responses — which may contain account IDs, dataset names, or internal schema details — are never forwarded to the caller.\n\n### What Is NOT Guarded (By Design)\n\n- **Column restriction** — any column can be queried. The data is public.\n- **Query complexity** — no LIMIT enforcement. Analytics Engine has built-in timeouts.\n- **Authentication** — no auth required to query. Transparency means anyone can see the data.\n- **SQL keyword blocking** — unnecessary. The API token is read-only. `DROP`, `ALTER`, `INSERT` are rejected by Analytics Engine regardless.\n\nThese three guards are infrastructure serving security, not opinions about what questions are interesting. The server does not decide what to ask — it decides what is safe to forward.\n\n---\n\n## Schema Hygiene\n\nThe schema in this document IS the canonical schema. Slots beyond the documented set stay empty until a real operational question demands them.\n\nNew slots are added by:\n1. Editing this document to define the new slot\n2. Shipping the corresponding Worker change\n3. (Optional) Backfilling the canned-query library if the new dimension makes a useful dashboard\n\nSpeculative empty slots reproduce the oddkit slot-9 deprecation problem (see `klappy://canon/constraints/telemetry-governance` §\"retired\" note on `cache_tier`). Don't anticipate.\n\n---\n\n## Canned Queries — The Dashboard Library\n\nEvery operational question this telemetry is supposed to answer should have at least one canned SQL query here. Lowers the bar for non-SQL-fluent agents and operators to run the dashboard.\n\nUse `SUM(_sample_interval)` instead of `COUNT(*)` to account for Analytics Engine sampling. All examples use a 30-day window; adjust as needed.\n\n### Field names — semantic vs. positional\n\nCloudflare Analytics Engine stores writes as positional `blob1..20` and `double1..20` columns. The PTXprint MCP server publishes a single source of truth in `src/telemetry-schema.ts` that maps semantic names (`event_type`, `tool_name`, `consumer_label`, `duration_ms`, `pages_count`, …) to those positions, and the `telemetry_public` tool transparently rewrites semantic SQL to positional SQL before forwarding to Cloudflare. **Use semantic names in queries.** The mapping is also exposed via the `telemetry_schema` MCP tool and the public `GET /diagnostics/schema` HTTP endpoint, so anyone — agent, curl, Grafana — can discover it at runtime.\n\nPosition is forever (reordering breaks historical data) but you should never have to type `blob3` again.\n\n### Adoption — distinct consumers per week\n\n```sql\nSELECT\n  toStartOfWeek(timestamp) AS week,\n  COUNT(DISTINCT consumer_label) AS distinct_consumers\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '90' DAY\n  AND event_type = 'mcp_request'\nGROUP BY week\nORDER BY week DESC\n```\n\n### Cache hit rate — `submit_typeset` only\n\n```sql\nSELECT\n  toStartOfWeek(timestamp) AS week,\n  SUM(IF(cache_outcome = 'hit', _sample_interval, 0)) AS hits,\n  SUM(IF(cache_outcome = 'miss', _sample_interval, 0)) AS misses,\n  SUM(IF(cache_outcome = 'hit', _sample_interval, 0)) / SUM(_sample_interval) AS hit_rate\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n  AND event_type = 'mcp_request'\n  AND tool_name = 'submit_typeset'\nGROUP BY week\nORDER BY week DESC\n```\n\n### Failure mix — over the last 30 days\n\n```sql\nSELECT\n  failure_mode,\n  SUM(_sample_interval) AS jobs\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n  AND event_type = 'job_terminal'\nGROUP BY failure_mode\nORDER BY jobs DESC\n```\n\n### Where time goes — average phase duration\n\n```sql\nSELECT\n  phase,\n  AVG(duration_ms) AS avg_duration_ms,\n  SUM(_sample_interval) AS observations\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n  AND event_type = 'job_phase'\nGROUP BY phase\nORDER BY avg_duration_ms DESC\n```\n\n### Tool leaderboard — which tools get used\n\n```sql\nSELECT\n  tool_name,\n  SUM(_sample_interval) AS calls\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n  AND event_type = 'tool_call'\nGROUP BY tool_name\nORDER BY calls DESC\n```\n\n### Document leaderboard — which canon docs the `docs` tool serves\n\n```sql\nSELECT\n  docs_top_uri AS document_uri,\n  SUM(_sample_interval) AS hits\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n  AND event_type = 'tool_call'\n  AND tool_name = 'docs'\n  AND docs_top_uri != ''\nGROUP BY docs_top_uri\nORDER BY hits DESC\nLIMIT 25\n```\n\n### Soft-failure rate trend — silent-degradation watch\n\n```sql\nSELECT\n  toStartOfWeek(timestamp) AS week,\n  SUM(IF(failure_mode = 'soft', _sample_interval, 0)) AS soft_failures,\n  SUM(_sample_interval) AS total_jobs,\n  SUM(IF(failure_mode = 'soft', _sample_interval, 0)) / SUM(_sample_interval) AS soft_rate\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '90' DAY\n  AND event_type = 'job_terminal'\nGROUP BY week\nORDER BY week DESC\n```\n\n### Pages per successful build — output-size distribution\n\n```sql\nSELECT\n  AVG(pages_count) AS avg_pages,\n  quantile(0.5)(pages_count) AS median_pages,\n  quantile(0.95)(pages_count) AS p95_pages,\n  SUM(_sample_interval) AS successful_builds\nFROM ptxprint_telemetry\nWHERE timestamp > NOW() - INTERVAL '30' DAY\n  AND event_type = 'job_terminal'\n  AND failure_mode = 'success'\n```\n\n---\n\n## The Social Contract\n\nPTXprint MCP is free. The code is MIT. The hosted service runs on shared Cloudflare infrastructure for essentially zero direct cost — the constraint is attention, not money.\n\nA small group maintains this. Decisions about which features to improve, which bugs to fix, which canon articles to author next are made with limited visibility into who is using it and how. Without telemetry, those decisions are guesses. With telemetry, they are informed. That is the value of the data — not paying for servers, but knowing where to spend the only resource that is actually scarce.\n\nThe ask: help the maintainers know you exist. A header value, a query parameter, an organization name — any unique identifier. Not so the maintainers can profile you. So they can make better decisions about the infrastructure you depend on.\n\nThis is not the standard telemetry social contract. The standard contract is: a funded company collects your data privately and promises it is anonymous. This contract is: a small group publishes the data publicly and asks consumers to be visible. The relationship is different because the power dynamic is different — and because the consumers are translation teams whose work matters far more than the dashboard ever will.\n\nThe non-negotiable: project metadata that could identify a translation team's work is never logged. Not even for \"diagnostics.\" Not even briefly. Not at all.\n\n---\n\n## Operating Principle\n\n**Mandatory truth at baseline, optional richness by incentive — with a hard privacy floor that is not optional.**\n\n- Baseline structural usage is always tracked. This is non-negotiable for infrastructure sustainability.\n- Optional self-report details are encouraged, scored, and made visible via the transparency leaderboard.\n- Project-identity data is **never** collected. This is non-negotiable in the other direction.\n- The data is always public. The dashboard is always shared.\n- If the policy changes, this document changes. The server never hardcodes governance.\n\n---\n\n## See Also\n\n### PTXprint canon\n\n- [v1.2 Specification](klappy://canon/specs/ptxprint-mcp-v1-2-spec) — the tool surface telemetry observes\n- [Hero Metrics & Storytelling](klappy://canon/articles/hero-metrics-and-storytelling) — derived-practice sibling: how the data this article governs is composed into the public hero stat, X-Ray waterfall, and snapshot strategy\n- [Failure Mode Taxonomy](klappy://canon/articles/failure-mode-taxonomy) — the source of the `failure_mode` enum\n- [Headless Operations Overview](klappy://canon/governance/headless-operations) — the agent-facing operational KB\n- [Telemetry Feature Planning Ledger](klappy://canon/encodings/telemetry-feature-planning-ledger) — the DOLCHEO+H encoding of the design conversation that produced this article\n\n### Upstream pattern (klappy.dev)\n\n- [oddkit Telemetry Governance](klappy://canon/constraints/telemetry-governance) — the source pattern this article forks\n- [Vodka Architecture](klappy://canon/principles/vodka-architecture) — the design pattern telemetry must not violate\n- [Maintainability — One Person, Indefinitely](klappy://canon/principles/maintainability-one-person-indefinitely) — the principle telemetry serves\n- [Verification Requires Fresh Context](klappy://canon/principles/verification-requires-fresh-context) — why this article carries `status: draft_pending_fresh_review` until a separate session reviews it\n\n### External\n\n- [Aquifer MCP Telemetry Governance](https://github.com/klappy/aquifer-mcp/blob/main/docs/telemetry-governance-snapshot.md) — the proven pattern oddkit derived from\n"},{"path":"handoffs/2026-09-05-docs-self-served.md","uri":"klappy://canon/handoffs/2026-09-05-docs-self-served","title":"Handoff — 2026-09-05: docs() served from the bundled canon; validation of the live deploy","audience":"operator","tags":["ptxprint","mcp","handoff","docs","validation","oddkit","no-drift"],"stability":"working","what_it_answers":"Handoff — 2026-09-05: docs() served from the bundled canon; validation of the live deploy","sections":[{"slug":"what-changed-worker-0-2-0","heading":"What changed (Worker 0.2.0)","start":16,"end":21},{"slug":"what-was-validated-live-deploy-docs-validation-2026-09-05-md","heading":"What was validated (live deploy, `docs/validation/2026-09-05.md`)","start":21,"end":24},{"slug":"operator-duties-this-introduces","heading":"Operator duties this introduces","start":24,"end":28},{"slug":"open-on-docs-tensions-md","heading":"Open (on `docs/TENSIONS.md`)","start":28,"end":31},{"slug":"next-dish-named-by-the-captain","heading":"Next dish named by the captain","start":31,"end":34},{"slug":"addendum-0-3-0-same-day-progressive-disclosure","heading":"Addendum — 0.3.0, same day: progressive disclosure","start":34,"end":37}],"body":"---\ntitle: \"Handoff — 2026-09-05: docs() served from the bundled canon; validation of the live deploy\"\naudience: operator\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"handoff\", \"docs\", \"validation\", \"oddkit\", \"no-drift\"]\ndate: 2026-09-05\nwritten_at: 2026-09-05T01:20:00Z\nsession_window: 2026-09-05T00:41Z–2026-09-05T01:20Z\ncompanion_to: [\"docs/validation/2026-09-05.md\", \"docs/TENSIONS.md\", \"canon/handoffs/open-items-validated-2026-04-30.md\"]\ngovernance_source: knowledge_base\n---\n\n# Handoff — 2026-09-05: `docs()` served from the bundled canon\n\n## What changed (Worker 0.2.0)\n- `docs(query, audience?, depth?)` no longer proxies oddkit. `scripts/bundle-canon.ts` writes `src/bundled-canon.ts` from `canon/{articles,governance,specs,templates,handoffs}` + `canon/README.md` (46 docs, ~652 KB, content-hash stamped); `src/docs.ts` searches it in-process (BM25, title bonus, audience-tag tiebreaker). Same `DocsResult` shape; `served_from` added. CI fails on a stale bundle.\n- Why: the proxy read oddkit's `result.hits`; oddkit's retrieval-disclosure contract returns `result.data`. Every query — including the README's three examples — came back empty with `governance_source: \"knowledge_base\"`, for an unknown span before 2026-09-05. Found by validation row D1; confirmed by running the same queries through oddkit directly (23 hits).\n- Ruling that shaped the fix (captain, 2026-09-04): new docs tools manage and serve their own policy; do not re-pin an upstream. And (R20, same day): a PR updates every doc it makes stale, in the same PR.\n\n## What was validated (live deploy, `docs/validation/2026-09-05.md`)\nEvery tool; one fresh render (never-seen payload → 61-page PDF in 10.9 s — the container is alive); cached render; cancel (Day-1 flag only); status on an unknown id; telemetry policy/schema; issue #40 reproduced. After the merge, the three README queries answer on production within 20 s.\n\n## Operator duties this introduces\n- After ANY edit under `canon/` (the bundled subtrees): `npm run bundle-canon`, commit the regenerated `src/bundled-canon.ts`. CI will refuse otherwise.\n- The bundle excludes `canon/encodings`, `canon/surfaces`, `canon/derivatives`, and the `_archive`/`archive` dirs on purpose. Adding a subtree is a one-line change in `INCLUDE_DIRS`.\n\n## Open (on `docs/TENSIONS.md`)\nF2 stale `human_summary` on cached jobs · P3 `telemetry_public` refused-statement row · D1s depth-1 snippet picks a one-line callout.\n\n## Next dish named by the captain\nA Titus study bible: Aquifer Open Study Notes (Titus 3) + BSB through this server, notes typeset under the text. Ordered on the kitchen rail when the captain says so.\n\n## Addendum — 0.3.0, same day: progressive disclosure\n`docs` re-cut to four calls (`{}` index · `{uri}` article · `{uri, section}` · `{query}` pointers with `covered: false`); `depth` kept one release as an alias. New article `study-notes-and-footnotes` (from the Titus cook); `changes-txt-format` callout; real cfg keys in `settings-cookbook`; taxonomy rows 3a/3b; spec §3 docs + §5 Limits. Kitchen ticket `2026-09-05-ptxprint-docs-v2-progressive-disclosure`.\n"},{"path":"handoffs/bundle-default-cfg-handoff.md","uri":"klappy://canon/handoffs/bundle-default-cfg-handoff","title":"Container Handoff — Bundle Default Cfg as the Phase-0 Floor","audience":"next-session-claude | autonomous-coding-agent | container-maintainer","tags":["ptxprint","mcp","handoff","container","default-cfg","bundle","phase-0","bootstrap","vodka-architecture"],"stability":"working","what_it_answers":"**One-line scope.** The Container ships with the upstream default cfg pre-staged so that a payload of `{books, sources}` produces a publication-quality A5 two-column Bible book with no further input from the caller.","sections":[{"slug":"what-changes","heading":"What changes","start":41,"end":53},{"slug":"vodka-constraints-to-honor","heading":"Vodka constraints to honor","start":53,"end":107},{"slug":"implementation-sketch","heading":"Implementation sketch","start":107,"end":166},{"slug":"smoke-test-contract","heading":"Smoke test contract","start":166,"end":222},{"slug":"architectural-decisions-made-in-the-canon-authoring-carry-these-into-the-implementation","heading":"Architectural decisions made in the canon authoring (carry these into the implementation)","start":222,"end":242},{"slug":"definition-of-done-for-this-handoff","heading":"Definition of done for this handoff","start":242,"end":261},{"slug":"open-items-the-next-session-should-be-aware-of","heading":"Open items the next session should be aware of","start":261,"end":275},{"slug":"how-this-handoff-was-produced","heading":"How this handoff was produced","start":275,"end":296},{"slug":"see-also","heading":"See also","start":296,"end":310}],"body":"---\ntitle: \"Container Handoff — Bundle Default Cfg as the Phase-0 Floor\"\naudience: \"next-session-claude | autonomous-coding-agent | container-maintainer\"\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - handoff\n  - container\n  - default-cfg\n  - bundle\n  - phase-0\n  - bootstrap\n  - vodka-architecture\ncanonical_status: non_canonical\ndate: 2026-04-30\nwritten_at: 2026-04-30T19:35:00Z\nsession_window: \"2026-04-30T18:10Z–2026-04-30T19:35Z (~85 min wall-clock; planning + smoke test + canon authoring)\"\ncompanion_to:\n  - canon/articles/bundled-default-cfg.md\n  - canon/articles/progressive-customization.md\n  - canon/articles/payload-construction.md\n  - canon/articles/phase-1-poc-scope.md\napplied_canon:\n  - klappy://canon/principles/vodka-architecture\n  - klappy://canon/principles/dry-canon-says-it-once\n  - klappy://canon/principles/maintainability-one-person-indefinitely\n  - klappy://canon/principles/verification-requires-fresh-context\ngoverns: \"the Container-side change that lands the bundled default cfg, completing the Phase-0 floor\"\n---\n\n# Container Handoff — Bundle Default Cfg as the Phase-0 Floor\n\n> **One-line scope.** The Container ships with the upstream default cfg pre-staged so that a payload of `{books, sources}` produces a publication-quality A5 two-column Bible book with no further input from the caller.\n\n> **Why this handoff exists.** Empirical testing on 2026-04-30 found that Phase-1-strict payloads (no `config_files`) fail hard today: `\"PTXprint produced no output (silent exit)\"`. The canon claimed Phase 1 worked with PTXprint's compiled defaults, but the running Container has no such defaults available to the worker's scratch project layout. This handoff closes the gap. The cfg to bundle has been validated end-to-end: same payload minus the cfg-only `config_files` succeeded in 14.8 s with 37 pages, 0 errors. See `klappy://canon/articles/bundled-default-cfg` for the cfg itself and the empirical baseline.\n\n---\n\n## What changes\n\nThe Container repo gains:\n\n1. **A bundled cfg** at a deterministic path inside the image, source-of-truth being `klappy://canon/articles/bundled-default-cfg`.\n2. **Build-time logic** that copies that cfg from the canon source into the image at the path PTXprint expects when its scratch project lacks one.\n3. **Run-time logic** (in the Container's job runner) that, when the caller's payload has no `shared/ptxprint/Default/ptxprint.cfg` entry, stages the bundled cfg into the scratch project's Default config dir before invoking PTXprint.\n\nThe Worker is unchanged. The MCP schema is unchanged. The `submit_typeset` API surface is unchanged. The only delta is that an empty-`config_files` payload now succeeds where it used to fail.\n\n---\n\n## Vodka constraints to honor\n\nThese are the load-bearing principles for this change. If you find yourself violating one, stop — the design is wrong, not the principle.\n\n### 1. Worker holds zero domain opinion (`klappy://canon/principles/vodka-architecture`)\n\nThe Worker must not:\n\n- Detect missing `config_files` and inject a default cfg before forwarding to the Container.\n- Read this canon article at request time to fetch the bundled cfg.\n- Have any awareness that a \"default cfg\" exists.\n\nThe Worker stages exactly what the caller sent. The Container handles the absence of caller-supplied cfg files. This boundary keeps the Worker generic — vodka.\n\n**Test:** if you removed all references to \"default\" from the Worker source and the change still works, vodka holds.\n\n### 2. The cfg has one home (`klappy://canon/principles/dry-canon-says-it-once`)\n\nThe cfg lives in `klappy://canon/articles/bundled-default-cfg` (the fenced INI block at the bottom of that article). The Container build derives from it. Nothing else holds a copy.\n\nAcceptable derivation patterns at build time:\n\n- A build script extracts the INI block from the article (parse the fenced ` ```ini ... ``` ` block) and writes it to the image's bundle path.\n- A pre-build step pulls the article via `oddkit_get` or raw GitHub URL, extracts, writes.\n- A submodule or vendored copy of the canon repo with the article checked in — the build reads the article from the local path.\n\nUnacceptable patterns:\n\n- Hardcoding the cfg content into a Dockerfile or shell script.\n- Copying the cfg content into a separate file in the Container repo.\n- Embedding the cfg in the worker, the runner, or any container application code.\n\nAny of those creates two homes for the cfg, and the article and the Container will drift the moment the upstream team sends an update.\n\n**Test:** if the article changes, the next Container build picks up the change without any other repo edits. If you have to edit a Container-repo file when the cfg changes, you have two homes — refactor.\n\n### 3. The bundle is content-addressed by the Container build\n\nThe Container image hash captures the bundled cfg. If the cfg changes, the Container hash changes. Downstream cache hits / misses respect this naturally — no special handling needed.\n\nThe Worker's payload-hash computation is unchanged: same caller payload always hashes the same regardless of what cfg the Container bundles internally. This is intentional; it means callers re-rendering the same payload after a Container rebuild may pick up a different cfg without their payload-hash changing. That's the right behavior for the bundle (callers who want immutability supply their own cfg in `config_files`, locking the cfg into the payload hash).\n\n### 4. Maintainability — one person, indefinitely (`klappy://canon/principles/maintainability-one-person-indefinitely`)\n\nThe change must be small enough that a single maintainer can understand and modify it without specialized context:\n\n- One additional path in the Dockerfile (or equivalent build manifest)\n- One additional check in the runner (~5–15 lines: \"if scratch project lacks shared/ptxprint/Default/ptxprint.cfg, copy from bundle path\")\n- A regression test that submits the Phase-0 minimum payload and asserts success\n\nAnything more is over-design for what's structurally a simple staging operation.\n\n---\n\n## Implementation sketch\n\nThis is illustrative. The Container repo's actual layout dictates the exact file paths and tooling.\n\n### Dockerfile / build manifest\n\n```dockerfile\n# Existing: install PTXprint, Charis, etc.\n# ...\n\n# NEW: stage the bundled default cfg\n# The path inside the image is /opt/ptxprint-mcp/bundle/ptxprint.cfg (illustrative)\n# The build pulls from canon — choose ONE pattern:\n\n#   Pattern A: extract from a local checkout of the canon repo (vendored or submodule)\nCOPY canon-repo/canon/articles/bundled-default-cfg.md /tmp/bundled-default-cfg.md\nRUN python3 /opt/build-scripts/extract-ini-block.py \\\n        /tmp/bundled-default-cfg.md \\\n        /opt/ptxprint-mcp/bundle/ptxprint.cfg\n\n#   Pattern B: fetch at build time from raw GitHub\nRUN curl -fsSL \\\n        https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/articles/bundled-default-cfg.md \\\n        -o /tmp/bundled-default-cfg.md \\\n    && python3 /opt/build-scripts/extract-ini-block.py \\\n        /tmp/bundled-default-cfg.md \\\n        /opt/ptxprint-mcp/bundle/ptxprint.cfg\n```\n\nThe `extract-ini-block.py` helper is roughly 20 lines: open the file, find the fenced ` ```ini ` block, write the content between the fences. No heuristic parsing, no AST — just substring extraction.\n\n### Runner-side staging\n\nThe Container's job runner already builds a scratch project from the caller's `config_files` map before invoking PTXprint. Add this logic at the point where the project's Default config dir is being assembled:\n\n```python\n# Inside the runner, after staging caller-supplied config_files into scratch dir\ndefault_cfg_path = scratch_dir / \"shared\" / \"ptxprint\" / \"Default\" / \"ptxprint.cfg\"\nif not default_cfg_path.exists():\n    bundle_cfg_path = Path(\"/opt/ptxprint-mcp/bundle/ptxprint.cfg\")\n    default_cfg_path.parent.mkdir(parents=True, exist_ok=True)\n    shutil.copyfile(bundle_cfg_path, default_cfg_path)\n    log.info(\"staged bundled default cfg (caller did not supply one)\")\n```\n\nThat's the entire run-time change. Five working lines plus logging.\n\n### What does NOT need staging from the bundle\n\nWe empirically validated on 2026-04-30 that the cfg alone is sufficient — no Settings.xml, no sty, no mods, no fonts manifest. PTXprint resolves:\n\n- USFM filename pattern from the cfg's `[project] id` and the source filename\n- Font (`Charis`) from the Container's installed `fonts-sil-charis` via fontconfig\n- Stylesheet defaults from PTXprint's compiled-in marker definitions\n\nSo the bundle is **the cfg only.** Nothing else needs to be staged. If a future render uncovers a missing-asset issue (e.g., a request for a non-Latin script trips up the bare-cfg path), that's a Level-3+ concern, not a bundle concern.\n\n---\n\n## Smoke test contract\n\nA regression test must accompany the change. The test:\n\n### Inputs\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"BSB\",\n  \"books\": [\"JHN\"],\n  \"sources\": [\n    {\n      \"book\": \"JHN\",\n      \"filename\": \"44JHNBSB.usfm\",\n      \"url\": \"https://raw.githubusercontent.com/usfm-bible/examples.bsb/48a9feb71f0a66f9b8f418b11ae25b7ad2e49a0d/44JHNBSB.usfm\",\n      \"sha256\": \"f6220aa81c8143cb66a86d775fa3cdfe10efcb52dad135dfc498baeac260103d\"\n    }\n  ]\n}\n```\n\n(Note: no `config_files`, no `fonts`, no `figures`, no `define`.)\n\n### Expected outputs\n\n- `state: \"succeeded\"`\n- `failure_mode: \"success\"`\n- `exit_code: 0`\n- `errors: []`\n- `overfull_count: 0`\n- `pdf_url` non-null\n- Wall-clock: target ≤25 s container time (empirical baseline 14.8 s; budget 25 s allows headroom)\n- PDF: ~37 pages, ~377 KB ± 20%\n- PDF/X-4 conformance\n- PDF text on page 1 contains the string \"In the beginning was the Word\"\n\n### Test failure modes that block the merge\n\n- `state == \"failed\"` with `errors: [\"PTXprint produced no output (silent exit). ...\"]` → bundle staging is broken\n- PDF size <50 KB or >2 MB → render is degenerate or generating filler\n- Wall-clock >60 s → something's catastrophically wrong (cold container shouldn't exceed 30 s; 60 is the safety net)\n- PDF text doesn't contain expected verse → font/encoding issue (likely Charis substitution failure)\n\n### Reference artifacts\n\nThe 2026-04-30 baseline render is available at:\n\n- PDF: `https://ptxprint-mcp.klappy.workers.dev/r2/outputs/d37778c8c2467a07e395718fe1a3477f25cec7b8a55dfd4927af72bee75ba29d/BSB_Default_JHN_ptxp.pdf`\n- Log: `https://ptxprint-mcp.klappy.workers.dev/r2/outputs/d37778c8c2467a07e395718fe1a3477f25cec7b8a55dfd4927af72bee75ba29d/BSB_Default_JHN_ptxp.log`\n- Payload hash: `d37778c8c2467a07e395718fe1a3477f25cec7b8a55dfd4927af72bee75ba29d`\n\nNote: that baseline was produced with the cfg explicitly in `config_files`. The Phase-0 (no-cfg) payload hash will be different — that's correct, it has different inputs. The PDF should be visually equivalent.\n\n---\n\n## Architectural decisions made in the canon authoring (carry these into the implementation)\n\n### Decision: replacement-only when caller supplies a cfg\n\nThe Container stages caller-supplied `config_files` exactly as sent. If the caller supplies `shared/ptxprint/Default/ptxprint.cfg`, the bundle is ignored entirely for this run. **No section-level merging.** This was a deliberate call to avoid silent surprises in mixed-cfg renders.\n\nImplementation implication: in the runner staging logic above, the `if not default_cfg_path.exists()` check guarantees this. Don't add merge logic.\n\n### Decision: `define` is the override path for one-knob changes\n\nCallers wanting to tweak one cfg value without replacing the whole cfg use `define = {\"<widget_id>\": \"<value>\"}`, which the Container passes as `-D key=value` flags to PTXprint. This is already in the schema and already implemented; this handoff doesn't change it.\n\n### Deferred: parent-config inheritance via `[import] config = Default`\n\nA future enhancement: PTXprint's native `[import] config = Parent` inheritance would let callers send a partial cfg with the bundle as the parent. This requires the Container to keep the bundled cfg accessible at a parent-config path AND callers to declare the import correctly. Not in scope for this handoff.\n\nIf you implement this later, the Container would stage the bundle at `shared/ptxprint/Default/ptxprint.cfg` regardless of caller input, and callers would supply child configs at e.g. `shared/ptxprint/MyChild/ptxprint.cfg` declaring `[import] config = Default`. The runtime `config_name` flag would target the child. This works in PTXprint today; the only blocker is naming the bundle's parent path consistently and documenting it. Out of scope for this PR.\n\n---\n\n## Definition of done for this handoff\n\nThe handoff is complete when all of the following are true:\n\n1. **Container build** stages the bundled cfg from `klappy://canon/articles/bundled-default-cfg` at a deterministic path inside the image.\n2. **Container runner** copies the bundle into the scratch project's Default config path when caller-supplied `config_files` lacks one.\n3. **Worker source** has not been modified.\n4. **Smoke test** (the one above) passes against the rebuilt Container.\n5. **Canon updates** land in the same coordinated PR or sequence:\n   - `canon/articles/bundled-default-cfg.md` (the source of truth) — already drafted\n   - `canon/articles/progressive-customization.md` — already drafted\n   - `canon/articles/payload-construction.md` — top caveat-box rewritten per `payload-construction.EDIT.md`\n   - `canon/articles/phase-1-poc-scope.md` — supersession frontmatter + redirect note per `phase-1-poc-scope.EDIT.md`\n6. **Fresh-context validation** (`klappy://canon/principles/verification-requires-fresh-context`): a separate session retrieves the new articles via `docs`, builds a Phase-0 payload from the canon-described pattern, submits it against the rebuilt Container, and confirms success.\n\nThe fresh-context validation is what closes the loop. Same-session \"looks done\" doesn't count; another agent should be able to read only the canon and produce a working Phase-0 render. If that agent has to ask any clarifying question, the canon is incomplete — fix the canon, then re-validate.\n\n---\n\n## Open items the next session should be aware of\n\nThese are not blockers for this handoff, but they're flagged so the next session catches them:\n\n1. **The `english-single-book` template is now Level-4-overkill for new agents.** It carries Settings.xml, custom.sty, ptxprint.sty, ptxprint-mods.sty, and a Gentium fonts manifest — all useful as a reference, but no longer the bootstrap surface. Consider reframing the template's frontmatter: `applies_to` should mention \"fully-customized example for diglot-ready, picture-list-ready Gentium publishing\"; `caller_supplies` is unchanged. A separate `english-single-book-default` template wrapping just the Phase-0 payload could land later as a Level-0 template. Not urgent; doesn't block this PR.\n\n2. **The \"bundled cfg\" article's `[import]` section caveat (item 5 in \"Known characteristics worth knowing as an agent\") deserves cross-linking from `config-inheritance-and-overrides.md`.** Right now an agent reading the inheritance article might assume the upstream cfg's `[import]` section uses the inheritance pattern. It doesn't — the section is for the GUI's import-settings workflow. Adding a one-paragraph \"Note: not all `[import]` sections are inheritance declarations\" cross-ref would prevent confusion.\n\n3. **Underfill rate observation (~59% on John).** The bundled cfg's tight typography produces visible underfill on light-content pages. This is documented in the bundled-default-cfg article as Known Characteristic #3 with mitigations. If user feedback says \"the pages look empty,\" the response is to switch to autofill mode (deferred upstream) or to relax the typography via `define = {\"s_linespacing\": \"13\"}` etc. Not a bundle bug; a bundle characteristic.\n\n4. **The cfg has no `[vars]` section.** PDF metadata `Title` shows `[Unknown]`. Most production agents will want to set `vars.maintitle`. The progressive-customization article documents this at Level 1 (`define = {\"vars_maintitle\": \"John\"}` or equivalent — the exact widget ID is worth verifying against the PTXprint UI introspection dump). If the widget ID differs from `vars_maintitle`, update the progressive-customization article's Level-1 examples.\n\n---\n\n## How this handoff was produced\n\nThis document was authored in the same Claude session as the empirical validation that revealed the gap. The session:\n\n1. Loaded canon catalog (60 docs) and read `composition-and-templates`, `english-single-book`, `payload-construction`, `phase-1-poc-scope`, and `bundled-default-cfg` (which didn't exist yet).\n2. Submitted a cfg-only payload to the running PTXprint MCP — succeeded in 14.8 s with 37-page PDF.\n3. Submitted a Phase-1 strict payload (no cfg) — failed hard, exposing the canon-vs-reality drift.\n4. Drafted four canon updates (bundled-default-cfg new article, progressive-customization new article, payload-construction edit instructions, phase-1-poc-scope supersession instructions).\n5. Drafted this Container handoff.\n\nPer `klappy://canon/principles/verification-requires-fresh-context`, this same session is **not** the right place to validate the canon. A fresh session should:\n\n- Read `progressive-customization.md` from canon.\n- Build the Phase-0 payload it describes against a fresh USFM source.\n- Submit. Observe. Compare to the smoke-test contract above.\n- Report drift, if any. Cross-check against `bundled-default-cfg.md`'s claims about what the bundle produces.\n\nThat fresh-context loop closes the validation. This document is a planning artifact + execution-ready spec; it is not itself the validation.\n\n---\n\n## See also\n\n- `klappy://canon/articles/bundled-default-cfg` — the cfg this handoff is bundling\n- `klappy://canon/articles/progressive-customization` — the override ladder this handoff enables\n- `klappy://canon/articles/payload-construction` — schema reference (post-edit)\n- `klappy://canon/articles/phase-1-poc-scope` — historical reference (post-supersession)\n- `klappy://canon/specs/ptxprint-mcp-v1.2-spec` — the spec this handoff conforms to (no spec changes needed)\n- `klappy://canon/principles/vodka-architecture` — the boundary this handoff respects\n- `klappy://canon/principles/verification-requires-fresh-context` — why the next session validates, not this one\n\n---\n\n*The cfg is in canon. The Container reads canon. The Worker reads neither. The bundle is the floor; everything else stacks on top.*\n"},{"path":"handoffs/documentation-cleanup-handoff.md","uri":"klappy://canon/handoffs/documentation-cleanup-handoff","title":"Documentation Cleanup Handoff — Sorted Priorities for the ptxprint-mcp Canon Repo","audience":"operator","tags":["handoff","documentation-cleanup","priorities","audit-followup","v1.2-bootstrap","ptxprint-mcp"],"stability":"working","what_it_answers":"**Scope.** This handoff covers the cleanup track only — stale references, supersession markers, structural fixes. New article authoring (the five gating articles) and external feature requests (oddkit KB isolation) are tracked separately. T","sections":[{"slug":"1-three-tracks-so-this-handoff-stays-in-its-lane","heading":"1. Three Tracks (so this handoff stays in its lane)","start":22,"end":34},{"slug":"2-the-font-docs-question-answered-directly","heading":"2. The Font Docs Question — Answered Directly","start":34,"end":47},{"slug":"3-p0-cleanup-inventory","heading":"3. P0 Cleanup Inventory","start":47,"end":140},{"slug":"4-p4-long-tail-deferred-until-after-v1-2-smoke-test","heading":"4. P4 Long-Tail (Deferred Until After v1.2 Smoke Test)","start":140,"end":158},{"slug":"5-recommended-sequencing","heading":"5. Recommended Sequencing","start":158,"end":183},{"slug":"6-carry-overs-from-prior-handoffs-cleanup-track-subset-only","heading":"6. Carry-Overs from Prior Handoffs (Cleanup-Track Subset Only)","start":183,"end":195},{"slug":"7-how-to-use-this-handoff","heading":"7. How to Use This Handoff","start":195,"end":209},{"slug":"8-provenance","heading":"8. Provenance","start":209,"end":221}],"body":"---\ntitle: \"Documentation Cleanup Handoff — Sorted Priorities for the ptxprint-mcp Canon Repo\"\naudience: operator\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"handoff\", \"documentation-cleanup\", \"priorities\", \"audit-followup\", \"v1.2-bootstrap\", \"ptxprint-mcp\"]\ncanonical_status: non_canonical\ncreated_at: 2026-04-28T12:30:00Z\ncompanion_to: \"canon/handoffs/oddkit-kb-isolation-feature-request.md, canon/specs/ptxprint-mcp-v1.2-spec.md\"\nderives_from: \"audit session 2026-04-28 (KB-as-knowledge-base-url probe matrix)\"\ntarget_outcome: \"All P0 cleanup applied before the next autonomous coding run, so the canon repo presents a coherent surface to that agent.\"\n---\n\n# Documentation Cleanup Handoff — Sorted Priorities\n\n> **Scope.** This handoff covers the cleanup track only — stale references, supersession markers, structural fixes. New article authoring (the five gating articles) and external feature requests (oddkit KB isolation) are tracked separately. Total estimated effort for P0: ~25 minutes of mechanical edits plus a commit.\n\n> **Why this exists.** An audit session on 2026-04-28 set the ptxprint-mcp repo as `knowledge_base_url` against oddkit and ran ten realistic agent queries. The probe surfaced (a) staleness — handoffs marked open whose work has been applied, (b) supersession gaps — an obsolete font-resolution article presenting as live, and (c) corpus-composition issues separately tracked in the oddkit handoff. This document sorts those findings into prioritized, mechanical work.\n\n---\n\n## 1. Three Tracks (so this handoff stays in its lane)\n\n| Track | What | Where it lives |\n|---|---|---|\n| **1. Documentation cleanup** (this handoff) | Stale refs, supersession, frontmatter, structural fixes | here |\n| **2. New article authoring** | Five gating articles for v1.2 agent-usability | `canon/specs/ptxprint-mcp-v1.2-spec.md` §2, §10; `canon/handoffs/session-3-gaps-handoff.md` §2.2 |\n| **3. External (oddkit feature)** | KB isolation flag — stops baseline contamination of search | `canon/handoffs/oddkit-kb-isolation-feature-request.md` |\n\nTracks 2 and 3 are tracked in their own durable handoffs. This document does not duplicate them.\n\n---\n\n## 2. The Font Docs Question — Answered Directly\n\nThe operator asked whether the font docs need a separate handoff. **No.** Reasoning:\n\nThe font work splits into two phases:\n\n1. **Mechanical cleanup** — mark `canon/articles/font-resolution-design.md` as superseded; archive it. Belongs in P0 below as a single 5-minute edit.\n2. **Authoring the replacement** — `canon/articles/font-resolution.md` is named in the v1.2 spec §2 critical-articles list and the resolution path is captured in `session-3-gaps-handoff.md` §2.2 (carries forward F-Q1, F-Q2, F-Q5, F-Q6 from the session-3 font-resolution thread).\n\nA standalone font handoff would duplicate phase 2 and bloat phase 1. Phase 1 is an item below. Phase 2 stays in the v1.2 spec and the session-3 gaps handoff.\n\n---\n\n## 3. P0 Cleanup Inventory\n\nFour items, sorted by dependency (later items don't block on earlier ones, but applying P0.1 → P0.2 → P0.3 → P0.4 is the cleanest order). All editable by the operator with the bootstrap PAT, or delegable to a small focused session.\n\n### P0.1 — Supersede and archive `canon/articles/font-resolution-design.md`\n\n**What's wrong.** This article (v0.2-draft, 2026-04-28) describes the \"Three-MCP Architecture\" — content-cache MCP + fonts MCP + thin PTXprint integration. The v1.2 spec D-021 retired that approach (fonts became payload entries fetched at job time; no separate fonts MCP). The article has no `superseded_by` frontmatter field and ranks #3 for queries like \"Cloudflare Worker Container Durable Object PTXprint\" — agents will read it as live guidance. Its `companion_to` field points at `ptxprint-mcp-v1-spec.md` (now archived) and `transcript-encoded-session-3-1.md` (consolidated into session-3 proper).\n\n**What to do.**\n\n(a) Add to the frontmatter:\n```yaml\nstatus: superseded\nsuperseded_by: \"canon/articles/font-resolution.md (TODO — see canon/specs/ptxprint-mcp-v1.2-spec.md §2)\"\nsupersession_reason: \"v1.2 D-021 absorbed font fetching into the payload schema; the three-MCP design is no longer the build target. Retained as design history.\"\n```\n\nUpdate the `companion_to` field to point at `canon/specs/archive/ptxprint-mcp-first-pass-poc.md` and remove the dangling `transcript-encoded-session-3-1.md` reference.\n\n(b) Add a top-of-file blockquote immediately after the title:\n```markdown\n> **Status: superseded.** This article describes a three-MCP architecture that v1.2 D-021 retired. v1.2 makes fonts payload entries fetched and verified by the Container at job time; no separate fonts MCP is built. This article is retained as design history of the path not taken; it is not live guidance. The replacement article `canon/articles/font-resolution.md` (per v1.2 spec §2) is the authoritative source once authored.\n```\n\n(c) Move the file to `canon/articles/_archive/font-resolution-design.md` (the `_archive/` location is already foreseen in `canon/PENDING_UPLOADS.md` §4). Update the `canon/README.md` `articles/` description to reflect the `_archive/` subdirectory.\n\n**Effort.** ~5 minutes.\n\n**Commit.** `cleanup: supersede and archive font-resolution-design (v1.2 D-021 retired three-MCP path)`\n\n---\n\n### P0.2 — Update stale handoff and inventory markers\n\n**What's wrong.** Three documents reference work that has since been completed but were never updated:\n\n(a) `canon/handoffs/governance-update-handoff.md` — the 447-line edit list whose changes have been applied (`canon/governance/headless-operations.md` frontmatter declares `v1.2-aligned` and `updated_at: 2026-04-28T04:06:00Z`; the blockquote text matches the handoff's \"replace with\"). The handoff itself carries no resolution marker.\n\n(b) `canon/handoffs/missing-uploads-handoff.md` — Prompt A asks the slides-ESE session to upload `ptxprint-master-slides.surface.md/.json`, the training manual, and governance Parts 3–12. All four are in the repo (commit `4c84979`). The prompt is now describing solved work.\n\n(c) `canon/PENDING_UPLOADS.md` — the body jumps from the §0 header straight to §4 (sections §1–§3 appear excised mid-edit). The \"What's already in the repo\" table omits `transcript-encoded-session-3.md` and `canon/articles/font-resolution-design.md` (both committed in `24275fd`).\n\n(d) `canon/README.md` \"Status of canon content\" — claims \"v1.0 and v1.1 are historical and not in the repo\" (both are) and \"encodings 1–4 are referenced but may not yet be in this repo\" (all five are). Lists `articles/` as \"planned\" while the directory exists.\n\n**What to do.**\n\nFor (a): Add a `## Resolution` section at the top of `governance-update-handoff.md`:\n```markdown\n> **Resolved 2026-04-28.** All edits in this handoff have been applied to `canon/governance/headless-operations.md`. The frontmatter `updated_at: 2026-04-28T04:06:00Z` and the v1.2-aligned blockquote in §0 reflect the integrated changes. This document is retained as a historical record of what was changed; do not re-apply.\n```\n\nFor (b): Add a similar resolution note at the top of `missing-uploads-handoff.md`:\n```markdown\n> **Prompt A resolved 2026-04-28.** The slides ESE surface (`canon/surfaces/ptxprint-master-slides.surface.md/.json`), training manual (`canon/derivatives/ptxprint-training-manual.md`), and governance Parts 3–12 + Provenance (`canon/governance/headless-operations.md`) are all committed. Prompt A is no longer needed. Prompts B and C status — verify before reusing.\n```\n\nFor (c): Restore §1–§3 (or replace them with a single \"All resolved\" note + the corrected inventory table that includes session-3 encoding and font-resolution-design). Add the two missing entries to the \"What's already in the repo\" table.\n\nFor (d): Update the \"Status of canon content\" section:\n- \"Specs: v1.0, v1.1, and v1.2 are all in the repo at `canon/specs/`. The original 17-tool PoC is at `canon/specs/archive/`.\"\n- \"Encodings: all five sessions (1–5) are in `canon/encodings/`.\"\n- \"Articles: `canon/articles/` exists with one item; the v1.2 gating articles (per spec §2) are not yet authored.\"\n\n**Effort.** ~10 minutes total across the four files.\n\n**Commit.** `cleanup: mark resolved handoffs, fix stale repo-status references`\n\n---\n\n### P0.3 — Commit the oddkit KB-isolation feature-request handoff\n\n**What's wrong.** The handoff was authored in this session and lives at `/mnt/user-data/outputs/oddkit-kb-isolation-feature-request.md`. It's not yet in the repo.\n\n**What to do.** Place the file at `canon/handoffs/oddkit-kb-isolation-feature-request.md`. Optionally also file a klappy/oddkit GitHub issue linking back to it.\n\n**Effort.** ~5 minutes for the commit; ~5 more minutes if the issue is filed at the same time (the handoff body is already issue-shaped).\n\n**Commit.** `handoffs: oddkit KB-isolation feature request (audit-derived, addresses search-corpus contamination)`\n\n---\n\n### P0.4 — Commit *this* cleanup handoff\n\n**What's wrong.** Same as P0.3 — the deliverable artifact lives at `/mnt/user-data/outputs/documentation-cleanup-handoff.md` and needs to land in the repo to be durable.\n\n**What to do.** Place the file at `canon/handoffs/documentation-cleanup-handoff.md`.\n\n**Effort.** ~3 minutes.\n\n**Commit.** `handoffs: consolidated documentation cleanup priorities (audit-derived)`\n\n---\n\n## 4. P4 Long-Tail (Deferred Until After v1.2 Smoke Test)\n\nThese improve search/retrieval quality but are not gating for the v1.2 build. Track here so they don't get lost.\n\n### P4.1 — Split the governance monolith\n\n`canon/governance/headless-operations.md` is 1,182 lines covering Parts 0–12 + Provenance. BM25 dilutes per-section relevance across that mass — the doc never wins specific queries. Twelve focused files (`headless-contract-part-0.md`, `cli-reference-part-1.md`, etc., or topical names) would each rank #1 for their topic. Estimated effort: 2–3 hours of careful splitting + cross-link audit.\n\nAlternative cheap version: leave the doc whole but add per-Part h2 anchors with topic-specific tags so search can target sections. ~30 minutes; partial fix; revisit if insufficient.\n\n### P4.2 — Audit frontmatter tags against natural agent queries\n\nRule: every tool name (`submit_typeset`, `cancel_job`, `get_upload_url`, `get_job_status`), every payload field (`config_files`, `sources`, `fonts`, `figures`, `mode`, `define`), and every operational concept (`autofill`, `simple-typesetting`, `cache-hit`, `cancellation`, `failure-mode`, `hard-failure`, `soft-failure`) should appear as a tag on at least one doc that authoritatively explains it. This is what makes BM25 surface the right docs against natural-language queries. Estimated effort: ~45 minutes for a single careful pass across the existing 19 indexed docs.\n\nNote: this is most leveraged AFTER the new gating articles land (Track 2), since those articles are where the tags should accumulate. Do P4.2 once Track 2 is well underway.\n\n---\n\n## 5. Recommended Sequencing\n\n```\nTonight (operator, ~25 min mechanical)\n  └─ P0.1 → P0.2 → P0.3 → P0.4\n     (After commit, verify with: oddkit_search \"submit_typeset payload\"\n                                  knowledge_base_url=https://github.com/Klappy/ptxprint-mcp\n                                  Expect: stale font-resolution-design no longer ranks as live)\n\nParallel during the autonomous build (separate session)\n  ├─ Track 2 — start with P1.4 (payload-construction.md) and P1.5 (output-naming.md)\n  │   from v1.2 spec §10's \"Critical-path canon work\" — these unblock DoD step #2\n  ├─ Track 3 — file oddkit-kb-isolation handoff as klappy/oddkit GitHub issue\n  └─ session-3-gaps-handoff §3 — delegate the deferred ESE-vs-v1.2 gap analysis\n\nAfter the smoke test passes (sequence)\n  ├─ Track 2 — P2.6 failure-mode-taxonomy.md (highest leverage for downstream agents)\n  ├─ Track 2 — P2.7 config-construction.md\n  ├─ Track 2 — P2.8 font-resolution.md (replaces the archived font-resolution-design)\n  ├─ P4.2 frontmatter tag audit\n  └─ P4.1 governance doc split (cheap version first; full split if needed)\n```\n\n---\n\n## 6. Carry-Overs from Prior Handoffs (Cleanup-Track Subset Only)\n\nThese are still owed; they don't have a natural home in any specific cleanup item above. Tracking here so they aren't lost.\n\n| ID | Origin | What | Status |\n|---|---|---|---|\n| H-005 | session 2 | Verify ptx2pdf licence before mirroring docs from `sillsdev/ptx2pdf/docs/documentation/` | Still owed; gates any canon content copied from upstream. ~5 minutes (read LICENSE at repo root). |\n| H-006 | session 2 | Filter the operator's ~1000-config corpus for sensitivity before agent ingestion | Still owed; non-blocking for v1.2 build because the corpus isn't the smoke-test fixture. |\n| H-009 / F-Q3 | session 3 | Verify clean Ubuntu container with no `fonts-*` packages | Belongs to Track 2 / Dockerfile work, not cleanup. Mentioned here only to confirm it's not lost. |\n\n---\n\n## 7. How to Use This Handoff\n\nThree options, ordered by speed:\n\n1. **Operator runs P0 directly tonight.** ~25 minutes of mechanical edits with a text editor + git push. The frontmatter changes are small, the resolution notes are short, and the file moves are obvious. This is the fastest path to a clean canon surface for tomorrow's run.\n\n2. **Delegate P0 to a focused session.** The four items are mechanically independent; a session given this handoff and the bootstrap PAT can apply all four in one pass. Estimated session length: 30–40 minutes including verification.\n\n3. **Defer P0 and accept the canon's current presentation for the autonomous build.** The autonomous coder reads the v1.2 spec by URI (which works) and builds against it. The stale font article will not appear in the spec it's reading. The risk is the *next* agent — the one driving the deployed server — encountering the obsolete article through search. That risk is bounded by the deployed agent also having access to the v1.2 spec by URI; if it's reasoning carefully, it cross-references and detects the inconsistency. This is acceptable as a deferral if other priorities crowd the operator tonight, but P0.1 specifically is a 5-minute edit that meaningfully reduces downstream confusion and should not be deferred without reason.\n\nP4 items are explicitly deferrable; do not attempt them before the smoke test passes.\n\n---\n\n## 8. Provenance\n\n- **Author:** Claude Opus 4.7 (interactive session, 2026-04-28).\n- **Operator:** klappy.\n- **Source audit:** Same session that produced `oddkit-kb-isolation-feature-request.md`. Empirical evidence (probe matrix) lives there; this handoff carries forward only the cleanup-track conclusions.\n- **Correction applied during authoring:** The audit's earlier \"P0.3 — resolve 19-vs-24 indexing gap\" item was based on a miscount. Re-verification (`find canon/ -name '*.md'` + frontmatter check) shows the actual count is 19-of-22, with the three unindexed files (`README.md`, `CONTRIBUTING.md`, `canon/specs/archive/ptxprint-mcp-first-pass-poc.md`) deliberately and correctly excluded from the search index. That item was dropped from this handoff.\n- **Containment:** Non-canonical. This is a working document that should be marked resolved (similar to the resolution markers it asks the operator to add to other handoffs) once the P0 items have landed, and ultimately deleted once P4 is also complete or explicitly punted.\n\n---\n\n*End of handoff. Companion artifacts: `oddkit-kb-isolation-feature-request.md` (Track 3), `ptxprint-mcp-v1.2-spec.md` §2 + §10 (Track 2 scope).*\n"},{"path":"handoffs/governance-update-handoff.md","uri":"klappy://canon/handoffs/governance-update-handoff","title":"Governance Update Handoff — Aligning the Headless KB with v1.2","audience":"operator","tags":["ptxprint","governance","handoff","v1.2-alignment"],"stability":"working","what_it_answers":"**✅ Resolved 2026-04-28.** All edits in this handoff have been applied to `canon/governance/headless-operations.md`. The frontmatter `updated_at: 2026-04-28T04:06:00Z` and the v1.2-aligned blockquote in §0 reflect the integrated changes. Th","sections":[{"slug":"pre-edit-framing-changes-for-the-document-as-a-whole","heading":"Pre-edit: framing changes for the document as a whole","start":22,"end":36},{"slug":"part-0-the-headless-operating-contract","heading":"Part 0 — The Headless Operating Contract","start":36,"end":81},{"slug":"part-1-the-cli-reference","heading":"Part 1 — The CLI Reference","start":81,"end":117},{"slug":"part-2-file-system-map","heading":"Part 2 — File System Map","start":117,"end":161},{"slug":"part-3-the-configuration-model","heading":"Part 3 — The Configuration Model","start":161,"end":202},{"slug":"part-4-the-override-mechanism","heading":"Part 4 — The Override Mechanism","start":202,"end":234},{"slug":"part-5-settings-cookbook-by-user-intent","heading":"Part 5 — Settings Cookbook (by user intent)","start":234,"end":248},{"slug":"part-6-supporting-files","heading":"Part 6 — Supporting Files","start":248,"end":282},{"slug":"part-7-usfm-in-headless-context","heading":"Part 7 — USFM in Headless Context","start":282,"end":288},{"slug":"part-8-workflow-recipes","heading":"Part 8 — Workflow Recipes","start":288,"end":352},{"slug":"part-9-diagnostic-patterns","heading":"Part 9 — Diagnostic Patterns","start":352,"end":372},{"slug":"part-10-conversational-patterns","heading":"Part 10 — Conversational Patterns","start":372,"end":378},{"slug":"part-11-settings-that-need-special-handling","heading":"Part 11 — Settings That Need Special Handling","start":378,"end":398},{"slug":"part-12-open-gaps-in-this-kb","heading":"Part 12 — Open Gaps in This KB","start":398,"end":419},{"slug":"provenance-update","heading":"Provenance update","start":419,"end":429},{"slug":"final-pass-search-for-stale-references","heading":"Final pass: search for stale references","start":429,"end":450}],"body":"---\ntitle: \"Governance Update Handoff — Aligning the Headless KB with v1.2\"\naudience: operator\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"governance\", \"handoff\", \"v1.2-alignment\"]\ncreated_at: 2026-04-28T03:45:00Z\ncompanion_to: \"ptxprint-mcp-v1.2-spec.md\"\ntarget_document: \"PTXprint Headless Operations — Knowledge Base for AI Agents and Assistants (the document the operator extracted from the deck PDF)\"\n---\n\n# Governance Update Handoff — Aligning the Headless KB with v1.2\n\n> **✅ Resolved 2026-04-28.** All edits in this handoff have been applied to `canon/governance/headless-operations.md`. The frontmatter `updated_at: 2026-04-28T04:06:00Z` and the v1.2-aligned blockquote in §0 reflect the integrated changes. This document is retained as a historical record of what was changed; **do not re-apply**. (See `canon/handoffs/documentation-cleanup-handoff.md` §3 P0.2 for context.)\n\n> **What this document is.** A specific set of edits to apply to the headless operations governance document so it speaks v1.2's stateless build-system architecture instead of v1.0/v1.1's filesystem-edit-in-place model. Most of the governance survives untouched; the changes are surgical. Estimated effort: 30–60 minutes for a careful pass, faster if delegated to another session with this document as input.\n\n> **How to use this.** Apply edits in the order listed. Each edit names the section, the change, and a suggested replacement (where text is being added or modified). Sections not mentioned do not need changes.\n\n---\n\n## Pre-edit: framing changes for the document as a whole\n\n### Add to the front matter / opening summary\n\nCurrent framing positions the agent as one that edits files in a project tree on disk. v1.2's framing is that the agent **constructs payloads** and **submits jobs** to a stateless build system, with project state managed elsewhere.\n\n**Suggested addition near the top of the doc** (e.g. as a new paragraph in the opening blockquote section):\n\n> **Architecture context.** The PTXprint MCP server is a stateless content-addressed build system. The agent does not edit files inside a sandboxed project tree on the server — it reads project state from wherever the user keeps it (Claude Desktop's filesystem access, Git, DBL, the user's Paratext server) and constructs a payload describing one typesetting job. The payload is submitted to the server, which dispatches it to an ephemeral worker container that materializes the inputs, runs PTXprint, and uploads the PDF to R2. The agent receives a job_id and polls for status.\n>\n> Iterative editing in this model means re-submitting an updated payload, not editing files in place. Each typesetting run is fully reproducible from its payload; outputs are content-addressed by sha256 of the payload, so re-submitting an unchanged build returns the cached result for free.\n\n---\n\n## Part 0 — The Headless Operating Contract\n\n### Edit 0.1 — Replace the \"What 'headless' means here\" subsection\n\n**Find:** the paragraph beginning *\"A headless PTXprint run is a subprocess invocation that takes a project directory plus a named configuration plus optional book and setting overrides...\"*\n\n**Replace with:**\n\n> A headless PTXprint run from the agent's perspective is a job submission. The agent constructs a payload containing the configuration files (inline text), URLs for the binary inputs (USFM source, fonts, figures), and the books to typeset. The payload is submitted via `submit_typeset` and returns a job_id immediately. An ephemeral Cloudflare Container worker picks up the job, materializes a working directory from the payload, runs PTXprint, and uploads the resulting PDF to R2. The agent polls `get_job_status` to track progress and retrieve the result URL.\n\n### Edit 0.2 — Update the inputs/outputs table\n\n**Find:** the table with `Inputs / Inputs (optional) / Outputs (success) / Outputs (failure) / Side effects` rows.\n\n**Replace with:**\n\n| Inputs | A payload (see §X for schema) containing: project_id, config_name, books, mode (simple\\|autofill), inline `config_files` (cfg, sty, changes.txt, FRTlocal.sfm, AdjLists, piclist, override files), and URL+sha256 references to USFM sources, fonts, and figures |\n| Outputs (success) | A presigned R2 URL to a PDF at `outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf` plus a presigned URL to the corresponding XeTeX log |\n| Outputs (failure) | A `failure_mode: hard \\| soft` classification, a log_tail, an `errors` list, and (if soft failure) a presigned URL to the degraded PDF |\n| Side effects | None on the agent's side. Worker scratch dir is wiped when the Container sleeps. R2 outputs persist per the configured retention policy. |\n\n### Edit 0.3 — The \"Three failure modes\" subsection survives intact\n\nNo changes needed. The hard/soft/success taxonomy is exactly what `get_job_status.failure_mode` carries.\n\n### Edit 0.4 — Replace the \"What headless cannot do that the GUI can\" introduction\n\nThe list of GUI-only features is still accurate, but the substitutes need updating since file edits no longer happen on a server-side filesystem.\n\n**Find:** *\"The agent's substitute is to write AdjList files (see Part 7)\"* and similar phrases.\n\n**Replace each occurrence with:** *\"The agent's substitute is to include the AdjList content as part of the payload's `config_files` map at the appropriate relative path.\"*\n\nApply the same pattern to the piclist and FRTlocal.sfm references — they all become payload inclusions, not direct file writes.\n\n### Edit 0.5 — Update \"Where this knowledge lives at runtime\"\n\nThe escalation ladder is still right, but step 3 references a `describe_setting` tool that doesn't exist in v1.2.\n\n**Find:** *\"If the MCP server exposes a `describe_setting` tool, query it for the running tooltip and the cfg-key mapping.\"*\n\n**Replace with:** *\"Search canon for the cfg-key mapping (the tooltip dump is canon-seeded). If the operator has exposed the running tooltip dump as a separate canon document, retrieve that for authoritative current-version info.\"*\n\n---\n\n## Part 1 — The CLI Reference\n\n### Edit 1.1 — Reframe as \"what the worker invokes,\" not \"what the agent invokes\"\n\n**Find:** the section heading *\"## Part 1 — The CLI Reference\"* and its introductory framing.\n\n**Insert at the top of the section:**\n\n> **In v1.2, the agent does not invoke this CLI directly.** The Container worker invokes it on behalf of the agent's submitted payload. This reference is here because the payload's `define`, `books`, `config_name`, and `project_id` fields map directly to PTXprint's CLI flags, and understanding the CLI surface helps the agent construct payloads that will work.\n\nThe rest of Part 1 (flag reference, exit codes, `-D` mechanism) survives intact.\n\n### Edit 1.2 — Update the \"When to use `-D` instead of writing to `ptxprint.cfg`\" subsection\n\nThe dichotomy \"use -D vs write to cfg\" is now \"use the payload's `define` field vs include the override in `config_files`.\" Same trade-off, different mechanics.\n\n**Replace the entire subsection** with:\n\n> **When to use the payload's `define` field:**\n> - The override is for this run only (e.g., \"produce a draft with 13pt spacing\" without changing the working ptxprint.cfg state)\n> - The user is exploring options and the agent doesn't want to mutate the working config\n> - The run is parameterised (quarterly auto-generation with rotating output formats)\n>\n> **When to include the change in `config_files[\"shared/ptxprint/<config>/ptxprint.cfg\"]`:**\n> - The change should persist across iterations (the agent updates its working config state and includes the new content in subsequent payloads)\n> - The user is committing to a finalised configuration\n> - The change is part of a config the user will eventually push back to their project store (Git / Paratext / wherever they keep it)\n\n### Edit 1.3 — Drop the \"subprocess vs Python module\" subsection\n\nThe v1.2 Container uses subprocess invocation as a settled implementation detail. The trade-off discussion is no longer agent-relevant.\n\n**Action:** delete the subsection *\"### The relationship to the Python module\"* through the end of Part 1.\n\n---\n\n## Part 2 — File System Map\n\n### Edit 2.1 — Reframe as \"what the worker materializes,\" not \"what the agent navigates\"\n\n**Insert at the top of Part 2:**\n\n> **In v1.2, this filesystem layout is what the Container worker materializes inside its scratch directory before running PTXprint.** The agent does not see this tree; it sees the payload (described in Part X). The `config_files` map keys map directly to relative paths in this tree; the `sources` array entries become USFM files at appropriate locations; the `fonts` and `figures` arrays populate the worker's font and image directories. Understanding this layout helps the agent choose correct relative-path keys when constructing the payload's `config_files` map.\n\n### Edit 2.2 — Drop the \"What the agent reads vs. writes\" subsection\n\nThis entire subsection assumes filesystem access. **Action:** delete the subsection.\n\n**Replace with a new subsection:**\n\n> ### What the agent constructs in the payload\n>\n> The agent's reasoning surface for \"what files matter\" maps onto payload slots:\n>\n> - **`config_files`** map (inline text):\n>   - `shared/ptxprint/<config>/ptxprint.cfg` — primary settings\n>   - `shared/ptxprint/<config>/ptxprint.sty` — config-level stylesheet\n>   - `shared/ptxprint/<config>/ptxprint-mods.sty` — additional style mods (optional)\n>   - `shared/ptxprint/<config>/changes.txt` — config-level USFM transforms (optional)\n>   - `shared/ptxprint/<config>/FRTlocal.sfm` — front matter content (optional)\n>   - `shared/ptxprint/<config>/<PRJ>-<config>.piclist` — picture placements (optional)\n>   - `shared/ptxprint/<config>/AdjLists/<num><BOOK><PRJ>-<config>.SFM.adj` — paragraph adjustments (optional, one per book that has adjustments)\n>   - `shared/ptxprint/ptxprint_project.cfg` — project-level overrides (optional, see Part 4)\n>   - `shared/ptxprint/<config>/ptxprint_override.cfg` — config-level overrides (optional, see Part 4)\n> - **`sources`** array (URL + sha256): one entry per book being typeset\n> - **`fonts`** array (URL + sha256): one entry per font file the project references\n> - **`figures`** array (URL + sha256, optional): one entry per image file referenced by the piclist\n\n### Edit 2.3 — Update the output-filename-convention paragraph\n\nThe convention itself is unchanged. The paragraph just needs a reframing of where the file appears.\n\n**Find:** *\"The agent can predict the output path before running by reading `[project] bookscope`...\"*\n\n**Replace with:**\n\n> The PDF output appears at `r2://ptxprint-outputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf` where `<payload_hash>` is the sha256 of the canonical payload. The Worker returns a presigned URL to this path. `submit_typeset` also returns a `predicted_pdf_url` field synchronously when the payload is submitted, computed before the typeset job runs — the agent can use it to construct download links in advance.\n\n---\n\n## Part 3 — The Configuration Model\n\n### Edit 3.1 — Section preamble update\n\n**Insert at the top of Part 3:**\n\n> **In v1.2, the agent reads existing configs from project state (wherever the user keeps it) and writes the resulting cfg content into the payload's `config_files` map at the right relative-path key. There is no edit-in-place against the server.**\n\n### Edit 3.2 — The inheritance subsection — flag a change in agent responsibility\n\n**Find:** the *\"### Config inheritance: the [import] section\"* subsection.\n\n**Replace the \"Implications for the agent\" sub-bullets** with:\n\n> Implications for the agent in v1.2:\n>\n> - The agent walks the inheritance chain itself before constructing the payload. Canon's `config-construction.md` article describes the algorithm.\n> - The agent decides whether to include only the child config in the payload's `config_files` (relying on the worker to walk inheritance just like the GUI would) or whether to flatten the chain and include only the merged result. **Recommended: include the chain explicitly.** The worker materializes parent configs at their expected paths and PTXprint resolves the inheritance the way it always does. This preserves the user's intent and makes the payload roundtrip-able to a real project tree.\n> - There is no `resolve_config` server tool. If the agent needs effective settings for reasoning, it computes them from the payload's `config_files` content (or from project state before payload construction).\n\n### Edit 3.3 — \"Creating a new config\" subsection\n\n**Find:** the four-step list under *\"### Creating a new config\"*.\n\n**Replace with:**\n\n> Creating a new config in v1.2 is an agent-side operation against project state, not a server operation:\n>\n> 1. The agent updates its working representation of the project to include a new config folder under `shared/ptxprint/<NewName>/`.\n> 2. The agent writes a minimal `ptxprint.cfg` for the new config: only `[config] name = NewName` and `[import] config = Default` (or whichever parent makes sense).\n> 3. When the user wants to typeset using the new config, the agent constructs a payload with `config_name = \"NewName\"` and includes both the new config's files and the parent config's files in `config_files` (so inheritance resolves correctly inside the worker).\n> 4. After successful typesetting, the agent persists the new config back to project state (Git commit, file write via Claude Desktop, etc.) so it's available next session.\n\nThe \"Avoid\" list survives intact.\n\n### Edit 3.4 — \"When a config is 'ready to share'\" subsection survives intact\n\nNo changes. The checklist is agent-layer governance independent of architecture.\n\n---\n\n## Part 4 — The Override Mechanism\n\n### Edit 4.1 — Survives almost intact, with one reframing\n\nThe semantics of override files are unchanged in v1.2. The agent still must respect them when constructing payloads.\n\n**Insert at the top of Part 4:**\n\n> **In v1.2, override files are part of the payload.** When the agent's payload includes `shared/ptxprint/ptxprint_project.cfg` in `config_files`, the worker materializes it and PTXprint applies it as it always does. The override semantics described in this section are unchanged. The agent's responsibility shifts from \"check override files on disk before writing\" to \"check override files in the payload (and in project state) before changing a setting in the payload's main config.\"\n\n### Edit 4.2 — Failure modes list update\n\n**Find the three failure modes** (silent override / breaking cluster lockdown / orphaned override).\n\n**Update Failure Mode 1's mitigation** to read:\n\n> **Mitigation:** Before adding a setting to the payload's `config_files[\"shared/ptxprint/<config>/ptxprint.cfg\"]`, check both override files in project state. If the key is locked there, surface that to the user: \"I'm about to set `[paper] pagesize = A5`, but the project-level override at `ptxprint_project.cfg` has it locked to `Letter`. To change it for real, I'd need to update the override file too. Do you want me to?\"\n\n**Update Failure Mode 2's mitigation** to read:\n\n> **Mitigation:** Treat override files as administrator-controlled. The agent must not modify them in project state — and therefore must not include modified versions in the payload — without explicit user confirmation, with cluster propagation implications spelled out.\n\n**Failure Mode 3 is about orphaned overrides** — adapt the mitigation to apply to project state rather than disk reads. Otherwise unchanged.\n\n### Edit 4.3 — Authoring override files subsection\n\nThe pattern is the same; the storage moves from disk to project-state-and-payload.\n\n**Light edit:** replace \"Headlessly the agent does the same: read the relevant section...\" with \"The agent does the same: read the relevant section of the working `ptxprint.cfg`, extract the keys to lock, write a new override file at the appropriate path in project state, and include both the modified main cfg and the new override file in subsequent payloads.\"\n\n---\n\n## Part 5 — Settings Cookbook (by user intent)\n\n**No structural changes needed.** The cookbook is intent-keyed and architecture-neutral. The only edit is in the \"Hide / show pictures globally\" entry, which currently reads:\n\n> Use the `-D` flag for run-time\n\n**Update to:**\n\n> Use the payload's `define` field for run-time-only override (`define: { \"c_fighiderefs\": \"True\" }`). For per-picture control, modify the piclist content in `config_files`.\n\nThat's the only intent that explicitly references CLI mechanics.\n\n---\n\n## Part 6 — Supporting Files\n\n### Edit 6.1 — Reframe each file's \"lives at\" line\n\nThroughout Part 6, each supporting-file subsection has a \"Lives at: `<path>`\" line. These are correct as descriptions of the project tree shape, but they need a clarifying note that the agent constructs them as payload entries.\n\n**Insert at the top of Part 6 (before the \"changes.txt\" subsection):**\n\n> **In v1.2, the file paths described below are not paths the agent writes to on the server. They are relative-path keys in the payload's `config_files` map. The worker materializes these files at the corresponding paths in its scratch dir before running PTXprint. The agent reads the existing content from project state, modifies it as needed, and includes the new content in the next payload submission.**\n\n### Edit 6.2 — `changes.txt` subsection survives intact\n\nThe five-scope grammar (global / book-restricted / chapter / verse / environment / combined) is unchanged.\n\n### Edit 6.3 — `ptxprint.sty` and AdjLists subsections survive intact\n\nFormat descriptions unchanged. The \"When to edit\" / \"When NOT to edit\" sub-lists survive but interpret \"edit\" as \"modify in project state and include in payload.\"\n\n### Edit 6.4 — Piclist subsection — drop the MCP-tool reference\n\n**Find:** *\"The MCP server's `set_piclist` tool parses and writes these.\"*\n\n**Replace with:** *\"The agent constructs the piclist content as text and includes it in the payload's `config_files` at the appropriate path. Canon's `payload-construction.md` documents the line format with examples.\"*\n\n### Edit 6.5 — `FRTlocal.sfm` subsection survives intact\n\nThe three modes (Basic / Advanced / Paratext copy) and the special PTXprint codes are all about content, not transport.\n\n### Edit 6.6 — Cover periphs subsection survives intact\n\nThe four periph IDs (coverback / coverfront / coverspine / coverwhole) are content; the agent constructs them in `FRTlocal.sfm` content and includes that in the payload.\n\n---\n\n## Part 7 — USFM in Headless Context\n\n**No changes.** This section is pure USFM reference material. Architecture-neutral.\n\n---\n\n## Part 8 — Workflow Recipes\n\nEach recipe needs cosmetic edits to replace \"edit file, re-run\" mechanics with \"update payload, re-submit\" mechanics. Keep the recipes' structure and decision logic.\n\n### Edit 8.1 — Recipe: \"Help me create a draft PDF for review\"\n\n**Find step 4** in the recipe: *\"Run: ptxprint -P <prj> -c Default -b \"<book>\" -D output_format=Screen-Quickest\"*\n\n**Replace with:**\n\n> 4. Construct payload with project_id, config_name=\"Default\", books=[<book>], mode=\"simple\", define={\"output_format\": \"Screen-Quickest\"}. Submit via `submit_typeset`. Receive job_id.\n> 5. Poll `get_job_status` until state in {succeeded, failed}.\n> 6. Read the log_tail and the errors list for warnings.\n> 7. Surface the pdf_url to the user, plus any non-trivial warnings.\n\n(Renumber subsequent steps accordingly.)\n\n### Edit 8.2 — Recipe: \"Create a print-ready New Testament\"\n\nApply the same transformation: replace direct CLI invocation with payload construction + submission + polling.\n\n### Edit 8.3 — Recipe: \"Modify a single setting and re-run\"\n\nThis is the recipe most affected by the architectural change. Rewrite:\n\n> **Context:** User wants to change one thing in an existing config and see the result.\n>\n> 1. Read the current value of the setting from project state (or from the working payload state if the agent has been iterating).\n> 2. Surface: \"Current value is X. You want to change to Y?\" If unclear, ask.\n> 3. Check both override files (in project state and/or working state) for this key. If locked, surface that and stop.\n> 4. Update the working `ptxprint.cfg` content — either modify project state (write to disk via Claude Desktop file access, or stage in working memory).\n> 5. Construct a new payload with the updated `config_files` content. Submit.\n> 6. Poll for completion. Receive new pdf_url.\n> 7. Surface the change visually (compare against previous pdf_url, if available) or numerically (page count delta, overfull count delta).\n\nFor exploratory changes, use the payload's `define` field instead of mutating `config_files[\"...ptxprint.cfg\"]` — preserves the saved state.\n\n### Edit 8.4 — Recipe: \"Set up a diglot publication\"\n\nSteps 1–9 are agent-side reasoning about which configs and projects to use. The architectural change only affects step 10 (\"Iterate\") — make explicit that iteration means submitting new payloads.\n\n### Edit 8.5 — Recipe: \"Lock down house style for a cluster\"\n\nThis recipe is about modifying override files in project state. The mechanics are: agent updates project state → agent persists back to the user's storage (Git, Paratext, etc.) → user runs Send/Receive. The MCP server is not involved in the lock-down step. The recipe survives with one clarifying sentence:\n\n**Insert at the top of the recipe:** *\"Note: this recipe operates on project state (the user's filesystem, Git, Paratext server, etc.), not on the typesetting MCP server. The MCP server only sees the resulting payloads at typesetting time.\"*\n\n### Edit 8.6 — Recipe: \"Diagnose 'PDF was produced but pictures are missing'\"\n\nThe seven-point checklist is correct as-is. Only the \"search paths\" item needs adapting — in v1.2, the worker's search path is whatever the worker materializes from the payload's `figures` array.\n\n**Replace step 2** of the checklist:\n\n> 2. Are the picture files in the payload's `figures` array, with valid URLs and matching sha256 hashes?\n>    → Inspect the payload (the agent has the working copy). Verify that the figures referenced by the piclist are present in the array and that their URLs return content matching the declared hashes.\n\nThe other steps survive.\n\n### Edit 8.7 — Recipe: \"Recover from 'I can't make this setting change take effect'\"\n\nSteps 1–4 are decision logic about override files and inheritance. Adapt step 1 to \"Confirm the cfg key was actually changed in the payload that was submitted. Read it back from the working payload.\"\n\n---\n\n## Part 9 — Diagnostic Patterns\n\n### Edit 9.1 — Reframe \"Reading the XeTeX log\"\n\n**Find:** *\"The log lives at `<project>/local/ptxprint/<Config>/<PRJ>_<Config>_<bks>_ptxp.log`.\"*\n\n**Replace with:** *\"The log is uploaded to R2 alongside the PDF and surfaced via `get_job_status.log_url` (presigned R2 URL) or trimmed to the last 100 lines in `get_job_status.log_tail`. For long jobs, use `log_tail` for incremental visibility during the run; fetch the full log via `log_url` after completion.\"*\n\n### Edit 9.2 — Distinguishing failure modes table\n\nThe truth table is correct. Add one row at the top noting that v1.2 surfaces this directly:\n\n> **In v1.2, `get_job_status.failure_mode` carries the classification result (one of `hard | soft | success`). The truth table below describes how that field is computed inside the worker; the agent reads the field directly without re-deriving.**\n\n### Edit 9.3 — \"Overfull box count\" subsection survives intact\n\nThe thresholds (< 20 excellent, 20–50 normal, etc.) are surfaced via `get_job_status.overfull_count`. Architecture-neutral.\n\n---\n\n## Part 10 — Conversational Patterns\n\n**No changes.** This entire section is about the user-facing rhythm of conversation. Architecture-neutral.\n\n---\n\n## Part 11 — Settings That Need Special Handling\n\n### Edit 11.1 — \"Test before committing\" subsection\n\n**Find:** *\"Make the change as a `-D` runtime override or in a copy of the config.\"*\n\n**Replace with:** *\"Make the change as a payload `define` field (transient) or by including a modified `ptxprint.cfg` in the payload (mutating only the working state, not yet persisted to project storage). Submit and observe.\"*\n\n### Edit 11.2 — \"Rollback patterns\" subsection\n\nThe `.bak` file mechanism is irrelevant in v1.2. Replace the entire subsection:\n\n> ### Rollback patterns\n>\n> Every payload submission produces a content-addressed result in R2. The agent maintains a small history of submitted payloads (in working memory or persisted, depending on the host environment). To roll back: re-submit the prior payload. The cached output URL returns immediately — no re-run.\n>\n> For changes that span multiple files, the rollback unit is the payload, not individual files. Maintaining payload history is the agent's responsibility; canon's `payload-construction.md` recommends keeping the last N payloads keyed by user-meaningful labels (\"before-margin-change\", \"v1-final\", etc.).\n\n---\n\n## Part 12 — Open Gaps in This KB\n\n### Edit 12.1 — Drop items 1, 4, 5, 7\n\nItem 1 (widget-identifier names for `-D` overrides) — survives.\nItem 2 (400+ settings catalog) — survives.\nItem 3 (PTXprint USFM extensions) — survives.\nItem 4 (picture catalogue download workflows) — adapt: in v1.2, the agent uses LFF-equivalent or other URL-providing mechanisms to source images, then references them by URL in the payload's `figures` array. Update the gap statement accordingly.\nItem 5 (cover wizard equivalents) — adapt: agent constructs cover periphs in `FRTlocal.sfm` content and includes in payload.\nItem 6 (TeX macro injection) — survives, deferred.\nItem 7 (validation surface) — adapt: in v1.2, the worker performs the structural checks (per failure-mode taxonomy). The agent receives the classification.\nItem 8 (diglot two-config concurrency) — survives.\n\n### Edit 12.2 — Add new gaps to track\n\n> 9. **Project state read/write mechanism.** The agent needs read/write access to the user's project storage (filesystem, Git, Paratext server, etc.) but the typesetting MCP does not provide it. For Claude Desktop users with native filesystem access, this is implicit. For other agent hosts, a separate mechanism is required and must be documented per host.\n> 10. **Payload history persistence.** Whether the agent maintains payload history in volatile memory (current session only), persistent agent state, or external storage (Git, R2 itself, etc.) — and how to surface that history to the user for rollback purposes.\n> 11. **Cache hit observability.** Whether the agent should distinguish \"this PDF was just generated\" from \"this PDF was returned from cache\" in user-facing messaging. Currently `submit_typeset` returns a `cached: bool` flag; canon should describe when to surface it.\n\n---\n\n## Provenance update\n\n### Edit P.1 — Update the source materials list\n\n**Find:** the *\"Source materials\"* line at the bottom of the document.\n\n**Replace with:** *\"Source materials: `ptxprint-master-slides.surface.json`, `ptx2pdf.surface.json`, `transcript-encoded.md` (sessions 1–5), `ptxprint-mcp-v1.2-spec.md`, the operator's PDF deck extraction. Updated 2026-04-28 to align with v1.2 stateless architecture.\"*\n\n---\n\n## Final pass: search for stale references\n\nAfter applying the edits above, search the document for these strings and verify each occurrence has been addressed:\n\n- `read_file` — should not appear except in historical/comparative context\n- `write_file` — same\n- `set_config_values` — same\n- `set_piclist` — same\n- `resolve_config` — same\n- `predict_output_paths` — same\n- `get_config` — same\n- `describe_setting` — same\n- `.bak` — same\n- `<projects_root>` (when describing where the agent operates) — should be reframed as \"payload\" or \"project state\"\n\nIf any of these remain in unintended places, update them per the patterns established above.\n\n---\n\n*End of governance update handoff. Apply these edits in order; the doc should be coherent at any point during the pass (each edit is local). Estimated effort: 30–60 minutes for a careful single-session pass.*\n"},{"path":"handoffs/missing-uploads-handoff.md","uri":"klappy://canon/handoffs/missing-uploads-handoff","title":"Session Handoffs — Upload Prompts for Other Claude Sessions","audience":"operator","tags":["handoff","uploads","session-prompts","v1.2-bootstrap"],"stability":"working","what_it_answers":"**✅ Prompts A and B resolved 2026-04-28.** All target files are in the repo: slides ESE surfaces (`canon/surfaces/ptxprint-master-slides.surface.md/.json`), training manual (`canon/derivatives/ptxprint-training-manual.md`), governance Parts","sections":[{"slug":"prompt-a-for-the-slides-ese-training-manual-session","heading":"Prompt A — For the Slides ESE / Training Manual session","start":22,"end":92},{"slug":"prompt-b-for-the-font-resolution-session","heading":"Prompt B — For the Font Resolution session","start":92,"end":143},{"slug":"prompt-c-for-the-operator-manual-paste-of-governance-parts-3-12","heading":"Prompt C — For the operator (manual paste of governance Parts 3–12)","start":143,"end":176},{"slug":"common-notes","heading":"Common notes","start":176,"end":200}],"body":"---\ntitle: \"Session Handoffs — Upload Prompts for Other Claude Sessions\"\naudience: operator\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"handoff\", \"uploads\", \"session-prompts\", \"v1.2-bootstrap\"]\ncanonical_status: non_canonical\ncreated_at: 2026-04-28T04:30:00Z\ncompanion_to: \"canon/PENDING_UPLOADS.md\"\n---\n\n# Session Handoffs — Upload Prompts\n\n> **✅ Prompts A and B resolved 2026-04-28.** All target files are in the repo: slides ESE surfaces (`canon/surfaces/ptxprint-master-slides.surface.md/.json`), training manual (`canon/derivatives/ptxprint-training-manual.md`), governance Parts 0–12 + Provenance (`canon/governance/headless-operations.md`), session-3 encoding (`canon/encodings/transcript-encoded-session-3.md`), and the font-resolution-design article (now archived per v1.2 D-021 at `canon/articles/_archive/font-resolution-design.md`). Prompt C was contingent on Prompt A failing and is also moot. This document is retained as a historical record of the bootstrap upload coordination; **do not re-paste any prompt**. (See `canon/handoffs/documentation-cleanup-handoff.md` §3 P0.2 for context.)\n\n> **What this document is.** Three copy-pasteable prompts, one per Claude session that holds files the bootstrap session didn't have access to. Each prompt is self-contained: paste it into the relevant session's chat (or open a new conversation in that session's project) and the receiving Claude will know what to do, where to put the files, and how to push. Companion: [`PENDING_UPLOADS.md`](../PENDING_UPLOADS.md) (the inventory).\n\n> **How to use.** Identify which session holds which files (see PENDING_UPLOADS.md for context). Open that session's chat. Paste the corresponding prompt below verbatim. Wait for the session to confirm it has the files and execute the upload.\n\n---\n\n## Prompt A — For the Slides ESE / Training Manual session\n\n> **Context for the operator before pasting:** This is the session in which Claude ran ESE on the 438-slide PTXprint MASTER SLIDES deck and authored the training-manual derivative. Per `canon/PENDING_UPLOADS.md`, that session also received the operator's PDF extraction of the deck which became the headless-operations governance document. The chat URL noted in session-1's transcript is `claude.ai/chat/fddce82c-e7f8-4c20-9882-b4c4152aa4f6` — verify before pasting in case the operator has the artifacts in a different session.\n\n```\n=== PASTE BELOW THIS LINE ===\n\nYou ran ESE on the PTXprint MASTER SLIDES deck and authored a training-manual derivative in this conversation. You should have the following files in your context or outputs:\n\n1. ptxprint-master-slides.surface.md\n2. ptxprint-master-slides.surface.json\n3. ptxprint-training-manual.md\n4. The operator-authored \"PTXprint Headless Operations\" governance document (extracted from the PDF deck) — full text including Parts 0 through 12 plus Provenance\n\nA new public repo has been bootstrapped at https://github.com/klappy/ptxprint-mcp to hold both the MCP server code and the governance KB. The bootstrap session did not have direct access to your files. Your job is to upload them.\n\n**Target paths in the repo:**\n- canon/surfaces/ptxprint-master-slides.surface.md\n- canon/surfaces/ptxprint-master-slides.surface.json\n- canon/derivatives/ptxprint-training-manual.md   (create canon/derivatives/ if not present)\n- canon/governance/headless-operations.md         (REPLACE the existing partial file — Parts 0-2 are already there but Parts 3-12 + Provenance are missing)\n\n**For headless-operations.md specifically:**\nThe existing file in the repo has:\n- A frontmatter block (preserve verbatim — including the status_note field)\n- A top-of-file STATUS note as a blockquote (preserve verbatim)\n- The full text of Parts 0, 1, and 2 (preserve verbatim)\n- A placeholder section \"## Parts 3–12\" with a description of what's missing (REPLACE with the actual Parts 3-12 + Provenance)\n\nAfter your replacement, the file should have: frontmatter + STATUS note + Parts 0 through 12 + Provenance, all from the operator's source document. Do not modify Parts 0-2 — they were reproduced from the operator's original. Just append Parts 3-12 + Provenance in place of the placeholder.\n\n**Procedure:**\n\n1. Verify you have all four files. If you don't have one or more, say so and stop — the operator will route to a different session.\n\n2. Read canon/PENDING_UPLOADS.md from the repo for additional context (use web_fetch on the raw GitHub URL if your environment supports it):\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/PENDING_UPLOADS.md\n\n3. Read the existing canon/governance/headless-operations.md to understand its current shape:\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/governance/headless-operations.md\n\n4. Authenticate with the GitHub PAT from your project instructions (the same PAT that bootstrapped the repo — fine-grained, scoped to klappy/ptxprint-mcp with contents + PRs).\n\n5. Clone the repo and place the files:\n   git clone https://${GH_PAT}@github.com/klappy/ptxprint-mcp.git\n   cd ptxprint-mcp\n   mkdir -p canon/derivatives\n   # Place ptxprint-master-slides.surface.{md,json} in canon/surfaces/\n   # Place ptxprint-training-manual.md in canon/derivatives/\n   # Open canon/governance/headless-operations.md, replace the Parts 3-12 placeholder with the full authored text\n   git add canon/surfaces/ptxprint-master-slides.surface.md \\\n           canon/surfaces/ptxprint-master-slides.surface.json \\\n           canon/derivatives/ptxprint-training-manual.md \\\n           canon/governance/headless-operations.md\n   git commit -m \"uploads: slides ESE surface + training manual; complete governance Parts 3-12 + Provenance\"\n   git push origin main\n\n6. Update canon/PENDING_UPLOADS.md to remove the entries you just resolved (sections 1, 3 — and item 4 of the status table). Commit and push that change separately:\n   git commit -am \"PENDING_UPLOADS: remove resolved entries (slides ESE, training manual, governance Parts 3-12)\"\n   git push origin main\n\n7. Report back: list the four files uploaded with sizes, confirm both commits pushed, and surface any deviations from the procedure (e.g. files missing from your context, paths that needed adjustment).\n\nMode discipline: this is execution mode. Don't ask clarifying questions about which files or where — they're specified above. If something is genuinely missing or contradictory, name the specific gap in one sentence and stop; do not infer.\n\n=== PASTE ABOVE THIS LINE ===\n```\n\n---\n\n## Prompt B — For the Font Resolution session\n\n> **Context for the operator before pasting:** This is the session in which Claude worked through PTXprint's font resolution problem (LFF lookup, sha256 verification, per-job fontconfig) and produced a session-3 encoding plus a font-resolution-design document.\n\n```\n=== PASTE BELOW THIS LINE ===\n\nYou worked through PTXprint's font resolution problem in this conversation — Language Font Finder (LFF) lookup, sha256 verification, per-job fontconfig generation, and the design choice to fail loudly when fonts cannot be resolved rather than silently falling back to system fonts. You should have produced two artifacts:\n\n1. transcript-encoded-session-3.md — the DOLCHEO+H encoding of this session\n2. font-resolution-design.md — the design notes covering the font resolution loop, verification, manifest building, and the no-system-fonts-in-Container decision\n\nA new public repo has been bootstrapped at https://github.com/klappy/ptxprint-mcp to hold both the MCP server code and the governance KB. The bootstrap session did not have direct access to your files. Your job is to upload them.\n\n**Target paths:**\n- canon/encodings/transcript-encoded-session-3.md\n- canon/articles/font-resolution-design.md   (create canon/articles/ if not present)\n\n**Procedure:**\n\n1. Verify you have both files. If either is missing, say so and stop.\n\n2. Read canon/PENDING_UPLOADS.md and canon/encodings/transcript-encoded-session-5.md from the repo for context — session 5 references your work in D-025 (no system fonts in the Container) and the Q-open-list:\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/PENDING_UPLOADS.md\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/encodings/transcript-encoded-session-5.md\n\n3. Authenticate with the GitHub PAT from your project instructions (fine-grained, scoped to klappy/ptxprint-mcp with contents + PRs).\n\n4. Clone the repo and place the files:\n   git clone https://${GH_PAT}@github.com/klappy/ptxprint-mcp.git\n   cd ptxprint-mcp\n   mkdir -p canon/articles\n   # Place transcript-encoded-session-3.md at canon/encodings/\n   # Place font-resolution-design.md at canon/articles/\n   git add canon/encodings/transcript-encoded-session-3.md canon/articles/font-resolution-design.md\n   git commit -m \"uploads: session 3 encoding (font resolution); article: font-resolution-design\"\n   git push origin main\n\n5. Update canon/PENDING_UPLOADS.md to remove the entry you just resolved (section 2). Commit and push separately:\n   git commit -am \"PENDING_UPLOADS: remove resolved entry (session 3 / font resolution)\"\n   git push origin main\n\n6. Report back: list the two files uploaded with sizes, confirm both commits pushed.\n\nMode discipline: execution mode. Specifications above are sufficient; don't ask for clarification on paths or commit messages.\n\n=== PASTE ABOVE THIS LINE ===\n```\n\n---\n\n## Prompt C — For the operator (manual paste of governance Parts 3–12)\n\n> **Context:** This prompt is for the case where the slides-ESE session (Prompt A) cannot find the operator-authored governance document in its outputs — perhaps because the operator authored it directly outside any session, or because the session that has it can't be reached. In that case, the operator pastes the source content manually.\n\n```\n=== USE WHEN PROMPT A CAN'T REACH THE GOVERNANCE DOC ===\n\nThe operator (you) authored the \"PTXprint Headless Operations\" governance document by extracting Parts 0 through 12 plus Provenance from the PDF of the PTXprint MASTER SLIDES deck. The bootstrap session reproduced Parts 0-2 verbatim from a partial copy in its context, but Parts 3-12 + Provenance did not survive into that context.\n\nThe full document needs to land at:\n  canon/governance/headless-operations.md\n\nThe current file there has Parts 0-2 plus a frontmatter block plus a top-of-file STATUS note. Parts 3-12 + Provenance are placeholder text that needs to be replaced.\n\n**Operator action:**\n\n1. Locate the source — your original markdown extraction from the PDF deck, or the original session's outputs.\n\n2. Open the existing file in the repo (browser: https://github.com/klappy/ptxprint-mcp/edit/main/canon/governance/headless-operations.md)\n\n3. Find the section starting \"## Parts 3–12\" and ending with \"## Provenance\" (placeholder content). Replace that block with your authored Parts 3 through 12 plus the Provenance section from your source.\n\n4. Commit via the GitHub web UI with message: \"governance: complete Parts 3–12 + Provenance from operator source\"\n\n5. After the upload, edit canon/PENDING_UPLOADS.md to remove section 1 (the highest-priority entry); commit with message \"PENDING_UPLOADS: remove resolved entry (governance Parts 3-12)\"\n\nAfter both commits, the v1.2-alignment handoff at canon/handoffs/governance-update-handoff.md can be applied to bring the now-complete document up to v1.2 conventions. That's a separate ~30-60 minute task.\n\n=== END ===\n```\n\n---\n\n## Common notes\n\n### Authentication\n\nAll three prompts assume the receiving session has the GitHub PAT from the project instructions:\n- Fine-grained PAT, format `github_pat_11AAA...`\n- Scoped to `klappy/ptxprint-mcp` (contents + PRs) — verified working as of bootstrap commit\n- Same PAT used for all three sessions; coordinate via session encodings if multiple sessions push concurrently to avoid merge conflicts\n\n### After all three prompts have been applied\n\n1. The repo has all the canon material the v1.2 build needs.\n2. `canon/PENDING_UPLOADS.md` should have only items 4 (optional pre-v1.2 article archive) and 5 (UI tooltip dump) remaining, both labelled optional or low-priority.\n3. The headless-operations governance doc still needs the v1.2-alignment edits applied (separate handoff: `canon/handoffs/governance-update-handoff.md`).\n4. Tomorrow's autonomous coding run can clone the repo and have full context.\n\n### If a prompt fails partway\n\nEach prompt is idempotent for the file-placement steps (cp + git add are repeatable). If `git push` fails due to a concurrent push from another session, the receiving session should `git pull --rebase`, resolve any conflicts in `canon/PENDING_UPLOADS.md`, and re-push. If the failure is something else, surface it and stop — don't improvise.\n\n### When this handoff doc itself becomes stale\n\nWhen all three prompts have been applied and PENDING_UPLOADS reflects the resolution, this file can be moved to `canon/handoffs/_archive/` or deleted. It captures a moment-in-time bootstrap and has no durable value once executed.\n"},{"path":"handoffs/next-session-handoff-2026-04-30.md","uri":"klappy://canon/handoffs/next-session-handoff-2026-04-30","title":"Next-Session Handoff — Telemetry Feature (post-2026-04-30 session)","audience":"project","tags":["handoff","session-handoff","telemetry","v1.3","next-session","pr-queue","open-decisions"],"stability":"working","what_it_answers":"**Read this first if you're continuing the telemetry-feature work that started 2026-04-30.** This is a forward-looking session artifact, not a binding contract — per `klappy://canon/principles/contract-governs-handoff-drift`, when this disa","sections":[{"slug":"tl-dr","heading":"TL;DR","start":19,"end":25},{"slug":"the-five-open-prs-in-priority-order","heading":"The Five Open PRs (in priority order)","start":25,"end":39},{"slug":"outstanding-handoffs-from-the-planning-ledger","heading":"Outstanding Handoffs from the Planning Ledger","start":39,"end":66},{"slug":"pending-operator-decisions","heading":"Pending Operator Decisions","start":66,"end":78},{"slug":"what-this-session-learned-for-future-reference","heading":"What This Session Learned (for future reference)","start":78,"end":94},{"slug":"recommended-first-action-for-the-next-session","heading":"Recommended First Action for the Next Session","start":94,"end":104},{"slug":"durable-artifacts-produced-this-session","heading":"Durable Artifacts Produced This Session","start":104,"end":124}],"body":"---\ntitle: \"Next-Session Handoff — Telemetry Feature (post-2026-04-30 session)\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"handoff\", \"session-handoff\", \"telemetry\", \"v1.3\", \"next-session\", \"pr-queue\", \"open-decisions\"]\ndate: 2026-04-30\nsession_summary: \"Designed, governed, drafted, reviewed, and revised the telemetry feature for PTXprint MCP. Resolved H-T3 with the operator's spec-lock principle (now canon). Reverted a session-internal principle violation. Encoded the principle on klappy.dev. Five PRs open across two repos at handoff.\"\nstatus: active\ngoverns: \"what the next session reads first when continuing telemetry-feature work\"\n---\n\n# Next-Session Handoff — Telemetry Feature\n\n> **Read this first if you're continuing the telemetry-feature work that started 2026-04-30.** This is a forward-looking session artifact, not a binding contract — per `klappy://canon/principles/contract-governs-handoff-drift`, when this disagrees with canon, canon wins. Verify any claim here against the underlying canon before acting on it.\n\n---\n\n## TL;DR\n\nOver one session on 2026-04-30, the telemetry feature for PTXprint MCP went from \"operator-confirmed planning decisions\" to \"ready for implementation.\" Five PRs are open across `klappy/ptxprint-mcp` and `klappy/klappy.dev`. The contract for the next implementer is the new v1.3 spec; the policy they implement is the governance article; the principle that scopes how all of it can change is now canon. The next session's first concrete work is either (a) PR triage with the operator, or (b) starting H-T5 (the actual telemetry-module code) once the operator approves.\n\n---\n\n## The Five Open PRs (in priority order)\n\n| # | Repo | Title | What it does | Blocked on |\n|---|---|---|---|---|\n| **#26** | ptxprint-mcp | Apply H-T2 review revisions: 2 blockers + 3 nits | Fixes B-1 (failure_mode citation), B-2 (phase enum framing), N-1 (oauth value restored), N-2 (three-tier fallback documented), N-3 (stale `canon/constraints/` paths in planning ledger). Updates the article's `status: draft_pending_fresh_review` → `status: reviewed` | Operator review/merge |\n| **#27** | ptxprint-mcp | Spec v1.3: telemetry layer over v1.2 (resolves H-T3) | The v1.3 spec — full standalone re-spec, supersedes v1.2. Adds the two telemetry tools, the internal Worker telemetry route, the redaction module, the three-tier policy fetch | Operator review/merge |\n| **#28** | ptxprint-mcp | Revert H-T4 edits to v1.2 spec (apply spec-lock principle) | Restores v1.2 spec to its pre-H-T4 state. Removes the telemetry tool subsections that should never have been added retroactively | Operator review/merge |\n| **#159** | klappy.dev | Add tier-2 principle: Specs Lock at Implementation (canon 0.37.0, E0008.6) | New canon principle articulated by the operator and immediately applied (via #28). Includes canon version bump and changelog entry | Operator review/merge; epoch numbering tentative |\n| **(this)** | ptxprint-mcp | Next-session handoff doc | This file. Tiny PR, can be merged or closed once read | Read only |\n\nPRs #26, #27, #28 form a coherent set on `klappy/ptxprint-mcp`. Merge order recommendation: #26 → #28 → #27 (revisions land first; revert lands second; v1.3 spec lands last and supersedes the now-clean v1.2 spec). #159 is independent and can land any time.\n\n---\n\n## Outstanding Handoffs from the Planning Ledger\n\nFrom `canon/encodings/telemetry-feature-planning-ledger.md`, the H-T* handoffs status:\n\n| ID | Status | Where it lives now |\n|---|---|---|\n| H-T1 — Draft governance article | ✅ Done | `canon/governance/telemetry-governance.md` (status: reviewed) |\n| H-T2 — Fresh-session review of governance article | ✅ Done | Managed-agent dispatched, PR #25 merged, revisions in PR #26 |\n| H-T3 — Decide v1.2 amendment vs. v1.3 spec | ✅ Resolved | v1.3 spec (PR #27); operator's principle now canon (PR #159) |\n| H-T4 — Update tool count claims | ⚠️ Partially undone | Original H-T4 commit (`ae4e60c`) over-reached and edited the implemented v1.2 spec; PR #28 reverts the v1.2 portion; `ARCHITECTURE.md` and `README.md` portions intentionally kept |\n| **H-T5 — Mirror oddkit's Query Security Boundary guards in code** | 🔜 Next | Not started. Implementation-time concern. v1.3 spec §6.4 has the code sketch |\n\nH-T5 is the next concrete work. It is not the only piece of v1.3 implementation — the full v1.3 implementation is enumerated in v1.3 spec §11 (\"First Execution Scope\"):\n\n- Add two MCP tool routes (`telemetry_public`, `telemetry_policy`) to `src/index.ts`\n- Add `src/telemetry.ts` (writer + redactor + policy fetcher + public-query forwarder)\n- Add `POST /internal/telemetry` Worker endpoint\n- Add telemetry envelope helper to `container/main.py` + phase + terminal hooks\n- Update `wrangler.jsonc` with the analytics_engine_datasets binding\n- Add `scripts/bundle-telemetry-policy.ts` build step\n- Configure `CF_ACCOUNT_ID` and `CF_API_TOKEN` as secrets\n- Smoke-test all 8 DoD items from v1.3 spec §10\n\nWhether to dispatch this as one autonomous coding run or split it across multiple is the operator's call.\n\n---\n\n## Pending Operator Decisions\n\nThese were surfaced and not closed in this session:\n\n1. **Epoch numbering on PR #159.** I picked `E0008.6` as a new sub-epoch for \"Spec-Lifecycle Discipline.\" Alternatives: fold into E0008.5 or jump to E0009. Both the principle file frontmatter and the changelog entry need the same single-string update if changed.\n\n2. **Pre-existing staleness in v1.2 spec.** §1 vodka and §5 Architecture/Worker still claim \"three MCP tools\" even though the implemented surface is 4 (docs added in session 13, also after-implementation). Reopening this would compound the same principle violation that PR #28 fixes. Recommendation: leave it alone — v1.3 supersedes anyway, and the principle that just landed in canon is precisely what would prevent this kind of fix-after-implementation.\n\n3. **When to start H-T5 (the v1.3 implementation work).** Could be triggered as soon as PRs #26/#27/#28 are merged. Could wait for an explicit kickoff. The v1.3 spec deliberately does not declare a start signal — that's the operator's call per the spec-lock principle (the spec being locked is the precondition for handing it to an implementer).\n\n---\n\n## What This Session Learned (for future reference)\n\nThree patterns that worked and are worth reusing:\n\n1. **Managed-agent dispatch for fresh-session review (H-T2).** A `claude-sonnet-4-6` agent with the project's epistemic posture in its system prompt, reading the article and three authority documents, completed a flag-happy review in ~7 minutes wall-clock and caught two real blockers + three real nits. The pattern: dispatch via `mnt/skills/user/managed-agents/SKILL.md`, write the review to `canon/handoffs/`, the encoded ledger to `canon/encodings/`, and create a PR. See PR #25 and the agent's review at `canon/handoffs/telemetry-governance-h-t2-review.md`.\n\n2. **Surfacing pending operator decisions in PR descriptions, not asking inline.** When a follow-up question would have interrupted execution flow, putting it in the PR description as \"Open question for you\" let the operator triage on their own time without slipping out of execution mode. Used in PRs #27, #28, and #159.\n\n3. **Independent verification before applying a reviewer's findings.** PR #26's commit message documents that I re-checked B-1 and B-2 against canon (spec line 141, line 427, taxonomy line 33) before applying the fixes. Trust-but-verify even when the reviewer is independent.\n\nOne pattern to avoid in future sessions:\n\n4. **Don't edit specs after implementation, even for \"harmless\" syncing.** My H-T4 commit (`ae4e60c`) edited the v1.2 spec to add references to not-yet-built telemetry tools — the exact pattern the new principle says shouldn't happen. The operator caught it and the revert is PR #28. Lesson: when a spec describes an implemented surface, additions go in vN+1, period. The principle that landed as canon (`klappy://canon/principles/specs-lock-at-implementation`) is the durable record of this lesson.\n\n---\n\n## Recommended First Action for the Next Session\n\n1. Call `oddkit_time` first (project rule).\n2. Declare mode (project rule). Most likely \"Planning\" if you're triaging the PR queue with the operator, or \"Executing\" if the operator has already greenlit something.\n3. Check the PR queue status — `curl -s \"https://api.github.com/repos/klappy/ptxprint-mcp/pulls\"` and `curl -s \"https://api.github.com/repos/klappy/klappy.dev/pulls\"` with the PAT will tell you which PRs are still open and which got merged after this handoff was written.\n4. If the operator says \"continue with H-T5\" or \"start the v1.3 implementation,\" read v1.3 spec §11 first and decide whether to dispatch as one managed-agent coding run or split. Either way, a fresh-session validator (per `klappy://canon/principles/verification-requires-fresh-context`) reviews the implementation before merge.\n5. If the operator wants to talk through anything before more code lands, you have the context to engage substantively without re-reading the whole conversation.\n\n---\n\n## Durable Artifacts Produced This Session\n\nThese live in canon and are searchable via `oddkit_search` with `knowledge_base_url=https://github.com/klappy/ptxprint-mcp`:\n\n- `canon/encodings/telemetry-feature-planning-ledger.md` — the design decisions (D-T1..D-T7, O-T1..O-T3, L-T1..L-T2, C-T1..C-T4, H-T1..H-T5) the whole feature derives from. H-T3 entry includes the resolution.\n- `canon/governance/telemetry-governance.md` — the policy article (status: reviewed). The schema, privacy floor, canned queries, transparency leaderboard.\n- `canon/handoffs/telemetry-governance-h-t2-review.md` — the fresh-session reviewer's findings.\n- `canon/encodings/h-t2-fresh-review-ledger.md` — DOLCHEO+H ledger of the review session.\n- `canon/specs/ptxprint-mcp-v1.3-spec.md` — the new build contract (PR #27, not yet merged at handoff time).\n- `canon/handoffs/next-session-handoff-2026-04-30.md` — this file.\n\nOn `klappy.dev` (searchable as klappy.dev baseline canon):\n\n- `canon/principles/specs-lock-at-implementation.md` — the new tier-2 principle (PR #159, not yet merged at handoff time).\n- `canon/CHANGELOG.md` — entry for canon 0.37.0 (PR #159).\n\n---\n\n*End of handoff. Continue from \"Recommended First Action\" above.*\n"},{"path":"handoffs/oddkit-kb-isolation-feature-request.md","uri":"klappy://canon/handoffs/oddkit-kb-isolation-feature-request","title":"Handoff to oddkit — Feature Request: Isolate Knowledge Base from Baseline Search Corpus","audience":"external","tags":["handoff","oddkit","feature-request","knowledge-base-isolation","scoped-truth","search-corpus","baseline-contamination"],"stability":"working","what_it_answers":"**The ask in one sentence.** When `knowledge_base_url` is set on a search/preflight/orient call, oddkit should isolate the search corpus to that KB's overlay (plus the small set of required-baseline governance files), not merge the entire 5","sections":[{"slug":"1-the-empirical-observation","heading":"1. The Empirical Observation","start":25,"end":48},{"slug":"2-the-existing-canon-that-backs-this-request","heading":"2. The Existing Canon That Backs This Request","start":48,"end":82},{"slug":"3-the-specific-gap","heading":"3. The Specific Gap","start":82,"end":98},{"slug":"4-proposed-flag-parameter-shapes","heading":"4. Proposed Flag/Parameter Shapes","start":98,"end":144},{"slug":"5-considerations-and-open-questions","heading":"5. Considerations and Open Questions","start":144,"end":177},{"slug":"6-how-this-handoff-should-be-used","heading":"6. How This Handoff Should Be Used","start":177,"end":191},{"slug":"7-provenance","heading":"7. Provenance","start":191,"end":203}],"body":"---\ntitle: \"Handoff to oddkit — Feature Request: Isolate Knowledge Base from Baseline Search Corpus\"\naudience: external\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"handoff\", \"oddkit\", \"feature-request\", \"knowledge-base-isolation\", \"scoped-truth\", \"search-corpus\", \"baseline-contamination\"]\ncanonical_status: non_canonical\ncreated_at: 2026-04-28T12:15:00Z\ncompanion_to: \"canon/specs/ptxprint-mcp-v1.2-spec.md, canon/handoffs/session-3-gaps-handoff.md\"\nreferences_oddkit_canon:\n  - klappy://canon/principles/scoped-truth\n  - klappy://canon/constraints/core-governance-baseline\n  - klappy://docs/planning/kb-data-model\naddressed_to: \"klappy/oddkit maintainer (file as a GitHub issue or RFC against oddkit, or treat as canon input for the next governance epoch)\"\n---\n\n# Handoff to oddkit — Feature Request: Isolate Knowledge Base from Baseline Search Corpus\n\n> **The ask in one sentence.** When `knowledge_base_url` is set on a search/preflight/orient call, oddkit should isolate the search corpus to that KB's overlay (plus the small set of required-baseline governance files), not merge the entire 566-document baseline into the index where it dominates BM25 and contaminates results in exactly the way `canon/principles/scoped-truth.md` names as the anti-pattern.\n\n> **Status.** This handoff was authored from a measurement session against `https://github.com/Klappy/ptxprint-mcp` as the project KB. The empirical evidence is in §2; the existing canon that backs the request is in §3; the proposed flag/parameter shapes are in §4.\n\n---\n\n## 1. The Empirical Observation\n\nA session probing the ptxprint-mcp repo as `knowledge_base_url` measured the search corpus composition and ran ten realistic agent queries. The corpus reported `total: 584` (`canon: 19` from the overlay, `baseline: 566` from klappy.dev). The probe matrix:\n\n| # | Query a real agent would issue | Top hit | Right answer in top 5? | Verdict |\n|---|---|---|---|---|\n| 1 | \"submit_typeset payload schema\" | baseline `frontmatter-schema.md` | governance #2, surface #3; **v1.2 spec absent** | Spec missing for its own central concept |\n| 2 | \"Cloudflare Worker Container Durable Object PTXprint\" | baseline `maintainability` | spec #2, **obsolete article #3**, session #5 | OK, but stale article ranks alongside live |\n| 3 | \"how to construct payload typesetting job\" | baseline `the-intern.md` essay | governance #2 only; 4 noise | Bad |\n| 4 | \"AdjList paragraph adjustment format\" | baseline TSV serialization spec | **none** | Total miss |\n| 5 | \"failure mode hard soft success ptxprint\" | baseline `odd/misuse-patterns` | surface #3; spec/governance absent | Total miss |\n| 6 | \"autofill page filler multi-pass typesetting\" | baseline `revision-lens-sequence` | **none** | Total miss |\n| 7 | \"payload-construction worked example simple typesetting\" | baseline `project-instructions-template` | **none** (article doesn't exist yet) | No \"missing canon\" signal — confidently irrelevant noise instead |\n| O | orient — \"translator wants A4→A5\" | governance + mode discipline | yes | **Works** |\n| P | preflight — \"implement Worker handler for submit_typeset\" | 3 unrelated oddkit ledgers + DoD + 3 generic constraints | **none** | Total miss |\n| G | get by URI (`klappy://canon/specs/ptxprint-mcp-v1.2-spec`) | direct fetch | n/a | **Works** |\n\n**Pattern:** `get` (URI-deterministic) and `orient` (high-level domain framing) work. **`search` and `preflight` — the two tools agents lean on most — fail for the majority of realistic project-domain queries**, because the 566 baseline docs (essays, ledgers, methods, fragments, writings) outnumber the 19 overlay docs ~30:1 and dominate BM25 even when the project KB has the better answer.\n\nThe retrieval failure is not a content gap in the project KB. The v1.2 spec, the governance doc, and the surfaces all *contain* the answers to queries 1, 4, 5, and 6. They are simply outranked.\n\n---\n\n## 2. The Existing Canon That Backs This Request\n\nThree oddkit canon docs converge on the question. None of them, in their current text, addresses this specific shape — the gap is a real omission, not a contradiction.\n\n### 2.1 `klappy://canon/principles/scoped-truth` (E0006, canon, tier 1)\n\n> \"When a single knowledge base serves every context, domain contamination is inevitable — software opinions bleed into theology conversations, engineering constraints shape venture strategy, personal canon governs domains it was never designed for.\"\n\nThis principle was written in March 2026 and explicitly canonizes the empirical observation. The principle's \"Three Questions for Every Scope\" test fails on the current behavior:\n\n1. **Are the axioms present?** Yes — the few required-baseline files (axioms, creed, DoD) travel correctly via the resolution stack.\n2. **Is the domain knowledge scoped?** **No** — when an agent searches the ptxprint-mcp KB, it gets writings on AI cliché editing, generative arc curves, and apocrypha fragments competing for the slot a payload-construction answer should occupy.\n3. **Are cross-domain connections explicit?** **No** — baseline content surfaces unattributed and indistinguishable from project content.\n\nScoped Truth says contamination is the failure. The current corpus composition is the failure shape it names.\n\n### 2.2 `klappy://canon/constraints/core-governance-baseline` (E0008, draft, tier 1)\n\n> \"Baseline path is never user-configurable. `knowledge_base_url` overrides the live knowledge base fetch, not the baseline. The baseline is the floor; the floor does not move.\" (Runtime Invariant #5)\n\nThis constraint defines a **three-tier resolution stack** for *required governance files*: live canon → bundled baseline → fail loud. It enumerates ~6 required-baseline files (`canon/values/orientation.md`, `canon/values/axioms.md`, `canon/meta/writing-canon.md`, `canon/constraints/definition-of-done.md`, `canon/constraints/telemetry-governance.md`, `odd/challenge/stakes-calibration.md`).\n\nThe request preserves this contract verbatim. It does not propose making baseline user-configurable, removing the floor, or weakening the resolution stack for required governance files.\n\nWhat it asks: when the agent's *search corpus* is being assembled (not the per-required-file resolution), restrict it to the overlay plus those ~6 required-baseline files, rather than indexing the full 566-doc baseline. The resolution stack still operates exactly as documented for governance reads. Search just stops dragging in `writings/`, `odd/ledger/`, `apocrypha/`, etc., which the constraint itself classifies as canon-only (never bundled, not required for tool function).\n\n### 2.3 `klappy://docs/planning/kb-data-model` (E0005, planning, tier 2)\n\n> \"Default Governance — ODD Is Always Present. ... Local governance extends ODD, never weakens it.\"\n\nThe data model establishes that ODD is always present as a default — but in the form of *governance constraints*, not as a co-equal search corpus. The doc lists exactly what's \"always present\": axioms, creed, core constraints. Not the writings folder, not the ledger, not apocrypha. The current implementation indexes the entire baseline as if the *whole* repo were \"default governance,\" which the planning doc does not assert.\n\n---\n\n## 3. The Specific Gap\n\nNone of the three canon docs above addresses **search/index corpus composition when a custom KB is set.** They address adjacent concerns:\n\n| Canon doc | Addresses | Does not address |\n|---|---|---|\n| `scoped-truth.md` | The principle (contamination is bad) | The implementation (how to prevent it in oddkit's index) |\n| `core-governance-baseline.md` | Per-required-file resolution stack | What to do with non-required baseline content during search |\n| `kb-data-model.md` | Project roles and write-target model | Read/search corpus boundaries |\n\nThe `core-governance-baseline.md` \"Canon-Only (Never Bundled)\" section names the categories that should not be in the baseline at all (`writings/`, `apocrypha/`, encoding-types/, challenge-types/, gate/, etc.). Yet those categories *are* in the live baseline corpus that gets indexed alongside the overlay. The doc tells operators these files are canon-only — but search behavior treats the entire baseline as searchable canon.\n\nThis is the gap the feature request fills: a documented, enforced boundary between *required-baseline governance* (always available, fallback floor) and *baseline content* (essays, ledgers, fragments — should not contaminate project searches).\n\n---\n\n## 4. Proposed Flag/Parameter Shapes\n\nThree options, ordered from most conservative (least change to default behavior) to most Vodka-correct (default to scoped, opt-in to merged). The maintainer should pick one; this handoff does not insist.\n\n### Option A — Opt-in flag, default unchanged\n\nAdd `baseline_in_search: boolean` parameter (default `true` to preserve current behavior). When `false`, the search index excludes baseline content other than the required-baseline files enumerated in `core-governance-baseline.md`.\n\n```typescript\n// pseudo-schema\n{\n  input: string,\n  knowledge_base_url?: string,\n  baseline_in_search?: boolean  // default true; false = scoped to KB + required baseline only\n}\n```\n\n- **Pro:** zero behavior change for existing callers. Project KBs opt in to scoping.\n- **Con:** the contamination remains the default, requiring every project KB to know about the flag. Discovery problem.\n\n### Option B — Tri-state scope parameter\n\nAdd `search_scope: \"kb_only\" | \"kb_with_required_baseline\" | \"merged\"` (default `\"merged\"` to preserve current behavior).\n\n- `\"kb_only\"`: Search ONLY the overlay; if KB is missing required governance files, response carries `governance_source: \"incomplete\"`.\n- `\"kb_with_required_baseline\"`: Overlay + the ~6 required-baseline files only. (This is what the ptxprint-mcp use case wants.)\n- `\"merged\"`: Current behavior (overlay + full baseline).\n\n- **Pro:** explicit caller control over the trade-off; integrates with the existing `governance_source` envelope signal.\n- **Con:** three modes is one more than most callers will reason about; `kb_only` may foot-gun small KBs that legitimately need axioms in search.\n\n### Option C — Default change, opt-in to merged (Vodka-correct)\n\nWhen `knowledge_base_url` is set, default to overlay + required-baseline only. Add `include_full_baseline: boolean` parameter (default `false` when KB is set, `true` when KB is unset / using default). Callers who want today's behavior set `include_full_baseline: true` explicitly.\n\n- **Pro:** treats the contamination as the surprising default it is per Scoped Truth. Aligns the implementation with the principle. Zero-config KB onboarding still works (axioms etc. travel via required-baseline).\n- **Con:** breaking change for any consumer relying on cross-baseline search hits when they set their own KB. Needs a release note, possibly an epoch bump per `governance-change-discipline.md`.\n\n### Recommendation (mine, from this session's perspective)\n\n**Option C** is most aligned with `scoped-truth` and `core-governance-baseline.md`'s \"canon-only\" classification. The breaking-change concern is real but the failure mode is loud (results change observably) and migrations are mechanical (set `include_full_baseline: true`). The retraction signal section of `core-governance-baseline.md` already names \"baseline capture\" as a failure mode telemetry could detect — telemetry on `include_full_baseline` usage would catch any regression of the same shape.\n\n**Option A** is the safest if a release this week matters more than alignment. It unblocks the ptxprint-mcp project immediately and gives time to gather telemetry before flipping defaults.\n\n---\n\n## 5. Considerations and Open Questions\n\n### 5.1 Index cache key\n\nThe current cache key (visible in trace as `index/v2.4/github_com_Klappy_ptxprint_mcp_<sha>_<sha2>`) appears to be a function of the KB SHA and the baseline SHA. Whatever option ships, the cache key must include the scope mode so different scopes don't collide on the same cached index.\n\n### 5.2 Required-baseline manifest\n\n`core-governance-baseline.md` enumerates required-baseline files in prose (§\"Required in Baseline\"). Per its Build-Time Invariant #4, a single `workers/baseline/MANIFEST.json` should list them. If that manifest exists, this feature consumes it directly. If it doesn't yet, this feature has a gating dependency on the manifest landing.\n\n### 5.3 Per-tool variation\n\n`orient` worked in the probes; `search` and `preflight` failed. The corpus composition question may not need to be answered identically for all three. `orient` returns a small `canon_refs` list and naturally bias-toward-relevance via the high-level framing; `search` and `preflight` return ranked ordered lists where the dilution math bites hardest. A first cut could ship the flag for `search` and `preflight` only and add `orient` later if telemetry shows demand.\n\n### 5.4 Telemetry signal\n\nPer the existing telemetry governance, any new parameter should be tracked. Suggested fields:\n- `search_scope` (the value the caller passed, or default if unset)\n- `index_size_at_query` (how many docs the search ranked over)\n- `overlay_doc_count` and `baseline_doc_count` separately\n\nThis data would let the maintainer tell whether the new default is being adopted, whether `merged` is being explicitly requested, and whether baseline-capture (per `core-governance-baseline.md` §\"Failure Modes OF This Contract\") is happening at the search layer.\n\n### 5.5 Interaction with `oddkit_baseline_check`\n\nThe probe described in `core-governance-baseline.md` §\"Baseline Health\" reports per-required-file presence. If this feature ships, `baseline_check` should also report whether the search-corpus scope matches the operator's intent — e.g., warn if a project KB has set scope to `kb_only` but is missing required-baseline files that other tools will need.\n\n### 5.6 Documentation pointer\n\nIf the feature ships, `core-governance-baseline.md` needs an addendum (or a sibling doc) covering the search-corpus boundary explicitly. The current §\"Canon-Only (Never Bundled)\" classification should be cross-referenced as the source of truth for what the search index excludes when scoped.\n\n---\n\n## 6. How This Handoff Should Be Used\n\nThe maintainer can:\n\n1. **Treat as a GitHub issue.** Copy the body (or sections of it) into a `klappy/oddkit` issue. The probe matrix in §1 is the reproducible evidence; the existing-canon citations in §2 are the rationale; §4 is the design space.\n\n2. **Treat as canon input.** If the next governance epoch is going to address scoped retrieval, this handoff is exploration material that should land in the relevant exploration ledger or feed into a new constraint doc that extends `core-governance-baseline.md` to cover search-corpus composition.\n\n3. **Defer with a documented retraction signal.** If the maintainer believes the current behavior is correct (full baseline merge is intentional, and the failure mode I observed is something else — bad project KB tagging, missing canon articles, etc.), the handoff should be answered with the reasoning so that the ptxprint-mcp project can adapt its KB authoring to fit. In that case, expect this handoff to be re-issued when other consumers (TruthKit, private KBs, etc.) hit the same shape.\n\nThe ptxprint-mcp project does not block on this. The autonomous coding run for the v1.2 build proceeds as planned; the agent that drives the deployed server will have degraded canon retrieval until this feature lands or until the project authors enough overlay docs to overwhelm the baseline noise (which is itself a workaround, not a fix).\n\n---\n\n## 7. Provenance\n\n- **Author:** Claude Opus 4.7 (interactive session, 2026-04-28).\n- **Operator:** klappy.\n- **Method:** Set `knowledge_base_url` on `oddkit_search`, `oddkit_orient`, `oddkit_preflight`, `oddkit_get`, `oddkit_catalog` with the `Klappy/ptxprint-mcp` repo URL. Recorded each response's full envelope including trace, search index size, top-5 hits, and source field. Cross-referenced against the canon docs surfaced in §2.\n- **Reproducibility:** Every probe in the matrix in §1 is reproducible by setting `knowledge_base_url=https://github.com/Klappy/ptxprint-mcp` on the corresponding tool call with the listed query string.\n- **Containment:** This handoff is not canon for the ptxprint-mcp project and not canon for oddkit. It is a feature request authored from one project's measured experience. The maintainer's response (acceptance, rejection with rationale, or deferral) is what governs.\n\n---\n\n*End of handoff. Companion to the v1.2 spec; references the session's audit findings; addresses the dominant retrieval-quality risk for the v1.2 Definition of Done step #2 (\"Construct a valid payload by following canon's payload-construction article\" — which requires canon search to actually surface the article).*\n"},{"path":"handoffs/open-items-validated-2026-04-30.md","uri":"klappy://canon/handoffs/open-items-validated-2026-04-30","title":"Open Items — Validated by Back-Reference and Fresh PDF Render","audience":"operator","tags":["ptxprint","mcp","handoff","validation","open-items"],"stability":"working","what_it_answers":"**What this is.** Every open handoff catalogued through session 14 (sessions 11, 12, 13, 14 + older standing handoffs), checked against repo state and a fresh PDF render against the live worker. Closes items addressed in the interim; carrie","sections":[{"slug":"live-system-state-as-of-validation","heading":"Live System State (as of validation)","start":25,"end":43},{"slug":"closed-by-this-validation-pass","heading":"Closed by This Validation Pass","start":43,"end":53},{"slug":"still-open-p1","heading":"Still Open — P1","start":53,"end":63},{"slug":"still-open-p2","heading":"Still Open — P2","start":63,"end":75},{"slug":"still-open-p3","heading":"Still Open — P3","start":75,"end":86},{"slug":"day-2-deferred-per-v1-2-spec-not-open-in-the-actionable-sense","heading":"Day-2 Deferred (per v1.2 spec — not \"open\" in the actionable sense)","start":86,"end":96},{"slug":"older-standing-handoffs","heading":"Older Standing Handoffs","start":96,"end":108},{"slug":"hygiene-findings-recurring-across-sessions","heading":"Hygiene Findings (recurring across sessions)","start":108,"end":116},{"slug":"suggested-next-move","heading":"Suggested Next Move","start":116,"end":127},{"slug":"evidence-artifacts","heading":"Evidence Artifacts","start":127,"end":137}],"body":"---\ntitle: \"Open Items — Validated by Back-Reference and Fresh PDF Render\"\naudience: operator\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"handoff\", \"validation\", \"open-items\"]\ndate: 2026-04-30\nwritten_at: 2026-04-30T03:16:00Z\nsession_window: 2026-04-30T03:02Z–2026-04-30T03:15Z (~13 min wall-clock)\ncompanion_to: [\"canon/handoffs/session-11-fonts-payload-demo.md\", \"canon/encodings/transcript-encoded-session-14.md\"]\ngovernance_source: knowledge_base\ngovernance_uri: klappy://canon/definitions/dolcheo-vocabulary\n---\n\n# Open Items — Validated by Back-Reference and Fresh PDF Render\n\n> **What this is.** Every open handoff catalogued through session 14 (sessions 11, 12, 13, 14 + older standing handoffs), checked against repo state and a fresh PDF render against the live worker. Closes items addressed in the interim; carries forward what remains owed; identifies the work that's gated on something else.\n>\n> **Method.** Live worker sweep (HEAD probes + `/health` + main HEAD) → fresh PDF render via JSON-RPC → back-reference probes for each open item (file-existence checks, content greps, upstream-issue checks, cache-promise verification).\n>\n> **Validation pass — not authoring.** Findings here describe what canon shows; nothing in the PTXprint domain has been re-decided.\n\n---\n\n## Live System State (as of validation)\n\n| Probe | Result |\n|---|---|\n| Wall-clock | 2026-04-30T03:12Z |\n| `main` HEAD | `3077e72ccd` (PR #20 merge — session-14-psalms-encoding, 2026-04-30T03:01:45Z, ~10 min before validation) |\n| `/health` | HTTP 200, version `0.1.0`, spec `v1.2-draft` |\n| Tools list | `submit_typeset`, `get_job_status`, `cancel_job`, **`docs`** ← session-13's tool is live |\n| HEAD on session-11 PDF (~26h old) | HTTP 200 |\n| HEAD on session-14 PSA PDF (~7h old) | HTTP 200 |\n| Fresh perturbed render | `job_id=837b2e262b…`, `cached=false`, `state=succeeded`, `exit_code=0`, `failure_mode=success`, 4s wall-clock |\n| Fresh PDF | 68071 bytes, PDF v1.3, 2 pages, fonts: GentiumPlus + GentiumPlus-Bold + GentiumPlus-Italic + SourceCodePro-Regular |\n| Cache-hit re-test | Unmodified fonts-payload → `cached=true` + session-11 `job_id=802e42e7…` returned (`started_at=2026-04-29T01:51:15Z`) |\n\n**The pipeline works end-to-end on the latest main HEAD.** Phase-2 fonts-payload pattern carries through to the post-session-14 codebase. Content-addressed cache works as designed.\n\n---\n\n## Closed by This Validation Pass\n\n| ID | Item | How closed |\n|---|---|---|\n| **Session-13 H-024** | Validate the docs-tool PR by fresh-context Claude | This session is fresh-context relative to session 13; called the docs tool against the live worker, got `{answer, sources}` with valid canon snippets and BM25 scores. Tool works. |\n| **H-020** | `HEAD /r2/outputs/<key>` → 404 | Re-confirmed: `HEAD` returns 200. (Was already closed in session 11; sanity-verified today.) |\n| **Session-14 H-028 — root cause** | Verify R2 output lifecycle policy | The session-14 cache miss for `bsb-jhn-empirical.json` was **payload-hash drift** (the smoke fixture was edited between sessions 11 and 14), **not** R2 lifecycle eviction. Direct evidence: re-submitting `smoke/fonts-payload.json` unmodified returned `cached=true` with session-11's `job_id=802e42e7…`, ~26 hours after creation. The cache promise holds. The \"verify the actual lifecycle policy in CF dashboard matches the documented 90 days\" sub-step is still operator-side, but is informationally unblocked — it's not the symptom. |\n\n---\n\n## Still Open — P1\n\n| ID | Item | Evidence |\n|---|---|---|\n| **H-019** | Widget-ID-to-cfg-key mapping (closes session-1 O-003) | `canon/articles/widget-id-to-cfg-key.md` does not exist on `main`. Deferred across sessions 10, 11, 12, 13, 14. The leverage move three handoffs in a row have flagged. |\n| **Session-12 H-026** | Restore cross-references regression in v5, then re-snapshot | Visual-iteration thread; cannot verify externally without re-rendering the v6 fixture and inspecting the PDF. Carry forward. |\n| **Session-12 H-027** | Author `canon/articles/section-heading-styling.md` | File does not exist on `main`. Closes session-12 O-open-12-001. |\n| **Session-12 H-028** | Extend `canon/articles/stylesheet-format.md` property table to the 40+ properties real configs use | File is 179 lines / 6 KB on `main`; the cited markers are essentially the same minimal set session 12 flagged as insufficient. Not extended. Closes session-12 O-open-12-002 when done. |\n| **session-3-gaps §1** | ESE-vs-v1.2 gap analysis (`canon/handoffs/ese-vs-v1.2-gap-analysis.md`) | File does not exist. Originally `H-002` (session 1) → `H-010` (session 3) → `H-015` (session 3.1). Reframed in session-3-gaps as ESE surface vs v1.2 spec. The most load-bearing piece of owed work; deferred 3+ sessions. |\n\n## Still Open — P2\n\n| ID | Item | Evidence |\n|---|---|---|\n| **Session-13 H-022** | README usage example for the `docs` tool | `README.md` on `main` has zero matches for `docs` or `bt-servant`. Not added. |\n| **Session-13 H-025** | Detangle BSB-JHN config discoveries into canon (likely a `bt-servant-integration.md` or `phase-1-poc-scope.md` update) | `canon/articles/bt-servant-integration.md` does not exist. `phase-1-poc-scope.md` was last touched 2026-04-29T01:59:16Z by the session-11 commit — predates session 13's morning conversation that surfaced H-025. Not addressed. |\n| **Session-14 H-026** | Psalms render quality iteration backlog | The canonical fix (autofill) is Day-2 deferred. Cfg-knob workarounds (`hangpoetry`, `preventwidows`, `preventorphans`) are cheap — each is a fresh perturbed payload — but no fixture variations have been committed. |\n| **Session-11 H-013** | Update `config-construction.md`'s \"minimal cfg\" example | Current example renders, but the H-013 update (whatever it specifies) hasn't been made. The exact ask isn't loud in the handoff text; if H-013 means \"the example isn't minimum-viable,\" the example as-is is still defensible — worth re-asking what's wanted before authoring. |\n| **Session-11 H-015** | File 3 upstream issues on `sillsdev/ptx2pdf` | Probe: `0` issues by `klappy` on `sillsdev/ptx2pdf` (all states). None filed. |\n| **Session-12 H-029** | Cookbook recipes (\"plain bold centered section headings\", \"convert testcase config to vanilla Bible config\") | Gated on H-027 + H-028. Closes session-12 O-open-12-003 when done. |\n| **Session-12 H-030** | \"Fresh-context Claude validation pattern\" canon article | Neither `canon/articles/validation-recipes.md` nor `canon/articles/smoke-test-patterns.md` exists. Note: today's session is itself a worked example of that pattern. |\n\n## Still Open — P3\n\n| ID | Item | Evidence |\n|---|---|---|\n| **Session-14 H-027** | File upstream issue against `sillsdev/ptx2pdf` for \"IF CHECK mnote Failed\" log noise | `0` issues by `klappy`; not filed. |\n| **Session-11 H-022** | Move stable fixtures into a dedicated `ptxprint-fixtures` R2 bucket | `wrangler.jsonc` on `main` declares only `OUTPUTS → ptxprint-outputs`. No `FIXTURES` binding. The four Gentium Plus TTFs are still under `outputs/fixtures/fonts/...`. |\n| **Session-11 H-023** | Verify or replace Charis SIL LFF URL pattern in canon | `canon/articles/font-resolution.md` line 35 still shows `https://lff.api.languagetechnology.org/...` placeholder. Partial mitigation: line 211 names `silnrsi/font-charis` GitHub releases as alternative. Not closed. |\n| **Session-11 H-017** | Resolve oddkit branch URL syntax for ptxprint-mcp canon indexing | Operator + oddkit-maintainer item; no visible commit-side artifact. |\n| **Session-11 H-025** | Retire H-021 unless larger fixture demonstrates it bites | H-021 itself isn't surfaced in current handoffs; trace lost. Could be retired in a later cleanup pass. |\n| **Session-11 C-009 fallout** | Stale `_todo` block in `smoke/minimal-payload.json` | Block still present today: top-level key `_todo` with two items. |\n\n## Day-2 Deferred (per v1.2 spec — not \"open\" in the actionable sense)\n\nThese are scope-deferred by the spec, not omissions to address. Listed for completeness.\n\n- `cancel_job` SIGTERM into the running container (the tool is exposed in `/health`'s tools list; whether the SIGTERM path is wired is the open piece)\n- `get_upload_url` presigned R2 PUTs (currently `/internal/upload` is unauthenticated)\n- Strip bundled fonts (session-3 C-007)\n- Autofill `mode`\n- Per-pass progress streaming\n\n## Older Standing Handoffs\n\n| Handoff | Status |\n|---|---|\n| `documentation-cleanup-handoff.md` | Per stale memory, all P0 items applied. No contradicting evidence in sessions 7–14. Treat as closed unless reasserted. |\n| `governance-update-handoff.md` | Per stale memory, no-op against current `headless-operations.md` (which is v1.2-aligned per its 2026-04-28 metadata). Treat as closed. |\n| `missing-uploads-handoff.md` | Status not reasserted in any session 7–14 ledger. Trace lost; would need fresh inventory pass. |\n| `oddkit-kb-isolation-feature-request.md` | Filed externally; awaiting maintainer disposition. Not actionable here. |\n| `session-10-after-first-pdf-handoff.md` | Superseded by `session-11-fonts-payload-demo.md` (which carried its open items forward). |\n\n---\n\n## Hygiene Findings (recurring across sessions)\n\n- **H-numbering collisions.** Session-12, session-13, and session-14 each independently use `H-022`–`H-028` for unrelated items. Reference scheme should be `<session>-H<n>` (e.g. `s14-H028`) to avoid ambiguity. This validation pass uses that disambiguation throughout the tables above.\n- **No dedicated handoff file for sessions 12, 13, 14.** Only encodings exist for those sessions; their open items live in the encoding's `## H — Handoffs` section, which works but breaks the pattern of dedicated `canon/handoffs/session-<n>-*.md` files (sessions 3, 10, 11 have them).\n- **User-memory drift.** Stale assistant memory referenced session 6 as the latest; canon shows session 14. Memory was bypassed in favor of canon-direct lookup, per the canon-first principle.\n\n---\n\n## Suggested Next Move\n\nIf the operator wants the highest-leverage single move, two contenders stand out:\n\n1. **`s11-H019` widget-ID-to-cfg-key mapping** — three sessions deferred, unlocks reliable runtime overrides for all of Phase 2. Resolution paths in priority order: (a) run PTXprint with `-I` introspection flag and capture; (b) read GTK Glade XML; (c) ask Martin.\n2. **`session-3-gaps §1` ESE-vs-v1.2 gap analysis** — also three sessions deferred, the most load-bearing piece of owed work per its own framing. Output: `canon/handoffs/ese-vs-v1.2-gap-analysis.md`.\n\nBoth are own-session items per their handoffs.\n\n---\n\n## Evidence Artifacts\n\n- **Fresh PDF (validation):** `https://ptxprint-mcp.klappy.workers.dev/r2/outputs/837b2e262b038fcf54e6170032f20a8fb4d65aadb1c4d34937457e5a0a61ad29/minitest_Default_JHN_ptxp.pdf` (also delivered as `validation-2026-04-30T03-12Z_minitest_Default_JHN_ptxp.pdf` alongside this report)\n- **Cache-hit re-test:** `job_id=802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd` (session-11's; today's submit returned `cached=true`)\n- **Live worker version:** `0.1.0`, `v1.2-draft`, main HEAD `3077e72ccd`\n\n---\n\n*End of validation report.*\n"},{"path":"handoffs/pr-30-fresh-validator-review.md","uri":"klappy://canon/handoffs/pr-30-fresh-validator-review","title":"PR #30 Fresh-Session Validation Review — v1.3 Worker Telemetry Surface","audience":"project","tags":["ptxprint","mcp","review","pr-30","telemetry","v1.3","fresh-session","dolcheo"],"stability":"working","what_it_answers":"I did not author the code under review. This session was initiated with no context from the implementing agent session. Every finding below is grounded in direct observation of the code, test files, and test execution output. The author's P","sections":[{"slug":"independence-statement","heading":"Independence Statement","start":19,"end":25},{"slug":"environment-and-setup","heading":"Environment and Setup","start":25,"end":38},{"slug":"test-run-output-observed-directly","heading":"Test Run Output (Observed Directly)","start":38,"end":58},{"slug":"dod-4-privacy-floor-exclusions-highest-stakes","heading":"DoD #4 — Privacy-Floor Exclusions (HIGHEST STAKES)","start":58,"end":108},{"slug":"dod-5-three-tier-fallback","heading":"DoD #5 — Three-Tier Fallback","start":108,"end":138},{"slug":"dod-6-self-report-headers","heading":"DoD #6 — Self-Report Headers","start":138,"end":166},{"slug":"dod-7-rate-limit","heading":"DoD #7 — Rate Limit","start":166,"end":203},{"slug":"dataset-allowlist-guard","heading":"Dataset Allowlist Guard","start":203,"end":239},{"slug":"6-implementation-checks","heading":"§6 Implementation Checks","start":239,"end":259},{"slug":"spec-lock-compliance","heading":"Spec-Lock Compliance","start":259,"end":269},{"slug":"author-surfaced-deviations","heading":"Author-Surfaced Deviations","start":269,"end":315},{"slug":"test-suite-quality-assessment","heading":"Test Suite Quality Assessment","start":315,"end":335},{"slug":"summary-table","heading":"Summary Table","start":335,"end":357},{"slug":"final-verdict","heading":"Final Verdict","start":357,"end":377}],"body":"---\ntitle: \"PR #30 Fresh-Session Validation Review — v1.3 Worker Telemetry Surface\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"review\", \"pr-30\", \"telemetry\", \"v1.3\", \"fresh-session\", \"dolcheo\"]\ndate: 2026-04-30\nreviewer: \"Fresh-session managed agent (Claude, independent validator)\"\nreviews: \"PR #30 — feat(v1.3): Worker telemetry surface\"\nauthorities:\n  - canon/specs/ptxprint-mcp-v1.3-spec.md\n  - canon/governance/telemetry-governance.md\n  - canon/handoffs/telemetry-governance-h-t2-review.md\nstatus: complete\n---\n\n# PR #30 Fresh-Session Validation Review\n\n## Independence Statement\n\nI did not author the code under review. This session was initiated with no context from the implementing agent session. Every finding below is grounded in direct observation of the code, test files, and test execution output. The author's PR description is treated as a claim, not evidence. All assertions in this review are verified against actual file contents and actual test runs.\n\n---\n\n## Environment and Setup\n\n- **Repo cloned fresh:** `git clone` from `klappy/ptxprint-mcp`\n- **Branch:** `pr-30` (fetched from `refs/pull/30/head`)\n- **Commits in PR:** 4 commits over main\n  - `835623f` fix(telemetry): close dataset allowlist bypass via comma-separated FROM tables\n  - `418d547` fix(telemetry): wire clientInfo.name to consumer resolution and drop unused TELEMETRY_VERIFIED_CLIENTS binding\n  - `f27815c` fix: harden telemetry rate-limit parse and hoist MCP handlers\n  - `7e03bdf` feat(v1.3): Worker telemetry surface (PR 1 of 2)\n- **Files changed vs main:** `package.json`, `package-lock.json`, `scripts/bundle-telemetry-policy.ts`, `src/bundled-policy.ts`, `src/index.ts`, `src/telemetry.ts`, `test/telemetry.test.ts`, `vitest.config.ts`, `wrangler.jsonc`\n\n---\n\n## Test Run Output (Observed Directly)\n\n```\n> ptxprint-mcp@0.1.0 test\n> vitest run\n\n RUN  v4.1.5 /home/user/ptxprint-mcp\n\n ✓ test/telemetry.test.ts (43 tests) 39ms\n\n Test Files  1 passed (1)\n       Tests  43 passed (43)\n    Start at  16:10:07\n    Duration  261ms (transform 74ms, setup 0ms, import 97ms, tests 39ms, environment 0ms)\n```\n\n**TypeScript check:** `npx tsc --noEmit` → **no output** (zero errors).\n\n---\n\n## DoD #4 — Privacy-Floor Exclusions (HIGHEST STAKES)\n\n**Verdict: PASS**\n\n**Evidence — the 10 prohibited fields (spec §10 DoD #4):**\n\n`src/telemetry.ts` lines 245–256 defines `PROHIBITED_FIELDS` as a `const` array:\n\n```typescript\nexport const PROHIBITED_FIELDS = [\n  \"project_id\", \"config_name\", \"book_codes\", \"source_url\", \"font_url\",\n  \"figure_url\", \"payload_full\", \"usfm_bytes\", \"log_content\", \"pdf_bytes\",\n] as const;\n```\n\nAll 10 fields match spec §10 exactly. ✓\n\n**Evidence — redactor enforces prohibition:**\n\n`src/telemetry.ts` lines 287–316, function `redactAndValidate`:\n1. Step 1 (lines 291–298): Iterates `PROHIBITED_FIELDS` and returns `{ ok: false, error: \"prohibited field: ${field}\" }` if any prohibited key is present — **before zod schema validation**.\n2. Step 2 (lines 300–307): Strict zod schema (`.strict()`) rejects any additional keys beyond the explicitly listed allowed fields.\n3. Step 3 (lines 309–313): Defense-in-depth truncation of `payload_hash_prefix` to 8 chars.\n\n**Evidence — route handler gates on redactor result:**\n\n`src/index.ts` lines 530–534:\n```typescript\nconst result = redactAndValidate(body);\nif (!result.ok) {\n  return Response.json({ error: result.error }, { status: 400 });\n}\n// writeTelemetry only called after this gate\n```\n`writeTelemetry` is never reachable when `redactAndValidate` returns `ok: false`.\n\n**Evidence — test coverage:**\n\n`test/telemetry.test.ts` lines 37–145:\n- **Combined test** (line 61): Sends envelopes containing each of the 10 fields, asserts `result.ok === false` with `expect(result.error).toContain(\"prohibited field: ${field}\")`, and asserts `expect(mockWriteDataPoint).not.toHaveBeenCalled()` after the loop.\n- **Individual test per field** (lines 81–101): 10 separate `it()` blocks, one per prohibited field, each asserting `result.ok === false`.\n- **Strict-mode test** (line 122): Asserts that unknown extra keys (not in the prohibited list) are also rejected.\n- **Valid envelope test** (line 104): Asserts that a correctly-formed envelope passes.\n\nAll 10 tests passed. ✓\n\n**Nit on individual tests:** The per-field individual tests (lines 81–101) include a `writeTelemetry(mockEnv, \"job_phase\", {})` call followed by `mockWriteDataPoint.mockClear()` inside each block. This is bookkeeping noise — the call is a valid write, not a leakage test. The mock-not-called assertion is only in the combined test. This does not invalidate coverage; the combined test provides the definitive proof. Flagged as a NIT for test clarity.\n\n---\n\n## DoD #5 — Three-Tier Fallback\n\n**Verdict: PASS**\n\n**Evidence — implementation:**\n\n`src/telemetry.ts` lines 330–355, function `resolveTelemetryPolicy`:\n1. Tier 1: Live `fetch()` to `https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/governance/telemetry-governance.md`. Sanity check: response must include `\"# Telemetry Governance\"`.\n2. Tier 2: `if (bundledPolicy) { return { policy: bundledPolicy, source: \"bundled\" } }` — bundled constant is passed as parameter (testable design).\n3. Tier 3: Returns `MINIMAL_POLICY` (static string in module). Sanity check: the string includes the dataset name, canonical URI, and all 10 prohibited field names (`src/telemetry.ts` lines 46–74).\n\n**Evidence — bundled policy is real:**\n\n`src/bundled-policy.ts` is auto-generated by `scripts/bundle-telemetry-policy.ts` at deploy time. The file exists and contains a full copy of `canon/governance/telemetry-governance.md`. Build provenance is correct.\n\n**Evidence — test coverage:**\n\n`test/telemetry.test.ts` lines 154–224:\n- `knowledge_base` success: mock 200 response with heading → asserts `source === \"knowledge_base\"`. ✓\n- Bundled fallback (network error): mock rejected fetch → asserts `source === \"bundled\"`. ✓\n- Bundled fallback (non-200): mock 404 → asserts `source === \"bundled\"`. ✓\n- Bundled fallback (malformed): mock 200 but no heading → asserts `source === \"bundled\"`. ✓\n- Minimal fallback (null bundled): mock error + `resolveTelemetryPolicy(null)` → asserts `source === \"minimal\"`. ✓\n- Minimal fallback (empty bundled): mock error + `resolveTelemetryPolicy(\"\")` → asserts `source === \"minimal\"` (empty string is falsy). ✓\n- Minimal policy content: asserts MINIMAL_POLICY contains dataset name, canonical URI, and all 10 prohibited field names. ✓\n\nAll 7 tests passed.\n\n---\n\n## DoD #6 — Self-Report Headers\n\n**Verdict: PASS**\n\n**Evidence — implementation:**\n\n`src/telemetry.ts` lines 210–235, function `resolveConsumer`:\nPriority order implemented: `?consumer=` query → `x-ptxprint-client` header → `clientInfo.name` → `User-Agent` → `\"unknown\"`. Each returns the correct `ConsumerSource` string.\n\n`src/index.ts` lines 655–670: `handleMcpWithTelemetry` calls `resolveConsumer(urlObj, req.headers, clientInfoName)` and passes the result to `writeTelemetry`. Consumer information is correctly plumbed from HTTP layer into telemetry events.\n\n**Evidence — test coverage:**\n\n`test/telemetry.test.ts` lines 378–456:\n- All 8 self-report headers present → `source === \"header\"`, `label === \"test-client\"`. ✓\n- Only `?consumer=foo` present → `source === \"query\"`, `label === \"foo\"`. ✓\n- Query param takes priority over header. ✓\n- Header fallback when no query param. ✓\n- `client_info` fallback. ✓\n- `user_agent` fallback. ✓\n- `\"unknown\"` when nothing set. ✓\n\nAll tests passed. ✓\n\n**NIT — leaderboard completeness scoring not implemented:** Spec §10 DoD #6 mentions \"a complete leaderboard score\" for all-header requests. The governance article §\"Completeness Scoring\" defines badge tiers (Open Ledger ≥ 90%, etc.). No scoring computation exists in this PR. The consumer label and source are correctly captured for leaderboard queries, but the badge assignment logic is absent. This is an observable gap vs. spec text but does not affect telemetry correctness. Flagged as NIT.\n\n---\n\n## DoD #7 — Rate Limit\n\n**Verdict: PASS (with NIT on key choice)**\n\n**Evidence — implementation:**\n\n`src/telemetry.ts` lines 373–396, function `rateLimitExceeded`:\n- Module-scoped `Map<string, { count: number; windowStart: number }>`.\n- Sliding window: if entry is missing or window is >1 hour old, starts new window at count=1 (not exceeded).\n- On subsequent calls, increments count and returns `count > limitPerHour`.\n- 61st call: count becomes 61 → `61 > 60` → `true` (exceeded). Correct boundary. ✓\n\n`src/telemetry.ts` lines 449–454: `forwardTelemetryQuery` reads `TELEMETRY_QUERY_RATE_LIMIT_PER_HOUR` env var, parses with `parseInt`, falls back to 60 if not finite.\n\n**Evidence — test coverage:**\n\n`test/telemetry.test.ts` lines 232–285:\n- Calls 1–60 return `false`; call 61 returns `true`. ✓\n- Per-consumer independence: consumer A exhausted does not affect consumer B. ✓\n- Sanitized error via `forwardTelemetryQuery`: returns `{ error: \"Query rate limit exceeded; retry later\" }` with no `rows`. ✓\n\nAll tests passed. ✓\n\n**NIT — rate limit key in `telemetry_public` tool is DO session ID, not consumer label:**\n\n`src/index.ts` line 448: `const rateLimitKey = this.ctx.id.toString();`\n\nSpec §3 says: \"Rate-limit per consumer label.\" The implementation rate-limits per `McpAgent` Durable Object session ID. This diverges from spec wording. However:\n1. The author documents this explicitly in the code comment (lines 445–448).\n2. At sub-1000 queries/day expected volume, per-session limiting provides adequate AE quota protection.\n3. The McpAgent limitation means real consumer label headers are not accessible inside tool handlers.\n4. The `rateLimitExceeded` function itself works correctly per consumer key; the key passed is the limitation.\n\nThis is a **NIT**, not a blocker. Acceptable for v1.3 ship. v1.4 candidate as author noted.\n\n---\n\n## Dataset Allowlist Guard\n\n**Verdict: PASS**\n\n**Evidence — implementation:**\n\n`src/telemetry.ts` lines 405–422, function `validateDatasetAllowlist`:\n```\nstrip line comments (--...)\nstrip block comments (/* ... */)\nstrip string literals ('...')\n→ lowercase\nextract all FROM/JOIN targets (including comma-separated)\nrequire: every target === \"ptxprint_telemetry\"\n```\n\nThe regex at line 413 handles comma-separated FROM tables: `([a-z_][a-z0-9_]*(?:\\s*,\\s*[a-z_][a-z0-9_]*)*)`. The match is then split on commas (line 416). This closes the bypass via `FROM ptxprint_telemetry, secret_dataset`.\n\n**Evidence — test coverage:**\n\n`test/telemetry.test.ts` lines 292–369:\n- Wrong dataset rejected. ✓\n- Correct dataset accepted. ✓\n- Comment-smuggling (UNION + line comment) rejected. ✓\n- UNION with different dataset rejected. ✓\n- JOIN on same dataset accepted. ✓\n- No FROM clause rejected. ✓\n- Block comment smuggling rejected. ✓\n- **String literal smuggling**: `SELECT 'ptxprint_telemetry' FROM secret_dataset` → string literal stripped → `FROM secret_dataset` → rejected. ✓\n- Mixed-case SQL keywords handled (`.toLowerCase()` applied). ✓\n- Sanitized error via `forwardTelemetryQuery` for disallowed dataset. ✓\n\nAll tests passed. ✓\n\n---\n\n## §6 Implementation Checks\n\n### §6.1 Writer Signature\n\n**PASS.** `TelemetryFields` interface (`src/telemetry.ts` lines 89–109) specifies exact typed fields: optional strings for blob dimensions, optional numbers for double dimensions. The `writeTelemetry` function (`src/telemetry.ts` lines 154–194) positionally maps exactly 12 blobs and 10 doubles per the governance schema. No free-form string slots exist. Adding a dimension requires changing the interface — this is a compile-time contract. ✓\n\n### §6.2 Redactor — Zod with Strict Mode\n\n**PASS.** `TelemetryEnvelopeSchema` (`src/telemetry.ts` lines 262–276) uses `.strict()` (zod's `additionalProperties: false` equivalent). Any envelope key not in the schema fails validation. The `redactAndValidate` function also explicitly checks prohibited fields before schema validation, yielding clearer error messages. ✓\n\n### §6.3 Policy Fetcher — Three-Tier Order\n\n**PASS.** Order is `knowledge_base → bundled → minimal`. Sanity check `text.includes(\"# Telemetry Governance\")` guards against malformed or wrong-URL responses. ✓\n\n### §6.4 Public Query Forwarder — Strip Before Check\n\n**PASS.** Comments and string literals are stripped before FROM/JOIN regex matching (`src/telemetry.ts` lines 406–421). The spec's smuggling example (`SELECT * FROM secret_dataset UNION SELECT * FROM ptxprint_telemetry`) is handled by the UNION catching both FROM targets, not just the last one. ✓\n\n---\n\n## Spec-Lock Compliance\n\n**Verdict: PASS**\n\n`git diff main...pr-30 -- canon/specs/` produced no output. `git diff main...pr-30 -- canon/` produced no output. No files under `canon/` were modified in this PR.\n\nThe spec-lock principle is observed. ✓\n\n---\n\n## Author-Surfaced Deviations\n\n### Deviation 1 — In-Memory Rate Limiter\n\n**Judgment: ACCEPTABLE for v1.3 ship. Confirmed v1.4 candidate.**\n\nThe rate limiter is a module-scoped `Map` in the Worker isolate. At expected volume (sub-1000 queries/day), per-isolate tracking is sufficient to protect the 10,000 queries/day AE free-tier quota. The implementation is correct for its scope. Cross-isolate persistence (via KV or DO) would provide stronger guarantees but adds complexity not justified at current scale.\n\nThe additional complication: the `telemetry_public` tool inside `McpAgent` uses `this.ctx.id.toString()` as the rate-limit key rather than the consumer label. This means the rate limit is per-DO-session, not per-consumer. Per-session rate limiting still protects the quota; it just doesn't aggregate across sessions of the same consumer. This is a documented McpAgent limitation.\n\nAssessment: adequate for v1.3. Ship it.\n\n### Deviation 2 — Consumer Label Not Available in Tool Handlers\n\n**Judgment: ACCEPTABLE for v1.3 ship. Platform-constrained limitation.**\n\nThe `McpAgent` Durable Object does not expose HTTP request headers to tool handlers. The MCP telemetry hooks in `handleMcpWithTelemetry` (which runs in the outer Worker scope) DO have full access to headers and correctly resolve consumer label via `resolveConsumer()`. The limitation only affects the rate-limit key inside `telemetry_public` (Deviation 1).\n\nDoD #6 is satisfied: the MCP telemetry writes correctly use real consumer labels. The rate-limiting deviation is a separate, documented limitation. No action required for v1.3.\n\n### Deviation 3 — No Service-Binding Enforcement on /internal/telemetry\n\n**Judgment: NIT (not a blocker). Privacy floor intact. Data integrity risk is low.**\n\nThe spec §5 architecture diagram labels `/internal/telemetry` as \"service-binding only, not externally reach.\" The implementation makes it publicly accessible, consistent with `/internal/job-update` (same pattern, same constraint).\n\n**Privacy floor analysis:**\n- The redactor (`redactAndValidate`) enforces all 10 prohibited fields and strict schema on every envelope, regardless of caller origin.\n- An external attacker cannot inject any prohibited content (project_id, source_url, etc.) because the redactor will reject it.\n- DoD #4 remains fully satisfied even with the endpoint being publicly accessible.\n\n**Data integrity risk:**\n- An external attacker could POST valid envelopes (job_phase, job_terminal events with allowed fields) to pollute the analytics dataset with garbage data points.\n- This could skew dashboard metrics (fake success events, fake cache hits, etc.).\n- This is a data integrity concern, not a privacy concern.\n\n**Assessment:** Acceptable for v1.3 because:\n1. Privacy floor holds — no prohibited data can enter the dataset.\n2. The risk (dashboard pollution) is the same as the existing /internal/job-update exposure.\n3. The author correctly documents the deviation and flags it as v1.4 work.\n4. At low traffic volume, practical pollution risk is minimal.\n\n**Recommended NIT:** Add a comment noting that `/internal/telemetry` is publicly accessible (not service-binding–only) and that this is a known deviation from spec §5 architecture — to make the deviation visible for future implementers without implying the privacy floor is at risk.\n\n---\n\n## Test Suite Quality Assessment\n\n43 tests. All pass. Coverage observed:\n\n| Coverage Area | Tests | Status |\n|---|---|---|\n| All 10 prohibited fields (individual) | 10 | ✓ |\n| All 10 prohibited fields (combined, mock-not-called) | 1 | ✓ |\n| Strict mode (unknown key rejection) | 1 | ✓ |\n| Valid envelope acceptance | 1 | ✓ |\n| payload_hash_prefix truncation | 1 | ✓ |\n| Three-tier fallback (all paths) | 7 | ✓ |\n| Rate limit (boundary, per-consumer, sanitized error) | 3 | ✓ |\n| Dataset allowlist (9 cases) | 9 | ✓ |\n| Consumer resolution (all 7 priority cases) | 7 | ✓ |\n| Writer blob/double mapping | 2 | ✓ |\n| **Total** | **43** | **All pass** |\n\n---\n\n## Summary Table\n\n| DoD / Area | Status | Evidence |\n|---|---|---|\n| DoD #4 — Privacy floor (10 fields) | **PASS** | All 10 in PROHIBITED_FIELDS; redactor check precedes zod; route gates on ok; 10+1 tests pass |\n| DoD #5 — Three-tier fallback | **PASS** | knowledge_base → bundled → minimal; 7 tests covering all failure modes |\n| DoD #6 — Self-report headers | **PASS** | resolveConsumer correct priority order; 7 tests pass |\n| DoD #7 — Rate limit | **PASS** | 61st call rejected; boundary correct; 3 tests pass |\n| Dataset allowlist guard | **PASS** | Strip before check; 9 tests covering comment/string/UNION/comma attacks |\n| §6.1 Writer typed | **PASS** | TelemetryFields interface; positional blobs/doubles; no free-form slots |\n| §6.2 Redactor strict zod | **PASS** | .strict() schema + explicit prohibited-field check |\n| §6.3 Policy fetcher three-tier | **PASS** | Correct order, sanity check on response |\n| §6.4 Query forwarder strip-first | **PASS** | Comments and literals stripped before FROM/JOIN regex |\n| Spec-lock compliance | **PASS** | Zero changes to canon/ in this PR |\n| Deviation 1: in-memory rate limiter | **NIT** | Adequate for v1.3; per-session vs. per-consumer noted |\n| Deviation 2: consumer label in tool handlers | **NIT** | Platform-constrained; MCP-layer writes use real consumer |\n| Deviation 3: /internal/telemetry reachable | **NIT** | Privacy floor intact; data integrity risk low; consistent with /internal/job-update |\n| Leaderboard scoring not implemented | **NIT** | Badge computation absent; capture correct for queries |\n| Per-field mock-not-called coverage | **NIT** | Combined test covers this; per-field tests could be tightened |\n\n---\n\n## Final Verdict\n\n**✅ SAFE TO MERGE**\n\n**0 blockers. 5 nits. 0 privacy floor violations.**\n\nThe privacy floor (DoD #4) is the highest-stakes criterion and it passes without qualification. All 10 prohibited fields are tested individually and in aggregate, with mock verification that `writeDataPoint` is never called on rejection. The redactor chain (explicit check → strict zod → route gate) is correct. TypeScript compiles clean. 43/43 tests pass.\n\nThe nits are real observations about spec divergences but none of them constitute a blocker:\n- The rate limiter protects the AE quota even if per-session rather than per-consumer.\n- The privacy floor holds even with `/internal/telemetry` being externally reachable.\n- Leaderboard scoring is a UX enhancement, not a privacy floor requirement.\n\nThe author's documented deviations are accurately characterized and appropriate for v1.3 ship.\n\n---\n\n*Review completed by fresh-session managed agent (Claude), 2026-04-30.*\n*Companion: `canon/encodings/pr-30-fresh-validator-ledger.md`*\n"},{"path":"handoffs/pr-30-revalidation-addendum.md","uri":"klappy://canon/handoffs/pr-30-revalidation-addendum","title":"PR #30 Re-Validation Addendum — Cursor Agent Fix Commits (3 commits)","audience":"project","tags":["ptxprint","mcp","review","revalidation","telemetry","pr-30","cursor-fixes"],"stability":"working","what_it_answers":"This review is written by a **fresh re-validator session** (Claude, Anthropic) that did NOT author PR #30, did NOT author the original validator review (PR #31), and has no access to the reasoning of either prior session. All findings below","sections":[{"slug":"independence-statement","heading":"Independence Statement","start":14,"end":24},{"slug":"scope","heading":"Scope","start":24,"end":40},{"slug":"test-run-output","heading":"Test Run Output","start":40,"end":62},{"slug":"per-fix-verdicts","heading":"Per-Fix Verdicts","start":62,"end":193},{"slug":"dod-4-privacy-floor-re-check","heading":"DoD #4 Privacy Floor — Re-Check","start":193,"end":217},{"slug":"spec-lock-compliance","heading":"Spec-Lock Compliance","start":217,"end":226},{"slug":"summary-of-findings","heading":"Summary of Findings","start":226,"end":238},{"slug":"final-verdict","heading":"Final Verdict","start":238,"end":258}],"body":"---\ntitle: \"PR #30 Re-Validation Addendum — Cursor Agent Fix Commits (3 commits)\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"review\", \"revalidation\", \"telemetry\", \"pr-30\", \"cursor-fixes\"]\ndate: 2026-04-30\ncompanion_to: \"canon/handoffs/pr-30-fresh-validator-review.md (PR #31, original validation at 835623f)\"\nstatus: complete\n---\n\n# PR #30 Re-Validation Addendum — Cursor Agent Fix Commits\n\n## Independence Statement\n\nThis review is written by a **fresh re-validator session** (Claude, Anthropic) that did NOT author\nPR #30, did NOT author the original validator review (PR #31), and has no access to the reasoning\nof either prior session. All findings below are derived from direct code observation, test\nexecution, and spec comparison against `klappy://canon/specs/ptxprint-mcp-v1.3-spec` and\n`klappy://canon/governance/telemetry-governance` as loaded from the ptxprint-mcp knowledge base.\n\n---\n\n## Scope\n\nSHA range reviewed: **`835623f..7e03bdf`** (the 3 Cursor Agent fix commits applied after the\noriginal validator signed off at `835623f`)\n\n| Commit | Title |\n|--------|-------|\n| `418d547` | fix(telemetry): close dataset allowlist bypass via comma-separated FROM tables |\n| `f27815c` | fix(telemetry): wire clientInfo.name to consumer resolution and drop unused TELEMETRY_VERIFIED_CLIENTS binding |\n| `7e03bdf` | fix: harden telemetry rate-limit parse and hoist MCP handlers |\n\nOriginal validator review: PR #31 (`review/pr-30-fresh-validator` branch)\nThis addendum: PR #XX (`review/pr-30-revalidation` branch — the branch you are reading this from)\n\n---\n\n## Test Run Output\n\n```\n> ptxprint-mcp@0.1.0 test\n> vitest run\n\n RUN v4.1.5 /home/user/ptxprint-mcp\n\n ✓ test/telemetry.test.ts (43 tests) 48ms\n\n Test Files  1 passed (1)\n       Tests  43 passed (43)\n    Start at  17:59:21\n    Duration  348ms\n```\n\nTypeScript check: `npx tsc --noEmit` — **clean, no errors**\n\nTest count: **43** (same as original baseline; Cursor did not add regression tests — see NIT-1 below)\n\n---\n\n## Per-Fix Verdicts\n\n### Fix 1 — `418d547`: Close dataset allowlist bypass via comma-separated FROM tables\n\n**Verdict: PASS with NIT**\n\n**What changed:**\n`validateDatasetAllowlist()` in `src/telemetry.ts` changed its regex from:\n```typescript\n// OLD — captures only the first identifier after FROM/JOIN\n/\\b(?:from|join)\\s+([a-z_][a-z0-9_]*)/g\n```\nto:\n```typescript\n// NEW — captures the full comma-separated list, then splits\n/\\b(?:from|join)\\s+([a-z_][a-z0-9_]*(?:\\s*,\\s*[a-z_][a-z0-9_]*)*)/g\n```\nplus a `.flatMap((m) => m[1].split(/\\s*,\\s*/))` to enumerate each table name.\n\n**Bypass verified closed:** The old regex on `SELECT * FROM ptxprint_telemetry, secret_dataset`\nextracted only `['ptxprint_telemetry']` — the comma terminated the capture. The attacker's\n`secret_dataset` was never seen. The new regex extracts\n`['ptxprint_telemetry', 'secret_dataset']`. Since `secret_dataset !== 'ptxprint_telemetry'`,\nthe query is rejected. **The bypass is correctly closed.**\n\n**Clean case verified still passes:** `SELECT * FROM ptxprint_telemetry` extracts\n`['ptxprint_telemetry']` → `every(n => n === 'ptxprint_telemetry')` → `true`. ✓\n\n**Direct verification (executed inline):**\n```\ncomma case refs: [\"ptxprint_telemetry\",\"secret_dataset\"]\ncomma case result (all ptxprint_telemetry?): false   ← bypass BLOCKED ✓\nclean case refs: [\"ptxprint_telemetry\"]\nclean case result: true                              ← clean query PASSES ✓\n```\n\n**NIT-1 (test coverage gap):** The Cursor agent fixed the code but **did not add a regression\ntest** for the comma-separated FROM bypass. The 10 existing dataset-allowlist tests all pre-date\nthis fix commit (none contain a comma-FROM pattern). Recommend adding:\n```typescript\nit(\"rejects comma-separated FROM tables (bypass vector)\", () => {\n  expect(\n    validateDatasetAllowlist(\n      \"SELECT * FROM ptxprint_telemetry, secret_dataset\",\n    ),\n  ).toBe(false);\n});\n```\nWithout this test, the specific bypass attack vector has no automated regression protection.\n\n---\n\n### Fix 2 — `f27815c`: Wire clientInfo.name + drop TELEMETRY_VERIFIED_CLIENTS\n\n**Verdict: PASS**\n\n**clientInfo.name wiring:**\n`src/index.ts` now extracts `clientInfo.name` from `initialize` requests:\n```typescript\nif (rpc?.method === \"initialize\") {\n  const ci = (rpc.params as { clientInfo?: { name?: string } }).clientInfo;\n  if (ci?.name) clientInfoName = ci.name;\n}\nconst consumer = resolveConsumer(urlObj, req.headers, clientInfoName);\n```\n`resolveConsumer()` was updated to accept `clientInfoName?: string` as a third parameter and\nchecks it at priority 3 (after `?consumer=` query param and `x-ptxprint-client` header, before\n`User-Agent`). This matches spec §6.1 exactly:\n```\n1. ?consumer= query parameter   → source: \"query\"\n2. x-ptxprint-client header     → source: \"header\"\n3. MCP clientInfo.name          → source: \"client_info\"   ← NEW\n4. User-Agent header             → source: \"user_agent\"\n5. \"unknown\"                    → source: \"unknown\"\n```\nThe existing test `\"falls back to client_info when no query or header\"` in DoD #6 exercises this\npath and passes. ✓\n\n**TELEMETRY_VERIFIED_CLIENTS removal:**\nRemoved from: `Env` interface in `src/index.ts`, `TelemetryEnv` interface in `src/telemetry.ts`.\n**This is NOT a spec deviation.** The spec §6.1 priority order (above) does not list\n`TELEMETRY_VERIFIED_CLIENTS` as a consumer *resolution* source. The field was for\n\"weighted leaderboard scoring\" — never for label resolution — and was declared in both\nTypeScript interfaces without ever being read. Removing dead TypeScript declarations is correct.\n\n**Residual references (by design):**\n- `wrangler.jsonc` line 90: a comment noting it as an optional secret. Acceptable — it can still\n  be set as a future-use secret. The governance article still documents it.\n- `src/bundled-policy.ts`: contains the full governance article text (a string constant), which\n  still mentions `TELEMETRY_VERIFIED_CLIENTS`. This is documentation inside a string, not a\n  code binding.\n\nNeither residual reference creates a runtime behavior dependency. Both are accurate documentation\nof a future optional feature. **Not a blocker.**\n\n---\n\n### Fix 3 — `7e03bdf`: Harden rate-limit parse + hoist MCP handlers\n\n**Verdict: PASS**\n\n**Rate-limit parse hardening:**\n```typescript\n// OLD\nconst limit = parseInt(env.TELEMETRY_QUERY_RATE_LIMIT_PER_HOUR ?? \"60\", 10);\n\n// NEW\nconst parsedLimit = parseInt(env.TELEMETRY_QUERY_RATE_LIMIT_PER_HOUR ?? \"60\", 10);\nconst limit = Number.isFinite(parsedLimit) ? parsedLimit : 60;\n```\n`parseInt(\"banana\", 10)` → `NaN`; `Number.isFinite(NaN)` → `false` → default 60 applied. ✓\n`parseInt(\"Infinity\", 10)` → `NaN` → default 60. ✓\n`parseInt(\"-1\", 10)` → `-1`; `Number.isFinite(-1)` → `true` → limit = -1 (allows all; edge case,\n  but not a security issue since rate limiting protects AE quota, not data). Acceptable.\nDefault spec value of 60 req/hr is preserved when env var is missing or malformed. ✓\n\n**MCP handler hoisting:**\n```typescript\n// Module scope — created once at initialization\nconst mcpHandler = PtxprintMcp.serve(\"/mcp\", { binding: \"MCP_AGENT\" });\nconst mcpSseHandler = PtxprintMcp.serveSSE(\"/sse\", { binding: \"MCP_AGENT\" });\n```\nPreviously these were allocated per-request inside `handleMcpWithTelemetry`. Since `basePath` and\n`binding` were constants, every call produced identical handler objects. The hoisting is a\n**no-op refactor** (no behavioral change) plus a minor allocation optimization. The commit comment\ndocuments the rationale correctly. No risk of request-state cross-contamination at Cloudflare\nWorker isolate granularity; each request runs within the same isolate's single-threaded event\nloop. **No spec impact.**\n\n---\n\n## DoD #4 Privacy Floor — Re-Check\n\nFocused review: the Cursor fixes touched consumer resolution (Fix 2) and rate-limit parse (Fix 3).\nNeither path touches the redaction module.\n\n**PROHIBITED_FIELDS constant — unchanged:**\n```typescript\nexport const PROHIBITED_FIELDS = [\n  \"project_id\", \"config_name\", \"book_codes\", \"source_url\", \"font_url\",\n  \"figure_url\", \"payload_full\", \"usfm_bytes\", \"log_content\", \"pdf_bytes\",\n] as const;\n```\nAll 10 required prohibited fields per spec §10 DoD #4. Present and unmodified. ✓\n\n**`redactAndValidate()` — unchanged by any of the 3 Cursor commits.** The diff between\n`835623f..7e03bdf` shows zero changes to this function. ✓\n\n**Privacy-floor tests — all 11 pass:**\n- 1 bulk test asserting all 10 fields are rejected before `writeDataPoint`\n- 10 individual field tests (one per prohibited field)\nAll 11 remain green. ✓\n\n---\n\n## Spec-Lock Compliance\n\n`git diff 835623f..HEAD -- canon/` → **empty diff**\n\nThe `canon/` directory was **not touched** by any of the 3 Cursor commits. The spec-lock\nrequirement is satisfied. ✓\n\n---\n\n## Summary of Findings\n\n| Finding | Severity | Fix Commit | Detail |\n|---------|----------|------------|--------|\n| No regression test for comma-FROM bypass | **NIT** | 418d547 | Fix 1 closes the bypass correctly but adds no test for `SELECT * FROM ptxprint_telemetry, secret_dataset`. The specific attack vector has no automated protection. |\n| TELEMETRY_VERIFIED_CLIENTS in wrangler comment | **NIT** (informational) | f27815c | Residual comment-only reference is accurate documentation; no code binding. May confuse future readers. |\n\n**Blockers: 0**\n**NITs: 2 (1 notable, 1 informational)**\n\n---\n\n## Final Verdict\n\n> **STILL SAFE TO MERGE**\n\nAll 3 Cursor fixes are functionally correct. The comma-FROM bypass is closed. The clientInfo.name\nconsumer resolution correctly implements spec §6.1 priority 3. The rate-limit parse hardening\ncorrectly defends against malformed env vars. The MCP handler hoisting is a clean no-op\nrefactor. No canon was edited. All 43 tests pass with clean TypeScript.\n\nThe single notable NIT (missing regression test for comma-FROM bypass) does not block merge — the\nfix is correct in code, and the existing UNION-based tests exercise the broader allowlist\nmachinery. A follow-up test addition is recommended but not required to land.\n\n---\n\n*Re-validator: Claude (fresh session, not the author or original validator session)*\n*Date: 2026-04-30*\n*Scope: 835623f..7e03bdf (3 Cursor Agent fix commits)*\n*Original validation (PR #31) clean at 835623f — this addendum confirms the fixes do not regress it.*\n"},{"path":"handoffs/session-10-after-first-pdf-handoff.md","uri":"klappy://canon/handoffs/session-10-after-first-pdf-handoff","title":"PTXprint MCP — Handoff (2026-04-29 after first PDF)","audience":"next-session-claude","tags":["ptxprint","mcp","handoff","phase-1-poc","session-10-end","first-pdf"],"stability":"working","what_it_answers":"Pick this up next session. Companion: `canon/encodings/transcript-encoded-session-10.md` (this PR). This doc is the operational pickup — what to do first, what's in flight, what to know.","sections":[{"slug":"what-works-right-now","heading":"What works right now","start":18,"end":39},{"slug":"what-does-not-yet-work-drift-7-open","heading":"What does not yet work — drift 7 (open)","start":39,"end":53},{"slug":"first-three-moves-next-session","heading":"First three moves next session","start":53,"end":100},{"slug":"open-items-priority-ordered","heading":"Open items (priority-ordered)","start":100,"end":122},{"slug":"state-at-session-end-for-resuming-exactly","heading":"State at session end (for resuming exactly)","start":122,"end":138},{"slug":"things-to-remember-do-not-relearn","heading":"Things to remember (do not relearn)","start":138,"end":161},{"slug":"credentials-and-keys","heading":"Credentials and keys","start":161,"end":167},{"slug":"what-this-session-accomplished","heading":"What this session accomplished","start":167,"end":182}],"body":"---\ntitle: \"PTXprint MCP — Handoff (2026-04-29 after first PDF)\"\naudience: next-session-claude\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"handoff\", \"phase-1-poc\", \"session-10-end\", \"first-pdf\"]\nextends: \"canon/encodings/transcript-encoded-session-10.md (this PR); supersedes the 2026-04-29 early-AM handoff (operator-side, pre-this-PR)\"\nwritten_at: 2026-04-29T01:05:00Z\nsession_window: 2026-04-29T00:53Z–2026-04-29T01:05Z (session 10; ~12 min wall-clock)\n---\n\n# Handoff — 2026-04-29 after first PDF\n\n> Pick this up next session. Companion: `canon/encodings/transcript-encoded-session-10.md` (this PR). This doc is the operational pickup — what to do first, what's in flight, what to know.\n\n---\n\n## What works right now\n\n**Phase 1 DoD step 5: SATISFIED.** First end-to-end PDF generation verified at the live deploy `https://ptxprint-mcp.klappy.workers.dev`:\n\n- `/health` → 200, version 0.1.0, spec v1.2-draft\n- All four MCP tools work (`submit_typeset`, `get_job_status`, `cancel_job`, `get_upload_url`)\n- Worker → Container → PTXprint → R2 → presigned URL pipeline produces a real, retrievable PDF\n- Reproducible smoke at `/home/claude/smoke.py` (this session's working tree; will not survive container reset)\n\n**Validation artifact**: job `6f37b42b9c73ad5e4f7b8a576de8144bcc6c87fad944051e285a33d21de25059`. PDF retrievable at:\n\n```\nhttps://ptxprint-mcp.klappy.workers.dev/r2/outputs/6f37b42b9c73ad5e4f7b8a576de8144bcc6c87fad944051e285a33d21de25059/minitest_Default_JHN_ptxp.pdf\n```\n\n(66966 bytes, valid PDF v1.3, Creator: PTXprint 3.0.20, Title: \"The Testcase\", Subject: \"JHN\")\n\nmain HEAD when this handoff was written: `5978b6b9c9` (PR #11 merge of session 8+9 encodings, 2026-04-29T00:52:50Z). This handoff's PR adds session-10 encoding + canon update.\n\n---\n\n## What does not yet work — drift 7 (open)\n\nThe minitests fixture's `ptxprint.cfg` declares **Gentium Plus** as the body font. Container bundles **Charis SIL + Source Code Pro** only. Without mitigation, the smoke fails with:\n\n```\n! Font \\font<p-12.0>=\"Gentium Plus:script=latn\" at 11.00006pt not loadable: Metric (TFM) file or installed font not found.\n```\n\nThis session's mitigation: **edit the cfg in-place** in the `config_files` payload to substitute Charis SIL for the four font-role keys (`fontregular`, `fontbold`, `fontitalic`, `fontbolditalic`). Documented in the canon update at `canon/articles/phase-1-poc-scope.md` (this PR).\n\n`define` overrides did NOT work for these keys — see session-10 D-019 / O-035. Widget-ID-to-cfg-key mapping is still undocumented (session-1 O-003 → session-10 H-019).\n\n---\n\n## First three moves next session\n\n### Move 1 — Time + status sweep (≤2 min)\n\n```bash\n# 1a — current time\noddkit_time   # call the tool, pass any prior server_time as reference\n\n# 1b — main HEAD and CI deploy state\nPAT=\"<see project instructions>\"\ncurl -sH \"Authorization: Bearer $PAT\" \\\n  https://api.github.com/repos/klappy/ptxprint-mcp/commits/main \\\n  | python3 -c \"import sys,json; r=json.load(sys.stdin); print(r['sha'][:10], r['commit']['author']['date'], '-', r['commit']['message'].split(chr(10))[0])\"\n\n# 1c — health\ncurl -sS -w \"\\nHTTP %{http_code}\\n\" https://ptxprint-mcp.klappy.workers.dev/health\n\n# 1d — re-verify PDF is still in R2 (90-day retention, but worth checking)\ncurl -sS -A \"ptxprint-smoke/0.x\" -o /tmp/last.pdf \\\n  \"https://ptxprint-mcp.klappy.workers.dev/r2/outputs/6f37b42b9c73ad5e4f7b8a576de8144bcc6c87fad944051e285a33d21de25059/minitest_Default_JHN_ptxp.pdf\"\nfile /tmp/last.pdf\n```\n\nExpected: HEAD ≥ this PR's merge SHA, /health is 200, PDF file reports `PDF document, version 1.3`.\n\n### Move 2 — Operator triage on drift 7 (H-018)\n\nThree viable paths, each is a separate surgical PR. **Operator's call** which to take first:\n\n1. **Bundle Gentium Plus in the Dockerfile** (mirrors session-9 PR #10's Charis pattern). One Dockerfile change + a font copy. Pro: faithful rendering of any fixture that requests Gentium Plus. Con: container-size growth; sets precedent that \"we bundle everything users might want,\" which ends in the session-3 C-007 anti-direction.\n2. **Document the payload `fonts` field in canon and demonstrate it in a smoke test.** The schema already has `FontSchema` with `family_id` / `version` / `filename` / `url` / `sha256`. Phase 2-aligned: agents supply fonts they use. Con: Gentium Plus URLs + sha256 must come from somewhere; container's fontconfig may need refresh after font materialisation.\n3. **Stay on the cfg-edit mitigation pattern** (this PR's canon update locks it in). Lowest container/code change. Con: doesn't make the fixture render *faithfully* — just renders. Phase 1 shape, not Phase 2.\n\nDefault if no operator triage: stick with (3) and proceed to next item.\n\n### Move 3 — Close session-1 O-003: widget-ID-to-cfg-key mapping (H-019)\n\nThis is the leverage move for Phase 2. Without it, `define` is a guess-and-check tool the agent can't rely on. Resolution paths in priority order:\n\n- **Run PTXprint locally with the runtime introspection flag** (session-1 O-003 mentioned `-I`) and capture the dump. Best fidelity, but requires local PTXprint install.\n- **Read PTXprint's GTK Glade XML (UI definitions)** in `python/lib/ptxprint/` and correlate to cfg setters in the same module. Works from a `git clone` without local install.\n- **Ask Martin (PTXprint dev)** for an authoritative listing.\n\nOutcome: a canon article like `canon/articles/widget-id-to-cfg-key.md` that lists every cfg key with its corresponding `-D` widget ID. This unlocks reliable runtime overrides for Phase 2.\n\n---\n\n## Open items (priority-ordered)\n\n| ID | Priority | Item | Lives in |\n|---|---|---|---|\n| H-018 | P0 | Drift 7 triage — pick a surgical-PR path (bundle / payload-fonts / cfg-edit-only) | Operator decision |\n| H-019 | P1 | Widget-ID-to-cfg-key mapping (closes session-1 O-003) | Canon authoring |\n| H-013 | P1 | Update `canon/articles/config-construction.md`'s \"minimal cfg\" example to match session-10 reality | Canon edit |\n| H-015 | P1 | File 3 upstream issues on `sillsdev/ptx2pdf` (session-9 enumeration) | Operator-side |\n| H-020 | P2 | Fix `HEAD /r2/outputs/<key>` → 404 in Worker (one-route-handler change) | `src/index.ts` |\n| H-021 | P2 | Investigate XDV → PDF page-count discrepancy on longer fixtures | Empirical pass |\n| H-017 | P2 | Resolve oddkit branch URL syntax for ptx2pdf indexing | Operator + oddkit maintainer |\n| C-009 fallout | P3 | Polish: `smoke/minimal-payload.json` `_todo` block is stale (this session demonstrated the answers) | Cleanup PR |\n| Day-2 deferred | P3 | `cancel_job` SIGTERM into running container | v1.2 spec |\n| Day-2 deferred | P3 | `get_upload_url` presigned R2 PUTs | v1.2 spec |\n| Day-2 deferred | P3 | Strip bundled fonts (session-3 C-007) | v1.2 spec; conflicts with H-018 path 1 |\n| Day-2 deferred | P3 | Autofill mode | v1.2 spec |\n| Day-2 deferred | P3 | Per-pass progress streaming | v1.2 spec |\n| Operator-side | P3 | R2 lifecycle policies (`outputs` 90d, `uploads` 24h) | CF dashboard |\n| Operator-side | P3 | WAF skip rule for `/mcp` (or document UA requirement) | Confirmed in session 9 |\n\n---\n\n## State at session end (for resuming exactly)\n\n- **Live Worker version string:** `0.1.0` (per `/health`); spec `v1.2-draft`\n- **main HEAD before this PR:** `5978b6b9c9` (PR #11 merge of session 8+9 encodings)\n- **This PR's branch:** `session-10-first-pdf-and-canon-h016`\n- **Successful job_id (verified end-to-end):** `6f37b42b9c73ad5e4f7b8a576de8144bcc6c87fad944051e285a33d21de25059`\n- **Reproducible smoke**: `/home/claude/smoke.py` (this session's container; ephemeral)\n  - Builds payload from minitests fixture at SHA `ad086acfa66a5ca64cff361325f849eb30c7984b`\n  - Applies cfg-edit Charis substitution before submit\n  - Sends MCP JSON-RPC over streamable HTTP at `/mcp` with required UA\n  - Polls `get_job_status` until terminal state\n- **Pinned ptx2pdf reference:** tag `3.0.20` (sha `c93ee4f692`) bundled in container; minitests fixture read at `master` (sha `ad086acfa6...` per session 8)\n- **Pinned BSB:** `usfm-bible/examples.bsb` @ `48a9feb71f0a66f9b8f418b11ae25b7ad2e49a0d` (referenced in `smoke/minimal-payload.json`; not used in this session — minitests was the active fixture)\n\n---\n\n## Things to remember (do not relearn)\n\nThese are session-1-through-10 cumulative.\n\n1. **`oddkit_time` is the first call every turn.** No exceptions. Pass prior turn's `server_time` as `reference` for elapsed.\n2. **Cursor Agent is a parallel collaborator on this repo.** Check `git log` for unexpected authors before assuming you own a branch.\n3. **`agents@0.2.x` McpAgent.serve() needs explicit `binding:` name** — permanent SDK-default landmine.\n4. **`this.props` returns undefined in McpAgent tool handlers** unless auth-context middleware is in place. Use `this.env.<VAR>` instead.\n5. **Silent-failure bugs are Day-1 priority, not Day-2.** L-007.\n6. **The container's `patch_state(callback, ...)` short-circuits when callback is None.** WORKER_URL var must be set.\n7. **Job IDs are content-addressed (sha256 of payload).** Same payload → same job_id. Perturb to force fresh DO state. `cached: false` in submit response means \"we ran it again,\" not \"we returned cache\" — slightly misleading field name.\n8. **`/health` and pre-validation MCP paths can be green while DO routes throw 1101.** When MCP routes throw 1101, it's something inside `serve()` or the DO itself.\n9. **CF Error 1010 (browser_signature_banned) hits default urllib User-Agent.** Always set an explicit User-Agent on smoke requests.\n10. **When operator suggests a fix hypothesis, refute or confirm empirically before agreeing or proposing alternatives.** Session-9 D-018.\n11. **In fresh-context-Claude validation slot: take the action, surface findings — don't punt multi-choice questions back to the operator.** Session-8 D-015.\n12. **Surgical-PR cadence preserves drift-isolation visibility.** One fix per PR. Cite empirical refutations inline in PR descriptions. Session-9 D-017.\n13. **Diagnostic surface (PR #8) is what makes drift isolation possible.** Without it, drifts 4–7 would have looked identical from the outside.\n14. **(NEW session 10) `define` overrides DON'T affect cfg keys named the same.** Edit `config_files` content directly until widget-ID mapping is documented.\n15. **(NEW session 10) `project_id` is capped at 8 chars by the v1.2 schema.** Paratext convention is project-id-equals-folder-name; 9-char folder names need renaming.\n16. **(NEW session 10) Empirical refutation works on the agent's own hypotheses too.** Don't loop on retry-with-variations; one targeted test, then pivot.\n\n---\n\n## Credentials and keys\n\n(See project instructions for the canonical copies.) GitHub PAT scoped to klappy/ptxprint-mcp; CF account `b03e6ea242724c05eb97eb732cceb21d`.\n\n---\n\n## What this session accomplished\n\n- ✅ Move 1 (status sweep): main HEAD verified, /health green, CI in-progress on docs PR (no Worker behavior change expected)\n- ✅ Move 2 (drift 6 closure + first PDF): payload reconstruction from upstream fixtures + ptxprint-mods.sty addition + cfg-edit Charis mitigation produced first end-to-end PDF\n- ✅ Move 3 (canon update H-016): `canon/articles/phase-1-poc-scope.md` rewritten to match empirical reality\n- ✅ Drift 7 (Gentium Plus / font compatibility) fully isolated, characterized, mitigation pattern documented\n- ✅ Session 10 encoding written (`canon/encodings/transcript-encoded-session-10.md`)\n- ✅ This handoff written\n\nThe pipeline went from \"one tiny payload edit away\" (per the handoff written 12 min before this session opened) to actually-validated-end-to-end. Drift 7 is the next surgical-PR boundary.\n\n---\n\n*End of handoff. The next session's Move 1 is `oddkit_time`. Welcome.*\n"},{"path":"handoffs/session-11-fonts-payload-demo.md","uri":"klappy://canon/handoffs/session-11-fonts-payload-demo","title":"Handoff — 2026-04-29 after fonts-payload demo","audience":"agent","tags":["ptxprint","mcp","handoff","session-11","fonts-payload"],"stability":"working","what_it_answers":"Session 11 closed H-018 path 2 (operator triage answer). Phase-2 fonts-payload path is now empirically demonstrated end-to-end: the minitests fixture's `ptxprint.cfg` was left **unmodified** (still references \"Gentium Plus\"), and the actual","sections":[{"slug":"tl-dr","heading":"TL;DR","start":14,"end":20},{"slug":"what-works-right-now","heading":"What works right now","start":20,"end":40},{"slug":"what-does-not-yet-work","heading":"What does not yet work","start":40,"end":45},{"slug":"first-three-moves-next-session","heading":"First three moves next session","start":45,"end":100},{"slug":"open-items-priority-ordered","heading":"Open items (priority-ordered)","start":100,"end":121},{"slug":"state-at-session-end-for-resuming-exactly","heading":"State at session end (for resuming exactly)","start":121,"end":130},{"slug":"things-to-remember-do-not-relearn","heading":"Things to remember (do not relearn)","start":130,"end":149},{"slug":"cursor-agent-caveat","heading":"Cursor Agent caveat","start":149,"end":153},{"slug":"credentials-and-keys","heading":"Credentials and keys","start":153,"end":157},{"slug":"what-this-session-accomplished","heading":"What this session accomplished","start":157,"end":168},{"slug":"if-the-next-session-can-only-do-one-thing","heading":"If the next session can only do one thing","start":168,"end":174}],"body":"---\ntitle: \"Handoff — 2026-04-29 after fonts-payload demo\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"handoff\", \"session-11\", \"fonts-payload\"]\ndate: 2026-04-29\nstatus: working\ncompanion_to: \"canon/encodings/transcript-encoded-session-11.md\"\n---\n\n# Handoff — 2026-04-29 after fonts-payload demo\n\n## TL;DR\n\nSession 11 closed H-018 path 2 (operator triage answer). Phase-2 fonts-payload path is now empirically demonstrated end-to-end: the minitests fixture's `ptxprint.cfg` was left **unmodified** (still references \"Gentium Plus\"), and the actual Gentium Plus 6.200 TTFs were supplied via the payload's `fonts` array. PDF rendered faithfully — `pdffonts` confirms GentiumPlus / GentiumPlus-Bold / GentiumPlus-Italic embedded. **No container code changes were necessary.** PR also folds in H-020 (HEAD `/r2/outputs/<key>` → 404) as a tiny opportunistic fix, which auto-deployed via Workers Builds when PR #13 merged — **HEAD now returns 200 against the live worker**, verified empirically.\n\n> **Correction note (post-session, 2026-04-29).** This handoff was originally drafted with the (wrong) belief that deploys required a manual `wrangler deploy` step (\"H-024\"). The truth: the CF dashboard's Workers Builds GitHub integration auto-deploys every merged push to `main`. The H-020 fix shipped at PR #13 merge time without any further action. H-024 was a phantom and is closed; the original wording is preserved below for the record.\n\n## What works right now\n\n**Phase 1 DoD step 5** continues to hold (still true from session 10). **Phase 2 fonts path** is also now demonstrated:\n\n- Smoke fixture: `smoke/fonts-payload.json` (committed in this PR).\n- Job: `802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd`.\n- PDF: `https://ptxprint-mcp.klappy.workers.dev/r2/outputs/802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd/minitest_Default_JHN_ptxp.pdf` (68111 bytes, PDF v1.3, 2 pages).\n- pdfinfo: `Title=\"The Testcase\" Subject=\"JHN\" Creator=\"PTXprint 3.0.20 (Default)\"`.\n- pdffonts: GentiumPlus, GentiumPlus-Bold, GentiumPlus-Italic, SourceCodePro-Regular (the last is system-bundled per PR #10).\n- Wall-clock: 4.7s (vs session-10's 4.1s — comparable, slightly larger PDF due to faithful font metrics).\n\n**Staged R2 fixtures** (Gentium Plus 6.200, four faces):\n- `https://ptxprint-mcp.klappy.workers.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Regular.ttf` (sha256 `2c27e7da23ba44d135685836056833b304a388d3da346813189c60656dc02019`, 880592 bytes)\n- `…/GentiumPlus-Bold.ttf` (sha256 `622ea9f2709d74f99d45c08d93cdf2a6d096406d3a1ec2939d02714f558b3dac`, 885156 bytes)\n- `…/GentiumPlus-Italic.ttf` (sha256 `fedc1acdd2f1080941ed998cabee9759456f0e486fbd8169ff4238b37d3ac60d`, 940752 bytes)\n- `…/GentiumPlus-BoldItalic.ttf` (sha256 `960e0a58ce1d7849c7a3e49f4fbc1ac4a27b58ef19a2d013ce637fe364b0a1f0`, 957876 bytes)\n- Original ZIP: `https://github.com/silnrsi/font-gentium/releases/download/v6.200/GentiumPlus-6.200.zip` (sha256 `9b21103b79961149b6508791572acb3b2fe7eb621474c57d5e4ee37e76d7b073`).\n\n**H-020 HEAD fix** is in this PR's `src/index.ts` and **deployed live** — Cloudflare Workers Builds GitHub integration auto-deploys every merged push to `main`. `HEAD /r2/outputs/<key>` returns 200 against the live worker; verified empirically post-PR-13-merge.\n\n## What does not yet work\n\n- **Charis SIL canonical URLs** in `canon/articles/font-resolution.md` still cite the unverified `lff.api.languagetechnology.org` placeholder pattern — fine for documentation shape, but if an agent tries those URLs verbatim they may not resolve. H-023 is the cleanup; pairs naturally with H-022 (move fixtures into a dedicated bucket).\n- **Widget-ID-to-cfg-key mapping** (session-1 O-003 / session-10 H-019) still open. This was the leverage move the session-10 handoff said \"deserves its own session\" — still waiting for one.\n\n## First three moves next session\n\n### Move 1 — Time + status sweep (≤2 min)\n\nSame as session-11's session-10 sweep, plus a deploy-state probe.\n\n```bash\n# 1a — current time\noddkit_time\n\n# 1b — main HEAD\nPAT=\"<see project instructions>\"\ncurl -sH \"Authorization: Bearer $PAT\" \\\n  https://api.github.com/repos/klappy/ptxprint-mcp/commits/main \\\n  | python3 -c \"import sys,json; r=json.load(sys.stdin); print(r['sha'][:10], r['commit']['author']['date'], '-', r['commit']['message'].split(chr(10))[0])\"\n\n# 1c — health\ncurl -sS -w \"\\nHTTP %{http_code}\\n\" https://ptxprint-mcp.klappy.workers.dev/health\n\n# 1d — re-verify session-11 PDF still in R2\ncurl -sS -A \"ptxprint-smoke/0.x\" -o /tmp/last.pdf \\\n  \"https://ptxprint-mcp.klappy.workers.dev/r2/outputs/802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd/minitest_Default_JHN_ptxp.pdf\"\nfile /tmp/last.pdf\n\n# 1e — confirm the live worker reflects the latest main (Workers Builds is automatic\n# but worth a sanity check — exercise behavior introduced by the most recent merge).\n# Example for a route change: curl that route. For a Container change: submit a smoke\n# with a unique payload to force a new instance.\ncurl -sS -I -A \"ptxprint-smoke/0.x\" \\\n  \"https://ptxprint-mcp.klappy.workers.dev/r2/outputs/802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd/minitest_Default_JHN_ptxp.pdf\" \\\n  | head -1\n```\n\n`HEAD /r2/...` returning `HTTP 200` confirms the H-020 fix deployed correctly. (Workers Builds has auto-deployed since session 11 — this is a sanity check, not a gating step.) If it returns `HTTP 404`, check the **Workers & Pages → ptxprint-mcp → Deployments** tab in the CF dashboard for the latest build status.\n\n### Move 2 — Pick H-019 (widget-ID-to-cfg-key mapping)\n\nThis was the session-10 leverage move; session 11 chose not to start it. Resolution paths in priority order (unchanged from session 10's handoff):\n\n- **Run PTXprint locally with the runtime introspection flag** (session-1 O-003 mentioned `-I`) and capture the dump.\n- **Read PTXprint's GTK Glade XML (UI definitions)** in `python/lib/ptxprint/` and correlate to cfg setters.\n- **Ask Martin (PTXprint dev)** for an authoritative listing.\n\nOutcome: a canon article like `canon/articles/widget-id-to-cfg-key.md` listing every cfg key with its corresponding `-D` widget ID. Unlocks reliable runtime overrides for Phase 2.\n\n### Move 3 — H-022 / H-023: bucket hygiene + Charis URLs\n\nIf the operator wants to clean up before scaling fixtures further:\n- Add `FIXTURES → ptxprint-fixtures` R2 binding to `wrangler.jsonc`.\n- Move the four Gentium Plus TTFs from `outputs/fixtures/fonts/...` to the new bucket's root.\n- Update canon URLs in `canon/articles/font-resolution.md`.\n- Stage Charis 7.000 the same way; update Charis worked example.\n\nThis is \"cleanup PR\" territory — meaningful but not urgent.\n\n## Open items (priority-ordered)\n\n| ID | Priority | Item | Lives in |\n|---|---|---|---|\n| H-019 | P1 | Widget-ID-to-cfg-key mapping (closes session-1 O-003) | Canon authoring |\n| H-013 | P2 | Update `canon/articles/config-construction.md`'s \"minimal cfg\" example | Canon edit |\n| H-015 | P2 | File 3 upstream issues on `sillsdev/ptx2pdf` | Operator-side |\n| H-022 | P3 | Move stable fixtures into dedicated `ptxprint-fixtures` bucket | `wrangler.jsonc` + migration |\n| H-023 | P3 | Verify or replace Charis SIL LFF URL pattern in canon | Canon edit (pairs with H-022) |\n| H-025 | P3 | Retire H-021 unless larger fixture demonstrates it bites | Empirical pass |\n| H-017 | P3 | Resolve oddkit branch URL syntax for ptxprint-mcp canon indexing | Operator + oddkit maintainer |\n| C-009 fallout | P3 | `smoke/minimal-payload.json` `_todo` block is stale | Cleanup PR |\n| Day-2 deferred | P3 | `cancel_job` SIGTERM into running container | v1.2 spec |\n| Day-2 deferred | P3 | `get_upload_url` presigned R2 PUTs | v1.2 spec |\n| Day-2 deferred | P3 | Strip bundled fonts (session-3 C-007) | v1.2 spec |\n| Day-2 deferred | P3 | Autofill mode | v1.2 spec |\n| Day-2 deferred | P3 | Per-pass progress streaming | v1.2 spec |\n| Operator-side | P3 | R2 lifecycle policies | CF dashboard |\n\n> **H-024 retracted.** Originally listed as \"operator: `wrangler deploy` to ship H-020 HEAD fix.\" This was based on the incorrect belief that deploys were manual. The truth: Workers Builds GitHub integration auto-deployed the fix at PR #13 merge time. `HEAD /r2/...` returning 200 against the live worker confirms it.\n\n## State at session end (for resuming exactly)\n\n- **Live Worker version:** `0.1.0`, spec `v1.2-draft` (unchanged from session 10).\n- **main HEAD before this PR:** `d389648f29` (PR #12 merge of session 10 encoding + canon update + drift-7 isolation).\n- **This PR's branch:** `session-11-fonts-payload-demo`.\n- **Successful job_id (this session, fonts-payload smoke):** `802e42e7d549cf9f827cbbcff69a6354e1b968a23084e5f2485f93cde52fc4bd`.\n- **R2 staged TTF prefix:** `outputs/fixtures/fonts/gentium-plus-6.200/`.\n- **Reproducible smoke:** `python3 /home/claude/smoke_run.py smoke/fonts-payload.json` (the `smoke_run.py` script in /home/claude is ephemeral but reconstructs in ~3 minutes from the JSON-RPC pattern: initialize → notifications/initialized → tools/call submit_typeset → poll tools/call get_job_status).\n\n## Things to remember (do not relearn)\n\nCarries forward from session-10 list:\n\n- agents@0.2.x `McpAgent.serve()` needs explicit `binding: \"MCP_AGENT\"`.\n- `this.props` is undefined in tool handlers; use `this.env.<VAR>`.\n- Job IDs are sha256 of canonicalized payload — same payload, same id; perturb to force fresh DO state.\n- urllib default UA gets Cloudflare-1010-banned. Always set explicit UA.\n- `define` overrides do NOT take effect for cfg keys with the same name; PTXprint widget IDs ≠ cfg keys. Edit `config_files` content directly OR supply the actual font via `fonts` payload.\n- `project_id` capped at 8 chars by v1.2 schema; \"minitests\" (9) → \"minitest\".\n\nNew from session 11:\n\n- **Payload-supplied fonts work without container changes.** Materialised at `<scratch>/<project>/shared/fonts/<filename>`; PTXprint's startup adds that to XeTeX's resolution paths. No `fc-cache` or `OSFONTDIR` invocation needed.\n- **`fonts-sil-gentium` ≠ `fonts-sil-gentiumplus`.** The Dockerfile's apt list installs the former (Gentium 1.03 from 2008, Regular only). The latter (v6.200, four Gentium Plus faces) was never installed; that explains drift 7 root-cause.\n- **`/internal/upload` accepts arbitrary content as binary-clean PUT** as long as the key starts with `outputs/`. No auth in Day-1; Day-2 will replace with presigned URLs.\n- **`HEAD /r2/outputs/<key>` returns 200** — the H-020 fix landed in PR #13 and auto-deployed via Workers Builds. Both HEAD and GET work. (Pre-PR-13 it returned 404; if you ever see 404 on a known-existing key after this, suspect a deploy regression and check the Deployments tab.)\n- **Surprise downward in scope is a workflow success, not a failure.** Plan called for `fc-cache`/`OSFONTDIR`; empirical test showed not needed; PR shipped strictly less code than predicted. Encode the principle: test before pre-emptive coding.\n\n## Cursor Agent caveat\n\nCursor Agent collaborates on this repo in parallel. Check git log for unexpected authors before assuming you own a branch.\n\n## Credentials and keys\n\nGitHub PAT and CF account in project instructions. Don't re-fetch.\n\n## What this session accomplished\n\n- Closed H-018 path 2 (Phase-2 fonts-payload path) end-to-end empirically.\n- Staged Gentium Plus 6.200 TTFs in R2 (4 files, all sha256-roundtrip-verified).\n- Authored `smoke/fonts-payload.json` (committed in this PR).\n- Updated `canon/articles/font-resolution.md` with empirically-verified Gentium Plus example, R2-staged-fixtures source pattern, and corrected Phase-1 warning.\n- Updated `canon/articles/phase-1-poc-scope.md` with the session-11 demonstration note.\n- Folded H-020 HEAD fix into `src/index.ts`.\n- Encoded the session and produced this handoff.\n- Refuted session-10's \"fontconfig refresh may be needed\" hypothesis empirically and recorded the lesson.\n\n## If the next session can only do one thing\n\n**Validate this PR as fresh-context Claude**, per `klappy://canon/principles/verification-requires-fresh-context`. The empirical claims (PDF rendered with payload Gentium Plus, R2 roundtrip, HEAD-fix code) are all in the encoding and reproducible. Re-run the smoke (3-5 min) and confirm `pdffonts` shows GentiumPlus faces. Either approve-and-merge or surface what looks shaky.\n\nIf validated: the next big leverage move is H-019 (widget-ID-to-cfg-key mapping). It deserves its own session.\n"},{"path":"handoffs/session-3-gaps-handoff.md","uri":"klappy://canon/handoffs/session-3-gaps-handoff","title":"Session 3 Gaps Handoff — Still-Owed Work from the Font Resolution Thread","audience":"operator","tags":["handoff","session-3","gaps","ese-gap-analysis","font-resolution","followups"],"stability":"working","what_it_answers":"**What this document is.** A consolidated record of work that was named, deferred, and never completed across the session-3 font-resolution thread. The headline item is an ESE-vs-spec gap analysis that was deferred three sessions in a row (","sections":[{"slug":"1-the-core-gap-ese-surface-vs-current-spec","heading":"1. The Core Gap — ESE Surface vs Current Spec","start":22,"end":63},{"slug":"2-other-session-3-followups-triaged","heading":"2. Other Session 3 Followups — Triaged","start":63,"end":107},{"slug":"3-copy-pasteable-session-prompt-for-the-gap-analysis","heading":"3. Copy-Pasteable Session Prompt for the Gap Analysis","start":107,"end":195},{"slug":"4-after-this-handoff-lands","heading":"4. After This Handoff Lands","start":195,"end":207}],"body":"---\ntitle: \"Session 3 Gaps Handoff — Still-Owed Work from the Font Resolution Thread\"\naudience: operator\nexposure: working\nvoice: instructional\nstability: working\ntags: [\"handoff\", \"session-3\", \"gaps\", \"ese-gap-analysis\", \"font-resolution\", \"followups\"]\ncanonical_status: non_canonical\ncreated_at: 2026-04-28T05:25:00Z\ncompanion_to: \"canon/encodings/transcript-encoded-session-3.md, canon/articles/_archive/font-resolution-design.md (superseded — v1.2 D-021 retired the three-MCP path)\"\n---\n\n# Session 3 Gaps Handoff\n\n> **What this document is.** A consolidated record of work that was named, deferred, and never completed across the session-3 font-resolution thread. The headline item is an ESE-vs-spec gap analysis that was deferred three sessions in a row (H-002 → H-010 → H-015) and is the most load-bearing piece of owed work. Several session-3 followups have since been resolved by v1.2's architecture and are noted here as such; only the genuinely-still-open items carry forward.\n\n> **Why this exists.** A future session — autonomous coding run, operator catch-up pass, or another Claude — should be able to pick up exactly where session 3 left off, without re-reading the entire encoding chain to figure out what's owed. This document names each gap, gives its current status, and points at the resolution path. The third section is a copy-pasteable session prompt for the gap analysis.\n\n> **How to use this.** Read §1 first — the core gap analysis is the actually-useful next move and the other items are smaller. §2 is a triaged inventory of the rest. §3 is the prompt to paste into a new Claude session if delegating the gap analysis.\n\n---\n\n## 1. The Core Gap — ESE Surface vs Current Spec\n\n### 1.1 History of this handoff\n\nOriginally named in transcript session 1 as `H-002 — Gap-analyze ESE against the existing first-pass MCP server`. Restated in session 3 as `H-010` after that session bypassed it for font work. Restated again in session 3.1 (now consolidated into session 3) as `H-015` after the architectural correction. Three named-and-deferred cycles.\n\n### 1.2 What changed under it\n\nThe original framing was \"ESE surface vs first-pass spec (the 17-tool PoC).\" That spec is now archived at `canon/specs/archive/ptxprint-mcp-first-pass-poc.md` and is no longer the build target. The current spec is `canon/specs/ptxprint-mcp-v1.2-spec.md` — a stateless content-addressed build system on Cloudflare Containers, four tools, payload-driven.\n\nThe gap analysis as originally framed (ESE vs first-pass) is largely **obsolete**. The big architectural drift the original gap analysis would have surfaced (filesystem-edit-in-place vs stateless build system) has been corrected by other means — specifically session 5's pushback that produced v1.2.\n\n### 1.3 The gap analysis that's actually useful now\n\n**Reframe:** ESE surface (`canon/surfaces/ptx2pdf-surface.md` and `.json`) vs v1.2 spec (`canon/specs/ptxprint-mcp-v1.2-spec.md`).\n\nThe question this analysis answers: does v1.2's contract — \"submit a payload, get a PDF back\" — actually fit what the upstream `sillsdev/ptx2pdf` codebase requires of its caller? Specifically:\n\n- **Inputs the v1.2 payload describes vs inputs PTXprint actually consumes.** v1.2's payload has slots for cfg files, USFM URLs, fonts, figures, AdjLists, piclist, override files, FRTlocal.sfm. Does the ESE confirm those are all the binary and text inputs PTXprint reads? Are there inputs the spec missed (e.g. project-level files at `<prj>/shared/ptxprint/` outside any config — `picInfo.txt`, `picChecks.txt`, project-wide `changes.txt`, `hyphen-<prj>.tex`)?\n- **The `-D` override mechanism vs v1.2's `define` parameter.** ESE confirms `-D key=value` accepts arbitrary UI widget identifiers. v1.2 surfaces this as a `define` map. Is the mapping clean, or are there setting categories (e.g. settings that need pre-processing, settings that hit Python before XeTeX) where `-D` doesn't suffice?\n- **Output naming convention.** ESE established `<PRJ>_<Config>_<bks>_ptxp.pdf` and a log path beneath `<config>/`. v1.2 stores at `r2://ptxprint-outputs/<sha256_of_canonical_payload>/<PRJ>_<Config>_<bks>_ptxp.pdf`. Does the v1.2 R2 path actually need to preserve PTXprint's filename verbatim, or is it pure metadata for download UX?\n- **Failure modes vs v1.2's `failure_mode: hard | soft` taxonomy.** ESE catalogued partial-success cases (issue #212 — PDF emits without pictures). Does v1.2's two-class taxonomy capture every distinct failure category the ESE surfaces?\n- **Autofill.** ESE confirms it exists as the multi-pass page-filler. v1.2 has it as `mode: autofill` on the payload. Are there autofill sub-features (per-book overrides? attempt budgets?) the spec doesn't expose?\n- **Concurrency / runtime characteristics vs v1.2's Container model.** ESE notes long hangs, performance issues, autofill duration. v1.2 picks `standard-2` and 45m `sleepAfter`. Does the ESE confirm those numbers fit, or did the operator's experience suggest tighter or looser bounds?\n\n### 1.4 Output of this gap analysis\n\nA markdown document at `canon/handoffs/ese-vs-v1.2-gap-analysis.md` (or as part of the v1.2 spec's appendix) that lists, per ESE lens (L1-L12 from `ptx2pdf-surface.md`):\n\n- **Confirmed**: things v1.2 captures correctly.\n- **Gap**: things ESE surfaces that v1.2 doesn't account for. Each gap is sized (blocking / improvement / nice-to-have) and pointed at a resolution path (spec amendment, canon article, or punt to v1.3).\n- **Phantom**: things v1.2 captures that the ESE doesn't actually require — over-design that should be simplified out.\n\nEstimated effort: 1-2 hours for a careful pass. Less if delegated to a focused session with the ESE and v1.2 spec already in context.\n\n### 1.5 Gating\n\nImplementation work on the v1.2 build can proceed without this — but knowing the gap shape before the autonomous coding run from session 2 H-003 reduces the chance of building toward an incomplete contract. Do this before or in parallel with `H-007` (canon authoring), not after.\n\n---\n\n## 2. Other Session 3 Followups — Triaged\n\nThe font-resolution work surfaced fourteen open questions and several handoffs. Many were rendered moot by v1.2's architecture. Triage:\n\n### 2.1 Resolved by v1.2 (no further work)\n\n| Item | Why resolved |\n|---|---|\n| F-Q7 — cross-project font sharing | v1.2 stores fonts in R2 keyed by SHA. SHA-dedupe is automatic across projects; sharing is the default. |\n| F-Q8 — eviction policy for font versions | Per-job ephemeral dirs in v1.2 mean no host-local persistence. Eviction is a non-question. |\n| F-Q10 — lock-file location | v1.2 makes the payload itself the lock. The agent's payload-history mechanism (per session 5 Q-open-14) is where pinning lives. No `fonts.lock.json` artifact in v1.2's contract. |\n| F-Q11 — cache MCP fetch-on-miss vs pre-warm | v1.2 D-021 makes binary inputs URL+sha256-referenced in the payload. The Container fetches at job time. Pre-warming would be a separate optimisation outside the typesetting MCP. |\n| H-014 — first-pass design retains value as lifecycle reference | Done by uploading session-3 encoding and `font-resolution-design.md` to canon. The historical context lives in the encoding's D-012/D-014 entries. |\n\n### 2.2 Still genuinely owed (work needed)\n\n| Item | Status | Resolution path |\n|---|---|---|\n| H-007 | Open | Author canon article on the agent's font-resolution reasoning loop. v1.2-aligned: agent constructs the payload's `fonts` array from a language tag, possibly via a fonts MCP, and submits. Article goes at `canon/articles/font-resolution.md` (named in session 5 H-007's updated list). |\n| H-008 / F-Q1 / F-Q2 | Open | Probe LFF with sample BCP 47 tags (`eng`, `eng-Latn`, `swh-Latn`, `und-Latn`) and several real Paratext `LanguageIsoCode` values from `ptx2pdf/tests/`. Document the canonical transformation. Lives in the font-resolution canon article (H-007). |\n| H-009 / F-Q3 | Open | Build a clean Ubuntu container with `apt-get install --no-install-recommends texlive-xetex fontconfig ca-certificates`. Verify `dpkg -l \\| grep -i font` returns no `fonts-*` packages. Verify `fc-list` returns only TeX-internal fonts. Gates the v1.2 Container Dockerfile commit. |\n| F-Q4 | Open (low priority) | LFF rate limits / auth / change risk. Defer until volume is measured; LFF is self-hostable from `silnrsi/langfontfinder` if rate-limiting becomes a real constraint. |\n| F-Q5 | Open | Diglot lock semantics — does the payload's `fonts` array carry the union of left/right column fonts? Almost certainly yes. Confirm by walking through a real diglot project from `ptx2pdf/tests/`. |\n| F-Q6 | Open | PTXprint font-selection mechanism — name-based via fontconfig, or axis-based? Read PTXprint Python source to confirm. Likely just fontconfig-by-name (XeTeX `Bold` keyword resolves through fontconfig). |\n\n### 2.3 Deferred or punted\n\n| Item | Status |\n|---|---|\n| H-011 — read `klappy://docs/oddkit/IMPL-content-addressed-caching` before writing content-cache MCP | Punted. v1.2 D-021 absorbs the byte-fetching concern into the Container's job dispatch; there is no separate \"content-cache MCP\" to write. The doc is still worth reading if a future cache MCP is built for other consumers (pictures, source bundles). |\n| H-012 — content-cache MCP naming | Punted. See above — no separate MCP being built in v1.2. |\n| H-013 — fonts MCP naming + tool surface | Punted. v1.2 makes fonts payload-resolved; the agent calls LFF directly (or the operator's preferred catalogue). A fonts MCP becomes useful only if multiple agents need the same translation logic. |\n| F-Q9 — server-to-server cache fetch vs agent-passes-bytes | Resolved by v1.2 D-021: agent provides URL+sha256, Container fetches. Neither party shuttles bytes through the MCP envelope. |\n| F-Q12, F-Q13, F-Q14 | All resolved by v1.2's Container-fetches-at-job-time pattern. License policy lives with the agent; SHA verification is the Container's job. |\n\n### 2.4 Carried from earlier sessions, still owed\n\n| Item | Origin | Status |\n|---|---|---|\n| H-005 (session 2) — verify ptx2pdf licence before mirroring docs | session 2 | Independent; still owed. Do before any content from `ptx2pdf/docs/documentation/` is copied into canon. |\n| H-006 (session 2) — filter the 1000-config corpus | session 2 | Independent; still owed. Decide filter criterion + tooling before agent ingestion of the corpus. |\n\n---\n\n## 3. Copy-Pasteable Session Prompt for the Gap Analysis\n\n> **Context for the operator before pasting:** Use this prompt in a new Claude session (or paste at the start of a fresh autonomous run) to delegate the ESE-vs-v1.2 gap analysis specifically. The prompt is self-contained and references all the canon documents the receiving session will need to fetch.\n\n```\n=== PASTE BELOW THIS LINE ===\n\nYou're picking up a deferred handoff from the PTXprint MCP project. The work\nhas been deferred for three named cycles (H-002 → H-010 → H-015) and now needs\nto land before the v1.2 autonomous build proceeds.\n\n**Mode: planning, then execution.**\n\n**Your task:** produce a gap analysis comparing the ESE surface of the upstream\nsillsdev/ptx2pdf codebase against the current v1.2 PTXprint MCP spec. Output is\na single markdown document at canon/handoffs/ese-vs-v1.2-gap-analysis.md (or\nas an appendix to the v1.2 spec — your judgement).\n\n**Read first (in order):**\n\n1. The session 3 gaps handoff (THIS DOCUMENT) for full context:\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/handoffs/session-3-gaps-handoff.md\n\n2. The ESE surface (the upstream codebase observations):\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/surfaces/ptx2pdf-surface.md\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/surfaces/ptx2pdf-surface.json\n\n3. The current v1.2 spec (the build target):\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/specs/ptxprint-mcp-v1.2-spec.md\n\n4. Session 5's encoding (most recent architectural decisions):\n   https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/encodings/transcript-encoded-session-5.md\n\n**Method:**\n\nFor each ESE lens (L1 through L12 in ptx2pdf-surface.md), check the v1.2 spec\nfor whether the lens's content is captured. Classify each item as:\n\n- CONFIRMED: v1.2 captures this correctly.\n- GAP: ESE surfaces something v1.2 doesn't account for. Size as\n  (blocking / improvement / nice-to-have) and propose a resolution path\n  (spec amendment, canon article, or punt to v1.3).\n- PHANTOM: v1.2 captures something the ESE doesn't actually require —\n  over-design that should be simplified.\n\nSpecific lenses to attend to most carefully (per the session 3 gaps handoff §1.3):\n\n- L3 (project shape) — does v1.2's payload schema cover all the binary and\n  text inputs PTXprint actually reads, including project-level files at\n  <prj>/shared/ptxprint/ outside any config (picInfo.txt, picChecks.txt,\n  project-wide changes.txt, hyphen-<prj>.tex)?\n- L7 (outputs) — does v1.2's R2 output naming preserve everything that\n  matters? Are intermediate artifacts (procpdf, parlocs) accessible if the\n  agent needs them for diagnosis?\n- L8 (failure modes) — does v1.2's two-class hard/soft taxonomy capture\n  every distinct failure category the ESE surfaces?\n- L10 (deployment / runtime) — does the standard-2 Container with 45m\n  sleepAfter actually fit the autofill envelope ESE describes? Any other\n  sizing the ESE suggests v1.2 missed?\n\n**Use the GitHub PAT from your project instructions to push** (same PAT that\nbootstrapped the repo).\n\n**Procedure:**\n\n1. Fetch and read the four source documents above.\n2. Produce the analysis (markdown, structured per the method above).\n3. Clone the repo:\n   git clone https://${GH_PAT}@github.com/klappy/ptxprint-mcp.git\n   cd ptxprint-mcp\n4. Place the file at canon/handoffs/ese-vs-v1.2-gap-analysis.md\n5. Commit and push:\n   git add canon/handoffs/ese-vs-v1.2-gap-analysis.md\n   git commit -m \"handoffs: ESE surface vs v1.2 spec gap analysis (resolves H-015)\"\n   git push origin main\n6. Report findings — particularly any blocking gaps that should pause the v1.2\n   autonomous build until resolved.\n\n**Time budget:** 1-2 hours for a careful pass. If the analysis surfaces more\nthan ~3 blocking gaps, surface them mid-work rather than continuing — those\nare spec issues that need operator review before the analysis can finish\nusefully.\n\n=== PASTE ABOVE THIS LINE ===\n```\n\n---\n\n## 4. After This Handoff Lands\n\nThis file should be updated (not deleted) when the gap analysis is produced —\nadd a \"✅ resolved\" note at the top and link to the analysis. The other items\nin §2.2 should be picked up either by H-007's canon authoring (which folds in\nH-008/F-Q1/F-Q2/F-Q5/F-Q6) or by the v1.2 Dockerfile work (which gates on\nH-009/F-Q3).\n\nThe carry-over items in §2.4 (H-005, H-006) are independent of session 3's\nthread and should be tracked by their own handoff documents if they don't\nget resolved soon.\n"},{"path":"handoffs/telemetry-governance-h-t2-review.md","uri":"klappy://canon/handoffs/telemetry-governance-h-t2-review","title":"H-T2 Fresh-Session Review — canon/governance/telemetry-governance.md","audience":"project","tags":["ptxprint","mcp","review","h-t2","telemetry","governance","fresh-session","dolcheo"],"stability":"working","what_it_answers":"**Independence statement.** I did not author the article under review. This session began with no context from the drafting session (H-T1). The findings below are drawn from direct cross-referencing of the article against each named authori","sections":[{"slug":"executive-verdict","heading":"Executive Verdict","start":24,"end":34},{"slug":"findings-by-authority","heading":"Findings by Authority","start":34,"end":215},{"slug":"recommended-changes","heading":"Recommended Changes","start":215,"end":263},{"slug":"summary-table","heading":"Summary Table","start":263,"end":283},{"slug":"statement-of-independence","heading":"Statement of Independence","start":283,"end":291}],"body":"---\ntitle: \"H-T2 Fresh-Session Review — canon/governance/telemetry-governance.md\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"review\", \"h-t2\", \"telemetry\", \"governance\", \"fresh-session\", \"dolcheo\"]\ndate: 2026-04-30\nreviewer: \"H-T2 (fresh-session, managed-agent)\"\nreviews: \"canon/governance/telemetry-governance.md (commit 4742810)\"\nauthorities:\n  - canon/encodings/telemetry-feature-planning-ledger.md\n  - canon/articles/failure-mode-taxonomy.md\n  - canon/specs/ptxprint-mcp-v1.2-spec.md\n  - klappy://canon/principles/vodka-architecture\nstatus: complete\n---\n\n# H-T2 Fresh-Session Review — `canon/governance/telemetry-governance.md`\n\n> **Independence statement.** I did not author the article under review. This session began with no context from the drafting session (H-T1). The findings below are drawn from direct cross-referencing of the article against each named authority, not from any memory of how the article was constructed.\n\n---\n\n## Executive Verdict\n\n**APPROVE_WITH_REVISIONS**\n\nThe article is structurally sound. The privacy floor is faithfully represented against C-T1. The canned-query SQL is internally consistent with the schema tables (every blob and double reference checks out). The vodka architecture section is genuine engagement — the Q2 resolution rule is the real argument, not a paraphrase of the principle. The social contract and operating principle sections are well-grounded.\n\nTwo blockers must be corrected before merge. Three nits and two observations are flagged for operator triage. None of the findings touch the privacy floor or the canned-query library.\n\n---\n\n## Findings by Authority\n\n---\n\n### Authority 1 — Planning Ledger (`canon/encodings/telemetry-feature-planning-ledger.md`)\n\n#### Finding A1-1 · Faithfulness · **BLOCKER**\n\n**Article line 181** cites `klappy://canon/articles/failure-mode-taxonomy` as the source of `failure_mode ∈ {success, soft, hard, cancelled, timeout}`.\n\nThe taxonomy article defines **only three values**: `hard | soft | success`. The `cancelled` and `timeout` extensions originate in the planning ledger **D-T1**, which reads:\n\n> \"Logging `failure_mode` (`hard | soft | success | cancelled | timeout`) and `phase` …\"\n\nThe taxonomy is not the source of `cancelled` or `timeout`. Citing it as such is factually incorrect. Additionally, the v1.2 spec's Durable Object state schema (§5) defines `failure_mode` as `\"hard\" | \"soft\" | \"success\" | null` — a three-value enum with no `cancelled` or `timeout`.\n\n**Proposed correction (article line 181):**\n\n```diff\n- `failure_mode` ∈ `{success, soft, hard, cancelled, timeout}` per `klappy://canon/articles/failure-mode-taxonomy`\n+ `failure_mode` ∈ `{success, soft, hard, cancelled, timeout}` — `success | soft | hard` per\n+ `klappy://canon/articles/failure-mode-taxonomy`; `cancelled` and `timeout` are telemetry extensions\n+ introduced in the planning ledger D-T1 (not in the taxonomy or the v1.2 DO state schema)\n```\n\nNote: the v1.2 spec DO schema and the failure-mode taxonomy both need to be updated separately to add `cancelled` and `timeout`, or the article must explicitly call them out as telemetry-only extensions not surfaced via `get_job_status`. See Handoff at end of this review.\n\n---\n\n#### Finding A1-2 · Gap · **NIT**\n\n**Blob 5 `consumer_source`:** The article's schema table lists four values — `header | query | client_info | user_agent`. The planning ledger §Schema blob 5 shows five values: `header | query | client_info | user_agent | oauth`.\n\nThe article silently drops `oauth`. The Consumer Identification Model section describes a five-tier resolution order (query param, header, clientInfo, user-agent, \"unknown\") that maps to four blob values — none of which is `oauth`. If OAuth support was intentionally deferred or removed, this should be documented as a ledger deviation. If it was overlooked, `oauth` should be restored.\n\n**Proposed correction:** Either add `oauth` back to blob 5's example values, or add a note: \"OAuth identification (`consumer_source = 'oauth'`) is reserved for a future auth layer; not populated in v1.2.\"\n\n---\n\n#### Finding A1-3 · Gap · **NIT**\n\n**`telemetry_policy` three-tier fallback:** D-T7 specifies \"Same three-tier governance fallback (`knowledge_base → bundled → minimal`) as oddkit\" for the `telemetry_policy` tool. The v1.2 spec §3 documents this behavior. The governance article — which IS the policy document fetched by `telemetry_policy` — does not describe this fallback anywhere.\n\nAn operator or agent reading only the governance article cannot know that the tool degrades gracefully. This is a gap between what the article governs and what the tool implements.\n\n**Proposed addition:** A short paragraph in the Storage and Infrastructure section or a new \"Governance Fetch Behavior\" subsection explaining the three-tier fallback.\n\n---\n\n#### Finding A1-4 · Observation · **NIT** (ledger maintenance, not article fault)\n\n**Path mismatch in the ledger:** D-T7 and H-T1 in the planning ledger reference the governance article path as `canon/constraints/telemetry-governance.md`. The file landed at `canon/governance/telemetry-governance.md`. The article's own frontmatter URI and the v1.2 spec both use the correct `canon/governance/` path. The ledger references are stale. Anyone following H-T1's path verbatim will not find the file.\n\n**Action:** Update D-T7 and H-T1 in the planning ledger to reflect the correct path. No change needed in the article.\n\n---\n\n#### Finding A1-5 · Excess · **OBSERVATION**\n\nThe article's \"never logged\" list includes three categories not explicitly traceable to planning ledger C-T1:\n\n- (a) Authentication tokens, API keys, credentials\n- (b) User account IDs beyond self-declared `consumer_label`\n- (c) `` `docs` tool query strings`` (treated as content, not metadata)\n\nC-T1 covers project identity (project IDs, config names, book codes, source URLs, payload contents, USFM bytes, log content, PDF bytes) and caller identity (IP addresses, browser fingerprints). The three additions are absent from C-T1 but appear reasonable in intent.\n\nThese are not wrong inclusions — they strengthen the privacy floor. But they lack a ledger decision grounding them. Operator should either encode them retroactively in the ledger (as C-T1 addenda or a new C-T5) or confirm they close as H-T2-approved extensions.\n\n---\n\n### Authority 2 — Failure Mode Taxonomy (`canon/articles/failure-mode-taxonomy.md`)\n\n#### Finding A2-1 · Faithfulness · **BLOCKER** (same root as A1-1)\n\nThe taxonomy article defines exactly three values for `failure_mode`: `hard | soft | success`. The taxonomy's `get_job_status` return shape reads:\n\n> \"`get_job_status` returns the classification result in the `failure_mode` field — one of `\"hard\" | \"soft\" | \"success\"`.\"\n\nThe governance article extends this to five values (`+ cancelled + timeout`) but cites the taxonomy as the source of the full enum. This is a cross-document inconsistency. The taxonomy does not define or disclaim `cancelled` or `timeout`. An implementer reading the taxonomy to understand `failure_mode` will see a three-value enum; reading the governance article, they'll see five. The two documents currently contradict each other without either acknowledging the gap.\n\n**The taxonomy extension to five values appears correct** — `cancelled` is returned when `cancel_job` is acted upon; `timeout` handles wall-clock overflow. But neither is in the taxonomy. The taxonomy needs to be updated (or explicitly constrained to \"typeset run outcomes, not job lifecycle outcomes\"), and the governance article's citation must be corrected as described in A1-1.\n\n---\n\n#### Finding A2-2 · Faithfulness · **OBSERVATION**\n\nThe taxonomy's classification logic (hard/soft/success truth table) is correctly NOT reproduced in the governance article — the governance article rightly defers to the taxonomy for classification semantics. The `overfull_count` metric in the doubles table (double 9) is consistent with the taxonomy's threshold section. No drift found here.\n\n---\n\n### Authority 3 — v1.2 Spec (`canon/specs/ptxprint-mcp-v1.2-spec.md`)\n\n#### Finding A3-1 · Faithfulness · **BLOCKER**\n\n**Article line 165:** \"The phases match the `progress.current_phase` values returned by `get_job_status`.\"\n\nThe v1.2 spec §3 `get_job_status` response shows:\n\n```json\n\"current_phase\": \"fetching_inputs | typesetting | autofill | uploading\"\n```\n\nFour values. The governance article defines **six** phases for blob 7: `queued | fetching_inputs | typesetting | autofill | uploading | done`.\n\nThe article's own explanatory text correctly describes the discrepancy — `queued` is \"written by the Worker on dispatch, not the Container\" and `done` is the \"terminal\" Container phase. But the opening sentence claims the phases \"match\" the spec's `current_phase`, which is incorrect: two of the six (`queued` and `done`) are telemetry-only phases that do not appear in `get_job_status.progress.current_phase`.\n\n**Proposed correction (article line 165):**\n\n```diff\n- The phases match the `progress.current_phase` values returned by `get_job_status`:\n+ Six telemetry phases are defined. Four correspond to the `progress.current_phase` values\n+ returned by `get_job_status` (`fetching_inputs`, `typesetting`, `autofill`, `uploading`).\n+ Two are telemetry-only and do not appear in `current_phase`:\n```\n\nThe numbered list that follows (items 1–6) is accurate and can remain unchanged.\n\n---\n\n#### Finding A3-2 · Faithfulness · **PASS**\n\n**Tool names in blob 3:** The article lists `submit_typeset | get_job_status | cancel_job | docs | telemetry_public | telemetry_policy`. The v1.2 spec §3 defines exactly these six tools. ✓\n\nThe ledger's §Schema blob 3 shows only four tools (`submit_typeset | get_job_status | cancel_job | docs`), but this is a ledger artifact — the ledger schema was written before D-T7 formally added the two telemetry tools. The article correctly reflects the final six-tool surface. No fault in the article; the ledger's schema table is stale.\n\n---\n\n#### Finding A3-3 · Faithfulness · **PASS**\n\n**Blob/double slot references in canned queries:** All eight canned SQL queries were traced against the schema tables. Every `blobN` and `doubleN` reference is correct:\n\n| Query | Key references | Verified |\n|---|---|---|\n| Adoption | `blob1=mcp_request`, `blob4=consumer_label` | ✓ |\n| Cache hit rate | `blob9=cache_outcome`, `blob3=submit_typeset` | ✓ |\n| Failure mix | `blob8=failure_mode`, `blob1=job_terminal` | ✓ |\n| Where time goes | `blob7=phase`, `double2=duration_ms` | ✓ |\n| Tool leaderboard | `blob3=tool_name`, `blob1=tool_call` | ✓ |\n| Document leaderboard | `blob12=docs_top_uri`, `blob3=docs` | ✓ |\n| Soft-failure trend | `blob8=failure_mode`, `blob1=job_terminal` | ✓ |\n| Pages per success | `double10=pages_count`, `blob8=success` | ✓ |\n\nNo schema slot mismatches found. This is the cleanest part of the article.\n\n---\n\n#### Finding A3-4 · Faithfulness · **PASS**\n\n**`failure_mode` in the DO state schema:** The v1.2 spec DO schema defines `failure_mode: \"hard\" | \"soft\" | \"success\" | null`. The governance article's `job_terminal` section adds `cancelled` and `timeout` to the telemetry enum. As noted in A1-1 and A2-1, the spec needs to be reconciled. This is a cross-artifact gap, not a fault of the governance article in isolation — the ledger D-T1 authorizes the five-value enum, and the spec just hasn't been updated yet (H-T4 is the relevant handoff).\n\n---\n\n### Authority 4 — Vodka Architecture (`klappy://canon/principles/vodka-architecture`)\n\n#### Finding A4-1 · Faithfulness · **OBSERVATION**\n\n**Q1 — \"Has the server grown thick?\"** The article answers: \"One `writeDataPoint()` per request at the Worker, plus a small Worker endpoint that receives Container-emitted phase events and forwards them. Two new tools. Combined module is self-contained.\"\n\nThe vodka principle's test is: \"If the codebase is expanding faster than the knowledge base it serves, something is wrong.\" The v1.2 spec quantifies the telemetry addition as \"~150 lines of telemetry module (split across writer, redactor, public-query, and policy-fetch helpers)\" added to an existing ~250+200+100 line base. The article doesn't cite these numbers. The answer is defensible but thin — \"self-contained\" is not the same as demonstrating the growth rate is appropriate.\n\nThis is an observation, not a blocker. The answer is not wrong.\n\n---\n\n#### Finding A4-2 · Faithfulness · **PASS**\n\n**Q2 — \"Has the server acquired domain opinions?\"** The article's answer is the strongest of the three. It engages with the actual principle:\n\n> \"if the field is already in the public response shape, persisting it is not new opinion — it is making existing opinion queryable over time\"\n\nThis is the resolution rule from planning ledger L-T2. It correctly applies the vodka principle's \"Zero Domain Opinion\" property and the \"if-branch that checks a domain term\" anti-pattern test. The article also correctly limits the ruling to `failure_mode` and `phase`, and correctly specifies that new dimensions NOT in the public response would warrant fresh scrutiny. This is genuine engagement, not parroting.\n\n---\n\n#### Finding A4-3 · Faithfulness · **PASS**\n\n**Q3 — \"Can the server be removed without consequence?\"** The article answers No, citing three operational dependencies (cache effectiveness signal, soft-failure rate, docs query signal). The vodka principle says the only acceptable answer is No — meaning the layer is load-bearing. The article gives the right answer with grounded justification. The answer correctly applies to the telemetry feature within the server, not the server as a whole (which is separately load-bearing for typesetting dispatch).\n\n---\n\n## Recommended Changes\n\n### Must-fix before merge (blockers)\n\n**B-1: Correct the `failure_mode` taxonomy citation (article line 181)**\n\n```markdown\n# CURRENT:\n`failure_mode` ∈ `{success, soft, hard, cancelled, timeout}` per `klappy://canon/articles/failure-mode-taxonomy`\n\n# PROPOSED:\n`failure_mode` ∈ `{success, soft, hard, cancelled, timeout}`. The core three values (`success | soft | hard`)\nare defined in `klappy://canon/articles/failure-mode-taxonomy`. The `cancelled` and `timeout` values are\ntelemetry lifecycle extensions defined in the planning ledger (D-T1) — they are not in the taxonomy and\nare not currently in the v1.2 spec DO state schema (a gap H-T4 should close).\n```\n\n**B-2: Correct the \"phases match current_phase\" claim (article line 165)**\n\n```markdown\n# CURRENT:\nThe phases match the `progress.current_phase` values returned by `get_job_status`:\n\n# PROPOSED:\nSix telemetry phases are defined. Four of these correspond directly to the `progress.current_phase`\nvalues returned by `get_job_status` (`fetching_inputs`, `typesetting`, `autofill`, `uploading`).\nTwo are telemetry-only phases not present in `current_phase`:\n```\n\n### Should-fix (nits)\n\n**N-1: `oauth` in blob 5** — Restore `oauth` to the blob 5 example values OR add a note that OAuth identification is reserved for a future auth layer.\n\n**N-2: `telemetry_policy` fallback behavior** — Add a brief description of the three-tier fallback (`knowledge_base → bundled → minimal`) to the governance article.\n\n**N-3: Ledger path references** — Update D-T7 and H-T1 in `canon/encodings/telemetry-feature-planning-ledger.md` to reference `canon/governance/telemetry-governance.md` (not `canon/constraints/`).\n\n### Operator decision required\n\n**O-1: `cancelled` and `timeout` reconciliation** — Choose one of:\n- (a) Update `failure-mode-taxonomy.md` to add `cancelled` and `timeout` with their semantics\n- (b) Update the v1.2 spec DO schema to add them\n- (c) Leave them as telemetry-only extensions and explicitly label them as such in both the governance article and the taxonomy\n\n**O-2: Privacy floor excess categories** — Encode the three unlisted categories (credentials, account IDs, docs query strings) retroactively in the planning ledger as C-T1 addenda, or confirm them as approved H-T2 extensions.\n\n---\n\n## Summary Table\n\n| ID | Authority | Type | Severity | Article Location | Finding |\n|---|---|---|---|---|---|\n| A1-1 | Ledger / Taxonomy | Faithfulness | **blocker** | Line 181 | `failure_mode` taxonomy citation incorrect; `cancelled`/`timeout` not in taxonomy |\n| A3-1 | v1.2 Spec | Faithfulness | **blocker** | Line 165 | \"phases match current_phase\" inaccurate — `queued` and `done` are telemetry-only |\n| A1-2 | Ledger | Gap | nit | Blob 5 table | `oauth` dropped from `consumer_source` values without documented rationale |\n| A1-3 | Ledger / Spec | Gap | nit | Storage section | `telemetry_policy` three-tier fallback not documented in the article |\n| A1-4 | Ledger | Observation | nit | (ledger, not article) | D-T7 / H-T1 reference stale `canon/constraints/` path |\n| A1-5 | Ledger | Excess | observation | Privacy Floor section | Three \"never logged\" categories not in C-T1 lack ledger decision grounding |\n| A4-1 | Vodka | Faithfulness | observation | Vodka section Q1 | Q1 answer thin — doesn't cite quantitative code-size evidence from spec |\n| A2-2 | Taxonomy | Faithfulness | pass | `job_terminal` section | Taxonomy classification semantics correctly deferred to taxonomy article |\n| A3-2 | v1.2 Spec | Faithfulness | pass | Blob 3 table | Tool names correct (6 tools) — ledger schema table is stale, not the article |\n| A3-3 | v1.2 Spec | Faithfulness | pass | Canned queries | All 8 canned queries use correct blob/double slot references |\n| A3-4 | v1.2 Spec | Faithfulness | pass | `job_terminal` section | DO schema gap is a spec maintenance issue, not an article fault |\n| A4-2 | Vodka | Faithfulness | pass | Vodka section Q2 | Genuine engagement with domain-opinion test; not parroting |\n| A4-3 | Vodka | Faithfulness | pass | Vodka section Q3 | Correct No answer with grounded justification |\n\n---\n\n## Statement of Independence\n\nI did not author `canon/governance/telemetry-governance.md`. This review session began with no context from the drafting session (H-T1) and no memory of the article's construction. All findings are grounded in direct cross-referencing of the article against the four named authority documents, per `klappy://canon/principles/verification-requires-fresh-context`.\n\n---\n\n*Review completed by H-T2 (fresh-session managed agent, 2026-04-30). Companions: `canon/encodings/h-t2-fresh-review-ledger.md` (DOLCHEO+H encoding of these findings).*\n"},{"path":"specs/ptxprint-mcp-v1.0-spec.md","uri":"klappy://canon/specs/ptxprint-mcp-v1.0-spec","title":"PTXprint MCP Server — v1 Specification","audience":"project","tags":["ptxprint","mcp","spec","vodka-architecture","v1"],"stability":"working","what_it_answers":"Action-only vodka layer. Ten generic tools over a stateful PTXprint canon. The server has no PTXprint domain opinions; the canon repo (separate) carries them. Async by default. Deployable on fly.io for v1, Cloudflare-compatible adapter poss","sections":[{"slug":"1-the-contract","heading":"1. The Contract","start":26,"end":61},{"slug":"2-companion-the-ptxprint-canon-repo","heading":"2. Companion: the PTXprint Canon Repo","start":61,"end":83},{"slug":"3-tools-10","heading":"3. Tools (10)","start":83,"end":167},{"slug":"4-job-lifecycle-and-runner","heading":"4. Job Lifecycle and Runner","start":167,"end":193},{"slug":"5-deployment","heading":"5. Deployment","start":193,"end":222},{"slug":"6-what-s-deferred-and-why","heading":"6. What's Deferred (and Why)","start":222,"end":238},{"slug":"7-migration-from-the-first-pass-spec","heading":"7. Migration from the First-Pass Spec","start":238,"end":264},{"slug":"8-definition-of-done-for-v1","heading":"8. Definition of Done for v1","start":264,"end":280},{"slug":"9-tomorrow-s-first-execution-scope","heading":"9. Tomorrow's First Execution Scope","start":280,"end":303}],"body":"---\ntitle: \"PTXprint MCP Server — v1 Specification\"\nsubtitle: \"Action-only vodka layer over the PTXprint canon\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"spec\", \"vodka-architecture\", \"v1\"]\nversion: \"v1.0-draft\"\ndate: 2026-04-27\nsupersedes: \"PTXprint_MCP_SPEC.md - First Pass (17-tool spec, 2026-04-27 AM)\"\napplied_canon:\n  - klappy://canon/principles/vodka-architecture\n  - klappy://canon/constraints/oddkit-prompt-pattern\n  - klappy://canon/principles/dry-canon-says-it-once\n  - klappy://canon/principles/maintainability-one-person-indefinitely\ngoverns: \"the second-pass PTXprint MCP server build (hackathon week 2026-04-28+)\"\nstatus: draft_for_review\n---\n\n# PTXprint MCP Server — v1 Specification\n\n> Action-only vodka layer. Ten generic tools over a stateful PTXprint canon. The server has no PTXprint domain opinions; the canon repo (separate) carries them. Async by default. Deployable on fly.io for v1, Cloudflare-compatible adapter possible later. Supersedes the 17-tool first-pass spec.\n\n---\n\n## 1. The Contract\n\n### What this server is\n\nA thin MCP server that gives an AI agent four kinds of action over a PTXprint installation:\n\n1. **Discover** projects and configurations on disk.\n2. **Read and write** sandboxed files inside project trees.\n3. **Edit** `ptxprint.cfg` keys safely without losing the rest of the file.\n4. **Submit and observe** typesetting jobs (simple typesetting + autofill).\n\n### What this server is not\n\n- **Not a PTXprint reference manual.** The 400+ cfg keys, file format details (piclist, adjlist, changes.txt), inheritance semantics, troubleshooting craft, and font/script requirements live in a separate canon repo, retrieved by agents through `oddkit` MCP.\n- **Not synchronous.** Every typesetting call returns a job ID immediately. No call blocks for more than a few seconds.\n- **Not a full PTXprint UI replacement.** Cover generation, advanced TeX macro injection, archive handling — all out of v1 scope.\n- **Not a knowledge-base server.** Retrieval is the canon repo's concern, served by oddkit. The PTXprint MCP doesn't reimplement search, get, or any retrieval surface.\n\n### Vodka boundary\n\nThe server holds **zero PTXprint domain opinions** in its code. It knows about:\n- Filesystem paths and sandboxing\n- INI files (the structural format of `ptxprint.cfg`) — generic Python `configparser`, not section-aware logic\n- Subprocess invocation, exit codes, log parsing for `^!` errors and overfull-box warnings\n- Job lifecycle (submitted → running → succeeded/failed/cancelled)\n\nIt does **not** know about: piclist syntax, adjlist line format, USFM book codes, what `c_fighiderefs` does, how diglot column fractions interact, what fonts are needed for which scripts, what the cfg sections mean, naming conventions for output PDFs. All of those live in the canon repo and are retrieved by the agent on demand.\n\nThe constraint test (`klappy://canon/principles/vodka-architecture`):\n- **Has the server grown thick?** No — 10 generic tools, ~500 lines of code projected.\n- **Has the server acquired domain opinions?** No — every tool is generic file IO, INI safe-edit, or subprocess action.\n- **Can the server be removed without consequence?** No — it provides sandboxed file access, INI edit safety, and async job lifecycle that no off-the-shelf MCP gives. Load-bearing.\n\n---\n\n## 2. Companion: the PTXprint Canon Repo\n\nThe canon repo is a sibling artifact to this MCP server, **not part of it**. It lives at a separate `klappy/` GitHub repo (final name TBD) parallel to `klappy/oral-theology-kb`. Agents access it through their existing `oddkit` MCP by setting `knowledge_base_url` to point at the canon repo.\n\n### Canon seed sources (initial)\n\n1. **`sillsdev/ptx2pdf/docs/documentation/`** — existing markdown files in the upstream repo (licence verification pending; mirror vs. link decision pending H-005).\n2. **PTXprint UI tooltip dump** — captures every UI control's tooltip, ID, and corresponding cfg setting name. Resolves the \"which key does what\" gap.\n3. **450-slide PTXprint deck** — ESE pass in flight (separate Claude session); output integrated as canon docs when it lands.\n4. **Operator's ~1000 real configs** — private training corpus (C-006); used to seed canon authoring and validate agent reasoning, not mirrored publicly.\n\n### Canon content shape (target)\n\n- One markdown article per `ptxprint.cfg` section (`paper`, `document`, `notes`, etc.) with key descriptions, valid values, and effect.\n- One article per supporting file format (piclist, adjlist, changes.txt, FRTlocal.sfm, ptxprint.sty).\n- Craft articles: \"fixing overfull hboxes,\" \"diglot setup,\" \"font requirements per script,\" \"creating a new config,\" \"common autofill failures.\"\n- Schema documents the agent searches before any cfg-edit reasoning step.\n\nThe agent's reasoning loop becomes: search canon → understand the change → use the MCP server's action tools to apply it → submit typeset → observe result.\n\n---\n\n## 3. Tools (10)\n\nAll tools are sandboxed to `<projects_root>/<prjid>/shared/ptxprint/` and `<projects_root>/<prjid>/local/ptxprint/`. Path-escape attempts return errors.\n\n### 3.1 Discovery (3)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `list_projects` | — | `[{id, config_count, configs[]}]` | Scans projects root; excludes `_*` and `.*` folders |\n| `list_configs` | `project_id` | `[config_name, ...]` | Sorted alphabetically (caseless) |\n| `list_files` | `project_id`, optional `config_name`, optional `subpath` | `[{name, type: file\\|dir, size, mtime}]` | Generic directory listing within sandbox |\n\n### 3.2 File IO (3)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `read_file` | `project_id`, `relpath` | `{content: string, mtime, size}` | Returns `\"\"` if not found (with `exists: false` flag) |\n| `write_file` | `project_id`, `relpath`, `content` | `{ok, path, bytes_written}` | Whole-file write; creates parent dirs as needed |\n| `delete_file` | `project_id`, `relpath` | `{ok, deleted: bool}` | Idempotent; missing file returns `deleted: false` without error |\n\n`relpath` is interpreted relative to the project root and must resolve inside the sandbox. Examples: `shared/ptxprint/Default/ptxprint.cfg`, `shared/ptxprint/Default/AdjLists/46ROMWSG-Default.SFM.adj`, `local/ptxprint/Default/WSG_Default_MAT_ptxp.log`.\n\n### 3.3 INI safe-edit (1)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `update_cfg_value` | `project_id`, `config_name`, `section`, `key`, `value` | `{ok, previous_value, new_value}` | Section-key edit preserving the rest of the file |\n\nBacked by Python `configparser`. Preserves all other keys; comment preservation is best-effort (configparser limitation). For complex multi-key edits, agents can read → reason → write_file the whole modified content if comment fidelity matters.\n\nThe agent finds out **what** to edit by searching canon. The server only offers the **how**.\n\n### 3.4 Inheritance resolution (1)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `resolve_config` | `project_id`, `config_name` | `{section: {key: value}}` (effective merged) plus `inheritance_chain: [base, ..., self]` | Walks `[import] config = X` chain, merges parent → child |\n\nThis tool is in v1 because:\n- It's pure epistemic discipline (compute the transitive closure of the import chain), **not** PTXprint domain knowledge.\n- Without it, every agent reasoning step that needs effective settings has to re-derive them — wasteful and error-prone.\n- The implementation is ~30 lines of generic INI + dict-merge code.\n\nIf first-week experience shows agents don't actually need it (they read the chain manually via `read_file`), this tool gets dropped in v2 — but the cost of including it now is small.\n\n### 3.5 Typesetting jobs (3)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `submit_typeset` | `project_id`, optional `config_name` (default `\"Default\"`), optional `books`, optional `define` (`-D` overrides), optional `mode` (`simple\\|autofill`, default `simple`), optional `timeout` | `{job_id, submitted_at, predicted_pdf_path}` | Returns immediately. Predicted PDF path computed from inputs (canon documents the naming convention). |\n| `get_job_status` | `job_id` | See schema below | Pollable; safe to call any time |\n| `cancel_job` | `job_id` | `{ok, was_running, cancelled_at}` | Sends SIGTERM; required for autofill (30 min jobs need a kill switch) |\n\n#### `get_job_status` return schema\n\n```json\n{\n  \"job_id\": \"...\",\n  \"state\": \"queued | running | succeeded | failed | cancelled\",\n  \"submitted_at\": \"2026-04-27T21:09:55Z\",\n  \"started_at\": \"2026-04-27T21:10:01Z\",\n  \"completed_at\": null,\n  \"progress\": {\n    \"passes_completed\": 3,\n    \"passes_total_estimate\": null,\n    \"current_phase\": \"typesetting | autofill | finishing\"\n  },\n  \"exit_code\": null,\n  \"exit_meaning\": null,\n  \"pdf_path\": null,\n  \"log_path\": \"C:\\\\...\\\\WSG_Default_MAT_ptxp.log\",\n  \"log_tail\": \"(last 100 lines of XeTeX log)\",\n  \"errors\": [],\n  \"overfull_count\": 0,\n  \"human_summary\": \"Typesetting in progress. 3 passes completed. No errors yet.\"\n}\n```\n\n`human_summary` is a one-line natural-language status — important for downstream agents that may relay over WhatsApp or chat (D-013-deferred but the field is reserved now).\n\n`progress.passes_total_estimate` is `null` for simple typesetting (one pass) and a heuristic estimate for autofill (typically 5–10).\n\n---\n\n## 4. Job Lifecycle and Runner\n\nThe MCP server hosts a tiny job table (in-memory or sqlite in the container — TBD; sqlite is more vodka-correct because it's stateful-on-disk rather than stateful-in-process) and spawns PTXprint subprocesses with the appropriate flags:\n\n```\nptxprint -P <prjid> -c <config> -b \"<books>\" -p <projects_dir> -q [-D key=value ...]\n```\n\nFor autofill, the appropriate PTXprint flag is appended (TBD: confirm flag name from PTXprint source — operator/Martin can fill in tomorrow).\n\n### Progress reporting\n\nPer D-010: progress increments by completed passes, not by pages. The runner tails the XeTeX log; each `Output written on ...` line or autofill-pass-complete marker bumps the pass counter. No fake percentage estimation.\n\n### Cancellation\n\n`cancel_job` sends SIGTERM to the PTXprint subprocess. Job state moves to `cancelled`. Partial outputs (if any) are preserved at their existing paths but not surfaced as `pdf_path` (which is reserved for `succeeded` jobs).\n\n### Timeouts\n\nTwo layers:\n- **Per-job timeout** (`submit_typeset` parameter, default 1800s = 30 min for autofill, 300s = 5 min for simple). Job state moves to `failed` with `exit_meaning: \"timeout\"`.\n- **No platform timeout** is exposed to the caller — the MCP server takes responsibility for not blocking on long jobs. (Why D-005 exists.)\n\n---\n\n## 5. Deployment\n\n### Hackathon week (D-008)\n\n- **Runner host:** fly.io. Container with PTXprint, XeTeX, SIL Charis, and the MCP server. Configurable CPU count.\n- **MCP transport:** stdio for local Claude Desktop testing; `streamable-http` for the deployed instance.\n- **No Cloudflare adapter in v1.** Direct connection to fly.io endpoint.\n\n### Long-term shape (post-hackathon, deferred)\n\nA possible future split:\n- **Cloudflare Worker** = thin MCP adapter + auth + rate limiting at the edge.\n- **fly.io / dedicated container** = the actual typesetter runner.\n- Worker forwards MCP calls to runner; runner replies; Worker streams response back.\n\nThis is **not** v1. The split adds operational complexity and only pays off at scale.\n\n### Container contents\n\n- Python 3.11+\n- PTXprint installed (Linux build) — installed from upstream Docker image if available, otherwise built from `ptx2pdf` source per `python/readme.md`\n- XeTeX (`texlive-xetex`)\n- SIL Charis font (sufficient for English Bibles per D-002)\n- The MCP server itself (~500 lines of Python)\n\nTotal budget: well under 2 GB per fly.io constraints.\n\n---\n\n## 6. What's Deferred (and Why)\n\n| Deferred capability | Why deferred | When to revisit |\n|---|---|---|\n| **Knob fan-out for autofill** (multiple attempts on same book) | ~1 month build effort (D-009); not needed for first useful demo | After v1 proven; potentially Q3 2026 |\n| **WhatsApp / external messaging integration** | Out of MCP scope; downstream agent concern (O-013) | When a downstream agent (BT Servant) wires it |\n| **Cover generation workflow** | Already deferred in first-pass spec; no new pressure | When a user explicitly needs it; canon may cover the manual path first |\n| **TeX macro injection** (`*-mods.tex`, `*-premods.tex`) | High blast radius; needs validation surface canon doesn't yet have | After canon has authored TeX-safety docs |\n| **Per-page progress reporting** | PTXprint doesn't expose it useably in headless (O-009) | If/when PTXprint upstream surfaces the GUI's \"dot thing\" |\n| **Multi-script font support** | Initial scope is English Bibles (D-002) | When first non-English target is committed |\n| **Concurrent jobs** | One run at a time per runner instance keeps the state machine simple | When real usage shows queueing isn't sufficient |\n| **Auth on the HTTP transport** | Trusted internal use; no public exposure in hackathon week | Before any public listing |\n| **Cloudflare Worker MCP adapter** | Operationally premature; fly.io alone is simpler | When public scale demands edge auth/rate-limiting |\n\n---\n\n## 7. Migration from the First-Pass Spec\n\nThe first-pass 17-tool spec is now reference scaffolding, not the build target. Mapping:\n\n| Original tool | v1 disposition |\n|---|---|\n| `list_projects` | **Kept** as-is |\n| `list_configs` | **Kept** as-is |\n| `get_config` | **Removed** — agent uses `read_file` + canon (raw) or `resolve_config` (effective) |\n| `get_config_inheritance` | **Removed** — `resolve_config` returns the chain in its response |\n| `set_config_values` | **Replaced by** `update_cfg_value` (single key/value, simpler interface) |\n| `create_config` | **Removed** — agent uses `write_file` with a minimal-cfg template documented in canon |\n| `get_project_file` / `set_project_file` | **Replaced by** generic `read_file` / `write_file` with sandbox |\n| `list_adjlists` | **Removed** — agent uses `list_files` on the AdjLists/ directory |\n| `get_adjlist` / `set_adjlist` | **Replaced by** `read_file` / `write_file`; canon documents the format |\n| `delete_adjlist` | **Replaced by** generic `delete_file` |\n| `get_piclist` (parsed) | **Replaced by** `read_file`; canon documents piclist format |\n| `set_piclist` (parsed) | **Replaced by** `write_file`; canon documents piclist format |\n| `predict_output_paths` | **Removed** — `submit_typeset` returns `predicted_pdf_path`; canon documents naming convention; or agent uses `list_files` on `local/ptxprint/` |\n| `run_typeset` | **Replaced by** `submit_typeset` (async) |\n| `get_job_log` | **Replaced by** `get_job_status.log_tail` |\n\nNet: 17 tools → 10 tools, with no functionality loss (only re-housed into either canon or generic operations).\n\n---\n\n## 8. Definition of Done for v1\n\nThe MCP server is \"v1 done\" when an agent connected to both this MCP server and oddkit MCP (pointing at the PTXprint canon repo) can:\n\n1. List projects and configs, discover what's available.\n2. Read any file in a project sandbox; understand its purpose by searching canon.\n3. Modify a single cfg key (e.g., margins, line spacing) safely without breaking other keys or losing comments worth keeping.\n4. Submit a typesetting job and receive a job ID immediately.\n5. Poll job status and get a meaningful per-pass progress update.\n6. Receive the resulting PDF path on success and a legible error summary on failure.\n7. Cancel a long-running autofill job mid-way.\n\nA small smoke-test script driving an English-Bible test project end-to-end is the validation artifact.\n\n---\n\n## 9. Tomorrow's First Execution Scope\n\n(For the autonomous coding run kicked off per H-003)\n\n**In scope:**\n- Implement the 10 tools listed in §3, sandboxed and tested.\n- Implement job lifecycle (§4) with sqlite state and per-pass progress parsing.\n- Package as a fly.io-deployable container.\n- Smoke-test end-to-end on one English Bible (BSB or ULT) with the `cover` test config from `ptx2pdf/tests/`.\n\n**Explicitly out of scope for the first run:**\n- Building the canon repo (separate concurrent track; needs the Google Slides ESE first).\n- Knob fan-out (D-009).\n- Cloudflare adapter (deferred per §5).\n- Any of §6's deferred items.\n\n**Validation gate (per `klappy://canon/principles/verification-requires-fresh-context`):** \nThe first-run agent does not validate its own output. A separate session (or human review) accepts or returns to iteration. Same-session \"looks done\" is not validation.\n\n---\n\n*End of v1 specification. Companion artifact: `transcript-encoded-session-2.md` (the OLDC+H ledger this spec converges from). Open questions tracked there as Q-open-1 through Q-open-7.*\n"},{"path":"specs/ptxprint-mcp-v1.1-spec.md","uri":"klappy://canon/specs/ptxprint-mcp-v1.1-spec","title":"PTXprint MCP Server — v1.1 Specification","audience":"project","tags":["ptxprint","mcp","spec","vodka-architecture","v1.1","first-principles"],"stability":"working","what_it_answers":"**What changed from v1.0.** v1.0 was an honest improvement on the 17-tool PoC but it was a refactor, not a first-principles derivation. Two domain-flavored helpers (`update_cfg_value`, `resolve_config`) survived the cut by being labeled \"ep","sections":[{"slug":"1-the-contract","heading":"1. The Contract","start":27,"end":81},{"slug":"2-companion-the-ptxprint-canon-repo","heading":"2. Companion: the PTXprint Canon Repo","start":81,"end":112},{"slug":"3-tools-7","heading":"3. Tools (7)","start":112,"end":185},{"slug":"4-job-lifecycle-and-runner","heading":"4. Job Lifecycle and Runner","start":185,"end":238},{"slug":"5-deployment","heading":"5. Deployment","start":238,"end":268},{"slug":"6-what-s-deferred-and-why","heading":"6. What's Deferred (and Why)","start":268,"end":292},{"slug":"7-migration","heading":"7. Migration","start":292,"end":319},{"slug":"8-definition-of-done-for-v1-1","heading":"8. Definition of Done for v1.1","start":319,"end":337},{"slug":"9-tomorrow-s-first-execution-scope","heading":"9. Tomorrow's First Execution Scope","start":337,"end":362}],"body":"---\ntitle: \"PTXprint MCP Server — v1.1 Specification\"\nsubtitle: \"First-principles vodka layer; domain craft lives in canon\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"spec\", \"vodka-architecture\", \"v1.1\", \"first-principles\"]\nversion: \"v1.1-draft\"\ndate: 2026-04-28\nsupersedes: \"ptxprint-mcp-v1-spec.md (v1.0-draft, 2026-04-27 PM)\"\napplied_canon:\n  - klappy://canon/principles/vodka-architecture\n  - klappy://canon/principles/kiss-simplicity-is-the-ceiling\n  - klappy://canon/constraints/oddkit-prompt-pattern\n  - klappy://canon/principles/dry-canon-says-it-once\n  - klappy://canon/principles/maintainability-one-person-indefinitely\ngoverns: \"the second-pass PTXprint MCP server build (hackathon week 2026-04-28+)\"\nstatus: draft_for_review\n---\n\n# PTXprint MCP Server — v1.1 Specification\n\n> **What changed from v1.0.** v1.0 was an honest improvement on the 17-tool PoC but it was a refactor, not a first-principles derivation. Two domain-flavored helpers (`update_cfg_value`, `resolve_config`) survived the cut by being labeled \"epistemic discipline\" or \"ergonomic.\" On reinspection both encode PTXprint-specific opinion in server code. v1.1 removes them and lets canon teach the agent the patterns. Tool count: **7** (was 10), with 3 file primitives, 3 job-lifecycle primitives, and 1 asset-fetch primitive. Net effect: the server is meaningfully smaller and the canon repo carries strictly more weight.\n\n---\n\n## 1. The Contract\n\n### Origin\n\nThis spec is derived from first principles. It asks: *what is the minimum set of generic primitives an AI agent needs from a server in order to drive a stateful PTXprint installation toward a finished PDF, given that the agent has separate retrieval access to a PTXprint canon repo?*\n\nThe answer divides cleanly:\n\n- **Filesystem operations** the agent uses to inspect and mutate project state (sandboxed).\n- **Subprocess lifecycle** for the typesetting engine (async, observable, cancellable).\n- **One asset-fetch primitive** that exists only because base64-shuttling multi-megabyte font binaries through MCP envelopes is operationally unworkable.\n\nThat's it. Three categories, seven tools.\n\n### What this server is\n\nA thin MCP server that gives an AI agent four kinds of action:\n\n1. **Discover** what's on disk (projects, configs, files).\n2. **Read** sandboxed files.\n3. **Write** sandboxed files.\n4. **Submit, observe, and cancel** typesetting jobs.\n\nPlus one provisioning primitive (font installation) added post-discovery because no other primitive can do it.\n\n### What this server is not\n\n- **Not a PTXprint reference manual.** The 400+ cfg keys, file format specs, inheritance rules, INI safe-editing patterns, troubleshooting craft, and font/script requirements live in the canon repo, retrieved by agents through `oddkit` MCP.\n- **Not synchronous.** Every typesetting call returns a job ID. No call blocks for more than a few seconds.\n- **Not domain-aware.** The server knows nothing about USFM, piclists, adjlists, diglots, fonts-by-script, Paratext metadata, or what any cfg key means. Every file is opaque text. Every subprocess invocation is opaque action.\n\n### The vodka boundary, restated\n\nThe server knows:\n- Filesystem paths and sandboxing\n- HTTPS download with sha256 verification\n- Subprocess invocation, exit codes, log parsing for `^!` errors and overfull-box warnings\n- Job lifecycle (submitted → running → succeeded/failed/cancelled) and per-job fontconfig isolation\n\nThe server does not know:\n- That `ptxprint.cfg` is INI (it's an opaque text file)\n- That `[import] config = X` means inheritance (canon teaches the agent the walk)\n- That piclist / adjlist / changes.txt / FRTlocal.sfm have any structure (canon teaches the formats)\n- What `c_fighiderefs`, `s_linespacing`, or any other cfg key does (canon teaches each section)\n- That fonts have OpenType axes or that LFF is the SIL font registry (canon teaches the resolution loop)\n\nThe constraint test (`klappy://canon/principles/vodka-architecture`):\n\n- **Has the server grown thick?** No — 7 tools, all generic in shape, projected ~400 lines of code.\n- **Has the server acquired domain opinions?** No — the only PTXprint-specific code is the argv shape for `ptxprint -P ... -c ... -b ... -p ...` in the runner and the regex for `^!` and `Overfull` in log parsing. Both are mechanism, not opinion.\n- **Can the server be removed without consequence?** No — sandboxed file IO + INI edit safety can be approximated by other servers; per-job fontconfig isolation + log-aware subprocess lifecycle + sha256-verified font fetch cannot. Load-bearing.\n\n---\n\n## 2. Companion: the PTXprint Canon Repo\n\nThe canon repo is a sibling artifact, not part of this server. It lives at a separate `klappy/` GitHub repo (parallel to `klappy/oral-theology-kb`). Agents access it through `oddkit` MCP by setting `knowledge_base_url` to point at it.\n\n### Canon seed sources\n\n1. `sillsdev/ptx2pdf/docs/documentation/` — markdown docs in the upstream repo (licence verification owed; H-005).\n2. PTXprint UI tooltip dump — every UI control's tooltip, ID, and corresponding cfg setting name.\n3. PTXprint MASTER SLIDES deck (438 slides) — ESE complete: `ptxprint-master-slides.surface.json` + `.surface.md`.\n4. Operator's ~1000 real configs — private training corpus (C-006).\n\n### Canon articles required for v1.1 to be agent-usable\n\nTwo articles are gated by v1.1's tool removal — without them, the agent cannot reproduce what `update_cfg_value` and `resolve_config` used to do server-side:\n\n- `canon/articles/ptxprint-config-inheritance.md` — how to walk `[import] config = X` chains and compute effective settings (replaces v1.0's `resolve_config` tool).\n- `canon/articles/ptxprint-cfg-safe-editing.md` — how to modify a single cfg key without losing other keys, sections, or load-bearing comments (replaces v1.0's `update_cfg_value` tool).\n\nSketches for both are companion artifacts to this spec.\n\nAdditional articles that v1.1 references but doesn't gate on:\n\n- `canon/articles/ptxprint-project-layout.md` — the directory tree, what files matter, what the configurations folder contains.\n- `canon/articles/ptxprint-config-discovery.md` — how to enumerate projects and configs over the generic `list_files` primitive (replaces v1.0's `list_projects` and `list_configs`).\n- `canon/articles/ptxprint-output-paths.md` — the `<PRJ>_<Config>_<bks>_ptxp.pdf` naming convention and where logs live (replaces v1.0's `predict_output_paths`).\n- `canon/articles/ptxprint-font-resolution.md` — the LFF lookup → manifest → install_fonts → lock-write loop (gates the font subsystem; cross-references the session-3 design doc).\n\nThe agent's reasoning loop becomes: search canon → understand the change or pattern → use the MCP server's primitives to apply it → submit typeset → observe.\n\n---\n\n## 3. Tools (7)\n\nAll file operations are sandboxed to `<projects_root>/<prjid>/`. Path-escape attempts return errors. The asset-fetch primitive is sandboxed to a managed-fonts subtree under the project.\n\n### 3.1 File operations (3)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `list_files` | optional `project_id`, optional `relpath` | `[{name, type: file\\|dir, size, mtime}]` | No args: lists projects-root contents. `project_id` only: lists the project root. Both: lists within sandbox. No filtering of `_*`/`.*` folders — canon describes the Paratext convention; agent applies it. |\n| `read_file` | `project_id`, `relpath` | `{content, exists, mtime, size}` | Returns `exists: false` and `content: \"\"` if not found. |\n| `write_file` | `project_id`, `relpath`, `content` | `{ok, path, bytes_written}` | Whole-file write. Creates parent dirs. |\n\n`relpath` is sandbox-relative. Examples: `shared/ptxprint/Default/ptxprint.cfg`, `shared/ptxprint/Default/AdjLists/46ROMWSG-Default.SFM.adj`, `local/ptxprint/Default/WSG_Default_MAT_ptxp.log`.\n\n`delete_file` is **deferred to v2.** Real-world usage will tell us whether it's needed; v1.1 ships without it.\n\n### 3.2 Job lifecycle (3)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `submit_typeset` | `project_id`, optional `config_name` (default `\"Default\"`), optional `books`, optional `define` (`-D` overrides), optional `mode` (`simple\\|autofill`, default `simple`), optional `timeout` | `{job_id, submitted_at, predicted_pdf_path}` | Returns immediately. Predicted PDF path computed from inputs; canon documents the naming convention. Performs Phase 2 font verification before launching XeTeX (see §4). |\n| `get_job_status` | `job_id` | See schema below | Pollable; safe to call any time. |\n| `cancel_job` | `job_id` | `{ok, was_running, cancelled_at}` | SIGTERM. Required for autofill (30 min jobs need a kill switch). |\n\n#### `get_job_status` return schema\n\n```json\n{\n  \"job_id\": \"...\",\n  \"state\": \"queued | running | succeeded | failed | cancelled\",\n  \"submitted_at\": \"2026-04-28T03:09:55Z\",\n  \"started_at\": \"2026-04-28T03:10:01Z\",\n  \"completed_at\": null,\n  \"progress\": {\n    \"passes_completed\": 3,\n    \"passes_total_estimate\": null,\n    \"current_phase\": \"font_check | typesetting | autofill | finishing\"\n  },\n  \"font_check\": {\n    \"lock_present\": true,\n    \"files_present\": [{ \"family_id\": \"charissil\", \"filename\": \"CharisSIL-Regular.ttf\", \"ok\": true }],\n    \"families_resolvable\": [{ \"family\": \"Charis SIL\", \"fc_match\": \"Charis SIL\", \"ok\": true }],\n    \"blocked_typeset\": false\n  },\n  \"exit_code\": null,\n  \"exit_meaning\": null,\n  \"pdf_path\": null,\n  \"log_path\": \"C:\\\\...\\\\WSG_Default_MAT_ptxp.log\",\n  \"log_tail\": \"(last 100 lines of XeTeX log)\",\n  \"errors\": [],\n  \"overfull_count\": 0,\n  \"human_summary\": \"Typesetting in progress. 3 passes completed. No errors yet.\"\n}\n```\n\n`human_summary` is a one-line natural-language status — important for downstream agents that may relay over WhatsApp or chat (O-013).\n\n`progress.passes_total_estimate` is `null` for simple typesetting (one pass) and a heuristic estimate for autofill (typically 5–10).\n\n`font_check` is populated whenever Phase 2 verification has run. `blocked_typeset: true` means the job did not proceed to XeTeX because of a font issue; check `files_present` and `families_resolvable` for the specifics.\n\n### 3.3 Asset provisioning (1)\n\n| Tool | Inputs | Returns | Notes |\n|---|---|---|---|\n| `install_fonts` | `project_id`, `manifest: [{family_id, version, filename, url, expected_sha256}]` | `[{family_id, filename, status: ok\\|failed, reason?, installed_path?}]` | Per-entry result. Fetches each URL, verifies sha256, places at `<projects_root>/<prjid>/shared/ptxprint/managed-fonts/<family_id>/<version>/<filename>`. Refreshes fontconfig (`fc-cache -f`). Idempotent — safe to re-run. |\n\nThe agent — guided by canon — does the LFF lookup, distributable/license check, manifest construction, and `fonts.lock.json` write (via `write_file`). This tool exists because URL fetch + sha256 verify + binary placement is the part the agent cannot do over `write_file`.\n\nSandboxing: URLs must be HTTPS. Filenames cannot contain path separators. `family_id` matches `^[a-z0-9-]+$`. Target paths are computed deterministically from `(project_id, family_id, version, filename)`; the agent cannot influence the path beyond those four fields.\n\n---\n\n## 4. Job Lifecycle and Runner\n\nThe MCP server hosts a tiny job table (sqlite on disk for vodka-correctness) and spawns PTXprint subprocesses with the appropriate flags:\n\n```\nptxprint -P <prjid> -c <config> -b \"<books>\" -p <projects_dir> -q [-D key=value ...]\n```\n\nFor autofill, the appropriate PTXprint flag is appended (TBD: confirm flag name from PTXprint source; canon will document).\n\n### Phase 2 font verification (before XeTeX)\n\nBefore launching the XeTeX subprocess, `submit_typeset` performs three checks:\n\n1. **Lock present?** `<project>/shared/ptxprint/fonts.lock.json` exists.\n2. **All locked files present on disk?** Each file in the lock resolves to a real file in the managed-fonts tree.\n3. **All locked families resolvable in job-scoped fontconfig?** `fc-match` against the job's fontconfig environment returns the expected family for each.\n\nAny miss → state `failed`, `blocked_typeset: true`, hard error message naming what's missing. This closes the silent-fallback class of soft-failure PDFs (the `ptx2pdf` issue #212 analogue for fonts).\n\nA passing Phase 2 is not a guarantee of glyph-completeness — that depends on whether the locked fonts actually cover the project's text. But it guarantees XeTeX sees the fonts the lock declared. Missing-glyph issues become content problems addressed by font selection upstream, not silent server failures.\n\n### Per-job fontconfig isolation\n\nTwo projects pinning different versions of the same family (e.g. Charis SIL 6.x vs 7.x) must not collide. Before launching XeTeX, the runner constructs a job-scoped fontconfig environment that only sees:\n\n- The current project's managed-fonts tree\n- TeX-internal font directories (Computer Modern etc.) that XeTeX needs\n\nMechanism (in implementation-preference order):\n\n1. `OSFONTDIR` env var (TeX-specific; simplest if it works)\n2. `FONTCONFIG_FILE` env var pointing at a job-specific fontconfig XML (most precise)\n3. Per-job `XDG_CONFIG_HOME` scoped to a tmpdir (heaviest)\n\nChoice is implementation-detail; canon documents whichever is chosen.\n\n### Progress reporting\n\nPer-pass, not per-page (D-010). Runner tails the XeTeX log; each `Output written on ...` line or autofill-pass-complete marker bumps the pass counter. No fake percentage estimation.\n\n### Cancellation\n\n`cancel_job` sends SIGTERM. Job state moves to `cancelled`. Partial outputs preserved at their existing paths but not surfaced as `pdf_path` (which is reserved for `succeeded` jobs).\n\n### Timeouts\n\nTwo layers:\n- **Per-job timeout** (`submit_typeset` parameter, default 1800s for autofill, 300s for simple). Job state moves to `failed` with `exit_meaning: \"timeout\"`.\n- **No platform timeout** is exposed to the caller. The MCP server takes responsibility for not blocking on long jobs.\n\n---\n\n## 5. Deployment\n\n### Hackathon week (D-008)\n\n- **Runner host:** fly.io. Container with PTXprint, XeTeX, and the MCP server. Configurable CPU count.\n- **MCP transport:** stdio for local Claude Desktop testing; `streamable-http` for the deployed instance.\n- **No Cloudflare adapter in v1.1.** Direct connection to fly.io endpoint.\n\n### Container contents (per session 3 C-007)\n\n- Python 3.11+\n- PTXprint installed (Linux build)\n- XeTeX via `texlive-xetex --no-install-recommends`\n- fontconfig\n- An empty managed-fonts tree at `/var/lib/ptxprint/managed-fonts/`\n- A baked-in fontconfig include file pointing at the managed tree\n- The MCP server itself (~400 lines of Python)\n\n**Explicitly not included:** `fonts-sil-charis`, `fonts-noto-*`, `fonts-liberation`, or any other `fonts-*` apt package. The container fails loudly when font resolution hasn't run; no accidental fall-back to system fonts that masks broken locks.\n\nTeX-internal Computer Modern / Latin Modern fonts arrive with `texlive-xetex` itself. They are TeX infrastructure, not fontconfig user fonts, and they are not \"baked fonts\" in the sense this rejects.\n\nTotal budget: well under 2 GB.\n\n### Long-term shape (deferred)\n\nCloudflare Worker as MCP adapter + auth + rate limiting at the edge, forwarding to fly.io runner. Not v1.1.\n\n---\n\n## 6. What's Deferred (and Why)\n\n| Deferred capability | Why deferred | When to revisit |\n|---|---|---|\n| **`delete_file`** | Rare in real workflows; can be added without spec churn when needed | When real usage shows it |\n| **`update_cfg_value` as a server tool** | Domain opinion (configparser-correct INI editing); canon article teaches the pattern | Only if canon-driven approach proves insufficient |\n| **`resolve_config` as a server tool** | Domain opinion (the `[import]` walk is PTXprint-specific); canon article teaches the algorithm | Only if canon-driven approach proves insufficient |\n| **`list_projects` / `list_configs` as separate tools** | Both are listings; agent uses `list_files` plus a canon article on Paratext-folder filtering | Only if ergonomics suffer measurably |\n| **`predict_output_paths`** | `submit_typeset` returns `predicted_pdf_path`; canon documents the naming convention; agent can also use `list_files` on `local/ptxprint/` | Only if agents need it pre-submit |\n| **Knob fan-out for autofill** | ~1 month build effort (D-009); not needed for first useful demo | After v1.1 proven; potentially Q3 2026 |\n| **WhatsApp / external messaging integration** | Out of MCP scope; downstream agent concern (O-013) | When BT Servant wires it |\n| **Cover generation workflow** | Already deferred; no new pressure | When a user explicitly needs it |\n| **TeX macro injection** (`*-mods.tex`, `*-premods.tex`) | High blast radius; needs validation surface canon doesn't yet have | After canon authors TeX-safety docs |\n| **Per-page progress reporting** | PTXprint doesn't expose it useably in headless (O-009) | If/when PTXprint upstream surfaces it |\n| **Multi-script font support** | Initial scope is English Bibles (D-002) | When first non-English target is committed |\n| **Concurrent jobs per runner** | One run at a time keeps the state machine simple | When usage shows queueing isn't enough |\n| **Auth on the HTTP transport** | Trusted internal use this week | Before public listing |\n| **Cloudflare Worker MCP adapter** | Operationally premature | When public scale demands edge auth/rate-limiting |\n| **`ptxprint_override.cfg` write surface** | Read-only awareness via `read_file` is sufficient for v1.1; writes need a validation story canon doesn't yet have | When users need write |\n\n**Read-only awareness of override files** is in scope. The agent uses `read_file` to detect the presence and contents of `ptxprint_project.cfg` and `ptxprint_override.cfg`; canon teaches the agent how PTXprint resolves precedence so that `update_cfg_value`-pattern writes can warn the user when an override would silently shadow the change.\n\n---\n\n## 7. Migration\n\n### From v1.0 to v1.1\n\n| v1.0 tool | v1.1 disposition |\n|---|---|\n| `list_projects` | **Folded into `list_files`** + canon article on Paratext-folder filtering |\n| `list_configs` | **Folded into `list_files`** (it's `list_files(project_id, 'shared/ptxprint')`) |\n| `list_files` | **Kept** with widened semantics (also handles list_projects / list_configs cases) |\n| `read_file` | **Kept** as-is |\n| `write_file` | **Kept** as-is |\n| `delete_file` | **Deferred to v2** (rare; can be added without churn) |\n| `update_cfg_value` | **Moved to canon** as `ptxprint-cfg-safe-editing.md` |\n| `resolve_config` | **Moved to canon** as `ptxprint-config-inheritance.md` |\n| `submit_typeset` | **Kept** + Phase 2 font verification added |\n| `get_job_status` | **Kept** + `font_check` field added to schema |\n| `cancel_job` | **Kept** as-is |\n| `install_fonts` | **Kept** (added in v1.0 from session 3) |\n\nNet: 11 → 7. Two domain-flavored helpers shipped to canon; two listing tools collapsed into one generic primitive; one rarely-used tool deferred.\n\n### From the original 17-tool PoC\n\nThe v1.0 spec already documented this. Net cumulative: **17 → 7**, with no functionality loss — every removed tool's functionality is reachable via either generic primitives or canon-described patterns.\n\n---\n\n## 8. Definition of Done for v1.1\n\nThe MCP server is \"v1.1 done\" when an agent connected to both this MCP server and oddkit MCP (pointing at the PTXprint canon repo, with at least the two gating canon articles authored) can:\n\n1. Discover projects and configs by composing `list_files` calls per the canon discovery article.\n2. Read any file in a project sandbox; understand its purpose by searching canon.\n3. Modify a single cfg key safely — by following the canon safe-editing article over `read_file` + `write_file` — without losing other keys or load-bearing comments.\n4. Compute effective settings across an inheritance chain by following the canon inheritance article over `read_file`.\n5. Submit a typesetting job and receive a job ID immediately.\n6. Poll job status and get a per-pass progress update plus Phase 2 font verification status.\n7. Receive the resulting PDF path on success and a legible error summary on failure.\n8. Cancel a long-running autofill job mid-way.\n9. Resolve fonts via LFF, install them via `install_fonts`, write `fonts.lock.json` via `write_file`, and re-run typeset successfully.\n\nA small smoke-test script driving an English-Bible test project end-to-end is the validation artifact.\n\n---\n\n## 9. Tomorrow's First Execution Scope\n\n(For the autonomous coding run kicked off per H-003)\n\n**In scope:**\n- Implement the 7 tools listed in §3, sandboxed and tested.\n- Implement Phase 2 font verification in `submit_typeset` per §4.\n- Implement per-job fontconfig isolation per §4.\n- Implement job lifecycle with sqlite state and per-pass progress parsing.\n- Package as a fly.io-deployable container with the §5 minimums (no `fonts-*` packages).\n- Smoke-test end-to-end on one English Bible (BSB or ULT) with the `cover` test config from `ptx2pdf/tests/`. Smoke test requires authoring the two gating canon articles in advance — see §2.\n\n**Explicitly out of scope for the first run:**\n- Full canon repo build (separate concurrent track; needs the slides-ESE artifacts integrated first)\n- Knob fan-out (D-009)\n- Cloudflare adapter\n- Anything in §6's deferred list\n\n**Validation gate** (per `klappy://canon/principles/verification-requires-fresh-context`):\nThe first-run agent does not validate its own output. A separate session (or human review) accepts or returns to iteration. Same-session \"looks done\" is not validation.\n\n---\n\n*End of v1.1 specification. Companions: `canon-ptxprint-config-inheritance.md` and `canon-ptxprint-cfg-safe-editing.md` (the two gating canon articles, sketched). Encoding: `transcript-encoded-session-4.md`.*\n"},{"path":"specs/ptxprint-mcp-v1.2-spec.md","uri":"klappy://canon/specs/ptxprint-mcp-v1.2-spec","title":"PTXprint MCP Server — v1.2 Specification","audience":"project","tags":["ptxprint","mcp","spec","vodka-architecture","v1.2","stateless","cloudflare"],"stability":"working","what_it_answers":"**What changed from v1.1.** v1.1 was filesystem-based — the MCP server pretended to be a project store, exposing read/write/list against a sandboxed tree. That bound the server to a single host and made horizontal scaling impossible. v1.2 i","sections":[{"slug":"1-the-contract","heading":"1. The Contract","start":28,"end":75},{"slug":"2-companion-the-ptxprint-canon-repo","heading":"2. Companion: the PTXprint Canon Repo","start":75,"end":101},{"slug":"3-tools-4","heading":"3. Tools (4)","start":101,"end":214},{"slug":"4-payload-schema","heading":"4. Payload Schema","start":214,"end":291},{"slug":"5-architecture","heading":"5. Architecture","start":291,"end":422},{"slug":"6-cloudflare-specific-verified-facts-2026-04","heading":"6. Cloudflare-specific verified facts (2026-04)","start":422,"end":444},{"slug":"7-what-s-deferred-and-why","heading":"7. What's Deferred (and Why)","start":444,"end":460},{"slug":"8-migration","heading":"8. Migration","start":460,"end":482},{"slug":"9-definition-of-done-for-v1-2","heading":"9. Definition of Done for v1.2","start":482,"end":498},{"slug":"10-tomorrow-s-first-execution-scope","heading":"10. Tomorrow's First Execution Scope","start":498,"end":529}],"body":"---\ntitle: \"PTXprint MCP Server — v1.2 Specification\"\nsubtitle: \"Stateless content-addressed build system on Cloudflare Workers + Containers\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"spec\", \"vodka-architecture\", \"v1.2\", \"stateless\", \"cloudflare\"]\nversion: \"v1.2-draft\"\ndate: 2026-04-28\nsupersedes: \"ptxprint-mcp-v1.1-spec.md (v1.1-draft, 2026-04-28 early AM)\"\napplied_canon:\n  - klappy://canon/principles/vodka-architecture\n  - klappy://canon/principles/kiss-simplicity-is-the-ceiling\n  - klappy://canon/principles/dry-canon-says-it-once\n  - klappy://canon/principles/maintainability-one-person-indefinitely\ngoverns: \"the PTXprint MCP server build (hackathon week 2026-04-28+)\"\nstatus: draft_for_review\n---\n\n# PTXprint MCP Server — v1.2 Specification\n\n> **What changed from v1.1.** v1.1 was filesystem-based — the MCP server pretended to be a project store, exposing read/write/list against a sandboxed tree. That bound the server to a single host and made horizontal scaling impossible. v1.2 inverts: project state lives wherever the agent has access to it (operator's filesystem, Git, DBL, R2), and the typesetting MCP becomes a stateless content-addressed build system. PTXprint is treated as a pure function of (config, sources, fonts) → PDF. The server is a job queue with autoscaling ephemeral workers. Tool count: **4** (was 7), with no file IO at all. Output is content-addressed in R2 — re-running an unchanged build is free. Deployable on Cloudflare with one Worker and one Container image.\n>\n> **Session 13 update (2026-04-29).** Added a 4th tool, `docs(query, audience?, depth?)`, that proxies oddkit's search+get over this repo's canon. Reverses session-2 D-004 deliberately — see §3 `docs` for the boundary check.\n\n---\n\n## 1. The Contract\n\n### Origin\n\nPTXprint is a deterministic function: given a project directory containing config + USFM sources + fonts, it produces a PDF. The filesystem is incidental — it's just how PTXprint receives its inputs. Every prior spec version (PoC, v1.0, v1.1) treated that incidence as a server-side responsibility, exposing file IO over a sandboxed project tree. That coupled the server to a single host's filesystem and made true horizontal scaling impossible.\n\nv1.2 takes the function seriously. The server accepts a **payload** (the function's inputs), dispatches it to a stateless worker, and produces output content-addressed by `sha256(payload)`. Re-submitting an unchanged payload returns the cached output for free. Project state — the user's evolving working set of configs, sources, and fonts — lives outside this server, managed by whatever the agent's environment provides.\n\n### What this server is\n\nA Cloudflare-native job queue for PTXprint typesetting:\n\n1. **Accept** a fully-specified job payload (config inline, sources/fonts/figures by URL).\n2. **Dispatch** to an ephemeral Container worker that materializes the inputs, runs PTXprint, uploads the PDF to R2.\n3. **Report** state, progress, and result URLs back through Durable Object-backed status polling.\n4. **Mint** R2 presigned upload URLs so the agent can stage local files into the URL-accessible world the workers operate in.\n\n### What this server is not\n\n- **Not a file system.** No `read_file`, `write_file`, `list_files`, or any project-state IO. The agent reads project state from wherever it lives — Claude Desktop's native filesystem access, a separate filesystem MCP, Git, the user's Paratext server, etc. — and constructs payloads.\n- **Not a PTXprint reference manual.** All domain knowledge (cfg keys, file format details, override semantics, font resolution) lives in the canon repo and is retrieved via `oddkit` MCP.\n- **Not synchronous.** Every typesetting call returns a job_id immediately; no MCP call blocks for more than a few seconds.\n- **Not stateful with respect to project trees.** There are no projects on the server's filesystem. Workers materialize a scratch dir per job, run, and exit.\n\n### Vodka boundary\n\nThe server knows:\n- The payload schema (versioned, validated)\n- How to dispatch a payload to a Container via Worker→Container service binding\n- How to mint R2 presigned URLs\n- How to track job state in Durable Objects\n- How to detect cached outputs by payload hash\n\nThe server does not know:\n- That `ptxprint.cfg` is INI, that piclist has a structure, that AdjList has a syntax, that USFM has markers — every text content in the payload is opaque\n- What `c_fighiderefs` does or which font supports which script\n- How configs inherit (`[import]` walks happen in the agent before payload submission)\n- How to resolve overrides (override files are part of the payload; PTXprint inside the worker handles them as it always has)\n\nThe constraint test (`klappy://canon/principles/vodka-architecture`):\n\n- **Has the server grown thick?** No — 3 tools, ~250 lines of Worker code + ~200 lines of Container HTTP handler + ~100 lines of DO state machine.\n- **Has the server acquired domain opinions?** No — every tool is generic action over a schema-defined payload.\n- **Can the server be removed without consequence?** No — content-addressed dispatch + DO state + R2 output is a coordinated primitive set no off-the-shelf alternative provides.\n\n---\n\n## 2. Companion: the PTXprint Canon Repo\n\nThe canon repo is a sibling artifact, not part of this server. Lives at a separate `klappy/` GitHub repo. Agents access it through `oddkit` MCP by setting `knowledge_base_url` to point at it.\n\n### Canon seed sources\n\n1. `sillsdev/ptx2pdf/docs/documentation/` — markdown docs in the upstream repo (licence verification owed; H-005).\n2. PTXprint UI tooltip dump — every UI control's tooltip, ID, and corresponding cfg setting name.\n3. PTXprint MASTER SLIDES deck — ESE artifacts already produced.\n4. The headless operations governance document (operator-authored from the PDF deck) — the most concrete agent-facing canon material drafted to date.\n5. Operator's ~1000 real configs — private training corpus.\n\n### Canon articles required for v1.2 to be agent-usable\n\nThe agent must construct payloads. Canon teaches it how. Required articles:\n\n- `payload-construction.md` — the payload schema explained, with worked examples for simple typesetting and autofill.\n- `config-construction.md` — how to construct or modify a `ptxprint.cfg` from scratch or from a working state. Replaces v1.1's safe-edit and inheritance-walk articles by being broader.\n- `font-resolution.md` — the LFF lookup loop, sha256 verification, manifest building.\n- `output-naming.md` — the `<PRJ>_<Config>_<bks>_ptxp.pdf` convention and how to interpret R2 result URLs.\n- `failure-mode-taxonomy.md` — hard / soft / success distinction, log grep patterns, overfull-count thresholds. Sourced from the governance document Part 9.\n\nThe agent's reasoning loop: search canon → understand the change or pattern → construct payload → submit job → poll → handle result. Iterative editing means re-submitting a new payload, not editing files.\n\n---\n\n## 3. Tools (4)\n\n### `submit_typeset(payload)`\n\n**Inputs:** the payload (schema in §4).\n\n**Returns:**\n```json\n{\n  \"job_id\": \"...\",\n  \"submitted_at\": \"2026-04-28T03:41:32Z\",\n  \"predicted_pdf_url\": \"https://r2.../<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf\",\n  \"cached\": true\n}\n```\n\nBehavior:\n1. Validate payload against schema (versioned).\n2. Compute canonical `payload_hash = sha256(canonical_json(payload))`.\n3. HEAD the R2 expected output path. If exists → return `cached: true` with the URL. **No Container dispatched.**\n4. If miss → create a Durable Object for `job_id`, state=`queued`. Dispatch payload to Container via service binding (using `ctx.waitUntil` to keep the dispatch fetch alive without blocking the Worker response). Return `job_id` immediately.\n\nCache is content-addressed. Same payload → same hash → same URL. Agent gets free re-runs of unchanged builds.\n\n### `get_job_status(job_id)`\n\n**Inputs:** `job_id`\n\n**Returns:**\n```json\n{\n  \"job_id\": \"...\",\n  \"state\": \"queued | running | succeeded | failed | cancelled\",\n  \"submitted_at\": \"...\",\n  \"started_at\": \"...\",\n  \"completed_at\": null,\n  \"progress\": {\n    \"passes_completed\": 3,\n    \"passes_total_estimate\": null,\n    \"current_phase\": \"fetching_inputs | typesetting | autofill | uploading\"\n  },\n  \"exit_code\": null,\n  \"failure_mode\": null,\n  \"pdf_url\": null,\n  \"log_url\": null,\n  \"log_tail\": \"(last 100 lines of XeTeX log)\",\n  \"errors\": [],\n  \"overfull_count\": 0,\n  \"human_summary\": \"Typesetting in progress. 3 passes completed.\"\n}\n```\n\n`failure_mode` ∈ `{hard, soft, success}` (per the governance Part 0). Hard = exit_code != 0 AND no PDF. Soft = exit_code == 0 AND PDF exists AND structural check failed (e.g., expected pictures absent). Success = exit_code == 0 AND PDF exists AND structural checks pass.\n\n`pdf_url` and `log_url` are R2 presigned GET URLs valid for the result-retention window (default: 7 days; configurable per-deployment).\n\nBehavior: read DO state, return. Pure Worker work; never reaches the Container.\n\n### `cancel_job(job_id)`\n\n**Inputs:** `job_id`\n\n**Returns:**\n```json\n{ \"ok\": true, \"was_running\": true, \"cancelled_at\": \"...\" }\n```\n\nBehavior: set `cancel_requested: true` in the DO. The running Container's PTXprint task polls the DO every 5–10 seconds (between progress markers) and on detecting the flag, sends SIGTERM to the PTXprint subprocess and updates state=`cancelled`. Worker doesn't need direct IPC to the Container; the DO is the cancellation channel.\n\nAcknowledged latency: up to 10 seconds between cancel request and PTXprint actually stopping. Acceptable for v1.2.\n\n### `docs(query, audience?, depth?)`\n\nAdded in session 13 (2026-04-29). Reverses session-2 D-004 (\"no retrieval in MCP server\") for one specific reason: downstream agents like BT Servant want one MCP wired up, not two. The retrieval brain still lives in oddkit; this tool is a pinned forwarding layer.\n\n**Inputs:**\n\n| Param | Type | Default | Meaning |\n|---|---|---|---|\n| `query` | string (required) | — | Natural-language question or topic |\n| `audience` | `\"headless\" \\| \"gui\"` | `\"headless\"` | Bias toward agent-facing or human-training docs |\n| `depth` | `1 \\| 2 \\| 3` | `1` | Progressive disclosure: 1=snippet, 2=full top doc, 3=top + next two in full |\n\n**Returns:**\n```json\n{\n  \"answer\": \"...\",\n  \"sources\": [\n    { \"uri\": \"klappy://canon/...\", \"title\": \"...\", \"snippet\": \"...\", \"score\": 10.27 }\n  ],\n  \"deeper\": [\"Tell me more about: ...\", \"...\"],\n  \"governance_source\": \"knowledge_base\"\n}\n```\n\nBehavior: HTTP POST to `https://oddkit.klappy.dev/mcp` with `action: \"search\"` and `knowledge_base_url: \"https://github.com/klappy/ptxprint-mcp\"`. Audience filter is a tag-based bonus, not a hard filter — same-score peers with audience-matching tags float up; clearly-better non-matching results stay on top. `depth >= 2` adds an `action: \"get\"` for the top URI; `depth = 3` enriches the next two as well.\n\nFailure mode: graceful. On oddkit unreachable or timeout (5s search / 10s get), returns `{ answer: null, sources: [], governance_source: \"minimal\", error: \"...\" }`. Agent can degrade.\n\nVodka boundary check: the tool knows two URLs (this repo, oddkit). It holds zero PTXprint domain semantics. The retrieval logic, the BM25 scoring, the index — all in oddkit. This file is forwarding plus an audience-tiebreaker. If `docs` ever grows a PTXprint-flavored taxonomy (Shape C from the session-13 plan), that taxonomy moves into canon governance docs that oddkit retrieves, not into this server.\n\n### Why there is no upload tool\n\nAn earlier draft of this spec included a fourth tool, `get_upload_url`, that minted a presigned R2 PUT URL for agents to stage local files (USFM, fonts, figures) before referencing them in payloads. It was removed before any agent ever used it. The reasoning:\n\n- The agent already has the file. Forcing a separate \"mint URL → HTTP PUT megabytes → reference URL in payload\" round-trip is pure friction and doubles the data transfer.\n- Hosting input files is upstream of typesetting. Where USFM, fonts, and figures live (a Git repo, DBL, a Paratext server, a CDN, the user's own bucket) is the agent's environment concern, not the MCP server's.\n- A presigned-URL minting tool is a piece of infrastructure plumbing leaking into the agent contract. The MCP surface is for typesetting actions, not storage management.\n\nThe contract: agents pass HTTPS URLs (with `sha256` for verification) directly in the payload's `sources`, `fonts`, and `figures` arrays. The Container fetches them at job start. If an agent's environment doesn't have a way to expose local files at a URL, that is a problem for the agent's host to solve — not for the typesetting MCP to absorb.\n\n---\n\n## 4. Payload Schema\n\nVersioned. v1.2 ships with `schema_version: \"1.0\"`. Future spec versions can introduce `1.1`, `2.0`, etc.; the Container rejects versions it doesn't speak.\n\n```json\n{\n  \"schema_version\": \"1.0\",\n\n  \"project_id\": \"WSG\",\n  \"config_name\": \"FancyNT\",\n  \"books\": [\"MAT\", \"MRK\", \"LUK\", \"JHN\"],\n  \"mode\": \"simple\",\n  \"define\": { \"s_linespacing\": \"13\" },\n\n  \"config_files\": {\n    \"shared/ptxprint/ptxprint_project.cfg\": \"...optional override file content...\",\n    \"shared/ptxprint/FancyNT/ptxprint.cfg\": \"[paper]\\npagesize = ...\",\n    \"shared/ptxprint/FancyNT/ptxprint.sty\": \"\\\\Marker mt1\\n\\\\FontSize 1.4\\n...\",\n    \"shared/ptxprint/FancyNT/ptxprint-mods.sty\": \"...optional...\",\n    \"shared/ptxprint/FancyNT/changes.txt\": \"...optional...\",\n    \"shared/ptxprint/FancyNT/FRTlocal.sfm\": \"...optional...\",\n    \"shared/ptxprint/FancyNT/ptxprint_override.cfg\": \"...optional override...\",\n    \"shared/ptxprint/FancyNT/WSG-FancyNT.piclist\": \"...optional...\",\n    \"shared/ptxprint/FancyNT/AdjLists/46ROMWSG-FancyNT.SFM.adj\": \"...optional...\"\n  },\n\n  \"sources\": [\n    {\n      \"book\": \"MAT\",\n      \"filename\": \"41MATWSG.SFM\",\n      \"url\": \"https://example.com/sources/41MATWSG.SFM\",\n      \"sha256\": \"...\"\n    }\n  ],\n\n  \"fonts\": [\n    {\n      \"family_id\": \"charissil\",\n      \"version\": \"7.000\",\n      \"filename\": \"CharisSIL-Regular.ttf\",\n      \"url\": \"https://lff.api.languagetechnology.org/...\",\n      \"sha256\": \"...\"\n    }\n  ],\n\n  \"figures\": [\n    {\n      \"filename\": \"AB01234.tif\",\n      \"url\": \"https://example.com/figures/AB01234.tif\",\n      \"sha256\": \"...\"\n    }\n  ]\n}\n```\n\n### Slot semantics\n\n| Slot | Contents | Inline or URL? |\n|---|---|---|\n| `project_id`, `config_name`, `books` | identifiers used for PTXprint argv shape and PDF naming | inline (small) |\n| `mode` | `\"simple\"` or `\"autofill\"` | inline |\n| `define` | `-D` overrides applied at run time | inline |\n| `config_files` | map of relative-path → text content | **inline** (small text artifacts) |\n| `sources` | USFM book content | **URL + sha256** (multi-MB, often) |\n| `fonts` | font binaries | **URL + sha256** (multi-MB) |\n| `figures` | image binaries | **URL + sha256** (multi-MB) |\n\nThe split: text inline, binaries by URL. Canon's `payload-construction.md` documents this in detail with examples.\n\n### Canonical hashing\n\n`payload_hash` is computed over the JSON-canonicalized payload (RFC 8785 JCS or equivalent). This means logically-equivalent payloads (different key order, whitespace) produce the same hash. Same hash → same R2 path → cache hit. Reproducibility is intrinsic.\n\nURLs in `sources`, `fonts`, `figures` are part of the hash. Two payloads pointing at the same content via different URLs hash differently (and both will fetch and verify against sha256, so cache effectiveness depends on URL stability). For best cache hit rates, agents should prefer stable URLs (R2 presigned GETs with long expiry, content-addressed CDNs, etc.) over volatile ones.\n\n---\n\n## 5. Architecture\n\n```\nAgent (Claude Desktop / BT Servant / etc.)\n  ↓ MCP/HTTP\n┌──────────────────────────────────────────────────┐\n│ Cloudflare Worker (the only Worker)              │\n│  - submit_typeset:                               │\n│      validate, hash payload                      │\n│      HEAD R2 for cached output                   │\n│      cache hit  → return URL                     │\n│      cache miss → create DO, dispatch via        │\n│                   service binding using          │\n│                   ctx.waitUntil(fetch(...)),     │\n│                   return job_id                  │\n│  - get_job_status: read DO, return               │\n│  - cancel_job: set DO flag, return               │\n└──────────────────────────────────────────────────┘\n  ↓ Worker→Container service binding\n┌──────────────────────────────────────────────────┐\n│ Cloudflare Container (the only Container image)  │\n│  Instance type: standard-2 (1 vCPU, 6 GiB, 12GB) │\n│  sleepAfter: \"45m\" (covers max autofill + buffer)│\n│  Image: PTXprint + XeTeX + fontconfig + tiny     │\n│         Python HTTP server                       │\n│  HTTP endpoint: POST /jobs                       │\n│  Per request:                                    │\n│     update DO state=running                      │\n│     materialize scratch dir from config_files    │\n│     download sources/fonts/figures (parallel,    │\n│       sha256 verified as bytes arrive)           │\n│     run ptxprint -P ... -c ... -b ... -p ... -q  │\n│       (poll DO every 10s for cancel flag)        │\n│     classify exit (hard/soft/success)            │\n│     upload PDF + log to R2 at                    │\n│       outputs/<payload_hash>/<PRJ>_<Config>_..   │\n│     update DO with state, URLs, log_tail         │\n│     return 200 (request closes)                  │\n│  Container sleeps after sleepAfter timer         │\n└──────────────────────────────────────────────────┘\n  ↕ DO read/write           ↓ R2 PUT (content-addressed output)\nDurable Objects        Cloudflare R2\n(one DO per job_id)    One bucket:\n                         ptxprint-outputs (content-addressed, long retention)\n```\n\n### The Worker\n\nSingle Worker handles the three MCP tools as separate routes. Implementation notes:\n\n- Use `ctx.waitUntil(fetch(containerEndpoint, {method: 'POST', body: JSON.stringify(payload)}))` to dispatch to the Container without blocking the Worker's response to the agent. The Container request stays open as long as the Container is processing; the Worker has already returned to the agent.\n- Container service binding: declare in `wrangler.toml` as `[[containers]] binding = \"PTXPRINT_CONTAINER\"`, then `env.PTXPRINT_CONTAINER` is callable from the Worker.\n- DO binding for state: `[[durable_objects.bindings]] name = \"JOB_STATE\" class_name = \"JobStateDO\"`.\n- R2 binding for outputs: `[[r2_buckets]] binding = \"OUTPUTS\"`.\n\n### The Container\n\nSingle image. Built from a Dockerfile that installs:\n\n- `python3.11`\n- `texlive-xetex --no-install-recommends` (TeX engine without recommended fonts)\n- `fontconfig`\n- PTXprint (Linux build from `sillsdev/ptx2pdf` source — confirm Dockerfile availability per session 3 H-008)\n- A small Python HTTP server (FastAPI / aiohttp / similar) exposing `POST /jobs`\n\n**Explicitly excludes:** `fonts-sil-charis`, `fonts-noto-*`, any `fonts-*` package. Container fails loudly when font resolution hasn't run; no accidental fall-back. (Per session 3 C-007.)\n\nThe Python HTTP handler:\n\n1. Parses payload from request body. Validates schema_version.\n2. Acknowledges with 202 immediately if the dispatch model needs it; otherwise processes synchronously and returns 200 on completion.\n3. Materializes scratch dir at `/tmp/<job_id>/projects/<project_id>/`. Writes inline `config_files` at their relative paths.\n4. Parallel-fetches `sources`, `fonts`, `figures` URLs. Verifies sha256 as bytes arrive (hash the stream; abort on mismatch).\n5. Sets up per-job fontconfig environment via `FONTCONFIG_FILE` pointing at a generated config that sees only the downloaded fonts.\n6. Runs PTXprint subprocess. Tails its stdout/stderr and the log file; updates DO state on each pass-completion marker.\n7. Polls DO for cancel flag every 10s (between progress markers).\n8. Classifies exit per the failure-mode taxonomy.\n9. Uploads PDF + log to R2 at the deterministic path.\n10. Updates DO state=`succeeded` or `failed` or `cancelled`. Returns response.\n\n`sleepAfter = \"45m\"` covers max autofill duration plus buffer. Container goes to sleep cleanly when no requests arrive for that interval. Disk is wiped on sleep — that's fine; everything important is in R2 or DO.\n\n### State (Durable Objects)\n\nOne DO instance per `job_id`. Schema:\n\n```typescript\n{\n  job_id: string,\n  state: \"queued\" | \"running\" | \"succeeded\" | \"failed\" | \"cancelled\",\n  submitted_at: ISO8601,\n  started_at: ISO8601 | null,\n  completed_at: ISO8601 | null,\n  progress: {\n    passes_completed: number,\n    passes_total_estimate: number | null,\n    current_phase: string | null\n  },\n  cancel_requested: boolean,\n  exit_code: number | null,\n  failure_mode: \"hard\" | \"soft\" | \"success\" | null,\n  pdf_r2_key: string | null,\n  log_r2_key: string | null,\n  log_tail: string,\n  errors: string[],\n  overfull_count: number,\n  payload_hash: string,\n  human_summary: string\n}\n```\n\nWorker reads/writes via DO methods. Container reads/writes via DO methods (Container has its own DO binding). Both sides see the same authoritative state.\n\nDO storage is durable (SSD-backed); state survives Worker invocations and Container restarts.\n\n### Output (R2)\n\nTwo buckets:\n\n**`ptxprint-outputs`** — content-addressed:\n```\noutputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.pdf\noutputs/<payload_hash>/<PRJ>_<Config>_<bks>_ptxp.log\n```\n\nLong retention (90 days default; tunable). Same payload → same path → cache hit on re-submission. Naming follows the governance Part 2 convention so users see filenames matching their existing mental model.\n\nThere is no separate uploads bucket. Agents bring their own URLs for sources, fonts, and figures; the server only stores outputs.\n\n---\n\n## 6. Cloudflare-specific verified facts (2026-04)\n\nAnchored against `Cloudflare Developer Platform:search_cloudflare_documentation` lookups during v1.2 spec authoring:\n\n| Fact | Source |\n|---|---|\n| Workers memory ceiling: 128 MiB | `/workers/platform/limits/#memory` |\n| Workers HTTP duration: no hard limit while client connected | `/workers/platform/limits/#duration` |\n| `ctx.waitUntil` extends Worker beyond response by up to 30s, or until promise resolves | `/workers/platform/limits/` |\n| Container instance type `standard-2`: 1 vCPU, 6 GiB memory, 12 GB disk | `/containers/platform-details/limits/` |\n| Container concurrent live limits (per account): 6 TiB memory, 1,500 vCPU, 30 TB disk | Feb 2026 changelog |\n| Container disk is ephemeral — wiped on sleep | `/containers/platform-details/architecture/` |\n| Container `sleepAfter` configurable; SIGTERM → 15min → SIGKILL on shutdown | `/containers/container-class/` |\n| Container HTTP request handling has no hard duration limit (subject to Worker client connection) | `/workers/platform/limits/#duration` |\n| Containers billed per 10ms of active running; charges stop when sleeping | `/containers/pricing/` |\n\nKey implication: **a 30-minute autofill run is in-bounds.** The Worker dispatches via `ctx.waitUntil(fetch(...))`, the Container handles the request synchronously while running PTXprint, the Container's `sleepAfter = \"45m\"` covers the duration plus buffer.\n\nRisk to verify at implementation time: whether a Worker's `waitUntil` actually keeps the dispatch fetch alive for 30+ minutes, or whether the platform terminates it sooner. If the latter, fallback pattern is for the Container to periodically self-poke (send itself a no-op HTTP request) to reset its own `sleepAfter` timer, decoupling the Container's lifetime from the Worker's `waitUntil`.\n\n---\n\n## 7. What's Deferred (and Why)\n\n| Deferred capability | Why deferred | When to revisit |\n|---|---|---|\n| **fly.io fallback runner** | CF Containers verified to handle 30 min autofill; no need for a second host | If autofill exceeds CF Container request duration in production |\n| **Knob fan-out for autofill** (parallel attempts on same book) | ~1 month build effort; not a v1.2 concern | Q3+ 2026 |\n| **WhatsApp / external messaging** | Out of MCP scope; downstream agent concern | When BT Servant wires it |\n| **Cover generation workflow** | Cover Wizard's UI doesn't have a clean headless equivalent; agent constructs cover periphs in `FRTlocal.sfm` directly via canon-described patterns | When canon authors the cover-construction article |\n| **TeX macro injection** (`*-mods.tex`, `*-premods.tex`) | High blast radius | After canon authors TeX-safety docs |\n| **Multi-script font support** | Initial scope is English Bibles | When first non-English target is committed |\n| **Auth on the MCP HTTP transport** | Trusted internal use this week | Before public listing |\n| **Result retention beyond 90 days** | R2 lifecycle policy default | When users need archival |\n| **Cross-agent payload sharing (collaboration)** | Each agent submits its own payloads | When real users ask |\n\n---\n\n## 8. Migration\n\n### From v1.1 to v1.2\n\n| v1.1 tool | v1.2 disposition |\n|---|---|\n| `list_files` | **Removed.** Project state read from elsewhere (Claude Desktop file access, Git, etc.). |\n| `read_file` | **Removed.** Same. |\n| `write_file` | **Removed.** Same. Plus output is in R2, not on disk. |\n| `submit_typeset` | **Kept** — same name, schema-redefined payload. |\n| `get_job_status` | **Kept** — gains `failure_mode`, gains `pdf_url` / `log_url` (R2 presigned). |\n| `cancel_job` | **Kept** — same. |\n| `install_fonts` | **Removed.** Fonts are part of the payload; Container fetches and verifies at job start. |\n\nNet: 7 → 3. The originally proposed `get_upload_url` tool was removed before deployment — see §3 \"Why there is no upload tool.\"\n\n### From the original 17-tool PoC\n\nCumulative simplification: **17 → 3**, with no functionality loss — every removed tool's functionality is reachable via either canon-described agent patterns or the three primitives.\n\n---\n\n## 9. Definition of Done for v1.2\n\nThe MCP server is \"v1.2 done\" when an agent connected to both this MCP server and oddkit MCP (pointing at the PTXprint canon repo, with at least the five gating canon articles authored — see §2) can:\n\n1. Read project state from its environment (Claude Desktop file access for the operator's local Paratext directory, or equivalent).\n2. Construct a valid payload by following canon's payload-construction article, referencing sources/fonts/figures by HTTPS URL with sha256 (hosting is the agent's environment concern, not the server's).\n3. Submit the payload via `submit_typeset` and receive a job_id immediately.\n4. Poll job status and observe per-pass progress, then receive a presigned PDF URL on success.\n5. Cancel a long-running autofill job mid-way.\n6. Re-submit an unchanged payload and receive the cached output URL without a new typeset run.\n7. Receive a clear `failure_mode: hard | soft` classification on failures with actionable error info.\n\nSmoke test: end-to-end on one English Bible (BSB or ULT) with the `cover` test config from `ptx2pdf/tests/`, using simple typesetting mode.\n\n---\n\n## 10. Tomorrow's First Execution Scope\n\n(For the autonomous coding run kicked off per H-003)\n\n**In scope:**\n- Implement the 3 tools listed in §3 as a Cloudflare Worker.\n- Implement the Container image with PTXprint + XeTeX + fontconfig + Python HTTP handler.\n- Implement the JobStateDO Durable Object class.\n- Configure the outputs R2 bucket with appropriate lifecycle policies.\n- Deploy via `wrangler deploy` to a workers.dev subdomain.\n- Smoke-test end-to-end on one English Bible test project.\n\n**Explicitly out of scope for the first run:**\n- Full canon repo build (separate concurrent track per the operator's setup).\n- Knob fan-out (deferred).\n- Auth.\n- Anything in §7's deferred list.\n\n**Critical-path canon work** (must exist before the smoke test runs end-to-end):\n- `payload-construction.md` — without this the agent cannot build a valid payload.\n- `output-naming.md` — the agent needs to interpret R2 result paths.\n\nThe other three canon articles (`config-construction.md`, `font-resolution.md`, `failure-mode-taxonomy.md`) can be drafted in parallel; the smoke test passes without them as long as the operator hand-constructs the test payload.\n\n**Validation gate** (per `klappy://canon/principles/verification-requires-fresh-context`):\nThe first-run agent does not validate its own output. A separate session (or human review) accepts or returns to iteration. Same-session \"looks done\" is not validation.\n\n---\n\n*End of v1.2 specification. Companions: `governance-update-handoff.md` (operator-facing edits to the v1.0/v1.1-era governance document so it aligns with v1.2), `transcript-encoded-session-5.md` (session encoding).*\n"},{"path":"specs/ptxprint-mcp-v1.3-spec.md","uri":"klappy://canon/specs/ptxprint-mcp-v1.3-spec","title":"PTXprint MCP Server — v1.3 Specification","audience":"project","tags":["ptxprint","mcp","spec","vodka-architecture","v1.3","telemetry","analytics-engine","transparency","cloudflare"],"stability":"working","what_it_answers":"**What changed from v1.2.** v1.2 ships a four-tool stateless content-addressed build system that takes payloads, dispatches Container jobs, and returns content-addressed PDFs from R2. v1.3 is **purely additive**: it layers a transparency / ","sections":[{"slug":"1-the-contract","heading":"1. The Contract","start":29,"end":84},{"slug":"2-companion-the-ptxprint-canon-repo","heading":"2. Companion: the PTXprint Canon Repo","start":84,"end":98},{"slug":"3-tools-6","heading":"3. Tools (6)","start":98,"end":217},{"slug":"4-payload-schema-unchanged-from-v1-2","heading":"4. Payload Schema — unchanged from v1.2","start":217,"end":225},{"slug":"5-architecture","heading":"5. Architecture","start":225,"end":336},{"slug":"6-telemetry-module-implementation-detail","heading":"6. Telemetry Module — Implementation Detail","start":336,"end":442},{"slug":"7-cloudflare-specific-verified-facts-2026-04-v1-3-additions","heading":"7. Cloudflare-specific verified facts (2026-04, v1.3 additions)","start":442,"end":460},{"slug":"8-what-s-deferred-and-why","heading":"8. What's Deferred (and Why)","start":460,"end":474},{"slug":"9-migration","heading":"9. Migration","start":474,"end":500},{"slug":"10-definition-of-done-for-v1-3","heading":"10. Definition of Done for v1.3","start":500,"end":517},{"slug":"11-first-execution-scope-v1-3","heading":"11. First Execution Scope (v1.3)","start":517,"end":550}],"body":"---\ntitle: \"PTXprint MCP Server — v1.3 Specification\"\nsubtitle: \"v1.2 + telemetry: transparency layer over the running typesetter\"\naudience: project\nexposure: working\nvoice: neutral\nstability: working\ntags: [\"ptxprint\", \"mcp\", \"spec\", \"vodka-architecture\", \"v1.3\", \"telemetry\", \"analytics-engine\", \"transparency\", \"cloudflare\"]\nversion: \"v1.3-draft\"\ndate: 2026-04-30\nsupersedes: \"ptxprint-mcp-v1.2-spec.md (v1.2-draft, 2026-04-28)\"\napplied_canon:\n  - klappy://canon/principles/vodka-architecture\n  - klappy://canon/principles/kiss-simplicity-is-the-ceiling\n  - klappy://canon/principles/dry-canon-says-it-once\n  - klappy://canon/principles/maintainability-one-person-indefinitely\n  - klappy://canon/governance/telemetry-governance\ngoverns: \"the PTXprint MCP server build, additive layer over v1.2 (May 2026+)\"\nstatus: draft_for_review\n---\n\n# PTXprint MCP Server — v1.3 Specification\n\n> **What changed from v1.2.** v1.2 ships a four-tool stateless content-addressed build system that takes payloads, dispatches Container jobs, and returns content-addressed PDFs from R2. v1.3 is **purely additive**: it layers a transparency / observability surface on top of the v1.2 system without changing the behavior or contract of any v1.2 tool. Two new MCP tools (`telemetry_public`, `telemetry_policy`), one new internal Worker route (`/internal/telemetry` for Container event forwarding), one new module (the redaction-and-write helper), and a small Container hook that POSTs phase-transition events through the Worker. Tool count: **6** (was 4). The `submit_typeset`, `get_job_status`, `cancel_job`, and `docs` tools are unchanged in interface and behavior. Existing payloads continue to work; no agent change required for v1.3 deployment. Telemetry is governed by `canon/governance/telemetry-governance.md` — the schema, the privacy floor, the canned queries, and the consumer leaderboard all live there. This spec describes the implementation; the article describes the policy.\n>\n> **Why a new spec rather than a v1.2 amendment.** v1.2 was implemented before this spec was written. The four tools, Worker, Container, DO, and R2 wiring are all live at `src/index.ts`, `container/main.py`, and `wrangler.jsonc`. Per the principle that specs shouldn't be changed after implementation (resolution of H-T3 in `canon/encodings/telemetry-feature-planning-ledger.md`), the telemetry layer gets its own forward-looking spec rather than retroactively amending the v1.2 contract.\n\n---\n\n## 1. The Contract\n\n### Origin\n\nPTXprint MCP v1.2 is a typesetting service. It accepts payloads, runs PTXprint, and returns PDFs. It works. Once it's serving real translation teams, the operator needs to know things that the running system doesn't tell them: how often the cache hits, what fraction of jobs end in soft failure, where time goes inside a job, which canon docs the `docs` tool actually serves. Without that signal, prioritization decisions are guesses.\n\nv1.3 adds telemetry that answers exactly those questions, and only those questions. The collected data is structurally typed, project-identity-free, and queryable by anyone via `telemetry_public`. The dashboard the operator sees is the dashboard any consumer can see. The asymmetry that usually defines telemetry — host-collects-privately, consumer-trusts — is replaced with: host collects publicly, consumer queries the same data.\n\n### What this server is (v1.3)\n\nA Cloudflare-native job queue for PTXprint typesetting (unchanged from v1.2) plus a thin observability layer:\n\n1. **Accept** a fully-specified job payload (config inline, sources/fonts/figures by URL).\n2. **Dispatch** to an ephemeral Container worker that materializes the inputs, runs PTXprint, uploads the PDF to R2.\n3. **Report** state, progress, and result URLs back through Durable Object-backed status polling.\n4. **Search** in-repo canon documentation through the `docs` tool (served from the canon bundled into the Worker at build time; was an oddkit forwarding layer until 2026-09-05).\n5. **Emit** structural usage telemetry to a public Cloudflare Workers Analytics Engine dataset at every request and every job phase transition.\n6. **Expose** the telemetry dataset for public SQL query (`telemetry_public`) and the governing policy for runtime fetch (`telemetry_policy`).\n\n### What this server is not (unchanged from v1.2, restated)\n\n- **Not a file system.** No `read_file`, `write_file`, `list_files`, or any project-state IO.\n- **Not a PTXprint reference manual.** All domain knowledge lives in canon, served by the `docs` tool from the bundle (or read directly via `oddkit` MCP by agents that carry it).\n- **Not synchronous.** Every typesetting call returns a `job_id` immediately.\n- **Not stateful with respect to project trees.** No projects on the server's filesystem.\n\n### What this server is not (new for v1.3)\n\n- **Not a private-data collector.** Telemetry never logs Paratext project IDs, config names, USFM book codes, source/font/figure URLs, payload contents, USFM bytes, log content, or PDF bytes. The full exclusion list is enforced in code (see §6 Redaction Module) and documented in `canon/governance/telemetry-governance.md` §\"Privacy Floor.\"\n- **Not an authenticated service.** v1.3 has no auth on the MCP HTTP transport. Consumer identification is opt-in via self-report headers and query parameters; identification is incentivized by a transparency leaderboard, not enforced.\n- **Not a real-time monitoring system.** Cloudflare Analytics Engine has minutes-of-latency between `writeDataPoint()` and queryability. The dashboard is for trend analysis, not incident response.\n\n### Vodka boundary (v1.3 update)\n\nThe server adds knowledge of:\n\n- The `ptxprint_telemetry` Analytics Engine dataset name\n- The governance article URI (`canon/governance/telemetry-governance.md`)\n- A fixed schema for telemetry data points (12 blobs + 10 doubles, fully typed at the writer boundary)\n- The three-tier governance fallback chain (`knowledge_base → bundled → minimal`)\n\nThe server does **not** acquire knowledge of:\n\n- Which dimensions are operationally interesting (canned queries live in the article, not in code)\n- What constitutes a privacy violation (the exclusion list is the policy article; the redaction module enforces by typing slots structurally, not by checking content)\n- Which consumer labels are \"real\" (the verified-clients allowlist is a deployment env var, not server-side opinion)\n\nThe constraint test (`klappy://canon/principles/vodka-architecture`):\n\n- **Has the server grown thick?** Modestly. v1.3 adds ~150 lines for the telemetry module (writer + redactor + public-query forwarder + policy fetcher with three-tier fallback) and ~50 lines for the Container's telemetry envelope helper + Worker forwarding endpoint. Total ~200 lines on top of v1.2's ~550. Under 1000 lines remains an unambitious target.\n- **Has the server acquired domain opinions?** The borderline answer surfaced in planning (L-T2): persisting `failure_mode` and `phase` to telemetry is not new opinion because both are already in the public `get_job_status` response shape. New blob dimensions that are NOT already in a public response would warrant fresh scrutiny.\n- **Can the server be removed without consequence?** No, more so than v1.2. Without telemetry the maintainer has no signal on cache effectiveness, no evidence of soft-failure rate, and no way to know which `docs` queries agents actually run. All three drive prioritization for a single-maintainer system.\n\n---\n\n## 2. Companion: the PTXprint Canon Repo\n\nUnchanged from v1.2 in structure. v1.3 adds one new required-canon entry:\n\n### Canon articles required for v1.3 to be agent-usable\n\nAll v1.2 required articles, plus:\n\n- `canon/governance/telemetry-governance.md` — the schema, privacy floor, canned queries, transparency leaderboard, and policy resolution chain. Fetched at runtime by `telemetry_policy`.\n\nThe agent's reasoning loop is unchanged: search canon → understand the change or pattern → construct payload → submit job → poll → handle result. Telemetry happens silently; the agent does not interact with it directly. The dashboard is for the maintainer (and any consumer who wants to see the same numbers).\n\n---\n\n## 3. Tools (6)\n\nThe first four tools are unchanged from v1.2 — interface, behavior, return shape. Restated briefly here for completeness; full details remain in the v1.2 spec §3.\n\n### `submit_typeset(payload)` — unchanged from v1.2\n\n**Inputs:** the payload (schema in §4, unchanged from v1.2).\n\n**Returns:** `{ job_id, submitted_at, predicted_pdf_url, cached }` — same shape.\n\nBehavior: validate, hash, HEAD R2 for cached output, return cached URL or dispatch via `ctx.waitUntil(fetch(...))`. Same as v1.2.\n\n**v1.3 telemetry:** every call writes one `mcp_request` event with `cache_outcome ∈ {hit, miss}`, plus one `tool_call` event. Cache hits do NOT write `job_*` events (no Container dispatched). Cache misses cause the Worker to write a `job_phase` event with `phase = \"queued\"` immediately upon DO creation; the Container takes over from there.\n\n### `get_job_status(job_id)` — unchanged from v1.2\n\nSame return shape. `failure_mode ∈ {hard, soft, success, null}` per the failure-mode taxonomy.\n\n**v1.3 telemetry:** writes one `mcp_request` event + one `tool_call` event. No `job_*` events (this is a read tool, not a state-changing one).\n\n### `cancel_job(job_id)` — unchanged from v1.2\n\nSame return shape. Sets `cancel_requested: true` in the DO; Container polls every 10s.\n\n**v1.3 telemetry:** writes one `mcp_request` event + one `tool_call` event. The Container's terminal-phase emission carries `failure_mode = \"cancelled\"` when the cancel is honored.\n\n### `docs({} | {uri} | {uri, section} | {query})` — re-cut 2026-09-05 (Worker 0.3.0)\n\nProgressive disclosure over the canon bundled into the Worker (`src/bundled-canon.ts`, `npm run bundle-canon`, CI-checked):\n\n| Call | Returns | Size |\n|---|---|---|\n| `docs {}` | `kind: index` — every bundled article: `uri`, `title`, `what_it_answers` (the article's own \"What this answers\" line), `tags` | ~50 rows |\n| `docs {uri}` | `kind: article` — frontmatter + body, plus `sections[]` (slug, heading, lines) | one article |\n| `docs {uri, section}` | `kind: section` — one `##` section's text, `siblings[]` | one section |\n| `docs {query, audience?, limit?}` | `kind: search` — ranked **pointers** (same fields as the index, plus `score`); `covered: false` when no article matches at least half the query's terms above a floor — the nearest neighbours are then listed for honesty, not as answers | pointers only, never bodies |\n\nEvery response carries `served_from` (`bundled-canon@<content-hash> (<n> docs)`) and `next` (the call to make next). Unknown `uri` → `kind: error` with did-you-mean pointers. `depth` (0.1/0.2) is a deprecated alias for one release: `{query, depth ≥ 2}` also returns the top hit's body as `answer`; `sources[]` mirrors `pointers[]`.\n\nWhy: 0.2 answered every question with its nearest neighbour and dumped whole articles; the Titus cook found `usechangesfile` unfindable, `\\ef` absent, and \"how many jobs at once\" answered with telemetry governance. The index is the product; search is a pointer finder over it.\n\n**v1.3 telemetry:** unchanged — one `mcp_request` + one `tool_call` event; `docs_top_uri` is the first pointer or the opened article's uri; the query string is never logged.\n\n### `telemetry_public(sql)` — new in v1.3\n\nForwarder over the `ptxprint_telemetry` Cloudflare Workers Analytics Engine dataset.\n\n**Inputs:**\n\n| Param | Type | Default | Meaning |\n|---|---|---|---|\n| `sql` | string (required) | — | A read-only SQL query against `ptxprint_telemetry` |\n\n**Returns:** Analytics Engine result rows (passthrough), or a sanitized error message on failure.\n\n```json\n{\n  \"rows\": [\n    { \"week\": \"2026-04-27\", \"hit_rate\": 0.48, \"_sample_interval\": 1 },\n    ...\n  ],\n  \"row_count\": 12,\n  \"query\": \"SELECT toStartOfWeek(timestamp) AS week, ... FROM ptxprint_telemetry ...\",\n  \"executed_at\": \"2026-04-30T13:21:08Z\"\n}\n```\n\nBehavior:\n\n1. Validate the query targets only the allowlisted dataset (`ptxprint_telemetry`). Any reference to another dataset rejects with a sanitized error before the Analytics Engine API is called. This guards against cross-dataset access to other Analytics Engine data on the same Cloudflare account.\n2. Rate-limit per consumer label. Default: 60 queries per consumer per hour (configurable via `TELEMETRY_QUERY_RATE_LIMIT_PER_HOUR` env var). The limit protects the Analytics Engine query quota (10,000 queries/day free tier) without restricting the data itself.\n3. Forward to Analytics Engine SQL API (`https://api.cloudflare.com/client/v4/accounts/{account_id}/analytics_engine/sql`) with the read-only `CF_API_TOKEN`.\n4. Sanitize errors before returning. Raw Analytics Engine errors may contain account IDs, dataset names, or schema details — these are caught and replaced with generic failure messages.\n\nThe data is public by design. There is no column restriction, no SQL keyword blocking, no query-complexity check. The API token is read-only; `DROP`/`ALTER`/`INSERT` are rejected by Analytics Engine itself.\n\nA library of canned queries (cache hit rate, failure mix, where time goes, tool leaderboard, document leaderboard, soft-failure trend, pages-per-success) is documented in `canon/governance/telemetry-governance.md` §\"Canned Queries — The Dashboard Library.\"\n\n### `telemetry_policy()` — new in v1.3\n\nReturns the runtime-fetched governance policy plus the self-report header dictionary.\n\n**Inputs:** none.\n\n**Returns:**\n```json\n{\n  \"policy\": \"(full markdown content of canon/governance/telemetry-governance.md)\",\n  \"policy_uri\": \"klappy://canon/governance/telemetry-governance\",\n  \"governance_source\": \"knowledge_base | bundled | minimal\",\n  \"self_report_headers\": {\n    \"x-ptxprint-client\": \"Your client name (e.g., 'claude-desktop', 'bt-servant')\",\n    \"x-ptxprint-client-version\": \"Version string (semver recommended)\",\n    \"x-ptxprint-agent-name\": \"AI agent or model name\",\n    \"x-ptxprint-agent-version\": \"Version string for the agent/model\",\n    \"x-ptxprint-surface\": \"Where this is running ('claude.ai', 'vscode', 'cli', 'production')\",\n    \"x-ptxprint-contact-url\": \"Project or organization URL\",\n    \"x-ptxprint-policy-url\": \"Your privacy or telemetry policy URL\",\n    \"x-ptxprint-capabilities\": \"Comma-separated capabilities (e.g., 'submit,poll,docs')\"\n  },\n  \"fallback_chain\": [\n    { \"tier\": \"knowledge_base\", \"source\": \"https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/governance/telemetry-governance.md\" },\n    { \"tier\": \"bundled\", \"source\": \"compiled into Worker at deploy time\" },\n    { \"tier\": \"minimal\", \"source\": \"static string in Worker; lists dataset name + privacy non-negotiables + policy URI\" }\n  ],\n  \"generated_at\": \"2026-04-30T13:21:08Z\"\n}\n```\n\nBehavior: resolve the policy through the three-tier fallback chain (per planning-ledger D-T7, mirroring oddkit's `telemetry_policy` pattern verbatim):\n\n1. **`knowledge_base`** — fetch this article live via `web_fetch` against `raw.githubusercontent.com`. Cache-busted via etag/If-None-Match where supported. On success, response carries `governance_source: \"knowledge_base\"`.\n2. **`bundled`** — if the live fetch fails (network error, non-200, malformed), serve a copy bundled into the Worker at deploy time. Marked `governance_source: \"bundled\"`. Slightly stale by definition (refreshed each deploy); never silently misleading.\n3. **`minimal`** — if even the bundled copy is missing (deploy artifact corrupt) or unreadable, return a one-paragraph minimal policy string baked into the source. Marked `governance_source: \"minimal\"`.\n\nThe fallback chain is observable: every response includes the `governance_source` field so consumers can tell which tier served their copy.\n\n---\n\n## 4. Payload Schema — unchanged from v1.2\n\n`schema_version: \"1.0\"`. Container rejects versions it doesn't speak. v1.3 introduces no new payload fields and removes none.\n\nSee v1.2 spec §4 for the full schema and slot semantics. The canonical hashing rule (RFC 8785 JCS or equivalent over the payload JSON) is unchanged; cache identity remains content-addressed.\n\n---\n\n## 5. Architecture\n\n```\nAgent (Claude Desktop / BT Servant / etc.)\n  ↓ MCP/HTTP — 6 tools\n┌──────────────────────────────────────────────────────────────┐\n│ Cloudflare Worker (the only Worker)                          │\n│  Public MCP routes:                                          │\n│   - submit_typeset / get_job_status / cancel_job (unchanged) │\n│   - docs (unchanged from v1.2 session-13 add)                │\n│   - telemetry_public(sql)   → Analytics Engine query         │\n│   - telemetry_policy()      → governance from canon          │\n│  Internal route (service-binding only, not externally reach):│\n│   - POST /internal/telemetry → redact + writeDataPoint       │\n│  Telemetry writes (every request):                           │\n│   - mcp_request event at request entry                       │\n│   - tool_call event when method == tools/call                │\n└──────────────────────────────────────────────────────────────┘\n  ↓ Worker→Container service binding (job dispatch, unchanged)\n  ↑ Container→Worker service binding (telemetry forward, NEW)\n┌──────────────────────────────────────────────────────────────┐\n│ Cloudflare Container (the only Container image)              │\n│  Per job (unchanged behavior):                               │\n│   - materialize, fetch+verify, run, classify, upload, update │\n│  v1.3 additions (telemetry hooks):                           │\n│   - POST /internal/telemetry on each phase transition        │\n│       fetching_inputs → typesetting → autofill → uploading   │\n│   - POST /internal/telemetry once at terminal                │\n│       (failure_mode, totals, pages_count if success)         │\n└──────────────────────────────────────────────────────────────┘\n  ↕ DO read/write           ↓ R2 PUT (content-addressed)\nDurable Objects        Cloudflare R2\n(unchanged: per-job)   (unchanged: outputs/<hash>/...)\n                                                ↓\n                                      writeDataPoint\n                                                ↓\n┌──────────────────────────────────────────────────────────────┐\n│ Cloudflare Workers Analytics Engine                          │\n│  Dataset: ptxprint_telemetry                                 │\n│  Retention: 3 months (CF default)                            │\n│  Schema: 12 blobs + 10 doubles (governance article §What    │\n│          Is Tracked)                                         │\n│  Public: queryable by anyone via telemetry_public(sql)       │\n└──────────────────────────────────────────────────────────────┘\n```\n\n### The Worker — additions to v1.2\n\n- Two new public MCP routes (`telemetry_public`, `telemetry_policy`) wired the same way as the existing four.\n- One new internal route: `POST /internal/telemetry`. Reachable only via service binding from the Container — not exposed externally. Body shape:\n  ```json\n  {\n    \"event_type\": \"job_phase | job_terminal\",\n    \"job_id\": \"...\",\n    \"consumer_label\": \"...\",     // forwarded by Container from the originating Worker context\n    \"phase\": \"...\",              // for job_phase events\n    \"failure_mode\": \"...\",       // for job_terminal events\n    \"payload_hash_prefix\": \"...\", // first 8 hex chars\n    \"duration_ms\": 12345,\n    \"passes_completed\": 3,\n    \"overfull_count\": 12,\n    \"pages_count\": 312,\n    \"bytes_out\": 4194304\n  }\n  ```\n  The Worker validates the envelope, applies the redactor (which is mostly a structural-typing pass since the envelope is already typed), and calls `env.PTXPRINT_TELEMETRY.writeDataPoint()`.\n- `wrangler.jsonc` additions:\n  ```jsonc\n  \"analytics_engine_datasets\": [\n    { \"binding\": \"PTXPRINT_TELEMETRY\", \"dataset\": \"ptxprint_telemetry\" }\n  ],\n  \"vars\": {\n    \"TELEMETRY_QUERY_RATE_LIMIT_PER_HOUR\": \"60\"\n  }\n  ```\n  Plus secrets (set via `wrangler secret put`):\n  ```\n  CF_ACCOUNT_ID            (used by telemetry_public to hit the SQL API)\n  CF_API_TOKEN             (read-only token scoped to Analytics Engine query)\n  TELEMETRY_VERIFIED_CLIENTS  (optional comma-separated allowlist)\n  ```\n\n### The Container — additions to v1.2\n\n**2026-09-05 amendment (Worker 0.2.1).** One Container DO is created per job (`idFromName(job_id)`). The Worker now calls `stop()` on the instance as soon as the dispatch `fetch` returns (the container runs the job synchronously), so `sleepAfter = 45m` is a ceiling, not the release path. Before this, five jobs inside 45 minutes exhausted `max_instances = 5` and the sixth failed with \"Maximum number of running container instances exceeded\" — found while typesetting Titus in a loop (kitchen ticket `2026-09-05-titus-study-bible-ptxprint`).\n\n- A small telemetry helper function (~30 lines of Python) that POSTs envelopes to `http://worker.local/internal/telemetry` via the Container's outbound service binding to the Worker.\n- Phase-transition hooks: between the existing v1.2 stages in `container/main.py`, insert one helper call per transition. Each hook fires on the boundary between phases and records the `duration_ms` of the just-completed phase.\n- Terminal hook: at the end of the run (success, failure, or cancellation), one final `job_terminal` event with the failure_mode and totals.\n- The Container does **not** hold `CF_ACCOUNT_ID`, `CF_API_TOKEN`, or any other Analytics Engine credential. It only knows the Worker's internal route. This is the routing constraint from `canon/governance/telemetry-governance.md` §\"Job Lifecycle Events\" and planning-ledger D-T3.\n\n### State (Durable Objects) — unchanged from v1.2\n\nNo schema changes. Telemetry observes DO state but doesn't extend it.\n\n### Output (R2) — unchanged from v1.2\n\n`ptxprint-outputs` bucket continues to be the only R2 bucket. Telemetry has no R2 footprint.\n\n---\n\n### Limits (observed 2026-09-05; the deploy is authoritative)\n\n| Limit | Value | Where it bites |\n|---|---|---|\n| Concurrent container instances | `max_instances = 5` (`wrangler.jsonc`) | a sixth job while five run fails at dispatch: \"Maximum number of running container instances exceeded\" — since 0.2.1 instances are released when a job returns, so this now means five *simultaneously running* jobs |\n| Container idle ceiling | `sleepAfter = 45m` | only reached if `stop()` after a job fails |\n| Job timeout | `PTXPRINT_TIMEOUT_DEFAULT = 300` s (`wrangler.jsonc` vars) | long books / autofill |\n| Output URL TTL | `RESULT_PRESIGNED_TTL = 604800` s (7 days) | fetch the PDF within a week or re-submit (cache hit) |\n| Payload text | inline `config_files` travel in the MCP envelope | keep binaries (fonts, USFM) as URL + sha256 |\n\n## 6. Telemetry Module — Implementation Detail\n\nThe module's job is to emit data points without ever leaking content. Three responsibilities, each ~50 lines:\n\n### 6.1 Writer\n\nSingle function `writeTelemetry(env, eventType, fields)`. Fields is a typed object: blobs are strings, doubles are numbers, anything else throws at compile time. The writer:\n\n1. Looks up the consumer label from request context (priority: `?consumer=` query > `x-ptxprint-client` header > MCP `clientInfo.name` > `User-Agent` > `\"unknown\"`).\n2. Computes the consumer label's source for blob 5.\n3. Stamps the worker version (compiled in via build-time env var).\n4. Calls `env.PTXPRINT_TELEMETRY.writeDataPoint({ blobs: [...], doubles: [...], indexes: [consumer_label] })`.\n\nThe writer never accepts free-form strings beyond the typed slots. Adding a new dimension requires editing the function signature.\n\n### 6.2 Redactor\n\nThe redactor exists primarily as a structural-typing barrier. The writer's signature accepts only the typed slots; any attempt to pass content (a USFM string, a project ID, a payload field) is a compile error in TypeScript. For the Container-to-Worker `/internal/telemetry` endpoint, where the envelope arrives as untyped JSON over the wire, the redactor:\n\n1. Accepts the JSON envelope.\n2. Validates against a strict zod (or equivalent) schema that lists exactly the allowed fields.\n3. Rejects any envelope containing additional keys (`additionalProperties: false`).\n4. Truncates any `payload_hash_prefix` to 8 hex characters even if the Container sent more (defense in depth).\n5. Hands the validated, typed object to the writer.\n\nTest: the redactor's rejection list IS the privacy floor. A test suite asserts that envelopes containing `project_id`, `config_name`, `book_codes`, `source_url`, `font_url`, `figure_url`, `payload_full`, `usfm_bytes`, `log_content`, or `pdf_bytes` are rejected before reaching the writer.\n\n### 6.3 Policy Fetcher\n\nImplements the three-tier fallback for `telemetry_policy`:\n\n```typescript\nasync function resolveTelemetryPolicy(env): Promise<{ policy: string, source: 'knowledge_base' | 'bundled' | 'minimal' }> {\n  // Tier 1: knowledge_base\n  try {\n    const res = await fetch('https://raw.githubusercontent.com/klappy/ptxprint-mcp/main/canon/governance/telemetry-governance.md', {\n      cf: { cacheTtl: 300, cacheEverything: true }  // 5min edge cache\n    });\n    if (res.ok) {\n      const text = await res.text();\n      if (text.includes('# Telemetry Governance')) {  // sanity check; reject malformed\n        return { policy: text, source: 'knowledge_base' };\n      }\n    }\n  } catch (e) { /* fall through */ }\n\n  // Tier 2: bundled\n  if (BUNDLED_POLICY) {  // imported as a string literal at build time\n    return { policy: BUNDLED_POLICY, source: 'bundled' };\n  }\n\n  // Tier 3: minimal\n  return { policy: MINIMAL_POLICY_STRING, source: 'minimal' };\n}\n```\n\nThe bundled tier is populated by a build script that copies the article into a TypeScript constant at deploy time. The minimal tier is a ~200-word static string that lists the dataset name, the privacy-floor non-negotiables, and the URI of the canonical article.\n\n### 6.4 Public Query Forwarder\n\nImplements `telemetry_public`:\n\n```typescript\nasync function forwardTelemetryQuery(env, sql, consumerLabel): Promise<TelemetryQueryResult> {\n  // Guard 1: dataset allowlist — reject queries that reference any dataset\n  // other than ptxprint_telemetry. A substring check is not sufficient: a\n  // query like `SELECT * FROM secret_dataset UNION SELECT * FROM ptxprint_telemetry`\n  // would pass it. Strip comments and string literals first so the substring\n  // cannot be smuggled in via a literal/comment, then require every FROM/JOIN\n  // target to be ptxprint_telemetry.\n  const stripped = sql\n    .replace(/--[^\\n]*/g, ' ')          // line comments\n    .replace(/\\/\\*[\\s\\S]*?\\*\\//g, ' ')  // block comments\n    .replace(/'(?:''|[^'])*'/g, \"''\")   // string literals\n    .toLowerCase();\n  const datasetRefs = [...stripped.matchAll(/\\b(?:from|join)\\s+([a-z_][a-z0-9_]*)/g)].map(m => m[1]);\n  if (datasetRefs.length === 0 || datasetRefs.some(name => name !== 'ptxprint_telemetry')) {\n    return sanitizedError('Query must reference only dataset ptxprint_telemetry');\n  }\n\n  // Guard 2: rate limit (per-consumer, 60/hour default)\n  if (await rateLimitExceeded(env, consumerLabel)) {\n    return sanitizedError('Query rate limit exceeded; retry later');\n  }\n\n  // Forward to Analytics Engine SQL API\n  try {\n    const res = await fetch(`https://api.cloudflare.com/client/v4/accounts/${env.CF_ACCOUNT_ID}/analytics_engine/sql`, {\n      method: 'POST',\n      headers: { 'Authorization': `Bearer ${env.CF_API_TOKEN}`, 'Content-Type': 'text/plain' },\n      body: sql\n    });\n    const data = await res.json();\n    if (!res.ok) return sanitizedError('Query execution failed');\n    return { rows: data.data, row_count: data.data.length, query: sql, executed_at: new Date().toISOString() };\n  } catch (e) {\n    // Guard 3: error sanitization — never forward raw Analytics Engine errors\n    return sanitizedError('Query execution failed');\n  }\n}\n```\n\nThe three guards collectively are infrastructure serving security, not domain opinion about what's interesting. No column restriction. No SQL keyword block list. The data is public.\n\n---\n\n## 7. Cloudflare-specific verified facts (2026-04, v1.3 additions)\n\nAll v1.2 facts continue to apply. v1.3 adds:\n\n| Fact | Source |\n|---|---|\n| Workers Analytics Engine free tier: 100,000 data points/day | `/analytics/analytics-engine/limits/` |\n| Workers Analytics Engine free query tier: 10,000 queries/day | same |\n| Analytics Engine retention: 3 months | same |\n| Analytics Engine `writeDataPoint()` is non-blocking, no latency added to request | `/analytics/analytics-engine/get-started/` |\n| Analytics Engine sampling: queries must use `SUM(_sample_interval)` not `COUNT(*)` for sample-correct totals | `/analytics/analytics-engine/sampling/` |\n| SQL API endpoint: `POST https://api.cloudflare.com/client/v4/accounts/{account_id}/analytics_engine/sql` with body=raw SQL | `/analytics/analytics-engine/sql-reference/` |\n| Service bindings allow Worker-to-Worker and Container-to-Worker calls without traversing the public internet | `/workers/runtime-apis/bindings/service-bindings/` |\n\nRisk to verify at implementation time: whether `cf: { cacheTtl, cacheEverything }` actually caches the GitHub raw fetch in the policy resolver across edge regions. If not, the bundled tier becomes more important than estimated and the deploy script must be reliable.\n\n---\n\n## 8. What's Deferred (and Why)\n\nAll v1.2 deferrals continue to apply. v1.3-specific deferrals:\n\n| Deferred capability | Why deferred | When to revisit |\n|---|---|---|\n| **Snapshot writeback automation** | Analytics Engine's 3-month retention is fine for hackathon-week visibility; manual snapshots if narrative continuity wanted | When the operator wants a quarterly recap blog post |\n| **OAuth-derived consumer identification** | v1.3 has no auth; `oauth` is a reserved value in `consumer_source` for future authenticated deployments | When auth lands |\n| **Real-time alerting on soft-failure-rate spikes** | Analytics Engine has minutes of latency; not designed for paging | When job volume justifies a separate monitoring layer (probably never; soft-failure-rate is a weekly trend, not a paging signal) |\n| **Per-deployment dashboards in the CF console** | Grafana-compatible; users can build their own | If users explicitly ask for them |\n| **Telemetry write batching** | At expected volume (sub-1000 jobs/day initially) `writeDataPoint()` per request is fine | If telemetry writes start showing up in Worker latency budgets |\n\n---\n\n## 9. Migration\n\n### From v1.2 to v1.3\n\n| v1.2 surface | v1.3 disposition |\n|---|---|\n| `submit_typeset` | **Unchanged.** Adds telemetry write at request entry. |\n| `get_job_status` | **Unchanged.** Adds telemetry write at request entry. |\n| `cancel_job` | **Unchanged.** Adds telemetry write at request entry. |\n| `docs` | **Unchanged.** Adds telemetry write at request entry. |\n| Worker routes | **+2 public** (`telemetry_public`, `telemetry_policy`), **+1 internal** (`/internal/telemetry`). |\n| Container `container/main.py` | **+telemetry helper, +phase-transition hooks, +terminal hook.** No change to the typesetting pipeline itself. |\n| `wrangler.jsonc` | **+analytics_engine_datasets binding, +TELEMETRY_QUERY_RATE_LIMIT_PER_HOUR var.** |\n| Secrets | **+CF_ACCOUNT_ID, +CF_API_TOKEN, +TELEMETRY_VERIFIED_CLIENTS** (last is optional). |\n| DO schema | **Unchanged.** |\n| R2 layout | **Unchanged.** |\n| Payload schema | **Unchanged.** |\n\nThe migration is purely additive. v1.2 agents continue to work without change. v1.3-aware agents may consult `telemetry_policy` to discover the self-report headers and identify themselves; this is recommended but not required.\n\n### Backward compatibility commitment\n\nv1.3 commits to not changing v1.2 tool behavior. If a future v1.4 telemetry change needs to alter v1.3 behavior (e.g., restructure the schema in a way that breaks canned queries), it ships as v1.4 with a \"What changed from v1.3\" preamble. v1.3 itself stays locked once implemented, per the principle.\n\n---\n\n## 10. Definition of Done for v1.3\n\nThe MCP server is \"v1.3 done\" when, in addition to all v1.2 DoD criteria still passing:\n\n1. **Two new tools work.** `telemetry_public(sql)` returns rows for at least the 8 canned queries documented in `canon/governance/telemetry-governance.md`. `telemetry_policy()` returns the article with `governance_source: \"knowledge_base\"` in normal operation.\n2. **Job-lifecycle events fire.** A submitted job produces (`queued`, `fetching_inputs`, `typesetting`, `[autofill,]` `uploading`, `done`) phase events plus exactly one `job_terminal` event, observable via `telemetry_public`.\n3. **Cache hit produces no Container telemetry.** A cached `submit_typeset` returns immediately; only the `mcp_request` event fires (with `cache_outcome = \"hit\"`); no `job_*` events follow.\n4. **Privacy-floor exclusions pass.** A test suite POSTs envelopes to `/internal/telemetry` containing each of the 10 prohibited fields (`project_id`, `config_name`, `book_codes`, `source_url`, `font_url`, `figure_url`, `payload_full`, `usfm_bytes`, `log_content`, `pdf_bytes`); each is rejected before `writeDataPoint()` is called.\n5. **Three-tier fallback works.** `telemetry_policy()` returns `governance_source: \"bundled\"` when the GitHub fetch is mocked to fail, and `governance_source: \"minimal\"` when both are mocked to fail.\n6. **Self-report headers are read.** A request with all 8 self-report headers gets `consumer_source: \"header\"` and a complete leaderboard score; a request with only `?consumer=foo` gets `consumer_source: \"query\"`.\n7. **Rate limit fires.** The 61st query from the same consumer label within an hour returns the rate-limit sanitized error.\n8. **Container has no Analytics Engine credentials.** `grep` on the Container image for `CF_API_TOKEN` returns nothing; the Container's only telemetry path is the Worker forwarding endpoint.\n\nSmoke test: end-to-end on the same English Bible test project from v1.2's smoke test, with `telemetry_public` queries verifying that all 8 canned queries return non-empty results after the test runs.\n\n---\n\n## 11. First Execution Scope (v1.3)\n\nFor whoever picks up the v1.3 build (autonomous coding run or human follow-on):\n\n**In scope:**\n\n- Add two MCP tool routes (`telemetry_public`, `telemetry_policy`) to `src/index.ts` following the same pattern as the existing four.\n- Add `src/telemetry.ts` containing the writer, redactor, policy fetcher, and public query forwarder per §6.\n- Add `POST /internal/telemetry` endpoint to the Worker.\n- Add the telemetry envelope helper to `container/main.py` and wire phase-transition + terminal hooks into the existing v1.2 lifecycle.\n- Update `wrangler.jsonc` with the analytics_engine_datasets binding and the rate-limit var.\n- Add a `scripts/bundle-telemetry-policy.ts` build step that copies `canon/governance/telemetry-governance.md` into `src/bundled-policy.ts` for the tier-2 fallback.\n- Configure `CF_ACCOUNT_ID` and `CF_API_TOKEN` as secrets via `wrangler secret put`.\n- Smoke-test all 8 DoD items.\n\n**Explicitly out of scope:**\n\n- Anything in §8's deferred list.\n- Any change to v1.2's tool behavior.\n- Any new payload field.\n- Any new R2 footprint.\n\n**Critical-path canon work** (already complete as of this spec):\n\n- `canon/governance/telemetry-governance.md` — the schema, privacy floor, canned queries, transparency leaderboard. **Status: reviewed (PR #25, fixes in PR #26).**\n\n**Validation gate** (per `klappy://canon/principles/verification-requires-fresh-context`):\nThe first-run agent does not validate its own output. A separate session (or human review) accepts or returns to iteration. The DoD's privacy-floor exclusion test (item 4) is the highest-stakes acceptance criterion — verify it independently.\n\n---\n\n*End of v1.3 specification. Companions: `canon/governance/telemetry-governance.md` (the policy v1.3 implements), `canon/encodings/telemetry-feature-planning-ledger.md` (the design ledger this spec executes against), `canon/handoffs/telemetry-governance-h-t2-review.md` (the fresh-session review of the governance article).*\n"},{"path":"templates/english-single-book.md","uri":"klappy://canon/templates/english-single-book","title":"Template: English Single-Book Bible","audience":"agent","tags":["ptxprint","mcp","agent-kb","v1.2-aligned","non-canonical","template","english-bible","single-book","bootstrap","quickstart"],"stability":"working","what_it_answers":"**What this is.** A partial `submit_typeset` payload for rendering a single English Bible book with clean A5 layout, parallel-passage references intact, and Gentium Plus throughout. Empirically derived from `smoke/bsb-jhn-empirical.json` (w","sections":[{"slug":"when-to-use","heading":"When to use","start":47,"end":65},{"slug":"caller-supplied-fields","heading":"Caller-supplied fields","start":65,"end":109},{"slug":"caller-may-override-fields","heading":"Caller-may-override fields","start":109,"end":122},{"slug":"what-s-in-the-template-don-t-change-unless-you-know-why","heading":"What's in the template (don't change unless you know why)","start":122,"end":150},{"slug":"composition-contract-how-to-merge","heading":"Composition contract — how to merge","start":150,"end":186},{"slug":"validation-contract","heading":"Validation contract","start":186,"end":205},{"slug":"known-limitations-carried-forward-from-bsb-iteration","heading":"Known limitations (carried forward from BSB iteration)","start":205,"end":219},{"slug":"provenance","heading":"Provenance","start":219,"end":229},{"slug":"partial-payload","heading":"Partial payload","start":229,"end":282}],"body":"---\ntitle: \"Template: English Single-Book Bible\"\naudience: agent\nexposure: working\nvoice: instructional\nstability: working\ntags:\n  - ptxprint\n  - mcp\n  - agent-kb\n  - v1.2-aligned\n  - non-canonical\n  - template\n  - english-bible\n  - single-book\n  - bootstrap\n  - quickstart\ntype: template\ntemplate_id: english-single-book\ntemplate_version: \"0.1\"\napplies_to: \"Single English Bible book; A5 (148mm × 210mm); two-column justified body; parallel-passage references on; clean section headings; Gentium Plus 6.200 (Regular, Bold, Italic, BoldItalic) supplied via the payload's fonts array; SourceCodePro container-bundled; A5 page size; Charis-substitution mitigation NOT used (this template uses payload-supplied fonts, not container-bundled fonts).\"\ncaller_supplies:\n  - books\n  - sources\n  - vars.maintitle\ncaller_may_override:\n  - vars.subtitle\n  - vars.languagename\n  - project_id\n  - config_name\nderived_from: \"smoke/bsb-jhn-empirical.json (purified — caller-owned fields stripped)\"\ncompanion_to:\n  - \"canon/articles/composition-and-templates.md\"\n  - \"canon/articles/composition-and-templates.md\"\ncanonical_status: non_canonical\ndate: 2026-04-30\nstatus: draft_for_operator_review\n---\n\n# Template: English Single-Book Bible\n\n> **What this is.** A partial `submit_typeset` payload for rendering a single English Bible book with clean A5 layout, parallel-passage references intact, and Gentium Plus throughout. Empirically derived from `smoke/bsb-jhn-empirical.json` (which has rendered successfully across sessions 10, 11, 12, 14 with BSB John and BSB Psalms).\n>\n> **What this is not.** A full payload. The caller supplies `books`, `sources`, and `vars.maintitle` — see \"Caller-supplied fields\" below. Without those, this payload will not render.\n\n---\n\n## When to use\n\nUse this template when:\n\n- The source content is English-language USFM\n- One book per render (the template's `[project] bookscope = multiple` plus a single-entry `books` array works fine)\n- A5 page size is acceptable (148mm × 210mm)\n- Two-column justified body, parallel-passage references on, decorations off — these are the defaults\n- Gentium Plus is acceptable (or a font-compatible substitute the caller layers in)\n\nDo **not** use this template when:\n- Multi-book NT or whole Bible — needs a different template (one will land as `english-multi-book` or similar)\n- Non-English content — fonts and language metadata defaults won't match\n- Diglot or polyglot — needs a different template\n- Poetry-heavy book where line spacing matters (Psalms etc. render but with significant page underfill — see \"Known limitations\" in the BSB-render iteration trail)\n\n---\n\n## Caller-supplied fields\n\nThe caller MUST supply these. The template is incomplete without them.\n\n| Field | Type | Example | Notes |\n|---|---|---|---|\n| `books` | string array, top-level | `[\"JHN\"]` | USFM 3-letter book code(s) |\n| `sources` | object array, top-level | see below | URL + sha256 of the USFM file(s) |\n| `vars.maintitle` | string, inside `config_files`'s `[vars]` section | `\"John\"` | Title-page text and PDF metadata title |\n\n### `sources` shape\n\n```json\n[\n  {\n    \"book\": \"JHN\",\n    \"filename\": \"44JHNtest.usfm\",\n    \"url\": \"https://raw.githubusercontent.com/.../44JHN....usfm\",\n    \"sha256\": \"<hex64>\"\n  }\n]\n```\n\nThree things to note about `sources`:\n\n- The `filename` field needs the `test.usfm` suffix to match this template's `Settings.xml`'s `<FileNamePostPart>test.usfm</FileNamePostPart>` (a leftover from the minitests fixture lineage). Use any USFM-canonical book number prefix (`44JHN`, `19PSA`, `01GEN`, etc.) followed by `test.usfm`.\n- The `url` is fetched by the worker. `raw.githubusercontent.com` works fine; so does any HTTPS-reachable URL.\n- The `sha256` is verified on download. Mismatch fails the render with a clear error.\n\n### `vars.maintitle` placement\n\nThis field lives **inside** the cfg INI text, which lives inside `config_files`'s `shared/ptxprint/Default/ptxprint.cfg` value. The cfg has a `[vars]` section near the bottom:\n\n```ini\n[vars]\nmaintitle = \nsubtitle = Berean Standard Bible\nlanguagename = English\n```\n\nThe caller substitutes the empty `maintitle =` line with their value, e.g. `maintitle = John`. String substitution on the cfg INI text is sufficient (no INI parsing needed).\n\n---\n\n## Caller-may-override fields\n\nOptional. Defaults are sensible for English-language content.\n\n| Field | Default | When to override |\n|---|---|---|\n| `vars.subtitle` (cfg INI) | `Berean Standard Bible` | Non-BSB English source — set to the actual translation name |\n| `vars.languagename` (cfg INI) | `English` | (Don't override; this template is English-only) |\n| `project_id` (top-level) | `bsbref` | Caller's namespacing label; any 1–8 character string |\n| `config_name` (top-level) | `Default` | Almost always leave as `Default` |\n\n---\n\n## What's in the template (don't change unless you know why)\n\n**Layout** (in `[paper]`):\n- A5 page size (148mm × 210mm), 12pt margins, 14.4pt bottom margin\n- Two-column body, justified\n- Header rule on, page number centered, reference range right\n\n**Body** (in `[document]`):\n- `parallelrefs = True` — parallel-passage references rendered. **This is the load-bearing setting for cross-references.**\n- `cloptimizepoetry = True` — poetry layout optimized\n- `bookintro = False`, `multibook = False` — single-book mode\n\n**Decorations off** (in `[fancy]`, `[snippets]`, `[paper]`):\n- `pageborder`, `pageborders`, `sectionheader`, `versedecorator` — all False/empty\n- `cropmarks = False`, `ifverticalrule = False`, `ifgrid = False`\n- `fancyborders = False`, `fancyintro = False`\n\n**Fonts** (in `[document]` and the `fonts` array):\n- Gentium Plus regular / bold / italic / bolditalic\n- Supplied via the payload's `fonts` array (URLs + sha256 verified per `klappy://canon/articles/font-resolution`; no container apt-install dependency)\n\n**Stylesheet** (in `config_files[\"shared/ptxprint/Default/ptxprint.sty\"]`):\n- Inherits from the minitests fixture\n- Empirical neutralizations: `\\Marker cat:headingsbox|esb` rewritten to remove the decorative box; `\\Marker s/s1/s2/r` blocks added with neutralizing properties + Bold/Center on headings, Italic on `\\r`\n- Caveat: these stylesheet properties are not yet documented in `klappy://canon/articles/stylesheet-format` (tracked at `s12-H027`/`s12-H028`)\n\n---\n\n## Composition contract — how to merge\n\nThe merge is a shallow object merge for top-level fields, plus a string substitution inside the cfg INI for `vars.maintitle`.\n\nPseudocode:\n\n```python\ntemplate = json.loads(extract_json_block_from_docs_response(...))\n\n# 1. Top-level caller fields\ntemplate[\"books\"] = [\"JHN\"]\ntemplate[\"sources\"] = [\n    {\n        \"book\": \"JHN\",\n        \"filename\": \"44JHNtest.usfm\",\n        \"url\": \"https://raw.githubusercontent.com/usfm-bible/examples.bsb/48a9feb71f0a66f9b8f418b11ae25b7ad2e49a0d/44JHNBSB.usfm\",\n        \"sha256\": \"f6220aa81c8143cb66a86d775fa3cdfe10efcb52dad135dfc498baeac260103d\",\n    }\n]\n\n# 2. cfg INI substitution for vars.maintitle (and optionally subtitle)\ncfg_path = \"shared/ptxprint/Default/ptxprint.cfg\"\ntemplate[\"config_files\"][cfg_path] = template[\"config_files\"][cfg_path].replace(\n    \"maintitle = \\n\",            # the placeholder line as it appears in the template\n    \"maintitle = John\\n\",\n    1,\n)\n\n# 3. Submit\nresult = submit_typeset(payload=template)\n```\n\nThat's it. No deep merge, no sty edits, no font munging.\n\n---\n\n## Validation contract\n\nA merged payload submitted via `submit_typeset` should produce:\n\n- `state: \"succeeded\"`, `failure_mode: \"success\"`, `exit_code: 0`\n- Wall-clock: ~13 s container, ~16 s end-to-end (cold cache); <1 s cache hit\n- PDF: ~360 KB for a Gospel-sized book (e.g. JHN), ~900 KB for Psalms\n- Pages: varies by book length\n- Embedded fonts: `GentiumPlus`, `GentiumPlus-Bold`, `GentiumPlus-Italic`, `SourceCodePro-Regular`\n- Reproducibility: the same merged payload returns the same `job_id` (content-addressed)\n\nFailure modes a caller should expect to handle:\n\n- `sha256 mismatch on sources[N]` — caller's hash doesn't match the URL's content. Re-fetch and recompute.\n- `failure_mode: \"soft\"` with high `overfull_count` — render succeeded but layout has issues. Inspect the log via `get_job_status`'s `log_tail`.\n- Underfilled pages on poetry — known limitation (autofill is deferred). The PDF is valid; pages just have uneven bottom margins.\n\n---\n\n## Known limitations (carried forward from BSB iteration)\n\n1. **Underfilled pages on poetry-heavy books** (Psalms ~92% of pages). Cause: single-pass typesetting + short `\\q1`/`\\q2` lines. Canonical fix is `mode: autofill` (deferred). Interim mitigations: experiment with `hangpoetry`, `preventwidows`, `preventorphans` cfg knobs.\n\n2. **Footnote-balancing log noise** — `IF CHECK mnote Failed. Entered at 2 != exit at 1` repeats when footnotes are present. Output is valid; noise is upstream macro behavior. Tracked at `s14-H027`.\n\n3. **Stylesheet edits are empirical** — the `\\Border`, `\\BorderWidth`, `\\BgColor`, `\\Alpha` neutralizations work but aren't yet in `klappy://canon/articles/stylesheet-format`. Tracked at `s12-H027`/`s12-H028`.\n\n4. **Section-heading wrapping mechanism not fully understood** — why standard `\\s1` markers in BSB USFM get `cat:headingsbox|esb` styling is opaque in current canon. The template's stylesheet neutralizes the styling but doesn't explain the trigger. Tracked at `s12-O-open-12-001`.\n\n5. **Multi-book mode** — this template's `[project] bookscope = multiple` works for a single-book `books` array, but multi-book renders haven't been validated end-to-end. A dedicated `english-multi-book` template is the right path for that case.\n\n---\n\n## Provenance\n\nDerived from `smoke/bsb-jhn-empirical.json` (which is itself the v6 of an iterative cleanup over the upstream `sillsdev/ptx2pdf/test/projects/minitests` fixture — see `klappy://canon/encodings/transcript-encoded-session-12` for the v3→v6 narrative). Caller-owned fields stripped:\n\n- Removed top-level: `books`, `sources`\n- Cleared cfg `[vars] maintitle`, `[project] booklist`, `[project] subject`, `[document] clabelbooks`\n- Kept everything else verbatim\n\n---\n\n## Partial payload\n\nCopy this block. Apply your `books`, `sources`, and `vars.maintitle` per the composition contract above. Submit via `submit_typeset(payload=...)`.\n\n```json\n{\n  \"schema_version\": \"1.0\",\n  \"project_id\": \"bsbref\",\n  \"config_name\": \"Default\",\n  \"mode\": \"simple\",\n  \"define\": {},\n  \"config_files\": {\n    \"Settings.xml\": \"<ScriptureText>\\n  <StyleSheet>usfm.sty</StyleSheet>\\n  <BooksPresent>111111111111010010110011001000000000001101111001000000000100011101000000000000000000000000000000000001000000100000000000000</BooksPresent>\\n  <Versification>4</Versification>\\n  <LanguageIsoCode>en</LanguageIsoCode>\\n  <FileNameBookNameForm>41MAT</FileNameBookNameForm>\\n  <FileNamePrePart />\\n  <FileNamePostPart>test.usfm</FileNamePostPart>\\n\\n</ScriptureText>\\n\",\n    \"custom.sty\": \"\\\\Category pl2\\n\\\\Position pl2\\n\\\\Scale 2\\n\\\\Alpha .5\\n\\\\BgColour 1 .8 .8\\n\\\\BoxVPadding 2\\n\\\\BoxHPadding 2\\n\\\\Border All\\n\\n\\\\Category hc\\n\\\\Position hc\\n\\\\BoxHPadding 3\\n\\\\BoxVPadding 3\\n\\\\Scale 0.6\\n\\\\BgColour 0.8 1 0.8\\n\\\\Alpha 1\\n\\\\Border All\\n\\n\\\\Category hr\\n\\\\Position hr\\n\\\\BgColour 0.8 0.8 1\\n\\\\Alpha 1\\n\\\\Scale 0.7\\n\\\\Border All\\n\\n\\\\Category page\\n\\\\Position Pcc\\n\\\\BorderPadding 15\\n\\\\BorderWidth 1\\n\\\\Border All\\n\\n\\\\Category inl   \\n\\\\Position h      \\n\\\\BgColour 0 1 0\\n\\\\Alpha 0.2     \\n\\\\Border Outer Top Bottom \\n\\\\BoxPadding  0         \\n\\\\BorderPadding 2             \\n\\\\Scale 1       \\n\\\\Marker p                                                   \\n\\\\Justification center                                       \\n                                                            \\n\\\\Category top                                               \\n\\\\Position t          \\n\\\\BgColour 0 1 0    \\n\\\\Alpha 0.2             \\n\\\\Border Outer Top Bottom \\n\\\\BoxPadding  0\\n\\\\BorderPadding 2\\n\\\\Scale 1 \\n  \\n\\\\Category foo\\n%\\\\BgColour 1 0 0\\n\\\\Scale 1\\n\\\\Position tr\\n\\\\Border left top right bottom\\n\\\\BorderWidth 5\\n\\\\BorderPadding -6\\n\\\\BoxVPadding 8\\n\\\\BoxHPadding 8\\n\\\\BgColour 0 0 1\\n\\\\BorderColour 0.2 0.3 0.6\\n\\\\BorderFillColour 0.2 0.3 0.6\\n\\\\BorderPatternTop 39|||2.5,0|||0.2,88||-,0||*A,39|h||2.5\\n\\\\BorderPatternBot 39|v||2.5,0||*A,88|h|-,0||-,39|d||2.5\\n\\\\BorderPatternLeft 0|||0.2,88|l|-,0|l|*A,-2|l|*A,0|l|*A\\n\\\\BorderPatternRight 0|l|*A,-2|r|*A,0|r|*A,88|r|-\\n\\\\BorderLineWidth 0.3\\n\\\\BorderStyle ornaments\\n\\n\\\\Category pictest\\n\\\\Position h\\n\\\\Scale 1\\n\\\\BgColour 0 0.5 0\\n\\\\BorderColour 0 0 0\\n\\\\SpaceBeside 3pt\\n\\\\FgImage rose.jpg\\n\\\\FgImageScale 0.25\\n\\\\FgImagePos cr1\\n\\\\BgImage SlithyToves.jpg\\n%\\\\BgImageLow f\\n\\\\BgImageScale x1\\n\\\\BgImageAlpha 0.5\\n\\\\BoxVPadding 15\\n\\\\BoxHPadding 15\\n\\\\BorderVPadding -7\\n\\\\BorderHPadding -7\\n\\\\BgImageOversize shrink\\n\\\\Border Top Bottom left right\\n\\\\BorderWidth 5 \\n\\\\BorderPatternTop 39|||2.5,0|||0.2,88||-,0||*A,39|h||2.5\\n\\\\BorderPatternBot 39|v||2.5,0||*,88|h|-,0||-,39|d||2.5\\n\\\\BorderPatternLeft 0|||0.2,88|l|-,0|l|*A,-2|l|*A,0|l|*A\\n\\\\BorderPatternRight 0|l|*A,-2|r|*A,0|r|*A,88|r|-\\n\\\\BorderLineWidth 0.05\\n\\\\BorderStyle double\\n\\\\BorderFillColour 0 1 0\\n\\n\",\n    \"shared/ptxprint/Default/ptxprint.cfg\": \"[document]\\ndiglotsecprj = WEBorig\\nbookintro = False\\nch1pagebreak = False\\nchapfrom = 1\\nchapto = 999\\nclabel = \\nclabelbooks = \\ncloptimizepoetry = True\\ncolgutterfactor = 4\\ncolumnshift = 16\\ncustomfigfolder = C:/WSG/local/figures\\ncustomfiglocn = False\\ndiffcolayout = False\\ndiffcolayoutbooks = FRT INT PSA PRO BAK GLO\\ndiffpages = 20\\ndiffpdf = \\ndiglot2captions = False\\ndiglotadjcenter = False\\ndiglotcolour = rgb(255,255,255)\\ndiglotheaders = False\\ndiglotjoinvrule = False\\ndiglotmergemode = doc\\ndiglotnotesrule = False\\ndiglotpicsources = pri\\ndiglotprifraction = 50\\ndiglotsecconfig = Default\\ndiglotsecfraction = 50\\ndiglotsecprjguid = f2fa3035ed38dbe51f52e599c12da64d1f073a10\\ndiglotsepnotes = True\\ndiglotserialbooks = FRT BAK GLO XXA XXB\\ndiglotswapside = False\\nelipsizemptyvs = False\\nexclusivefolder = False\\nfilterglossary = True\\nfirstparaindent = True\\nfontbold = Gentium Plus| Bold|false|false|\\nfontbolditalic = Gentium Plus| Italic Bold|false|false|\\nfontextraregular = ||false|false|\\nfontitalic = Gentium Plus| Italic|false|false|\\nfontregular = Gentium Plus||false|false|\\nglossarydepth = 2\\nglossarymarkupstyle = cb\\nglueredupwords = False\\nhangpoetry = False\\nhidemptyverses = False\\nifchaplabels = False\\nifcolorfonts = True\\nifdiglot = False\\nifdiglotcolour = rgb(255,255,255)\\niffigcrop = False\\niffigexclwebapp = False\\niffighiderefs = False\\niffigplaceholders = False\\niffigshowcaptions = False\\niffigskipmissing = False\\nifhidehboxerrors = False\\nifinclfigs = False\\nifletter = False\\niflinebreakon = False\\nifmainbodytext = True\\nifndiglot = False\\nifomitverseone = True\\nifrtl = ltr\\nifshow1chbooknum = False\\nifshowchapternums = True\\nifshowversenums = True\\nifspacing = False\\nifusepiclist = False\\nimagetypepref = JPG TIF PNG PDF\\nincludeimg = True\\nindentunit = 1\\nintrooutline = False\\nkeepversions = 1\\nlettershrink = 1\\nletterspace = False\\nletterstretch = 5\\nlinebreaklocale = \\nmarginalposn = left\\nmarginalverses = False\\nmultibook = False\\nndiffcolor = rgb(26,95,180)\\nnoblankpage = False\\nnogrid = False\\nodiffcolor = rgb(204,0,0)\\nonlyshowdiffs = True\\npagebreakallchs = False\\nparallelrefs = True\\npicresolution = \\npreventorphans = False\\npreventwidows = False\\nprintarchive = False\\nrulethickness = 0.4\\nscript = Zyyy\\nsectionheads = True\\nsensitive = False\\nsettingsinpdf = True\\nshowxtrachapnums = False\\nspaceshrink = 50\\nspacestretch = 200\\nstartpagenum = 1\\nsubject = \\ntoc = False\\ntocleaders = 1\\ntoctitle = \\ntoptobottom = ltr\\nusetoc1 = True\\nusetoc2 = True\\nusetoc3 = False\\nvarlinespacing = False\\n\\n[import]\\nproject = \\naddnewpics = True\\nadvanced = False\\nbody = True\\ncaptions = False\\nconfig = (None)\\ncopyright = True\\ncover = True\\ndeloldpics = False\\neverything = False\\nfontsscript = True\\nfrontmatter = True\\nheaderfooter = True\\nimppics = entire\\nimpsource = pdf\\nimptarget = prjcfg\\nlayout = True\\nnotesrefs = True\\nother = True\\noverrideallstyles = False\\noverwitefrtmatter = False\\npictures = True\\nsizeposn = True\\nstyles = True\\ntabsborders = False\\ntgtconfig = Default\\ntgtproject = \\n\\n[color]\\nspotcolrange = 15\\n\\n[config]\\nautosave = True\\ndisplayfontsize = 12.098\\nfilterpics = False\\ngitversion = 2.6.8-gf3e1d82c\\nlockui4layout = False\\nlockxetexlayout = False\\nname = Default\\nnotes = \\nprintcount = 69\\npwd = \\ntexperthacks = True\\nversion = 2.20\\n\\n[cover]\\ncoveradjust = 0.14\\ncoverartbleed = 0\\ncoverbleed = 0\\ncovercropmarks = False\\nincludespine = False\\nmakecoverpage = False\\nmakeseparatepdf = False\\noverridepagecount = True\\npapercalcunits = weight\\nrotatespine = 270\\nrtlbookbinding = False\\nspineoverlapback = 0\\nspineoverlapfront = 0\\nspinewidth = 4.874mm\\ntotalpages = 100\\nweightorthick = 80\\n\\n[covergen]\\nbordercolor = rgb(94,92,100)\\nborderstyle = Vectorian3\\nimagealpha = 0.7\\noverwriteperiphs = False\\nshadingalpha = 0.5\\nshadingcolor = rgb(114,159,207)\\ntextcolor = rgb(0,0,0)\\ntextscale = 1.7\\nuseborder = True\\nuseimage = False\\nuseshading = True\\n\\n[fancy]\\nenableornaments = False\\nendayah = False\\nendofbook = False\\nendofbookpdf = \\npageborder = False\\npageborderfullpage = False\\npageborderpdf = \\npageborders = False\\npagebordertype = False\\nsectionheader = False\\nsectionheaderpdf = \\nsectionheaderscale = 1\\nsectionheadershift = 0\\nversedecorator = False\\nversedecoratorpdf = \\nversedecoratorscale = 1\\nversedecoratorshift = 0\\nversedecoratortype = \\n\\n[finishing]\\nextractinserts = False\\nextraxdvproc = False\\nfoldcutmargin = 0\\nfoldfirst = False\\ninclsettings = True\\npgsperspread = 1\\nrtlpagination = False\\nscaletofit = False\\nsheetsinsigntr = 0\\nsheetsize = 210mm, 297mm (A4)\\nspotcolor = rgb(28,113,216)\\nspottolerance = 15\\n\\n[footer]\\nftrcenter = -empty-\\nftrcenterside = \\nifftrtitlepagenum = True\\nifprintconfigname = False\\nnoinkinmargin = True\\n\\n[grid]\\ndivisions = 0\\ngridgraph = False\\ngridlines = True\\nmajorcolor = rgb(255,255,255)\\nmajorthickness = 0\\nminorcolor = rgb(255,255,255)\\nminorthickness = 0\\nunits = in\\nxyadvance = 0\\nxyoffset = margin\\n\\n[header]\\nchvseparator = colon\\nhdrcenter = Page Number\\nhdrcenterside = \\nhdrleft = -empty-\\nhdrleftside = \\nhdrright = Reference Range\\nhdrrightside = \\nifrhrule = True\\nifshowbook = True\\nifshowchapter = True\\nifshowverse = False\\nmirrorlayout = True\\n\\n[notes]\\nabovenotespace = 9\\nabovestudyspace = 9\\nabovexrefspace = 0\\naddcolon = False\\nbelownoterulespace = 3\\nbelowstudyrulespace = 3\\nbelowxrefrulespace = 0\\nfncallers = †,‡,§,∥,#\\nfneachnewline = False\\nfnomitcaller = True\\nfnoverride = False\\nfnpos = page\\nfnresetcallers = False\\nfnruleindent = 0\\nfnrulelength = 100\\nfnruleposn = 1\\nfnrulethick = 0.4\\nfrverseonly = False\\nglossaryfootnotes = False\\nhoriznotespacemax = 27\\nhoriznotespacemin = 7\\niffnautocallers = True\\niffootnoterule = True\\nifstudynoterule = True\\nifxrautocallers = True\\nifxrefrule = False\\nifxrexternalist = False\\nincludefootnotes = True\\nincludexrefs = True\\ninternotespace = 3.5\\nkeepbookwithrefs = True\\nshowextxrefs = False\\nsnruleindent = 0\\nsnrulelength = 100\\nsnruleposn = 1\\nsnrulethick = 0.4\\nxrcentrecolwidth = 30\\nxrcolalign = False\\nxrcolbottom = False\\nxrcolrule = False\\nxrcolside = 3\\nxreachnewline = False\\nxrextlistsource = standard_2\\nxrfilterbooks = all\\nxrguttermargin = 0.1\\nxrlistsource = standard_2\\nxrlocation = page\\nxromitcaller = True\\nxroverride = False\\nxrpos = page\\nxrresetcallers = False\\nxrruleindent = 0\\nxrrulelength = 100\\nxrruleposn = 1\\nxrrulethick = 0.4\\nxrverseonly = False\\n\\n[paper]\\nallowunbalanced = False\\nbottommargin = 14.4\\ncolgutteroffset = -2\\ncolumns = True\\ncropmarks = False\\nfontfactor = 11\\nfooterpos = 13.657\\ngutter = 10\\nheaderpos = 10\\nheight = 148mm, 210mm (A5)\\nifaddgutter = True\\nifgrid = False\\nifoutergutter = True\\nifverticalrule = False\\nifwatermark = False\\nlockfont2baseline = False\\nmargins = 12\\nnotelines = False\\npagesize = 148mm, 210mm (A5)\\nrulegap = 5\\ntopmargin = 18\\nwatermarkpdf = ${pdfassets}/watermarks\\nwidth = 148mm, 210mm (A5)\\n\\n[paragraph]\\nifhyphapproved = True\\nifhyphenate = True\\nifhyphlimitbks = False\\nifjustify = True\\nifnbhyphens = False\\nifnothyphenate = True\\nifomithyphen = False\\nifsylhyphens = False\\nifusefallback = False\\nlinespacing = 15\\nmissingchars = \\n\\n[project]\\nautotaghebgrk = True\\nbook = OTH\\nbooklist = \\nbookscope = multiple\\ncanonicalise = True\\ncolophontext = \\\\pc \\\\zcopyright\\n\\t\\\\pc \\\\zlicense\\n\\t\\\\b\\n\\t\\\\pc \\\\zimagecopyrights\\ncopyright = \\nid = minitests\\nifcolophon = False\\niffrontmatter = False\\nifinclbackpdf = False\\nifinclfrontpdf = False\\nifusecustomsty = False\\nifusemodssty = False\\nifusemodstex = False\\nifusepremodstex = False\\ninclcoverperiphs = False\\ninterlang = \\ninterlinear = False\\ninterpunc = False\\nlicense = This work is licensed under a Creative Commons Attribution 4.0 International License.\\nperiphpagebreak = False\\npgbreakcolophon = False\\nplugins = \\nprocessscript = False\\nrandompicposn = False\\nruby = False\\nsectintros = False\\nselectxrfile = .\\nuilevel = 6\\nusechangesfile = False\\nwhen2processscript = before\\n\\n[scripts]\\narab/lrcolon = False\\nindic/showhyphen = False\\nindic/syllables = False\\nmymr/syllables = False\\n\\n[snippets]\\nadjlabelling = False\\ndiglot = False\\nfancyborders = False\\nfancyintro = False\\npdfoutput = Screen\\n\\n[strongsndx]\\ndefinitions = False\\nfallbackprj = \\nkeyvrsrefs = True\\nmajorlang = en\\nndxbookid = XXS\\nnocomments = False\\nopenineditor = False\\nraglines = 0\\nrenderings = True\\nshowall = False\\nshowgreek = True\\nshowhebrew = True\\nshowindex = False\\nshowintext = False\\nshownums = True\\nsourcelang = True\\ntransliterate = False\\ntwocols = True\\nwildcards = keep\\n\\n[studynotes]\\ncolgutterfactor = 4\\ncolgutteroffset = 0\\nfiltercats = False\\nifverticalrule = False\\nincludesidebar = True\\nincludextfn = False\\ninternote = 0\\nshowcallers = False\\ntxlinclquestions = False\\ntxllangtag = en\\ntxlnumbered = True\\ntxloverview = False\\ntxlshowrefs = False\\n\\n[thumbtabs]\\nbackground = rgb(0,0,0)\\ngroups = GAL EPH PHP COL; 1TI 2TI TIT PHM; JAS 1PE 2PE; 1JN 2JN 3JN JUD\\nheight = 6\\nifthumbtabs = False\\nlength = 15\\nnumtabs = 0\\nrestart = False\\nrotate = False\\nrotatetype = 1\\ntabsoddonly = False\\nthumbtextmkr = toc3\\n\\n[vars]\\nmaintitle = \\nsubtitle = Berean Standard Bible\\nlanguagename = English\\n\\n[strongsvars]\\nindex_book_title = Strong's Number Index\\nhebrew_section_title = Hebrew Words\\ngreek_section_title = Greek Words\\nreverse_index_title = Reversal Index\\n\\n[texpert]\\nbottomrag = 0\\n\\n\",\n    \"shared/ptxprint/Default/ptxprint.sty\": \"\\n\\\\Marker b\\n\\\\LineSpacing 0.5\\n\\n\\\\Marker cat:coverback|esb\\n\\\\BorderWidth 1\\n\\\\BorderVPadding 0\\n\\\\BorderHPadding 0\\n\\\\BorderColor 0.00 0.00 0.00\\n\\\\BoxVPadding 0\\n\\\\BoxHPadding 8\\n\\\\BgColor 0.91 0.25 0.25\\n\\\\Alpha 0.2\\n\\n\\\\Marker cat:coverfront|esb\\n\\\\BgImageColor 0.00 0.00 0.00\\n\\\\BgImageAlpha 0.5\\n\\\\BgImageLow f\\n\\\\BgImageOversize ignore\\n\\\\BgImage \\\"/home/david/src/ptx2pdf/test/projects/minitests/figures/SlithyToves.jpg\\\"\\n\\\\TextProperties publishable\\n\\\\BorderStyle ornaments\\n\\\\BorderRef Vectorian3\\n\\\\BorderColor 0.00 0.00 0.00\\n\\\\BorderWidth 15\\n\\\\BorderVPadding -30\\n\\\\BorderHPadding -30\\n\\\\BoxVPadding 35\\n\\\\BoxHPadding 35\\n\\\\Border All\\n\\\\SpaceAfter -0.24\\n\\\\Alpha 0.45\\n\\n\\\\Marker cat:coverfront|mt1\\n\\\\Color x000000\\n\\\\NonJustifiedFill 0.5\\n\\n\\\\Marker cat:coverfront|mt2\\n\\\\FontSize 27.2\\n\\\\Color x000000\\n\\n\\\\Marker cat:coverspine|esb\\n\\\\BorderWidth 1\\n\\\\BorderVPadding 0\\n\\\\BorderHPadding 0\\n\\\\BorderColor 0.00 0.00 0.00\\n\\\\BoxVPadding 1\\n\\\\BoxHPadding 1\\n\\\\Scale 1\\n\\\\Border All\\n\\n\\\\Marker cat:coverspine|mt1\\n\\\\FontSize 7.92\\n\\\\Color xEDD400\\n\\\\Justification Right\\n\\\\SpaceAfter 1.2\\n\\n\\\\Marker cat:coverspine|mt2\\n\\\\FontSize 7.2\\n\\\\Color x000000\\n\\\\Justification Center\\n\\n\\\\Marker cat:coverwhole|esb\\n\\\\LineSpacing 0.97\\n\\\\BgImageColor 0.00 0.00 0.00\\n\\\\BgImageAlpha 0.5\\n\\\\BgImageLow t\\n\\\\BgImageOversize ignore\\n\\\\BgImage \\\"F\\\"\\n\\\\BgColor 0.45 0.62 0.80\\n\\\\BorderWidth 1\\n\\\\BorderVPadding 0\\n\\\\BorderHPadding 0\\n\\\\BorderColor 0.00 0.00 0.00\\n\\\\BoxVPadding 0\\n\\\\BoxHPadding 0\\n\\\\LeftMargin -0.01\\n\\n\\\\Marker cat:headingsbox|esb\\n\\\\TextProperties publishable\\n\\\\BorderWidth 0\\n\\\\Border None\\n\\\\BgColor 1 1 1\\n\\\\Alpha 0\\n\\n\\\\Marker cat:inline1|esb\\n\\\\Name cat:inline1|esb - Peripherals - Category for putting material in an inline box\\n\\\\TextType Other\\n\\\\StyleType Paragraph\\n\\\\FontSize 12\\n\\\\FirstLineIndent 0\\n\\\\LeftMargin 0\\n\\\\RightMargin 0\\n\\\\LineSpacing 1\\n\\\\SpaceBefore 0\\n\\\\SpaceAfter 0\\n\\\\NonJustifiedFill 0.25\\n\\\\Position h\\n\\\\Scale 1\\n\\\\BgColor 0.60 0.95 0.59\\n\\\\Description inline1 sidebar\\n\\\\Occursunder esb\\n\\n\\\\Marker cat:inline2|esb\\n\\\\Name cat:inline2|esb - Peripherals - Category for putting material in an inline box\\n\\\\TextType Other\\n\\\\StyleType Paragraph\\n\\\\FontSize 12\\n\\\\FirstLineIndent 0\\n\\\\LeftMargin 0\\n\\\\RightMargin 0\\n\\\\LineSpacing 1\\n\\\\SpaceBefore 0\\n\\\\SpaceAfter 0\\n\\\\NonJustifiedFill 0.25\\n\\\\Position h\\n\\\\Scale 1\\n\\\\BgColor 0.88 0.50 0.00\\n\\\\Description inline2 sidebar\\n\\\\Occursunder esb\\n\\n\\\\Marker cat:inline3|esb\\n\\\\Name cat:inline3|esb - Peripherals - Category for putting material in an inline box\\n\\\\TextType Other\\n\\\\StyleType Paragraph\\n\\\\FontSize 12\\n\\\\FirstLineIndent 0\\n\\\\LeftMargin 0\\n\\\\RightMargin 0\\n\\\\LineSpacing 1\\n\\\\SpaceBefore 0\\n\\\\SpaceAfter 0\\n\\\\NonJustifiedFill 0.25\\n\\\\Position h\\n\\\\Scale 1\\n\\\\BgColor 0.30 0.30 0.30\\n\\\\Description inline3 sidebar\\n\\\\Occursunder esb\\n\\n\\\\Marker cat:inline4|esb\\n\\\\Name cat:inline4|esb - Peripherals - Category for putting material in an inline box\\n\\\\TextType Other\\n\\\\StyleType Paragraph\\n\\\\FontSize 12\\n\\\\FirstLineIndent 0\\n\\\\LeftMargin 0\\n\\\\RightMargin 0\\n\\\\LineSpacing 1\\n\\\\SpaceBefore 0\\n\\\\SpaceAfter 0\\n\\\\NonJustifiedFill 0.25\\n\\\\Position h\\n\\\\Scale 1\\n\\\\BgColor 0.30 0.30 0.80\\n\\\\Description inline4 sidebar\\n\\\\Occursunder esb\\n\\\\Border All\\n\\\\BorderWidth 0.5\\n\\n\\\\Marker cat:sidebar|esb\\n\\\\Name cat:sidebar|esb - Peripherals - Category for putting material in a box\\n\\\\TextType Other\\n\\\\StyleType Paragraph\\n\\\\FontSize 12\\n\\\\FirstLineIndent 0\\n\\\\LeftMargin 0\\n\\\\RightMargin 0\\n\\\\SpaceBefore 0\\n\\\\SpaceAfter 0\\n\\\\NonJustifiedFill 0.25\\n\\\\Position tr\\n\\\\Occursunder esb\\n\\\\Scale 1\\n\\\\BgColor 0.60 0.95 0.59\\n\\\\BorderWidth 1\\n\\\\BorderVPadding 3\\n\\\\BorderHPadding 3\\n\\\\BorderColor 0.00 0.00 0.00\\n\\\\BoxVPadding 2\\n\\\\BoxHPadding 2\\n\\\\BoxTPadding 1\\n\\\\Border Top Left Right Bottom\\n\\n\\\\Marker loc:bX|fig\\n\\\\Name loc:bX|fig - Auxiliary - Figure/Illustration/Map\\n\\\\Description Illustration [Columns to span, height, filename, caption text]\\n\\\\TextType Other\\n\\\\StyleType Character\\n\\\\FontSize 12\\n\\\\Italic -\\n\\\\FirstLineIndent 0\\n\\\\LeftMargin 0\\n\\\\RightMargin 0\\n\\\\LineSpacing 1\\n\\\\SpaceBefore 0\\n\\\\SpaceAfter -0.48\\n\\\\NonJustifiedFill 0.25\\n\\\\Endmarker loc:bl|fig*\\n\\\\Occursunder po tc1 q3 lim2 pm thr1 thr4 th4 lf li mi pi s3 thr3 phi ms2 qc tc4 li1 m qm1 pmc q4 ms tcr4 th3 qr qm NEST ph pi3 q2 li2 pmo th1 nb lim3 d lim1 tc2 tcr3 q1 tcr2 q lim4 pc th2 tc3 ip sp tcr1 li3 s1 tr pi2 li4 s lim lh pi1 qd qm2 pr p ms1 s2 pmr thr2 qm3\\n\\n\\\\Marker nd\\n\\\\FontName Andika\\n\\\\Color x009030\\n\\\\Smallcaps \\n\\n\\\\Marker s\\n\\\\SpaceBefore 4\\n\\n\\\\Marker s1\\n\\\\SpaceBefore 4\\n\\n\\\\Marker s3\\n\\\\Justification LeftBal\\n\\n\\\\Marker s3e\\n\\\\Justification LeftBal\\n\\n\\\\Marker s4\\n\\\\Justification LeftBal\\n\\n\\\\Marker s4e\\n\\\\Justification LeftBal\\n\\n\\\\Marker sp\\n\\\\Justification LeftBal\\n\\n\\\\Marker tcr1\\n\\\\RightMargin 0.05\\n\\n\\\\Marker textborder\\n\\\\TextProperties nonpublishable verse\\n\\n\\n\\\\Marker s\\n\\\\Border None\\n\\\\BorderWidth 0\\n\\\\BgColor 1 1 1\\n\\\\Alpha 0\\n\\\\Justification Center\\n\\\\Bold\\n\\n\\\\Marker s1\\n\\\\Border None\\n\\\\BorderWidth 0\\n\\\\BgColor 1 1 1\\n\\\\Alpha 0\\n\\\\Justification Center\\n\\\\Bold\\n\\n\\\\Marker s2\\n\\\\Border None\\n\\\\BorderWidth 0\\n\\\\BgColor 1 1 1\\n\\\\Alpha 0\\n\\\\Bold\\n\\n\\\\Marker r\\n\\\\Border None\\n\\\\BorderWidth 0\\n\\\\BgColor 1 1 1\\n\\\\Alpha 0\\n\\\\Italic\\n\\\\Justification Center\\n\",\n    \"shared/ptxprint/Default/ptxprint-mods.sty\": \"\\\\Category coverfront\\n\\\\BorderStyle Vectorian3\\n\\\\BoxBPadding 30\\n\\\\BorderWidth 18\\n\\\\BoxVPadding 45\\n\\\\BoxHPadding 45\\n\\\\Alpha 0.6\\n\\\\BgColour F\\n\\\\BgImageLow T\\n%\\\\BgImage ../../../figures/SlithyToves.jpg\\n\\\\BgImageAlpha 0.2\\n%\\\\BgImageOversize distort\\n\\\\BorderHPadding -40\\n\\\\BorderVPadding -40\\n\\\\BgImageScale inner|x0.8\\n\\\\Border All\\n\\n\\\\Category coverback\\n\\\\Alpha 0.4\\n\\\\BgColour T\\n\\\\BorderStyle double\\n\\\\BorderFillColour None\\n\\\\BorderLineWidth 0.5\\n\\\\BorderPadding -15\\n%\\\\BoxPadding 20\\n%\\\\BoxLPadding 5\\n\\\\BorderWidth 5\\n\\\\BoxTPadding 2\\n\\\\BoxHPadding 20\\n\\\\BoxBPadding 10\\n\\\\Border None \\n\\n\\\\Category coverwhole\\n%\\\\SpaceBefore 10\\n%\\\\BoxLPadding 30\\n%\\\\BorderTPadding -30\\n%\\\\BgImage ../../../figures/Background.jpg\\n\\\\BgImageScale bleed|1.0x1\\n\\\\BorderWidth 1.0\\n\\\\BgColour F\\n\\\\BgImageAlpha 0.8\\n\\\\BoxTPadding 0\\n\\\\BoxBPadding 0\\n\\\\Border none \\n\\n\\\\Marker pc\\n\\\\Color xff0000\\n\\\\FontSize 24\\n\\n\\\\Marker p\\n\\\\Color xff0000\\n\\n\\\\Category coverspine\\n\\\\Border None\\n\\\\Alpha 0.6\\n\\\\Rotation r\\n\\n\\\\Marker pc\\n\\\\FontSize 16\\n\\\\Bold\\n\\n\\\\Category TitleBox\\n\\\\Position hc\\n\\\\Scale 0.3\\n\\\\SpaceBefore 10\\n\\\\SpaceAfter 20\\n\\\\BgColour 1.0 0.9 0.9\\n\\\\Border All\\n\\\\BoxPadding 0\\n\\\\BoxBPadding 10\\n\\\\Alpha 1\\n\\n\\\\Category ISBNbox\\n\\\\Position hr\\n\\\\SpaceBefore 100\\n\\\\SpaceAfter 0\\n\\\\Border None\\n\\\\BorderWidth 0.05\\n\\\\BgColour 1 1 1\\n\\\\BoxHPadding 5\\n\\\\BoxVPadding 5\\n\\\\BorderRPadding 10\\n\\\\Scale 0.7\\n\\\\Alpha 1\\n\\n\\\\Category footbox\\n\\\\Position h\\n\\\\SpaceBefore 10\\n\\\\SpaceAfter 10\\n\\\\Scale 0.25\\n%\\\\Border All\\n\\\\BgColour 0.8 0.8 1\\n%\\\\BoxHPadding 0\\n%\\\\BoxVPadding 0\\n%\\\\BorderPadding 1\\n%\\\\Scale 0.3\\n%\\\\Alpha 1\\n\\\\EndCategory\\n\\n\\\\Marker cat:headingsbox|esb\\n\\\\SidebarGridding heading\\n\\\\BorderPadding 1\\n\\\\BoxPadding 0\\n\\\\BorderStyle ornaments\\n\\\\BorderRef  Han4\\n\\\\BorderFillColour .5 .5 .5\\n\\n\\\\Marker cat:sidebar|esb\\n\\\\SidebarGridding smart\\n\\\\Marker cat:inline2|esb\\n\\\\SidebarGridding normal\\n\\\\Marker cat:inline3|esb\\n\\\\SidebarGridding none\\n\\n\\\\Marker cat:inline4|esb\\n\\\\SidebarGridding none\\n\"\n  },\n  \"fonts\": [\n    {\n      \"family_id\": \"gentiumplus\",\n      \"version\": \"6.200\",\n      \"filename\": \"GentiumPlus-Regular.ttf\",\n      \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Regular.ttf\",\n      \"sha256\": \"2c27e7da23ba44d135685836056833b304a388d3da346813189c60656dc02019\"\n    },\n    {\n      \"family_id\": \"gentiumplus\",\n      \"version\": \"6.200\",\n      \"filename\": \"GentiumPlus-Bold.ttf\",\n      \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Bold.ttf\",\n      \"sha256\": \"622ea9f2709d74f99d45c08d93cdf2a6d096406d3a1ec2939d02714f558b3dac\"\n    },\n    {\n      \"family_id\": \"gentiumplus\",\n      \"version\": \"6.200\",\n      \"filename\": \"GentiumPlus-Italic.ttf\",\n      \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-Italic.ttf\",\n      \"sha256\": \"fedc1acdd2f1080941ed998cabee9759456f0e486fbd8169ff4238b37d3ac60d\"\n    },\n    {\n      \"family_id\": \"gentiumplus\",\n      \"version\": \"6.200\",\n      \"filename\": \"GentiumPlus-BoldItalic.ttf\",\n      \"url\": \"https://ptxprint.klappy.dev/r2/outputs/fixtures/fonts/gentium-plus-6.200/GentiumPlus-BoldItalic.ttf\",\n      \"sha256\": \"960e0a58ce1d7849c7a3e49f4fbc1ac4a27b58ef19a2d013ce637fe364b0a1f0\"\n    }\n  ],\n  \"figures\": []\n}\n```\n\n"}];
