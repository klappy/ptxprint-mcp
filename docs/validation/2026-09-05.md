# Validation — ptxprint-mcp as deployed, 2026-09-05

Seat: Auggie (expeditor, kitchen ticket `2026-09-05-ptxprint-mcp-validate-and-fix`), a seat
that had not cooked on this repo. Target: `https://ptxprint.klappy.dev` as it stood at
2026-09-05 00:41–00:53 UTC (repo `main` `de39386f`, 2026-07-21). Method:
`klappy://canon/validation-as-epistemic-mode` — every row is observed / not observed /
failed, with the call and the bytes. Nothing in this PR changes `src/`.

## Rows

| # | Surface | Verdict | Call | Bytes |
|---|---|---|---|---|
| H1 | `/health` | ✅ observed | `GET /health` | 200 in 0.53 s · `{"ok":true,"service":"ptxprint-mcp","version":"0.1.0","spec":"v1.3-draft","tools":[7]}` |
| H2 | MCP handshake | ✅ observed | `POST /mcp initialize` → `notifications/initialized` | 200, `mcp-session-id` issued; `protocolVersion 2025-03-26`; initialized → 202 |
| T0 | `tools/list` | ✅ observed | | submit_typeset · get_job_status · cancel_job · docs · telemetry_policy · telemetry_public · telemetry_schema — matches `/health` |
| R1 | **Fresh render** (container must run) | ✅ observed | `submit_typeset` with `smoke/bsb-jhn-empirical.json` + one TeX comment appended to `ptxprint-mods.sty` (new hash) | job `ae95be1a…`, `cached:false`, `running` at 10 s (`current_phase: typesetting`), `succeeded` at 21 s: "PTXprint completed in 10.9 s. failure_mode=success; errors=0"; PDF fetched 200, **359 227 B, 61 pages** |
| R2 | Cached render | ✅ observed | same fixture unmodified | job `a1c54672…`, `cached:true`, `succeeded`, PDF 200, 359 197 B, 61 pages — served from R2, container not exercised |
| R3 | `minimal-payload.json` (define-only) | ⚠️ failed, as issue #40 predicts | `submit_typeset` | job `a8eb0823…`, failed in 1.8 s, exit 0, `log_tail`: "PTXprint exited 0 with no PDF and no log file", argv `ptxprint -P BSB -b JHN -p … -q`. Reproduces #40 exactly; comment left on the issue. Not a regression. |
| S1 | `get_job_status` unknown id | ✅ observed | 64 zeros | `isError:true`, `{"error":"job_id not found"}` — clean |
| S2 | `get_job_status` on cached job | ⚠️ finding | job `a1c54672…` | `human_summary` = "Cancellation requested. Container will SIGTERM PTXprint on next poll (≤10s)." on a `succeeded`, cached job. The summary is stale state from an earlier cancel on the same hash, replayed on cache hit. **Finding F2** (iterate; not fixed here). |
| C1 | `cancel_job` | ✅ observed (Day-1 semantics) | fresh job `2bc14eb7…` cancelled 1 s after submit | `{"ok":true,"was_running":false,"note":"Day-1 PoC: flag set; container-side SIGTERM is Day-2."}`; 15 s later status `running`, `cancel_requested:true`, then the job completes anyway — as the README's ⏳ Day-2 line says |
| D1 | `docs` — README's three examples, depth 1 and 2 | ❌ **failed** | `docs("phase 1 minimum payload")`, `("payload construction")`, `("english single book template")` | every call: `{"answer":null,"sources":[],"deeper":[…],"governance_source":"knowledge_base"}` |
| D2 | Same queries through oddkit directly | ✅ observed | `oddkit_search` with `knowledge_base_url=https://github.com/klappy/ptxprint-mcp` | 23 hits, top `klappy://canon/articles/phase-1-poc-scope` score 8.25 — the canon answers; the proxy does not. Cause: oddkit's retrieval-disclosure contract returns `result.data`; `src/docs.ts` reads `result.hits`. **Finding F1 → fix PR.** |
| P1 | `telemetry_policy` | ✅ observed | `{}` | policy markdown returned (bundled, tier-2) |
| P2 | `telemetry_schema` | ✅ observed | `{}` | dataset `ptxprint_telemetry`, blob/double columns listed |
| P3 | `telemetry_public` | ✅ observed (validation) | `{}` | `-32602 Invalid arguments` — the tool requires a `sql` string; a refused-statement row is owed once a SELECT is run (not run this pass: rate-limited quota, captain's account) |
| V1 | Version strings | ⚠️ drift | README badge · `canon/specs/` · `/health` | README says `spec v1.2-draft`; the repo carries `ptxprint-mcp-v1.3-spec.md`; `/health` says `v1.3-draft`. `package.json` and `/health` agree on `0.1.0`. **Finding F3** (TENSIONS). |
| G1 | Repo hygiene | ⚠️ | GitHub API | last human commit 2026-07-21; open PRs #47 #49 #50 are dependabot; open issue #40 |

## Findings and disposition

| Finding | Disposition | Where |
|---|---|---|
| **F1** `docs` returns nothing for every query — proxy reads a response shape oddkit no longer sends | **iterate → fixed** in the fix PR: the worker serves its bundled canon (captain ruling 2026-09-04: new docs tools manage their own policy; no re-pin) | PR `fix/2026-09-05-docs-self-served` |
| **F2** stale `human_summary` ("Cancellation requested…") on a cached, succeeded job | iterate — later | `docs/TENSIONS.md` |
| **F3** spec version drift README (v1.2) vs repo/`/health` (v1.3) | iterate — one-line README fix belongs to whoever next touches README | `docs/TENSIONS.md` |
| **F4** issue #40 reproduces today | accept — known; comment with today's bytes | issue #40 |
| **F5** homepage architecture SVG still draws oddkit as the `docs()` upstream | iterate — after the fix merges | `docs/TENSIONS.md` |

## What is proven

The container renders (R1: a never-seen payload produced a 61-page PDF in 10.9 s). The job
state machine, caching, and error surfaces behave. The one broken tool is `docs`, and it is
broken by an upstream contract change, not by this repo's canon.

## After merge — D1 re-run against production (2026-09-05 01:06 UTC, main `224297e4`)
| # | Surface | Verdict | Call | Bytes |
|---|---|---|---|---|
| D1' | `docs`, the three README queries, depth 1 and 2 | ✅ observed | production `/mcp`, ~20 s after merge | `served_from: bundled-canon@845ab0a0b210 (46 docs)`; top hits `articles/phase-1-poc-scope`, `articles/payload-construction`, `templates/english-single-book`; depth-2 answers 13 390 / 10 862 / 31 276 chars |
| H1' | `/health` | ✅ unchanged | | `0.1.0`, `v1.3-draft`, 7 tools |

Finding F1 closed. New nit for TENSIONS: depth-1 snippet for `phase 1 minimum payload` is 19 chars (the article opens with a one-line "Superseded" callout that wins the first-paragraph pick); depth 2 is unaffected.
